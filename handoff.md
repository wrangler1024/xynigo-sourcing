# Xynigo Sourcing 新会话交接

交接时间：**20260916 深夜**（采购售后模块：四链已通、已上测试机；**打磨批次暂停 → 交给 Codex 先熟悉模块代码（不动手开发）**）
仓库/基线：`origin/main` **`e33d196`**（本轮打磨 worktree `~/Documents/xynigo-worktrees/after-sale-polish`）
模块交接文档：**`docs/20260915_采购售后模块交接.md`**（第 10 节＝线上事实，第 11 节＝待办与接手须知，**第 12 节＝本次暂停点**）
Codex 熟悉任务提示词：**`docs/20260916_交接_售后模块熟悉任务_Codex提示词.md`**（可整段复制）

> 0915 首版的说明已并入上面那份模块文档（第 1~7 节）；本文件只做「现在的状态 + 下一步」的索引。

> **20260917 在途分支（ZCode）**：`codex/after-sale-claim-env-direct`（基线 `ab4c65a`）＝③「按环境提交」
> 改为**单遍直提**（点一次确认直接进环境发现订单并提交，无入口环境跳过并回报原因），去掉 v0.18.6 的
> 「先扫描 → 人工核对清单 → 再提交」两遍走；新增迁移 `0045_after_sale_env_results` 与新能力位
> `after.sale.claim-environment.v1`（老执行器由云端派发前拦截）。实现、测试与已知限制见
> **`docs/20260917_售后按环境单遍直提.md`**。**未合并、未部署**；部署顺序＝先云端（含迁移 0044），
> 后执行器新桌面包。

## 1. 一句话现状

采购售后（丢件退款）模块的**四条链**——② 扫描可申请、③ 提交售后（含补提失败 / 直接提交指定单）、
④ 退款跟踪回访（含导出）、③ 提交历史（列表 / 详情 / 回访本批 / 重提 / 批次导出）——**都已在测试机
`xynigo.samforo.icu` 上线并核验**；打磨批次已完成第 1 项（历史列表「环境数」列），**在分支上待评审、
未合并、未部署**。

## 2. 打磨批次（20260916 晚，暂停点）

| 项 | 状态 | 位置 |
|---|---|---|
| ① 历史列表补「环境数」列 | **已完成**（本地全量 1083 passed / 5 skipped；云端全量 314 passed） | 分支 `codex/after-sale-history-env-count` @ `5dce475`，已推 origin |
| ② 指定单「一行多组」拆组 | **未开始** | —— |
| ③ 打磨项：历史弹层 `Escape` / 状态条窄屏实测 | **未开始** | —— |
| ④ 执行器 0.18.1 发版 | **未开始**（只做了流程调研，结论见模块文档 §12.3） | 分发资产与发 Release **待 Jeff 授权** |
| 评审提示词（四项一起写一份即可） | **未写** | 照抄 `review-request-after-sale-export.md` 的四段式 |

分支 `codex/after-sale-history-env-count` 的内容：云端 `environmentCount` 字段（列表一次 group by 查齐、
详情同口径）、Web 历史列表加第 12 列（表头 / 行模板 / 空态 colspan 三处同步）、云端 + Web 各一条新用例、
两份原型重跑（并给原型补了「提交历史」弹层的合成数据，否则新列在原型里看不到）。

## 3. 上一轮交付（都有提交与测试，已上线）

| 提交 | 内容 |
|---|---|
| 收口 `b482e1f` | 合并四条修复分支（④ 导出 / 状态条落点 / ③ 字段 / 迁移 0039）+ **桥接层透传** |
| 三需求 `4bd73ff`…`254fe2c` | 补提失败（可恢复失败，排除 blocked）、直接提交指定单（粘贴解析）、提交历史（租户内互相可见）；评审「改后合并」，两项必修已闭合 |
| `297544f` | ③ 的「恢复最近一批」只恢复**本人**批次（共享视图在历史弹层） |
| `21a82cf` | 迁移 0039 revision id 缩短到 ≤32 字符（`alembic_version` 列宽，cherry-pick 自另一条线） |
| `f48ac70` | 运行状态条按原型补成 **① 之上的 sticky 常驻条**（此前只有 CSS、没有标记与 JS） |
| `ef66e32` | ③ 送达时间 / ④ 商品图改为**云端兜底**，不再依赖执行器版本 + 测试机数据回填 |

## 4. 唯一还卡体验的事：执行器 0.18.1

桥接层透传（`_execute_after_sale_claim` 不再丢 `deliveredAt`/`goodsImg`）**已在 main**，但桌面端
没发版，线上执行器仍是 0.18.0；云端兜底已经让这两列显示出来，所以**发版不是阻塞项**，但发版后
回传行自带值、历史批次重提也能带上。

发版现状要点（完整调研见模块文档 §12.3）：版本号散落在 12 个文件（含 `tests/test_release_v060.py` 的
硬钉与 `local_executor_release.py` 的 import 门禁）；CI 在 **push 任意分支**时构建双平台候选；
catalog 的 sha256/size 必须等 CI 产物下载后回填；tag 打**第二段元数据提交**。**上传 release-assets 与
发 GitHub Release 需 Jeff 明确授权**。

## 5. 接手入口（按顺序读）

1. `docs/20260915_采购售后模块交接.md` §10（线上事实）→ §11（待办 / 工程规矩 / 两套测试跑法 / 关键文件地图）→ **§12（本次暂停点与 0.18.1 调研结论）**
2. 需求原文：`docs/20260916_售后需求排期与暂缓.md`、`docs/20260916_需求_售后提交历史与从历史跟进.md`、
   `docs/20260916_需求_售后直接提交指定单.md`（三份都已改为「已实现」）
3. 原型定版：`docs/prototypes/20260915-procurement-after-sale-v2-multitype.md`（状态条与多类型设计）
4. 开场提示词：`docs/20260916_交接_售后模块接手提示词.md`（继续开发用）、
   `docs/20260916_交接_售后模块熟悉任务_Codex提示词.md`（**只熟悉代码、不动手开发**用）

## 6. 三条最容易踩的坑（细节都在模块文档里）

1. **改 Web 忘了同步 / 重跑原型**：`sync_web_workspace.py`（双副本必须逐字节一致），以及两份原型
   生成脚本；原型是生成时点的快照，忘了重跑就会「在浏览器里验的是旧代码」。
2. **部署迁移必须先 `build migrate`**（迁移烘进镜像），否则报 `Can't locate revision`，极易误判成代码问题。
3. **共享测试机上有同事的真机任务**：切换前必须排空（**连续两次为 0**），不要去杀别人的任务；
   磁盘常年偏高（当前 78%），预检先清构建缓存。
