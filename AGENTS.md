# Xynigo 三端协作规约

版本：v1 草案 · 20260910。适用于 Codex、Cursor、ZCode 及其他参与者；角色按任务分配。草案提交供联合评审，不代表部署或发布授权。

## 每单六条

1. **先确认边界。** 开工核对仓库、分支、HEAD、未提交改动和最近交接；明确交付范围、验收条件及已有授权。交接中的版本是带时间的快照，需按本次任务核验，不把历史记录当现状。
2. **只改权威源。** 按下表修改源文件并同步全部受影响输出；不得直接修补生成副本。新增输出须纳入一致性检查。涉及邻库输入输出时先读跨仓契约，验证受影响两端并说明兼容和发布顺序。
3. **隔离并行工作。** 一个可独立交付的改动单元使用一个分支，默认前缀 `codex/`；并行任务用独立 worktree。同一工作区只有一个写入者，暂存、切分支和构建也属于写入。共享测试库、服务器、浏览器环境和发布目录另行协调。
4. **提供可评审结果。** 开发者自查、自测并提交本人负责的改动；交接提交 SHA、diff 范围、验证结果和限制。合并或发布前由未参与实现的一方完成最终评审，关闭“必须改”，确认受影响组件的必需检查实际运行且通过；角色不绑定具体 agent。
5. **遵守安全与授权。** 公开文件、PR、日志和夹具不得包含真实凭证、服务器地址、内部链接或业务隐私。共享环境变更、对外分发和真实平台写入须在明确授权范围内执行。已有授权不逐条重问；超出范围时先完成可独立准备的工作，再说明新增影响。环境变更遵循部署手册的备份、任务保护、验证和回退要求。
6. **更新受影响记录。** 工作状态变化时更新对应交接；源码归属、协议、测试或部署实践变化时更新对应权威文档。重大授权边界、源码归属和不兼容策略调整由 Jeff 确认。只读评审不要求改写全局交接，也不因普通代码修改重复修改规约。

## 权威源与生成输出

所有路径相对本仓库根目录。表中同步脚本位于 `cloud/auth-service/deploy/`；生成清单和现阶段检查方法见 [CONTRIBUTING.md](CONTRIBUTING.md)。

| 权威源 | 同步脚本 | 云端输出 |
|---|---|---|
| `src/purchase_tool/web/` 中脚本列出的共享资源 | `sync_web_workspace.py` | `cloud/auth-service/src/xynigo_auth/web/` 中对应文件 |
| `src/purchase_tool/procurement_import.py` 及四份辅助源 | `sync_procurement_import_core.py` | 导入解析器及辅助模块 |
| `src/purchase_tool/env_batch.py`、`redaction.py` | `sync_environment_plan_core.py` | 环境计划核心和脱敏副本 |

生成头注不是唯一识别依据；未带头注的复制文件同样受约束。同步脚本目前没有 `--check`，不能将规划能力当成现有命令。

## 生态与邻库

- **本仓库**：主执行器、桌面客户端和云端工作台的正式工程仓库；维护共同契约的权威正文。
- **[crossborder-userscripts](https://github.com/wrangler1024/crossborder-userscripts)**：插件正式工程仓库，使用自己的规则、构建与分发流程；不继承本仓全部运行时要求。
- **私有业务工作空间**：保存业务 SOP、交付快照及尚待核对的遗留开发材料。快照不是当前版本依据；发现差异先核对归属，不覆盖或丢弃。公开文档不记录该空间的地址或访问方式。

共同契约包括 XYP2、云端采购提交与登录、本机采购助手 API、精准采购链接，以及物流订单标识与未来接入边界。修改前读取 [生态跨仓契约](docs/生态跨仓契约.md)。契约主从不构成跨目录自动继承；邻库位置按仓库身份确认，不假定 worktree 的父目录是另一仓库。

## 按任务读取

| 任务 | 入口 |
|---|---|
| 开发、测试、生成物检查 | [CONTRIBUTING.md](CONTRIBUTING.md) |
| 分工、评审、交接、规则维护 | [协作与交接](docs/协作与交接.md) |
| 跨仓接口、字段或版本联动 | [生态跨仓契约](docs/生态跨仓契约.md) |
| 共享环境变更、桌面包或云端发布 | [部署与发布手册](docs/部署与发布手册.md) |

`PROJECT_MEMORY.md`、`handoff.md` 和 `task.md` 若只存在于某个本地工作区，不是干净克隆的必备依赖。缺失时继续可验证的本地工作；涉及部署现状或授权时，取得对应记录后再执行依赖它的动作。长期公开规则以本仓已版本化文档为入口，敏感运维证据留在授权的私有记录中。
