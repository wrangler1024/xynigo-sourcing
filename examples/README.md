# Synthetic examples

Files in this directory contain synthetic data only. Replace every example
value in a copy stored outside the repository. Never save a workbook containing
real passwords, cookies, verification URLs, or account identifiers here.
