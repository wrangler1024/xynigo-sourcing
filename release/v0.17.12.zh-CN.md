# Xynigo Sourcing v0.17.12

- 修复采购手动添加 -1、-2 等子单号后重复导入的问题；保留原始母单键去重，不覆盖采购执行数据。
- 修复采购助手只能查询第一个子单的问题；各子单独立搜索和读取收件信息，旧母单标识存在歧义时要求重新选择。采购电脑需更新执行器。
- 导入分单新增订单背景色滑动开关，关闭后仍正常处理数据、行高、链接和图片；失败重试沿用原设置，历史记录不重新配色。
- 云端按账号记住最近 5 个已校验的目标协作表和工作表；更换文件保留选择，解析后自动重新校验，确认后才导入。
- 订单文件支持拖入单个 .xlsx（最大 20 MB）；无效输入保留原文件，忙碌期间禁止替换，并优化窄屏操作栏。

## 使用说明

1. 刷新云端页面后可使用新导入交互，目标表历史在成功校验后开始保存。
2. 采购电脑在执行器中选择“检查更新”，安装新版后重新搜索子订单；浏览器插件前端无需更换。
3. 采购手动拆单继续保留原“系统订单键”；明细变化和地址冲突仍按原规则阻断。

## 发布范围

延续内部 test 更新通道和 GitHub Prerelease；本版无新增数据库迁移或依赖升级。Windows 与 macOS 安装包按同一构建提交生成，运行时和文件校验值已核验。

## 安装包核验

双平台运行时：`0.17.12-e17841e6bd00`。

| 平台 | 文件大小（bytes） | SHA-256 |
|---|---:|---|
| windows-x86_64 | 15252155 | `552c8bd5fc55c481eecd51f8d90eeff12da731e42789f06c41df9c2683bd7fb7` |
| macos-arm64 | 11599028 | `a318517063c343c3a99c82ace50be4bb7dab59e4e18745dad24a562723761881` |
