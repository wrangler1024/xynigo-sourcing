# 0.18.0 采购售后与采购详情合并版本

本次为带新功能模块的版本：新增「采购售后（丢件退款）」（扫描 → 批量提交退款申请 → 退款跟踪回访，只读），并合入采购详情/回执线（回执留存、详情反馈与标记）。退款固定原路退回，已提交过的包裹平台自动划入不可退。

配套要求：先升级云端，再更新本机执行器与采购助手。老执行器不具备 `after.sale.*` 能力，新任务下不去。
回退锚点：本次部署前镜像 tag `pre-after-sale-20260916`，数据库备份 `pre-after-sale-20260916T042128Z.dump`。
