# Xynigo Sourcing 开发任务

最后更新：2026-09-06  
本地/远端基线：`92e29d9`；线上：v0.17.3；双平台运行时：`0.17.3-05f24ca27a7a`

## 当前阶段

用户于 20260906 已授权补 CI、交付三个提交并部署。本轮已完成，v0.17.3 已上线，最终提交 8 项 CI 全通过；现场业务验收和其他待跟进反馈仍未自动关闭。

## 抽检数量与进度修复

- [x] 先读取 `handoff.md` 的 1A 节及业务项目交付成果中的抽检根因报告；核对当前 HEAD 是否仍为 `94bf110`，避免覆盖其他任务的新改动。
- [x] 按用户恢复后的安排，统一“所选执行器本机抽检数量”的权威来源和生效时点；0 表示关闭，1 表示仅检测一个成功环境。
- [x] 修复 `web/index.html:10805` 把勾选状态转换为整批数量的规则，并处理 `11423` 只保留布尔值的问题。
- [x] 同步核对云端派发和本机执行入口：`cloud/auth-service/src/xynigo_auth/main.py`、`operation_executor.py`、`main.py`。避免旧前端、旧请求或任务重试继续绕过本机抽检设置；不要仅改 UI 文案。
- [x] 修复 IP 进度同步：运行中传输计划检测总数、已完成数、通过数；云端存储/返回与前端展示一致，禁止使用已完成数充当分母。
- [x] 增加跨层脱敏回归：创建 100、配置 1，只发生一次 IP 环境启动；配置 0 不检测；配置 3 检测 3；少于抽检数的成功环境、多采购员、绑号/备用/测试、本机/云端入口、重试均覆盖。
- [x] 增加进度回归：配置全量 100、完成 30 时显示 30/100；配置 1 时显示 0/1 → 1/1；顶部通过数随真实结果更新。
- [x] 变更 Web 后执行 `python3 cloud/auth-service/deploy/sync_web_workspace.py`，跑相关本地与云端测试、`git diff --check` 和安全审计。
- [x] 完成源码后向用户说明变更与验证；本次暂停前未授权这项修复的打包或发布，按后续安排执行。

历史故障复现脚本（针对修复前版本，修复后不再适用；当前验收请运行新增的本地和云端 `test_environment_verification.py`）：

```bash
/Users/jeff/Documents/xynigo-sourcing/.venv/bin/python 数据/脱敏样例/建环境抽检数量与进度复现_20260905.py /Users/jeff/Documents/xynigo-sourcing
```

本轮验证：本地全量 694 passed、3 skipped；云端相关 41 项通过。Web 镜像与语法、Python 编译、diff 检查和安全审计通过。

## v0.17.3 交付与后续验收

- [x] 抽检代码进入 `5bf30d5`；三项原始提交已随 v0.17.3 推送、打包并发布。
- [x] 云端服务及双平台执行器安装包已发布，数据库不变；旧执行器仍需检查更新。
- [x] 补齐本地 pytest 与云端 CI，最终 8 项通过。
- [x] 修复 CI 暴露的 Windows 卸载孤立进程，真机 runner 验证清理与用户数据保留。
- [x] 验证线上健康、就绪、版本、安装包哈希与原批次租约恢复。
- [ ] 新版本上线后，用脱敏/授权样例现场确认桌面设置 1、批次 100 时只有 1 次 IP 探测及 0/1 → 1/1。

## 其他待跟进反馈

- [ ] 6887 浏览器通信超时：在原 Windows 上核对单独重查结果，必要时补充脱敏的阶段、CDP 方法、耗时与异常类型；当前无法确认代理、页面卡死或本机负载谁是根因。
- [ ] Mac Chrome 150：等待或确认官方适配包/支持的客户端版本；当前列表最高 148，官方 API 返回 `eTag is nil`。不得把此事标成已修复，也不要改真实环境内核/指纹来绕过。
- [ ] 订单状态修复 `94bf110` 已随 v0.17.3 发布；旧线上结果仍需在新版执行器下重查更新。

## P0：发布后必须验证

- [ ] 在 Mac 使用 Xynigo 查询/预检 7702 或 7705，确认中文 `code=-1` 被识别为 Chrome 150 缺失。
- [ ] 确认桌面“诊断与维护”出现“下载并修复”按钮；管理员确认后验证下载、CDP 内核版本检查、环境关闭和审计日志完整。
- [ ] 在 Windows 与 macOS 各跑一批真实物流查询，确认没有新的 Local API 批次终止、永久 running 行或环境残留。
- [ ] 进行至少一轮 100 环境压力回归，记录成功、资源不足、超时、恢复、平均单环境耗时及批次累计耗时。
- [ ] 验证线上数据库仍为 `0030_logistics_auto_site` 且认证服务无持续错误。

## P1：功能与交互回归

- [ ] 混合输入至少一个 US 和一个 MX 环境，验证同一 `AUTO` 批次分别进入正确域名并按各自规则解析。
- [ ] 验证无法识别站点、同时存在 US/MX 页面时返回明确可重查错误，不静默猜站点。
- [ ] 验证旧执行器缺少 `logistics.auto-site.v1` 时只显示升级提示，不领取任务。
- [ ] 验证 HubStudio 分组只负责导入序号，序号查询不再受站点/分组限制。
- [ ] 验证云端查询操作栏没有“高级设置”和查询 ID；查询历史及历史详情仍可复制查询 ID。
- [ ] Windows 与 macOS 更新后确认默认窗口接近现场截图比例，小屏达到最小尺寸时页面仍可滚动、按钮不重叠。
- [ ] 跨电脑登录态矩阵：A 打开/关闭归档 → B 查询；A/B 同时打开；不同关闭顺序。记录最终云端状态并确认二次登录判定提示准确。
- [ ] 验证查询历史重查后按最近更新时间置顶，首次查询时间和稳定查询 ID 不变。
- [ ] 验证环境创建历史、创建任务 ID、创建用户、失败恢复和断点续跑信息完整。

## P1：可靠性观察

- [ ] 统计 `-10005`、`-10008`、Local API timeout/unreachable 的发生频率和恢复耗时，按设备和 HubStudio 版本分组。
- [ ] 检查“等待上一环境完全关闭”和正常查询状态是否随真实执行进度切换，避免提示长期停留。
- [ ] 检查完成、失败、取消、执行不确定四种终态，确认没有 pending/running 行和虚假 100% 进度。
- [ ] 检查异常重查后的本轮耗时、累计执行耗时和单环境平均耗时口径。

## P2：候选增强，需现场证据后决定

- [ ] 评估将内核修复需求持久化到安全运行状态，使执行器重启后仍显示待修复版本；只保存内核类型、版本和脱敏环境标识。
- [ ] 评估在登录态未生效时增加“等待其他设备归档后重试”的独立状态，区别于账号 Cookie 真正过期。
- [ ] 增加 HubStudio 控制链指标：start/stop 延迟、生命周期阶段、恢复次数、资源不足持续时间；不得记录账号或 Cookie。
- [ ] 根据 100/245 环境压力数据重新评估查询并发和 Local API 错峰参数，不以每分钟 300 次配额作为直接并发目标。
- [ ] 清理云端历史部署残留的 AppleDouble 元数据文件，并改进发布源码包以避免 GNU tar 扩展属性警告；执行前先确认目标清单和备份。
- [ ] 处理云端测试中的 Starlette `httpx` 弃用警告，单独升级并跑完整兼容测试。

## 已完成

- [x] 6619 订单“等待发货”误判“已发货”已现场确认，源码修复进入本地 `94bf110`，76 项相关测试通过，安全审计通过。
- [x] 确认 `94bf110` 直接继承 `37aa689`，当前本地代码包含桌面采购员默认设置可编辑功能。
- [x] 新刚“抽检数 1 实际超过 1”已匹配线上批次、确认数量丢失和进度分母两个根因，完成合成数据复现；已修复并随 v0.17.3 发布。
- [x] 保存本轮交接、项目记忆和开发任务，修改前备份原文档；按用户要求暂停。
- [x] v0.17.2 代码、发布目录、远端分支和 tag 已同步。
- [x] Windows/macOS 标准安装包构建、SHA-256 校验和线上上传。
- [x] 云端服务部署、数据库备份、迁移 0030 和公网 HTTP 200 检查。
- [x] 查询历史稳定 ID、最近更新时间排序、重查关联。
- [x] 环境创建历史和稳定任务 ID。
- [x] 逐环境执行耗时、平均耗时、本轮重查耗时、累计执行耗时。
- [x] 云端高级设置入口迁移到桌面客户端。
- [x] 查询结果状态说明帮助浮层和长错误详情展示。
- [x] 终态行收口、未执行行停止状态和异常重查。
- [x] Local API 全局门控、超时恢复、启动结果对账、关闭确认和资源恢复窗口。
- [x] `AUTO` 自动站点路由及旧执行器能力门禁。
- [x] HubStudio 3.58 中文缺内核错误识别。
- [x] 登录页二次确认。
- [x] Windows/macOS 桌面窗口比例调整。
