# Xynigo 当前开发任务

更新时间：2026-08-31 13:57 CST

本文件只记录当前有效任务。历史 Bug 与防复发经验见 [`docs/里程碑/20260828-v0.12.7统一入口执行器热修复与状态收口.md`](docs/里程碑/20260828-v0.12.7统一入口执行器热修复与状态收口.md)，当前发版证据见 [`docs/里程碑/20260831-v0.12.9号商接码链接兼容发布与共享测试部署.md`](docs/里程碑/20260831-v0.12.9号商接码链接兼容发布与共享测试部署.md)。

## 当前基线

- 共享测试：`0.12.9 / 0017_executor_workspace_rpc`，健康、就绪、页面和 OpenAPI 正常。
- 发布主线：`codex/release-v0.12.9-mailapi`，HEAD / 标签目标 `ba4a35463545fd628f0983b2b15bd31eb11954ac`。
- CI：候选 `33360795789`、固定产物 `33361380993`、标签 `33361549956`，均为七任务全绿。
- GitHub：`v0.12.9` 为 prerelease；Latest 仍为 `v0.12.6`。
- 云端默认包：Windows `Xynigo_Sourcing_Windows_Setup_v0.12.9.exe`；macOS `Xynigo_Sourcing_macOS_Standard_v0.12.9.pkg`；两个平台都保留同版本绿色包回退。
- 云端镜像：`sha256:fa1e23d4ae8f91c3283ebe6a422f79941a1bfd4831d64c9f8c120d5550c8b6a5`。

## 已完成：v0.12.9 号商链接兼容

- [x] 定位新表 18 行 `/api?type=html&mail=` 与旧解析白名单不兼容，确认邮箱、Cookie、列结构和其余 47 行旧链接无异常。
- [x] 实现严格白名单：精确路径、精确参数集、`type=html`、同行邮箱一致；错误/额外/重复参数 fail-closed。
- [x] 保持旧 `orderNo` 和 email query/path 格式兼容，并维持跨格式稳定引用与查重语义。
- [x] 修复校验失败仍显示上一批成功统计的问题。
- [x] 真实 65 行文件回归、桌面 413 项、云端 124 项、公开资源审计和三轮 CI 通过。
- [x] 发布 v0.12.9 prerelease 及 10 个资产，服务器端四个可下载包大小和 SHA-256 为 4/4 一致。
- [x] 完成源码、环境、PostgreSQL dump、旧镜像回滚点并部署共享测试；未部署生产。

## 已完成的版本收口

- [x] 冻结 v0.12.7 hotfix 列车，未创建 hotfix12、未移动旧标签、未覆盖旧资产。
- [x] 从完整 hotfix11 主线建立 v0.12.8，统一源码、Web、OpenAPI、安装包和资源目录版本。
- [x] Windows 标准安装、覆盖升级、安全卸载、状态中心和更新器自动验证通过。
- [x] macOS 标准包、应用载荷、绿色包和冻结运行时自动验证通过。
- [x] 修复“同一版本多次构建 Mac 包时旧顶层 manifest 哈希不刷新”，并增加回归测试。
- [x] 标签 CI 全绿后创建 v0.12.8 prerelease，10 个资产大小和 SHA-256 与锁定清单一致。
- [x] 部署前完成源码、PostgreSQL dump 和旧镜像回滚点；共享测试已切到 v0.12.8。
- [x] 验证公网健康/就绪、OpenAPI、Web 版本、Alembic、资源哈希和匿名下载 401。

## P0：真实设备切换验收

### 1. Windows 采购电脑覆盖安装与新号商表验收

- [ ] 从云端系统入口下载 v0.12.9 标准包，核对 SHA-256 `97ee3017261b0bebf262665e5977bb7f2261657cf8acae8f3dc2cde1b92cc7e6` 后覆盖安装。
- [ ] 验证原设备配对、`config.json`、日志、运行数据和 CurrentUser DPAPI 凭证保留。
- [ ] 重新载入 `65MX.xlsx`，确认 65 条账号全部通过、失败页不再显示旧批次统计，再人工启动建环境。
- [ ] 验证状态中心、托盘菜单、HubStudio 分组、物流查询、买家号文件解析、墨西哥/美国建环境和任务进度。
- [ ] 验证不会周期性闪出黑色控制台窗口，执行器退出后云端按 TTL 正确离线。
- [ ] 完成一次“发现更新 → 用户确认 → 安装 → 新运行时上线 → 旧进程退出”闭环。

验收：至少一台采购同事 Windows 电脑完成真实工作流；失败时保留脱敏日志、任务 ID、错误码和时间，不以重装掩盖原因。

### 2. macOS Apple Silicon 覆盖安装

- [ ] 核对标准包 SHA-256 `7284b21990ed48613e5359ce37cc944527ad96e6876ffdb7338eda6c84acfe42`，按内部未签名教程手动放行并覆盖安装。
- [ ] 确认 Keychain 凭证、设备配对和本地数据保留，不再反复弹出同一钥匙串授权。
- [ ] 验证冻结运行时使用随包 CA，通过 HTTPS 连云端并持续心跳。
- [ ] 验证 HubStudio 分组读取和至少一个只读 `workspace.rpc.v1` 任务。

### 3. 真实故障与回滚演练

- [ ] 断网、执行器退出、HubStudio 主程序退出但 Local API 子进程存活三个场景分别验证状态语义。
- [ ] 安装失败时验证绿色包显式回退，不自动覆盖已安装标准版数据。
- [ ] 在不触发真实采购外部写入的前提下演练服务镜像回滚和数据库备份可读取。

## P0：主工作区收口

- [ ] 逐项盘点 `codex/new-ui-migration` 未提交的系统订单键、采购助手、HubStudio、采购契约、文档、原型和输出目录。
- [ ] 区分“已进入 v0.12.8”“仍为内部候选”“纯业务输出/历史备份”，逐文件提交，不整树覆盖。
- [ ] `doc-fetch-resources/`、`outputs/`、品牌资产和历史备份按 `.gitignore` 与交付规则处理。
- [ ] 将复盘中的状态生命周期、权限负向矩阵、DPI、冻结运行时 TLS 和发布一致性继续转为自动化门禁。

## P1：稳定化任务

- [ ] Windows 安装包增加 Authenticode 可信签名和时间戳；Mac 增加 Developer ID、公证与 stapling。
- [ ] 将 Windows 在线更新的安装器交接、父子进程退出和失败回滚做成独立端到端测试。
- [ ] 为 HubStudio Local API 的端口、认证、限流、父子进程状态和错误码建立长期兼容矩阵。
- [ ] 持续监控 `workspace.rpc.v1` 租约、重复读取、进度合并、任务不确定态和敏感正文加密。
- [ ] 真实设备验收稳定后再决定是否把 v0.12.9 提升为 Latest；不得用“已部署共享测试”替代稳定版批准。

## 当前卡点

1. **未签名安装包**：Windows Smart App Control 无单应用放行能力；当前只能内部手动处理，长期必须签名。
2. **真实设备测试资源有限**：只有两位实际使用同事承担生产式测试，没有独立测试人员。
3. **主工作区较脏**：内部候选、已上线代码和业务输出并存，盲目合并或 `git add -A` 风险高。
4. **Latest 暂未提升**：v0.12.9 是内部切换 prerelease；真实设备验收完成前 Latest 保持 v0.12.6。
5. **生产仍未授权**：当前域名、服务器和数据库都是共享测试；不能把真实测试通过解释为生产上线。

## 禁止事项

- 不再为 v0.12.7 增加 hotfix12，不移动旧标签，不覆盖或删除历史 Release 资产。
- 不把阶段性的 `status-center-v2` 或历史 hotfix 安装包转发为升级包；统一使用 v0.12.9 目录中的锁定资产。
- 不把密码、Cookie、App Secret、Token、代理链接或完整收件信息写入仓库、任务、日志或截图。
- 不自动重跑外部写入结果不确定的物流、建环境、飞书或采购任务。
- 不在未确认真实设备验收、回滚点和生产闸门时把 v0.12.9 宣称为生产稳定版。

## 每次接手的最小检查

```bash
cd /Users/jeff/Documents/xynigo-sourcing
git status --short
git log -5 --oneline --decorate
git log -1 --oneline codex/release-v0.12.9-mailapi
gh release view v0.12.9
curl -fsS https://xynigo.samforo.icu/healthz
curl -fsS https://xynigo.samforo.icu/readyz
```

随后再通过受控 SSH 核对 OpenAPI `0.12.9`、Alembic `0017`、容器镜像和四个服务器资产的文件名、大小与 SHA-256。
