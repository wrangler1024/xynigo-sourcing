# Xynigo Sourcing 项目记忆

流程指引更新：2026-09-10（v1.1 分流方案经 Jeff 确认；本次未核验或改写线上业务状态）。下文 v0.17.3 状态为 2026-09-06 的历史快照。

本文件记录跨会话长期有效的工程事实、边界和发布规则。具体某一轮的现场状态见 `handoff.md`，待办见 `task.md`。

## 20260917 采购售后最新交接（私有增量）

- 核验时间：2026-09-17 14:47，Asia/Shanghai。用户安排：Codex 设计方案、写交接和更新记忆，**具体实现交给 ZCode**。新需求“系统匹配 0 单时到平台查找人工申请的退款并回访”目前仅完成源码核对与设计，尚未修改功能代码、运行新功能测试或部署。
- 完整开工方案、源码入口、协议字段、停止/恢复规则与验收清单见 [ZCode 开发交接](/Users/jeff/Documents/shein-dropshipping-ops/交付成果/20260917_Xynigo采购售后_平台查找退款单_ZCode开发交接.md)。该文件包含私有路径/测试范围，不得整体复制进公开仓。
- 当前可验证 main 位于 `/Users/jeff/Documents/xynigo-worktrees/after-sale-five-step-review`，HEAD `ab4c65abce7406b7c4a2949aebcba708c8b8e6f4`，工作区干净，与本地 origin/main 引用一致；本次未 fetch。常规检出位于侧会话分支 `codex/shein-store-auth-module-design`，HEAD `fea02c8059355c6eaefafe86cb3d3ea07bdbb12c`，不要切换或覆盖其工作；开工重新核验，不从已合入的旧售后分支继续开发。
- 最近部署记录为 v0.18.7：发布 SHA `ab4c65a`，构建 SHA `0312635c8bc65f9569190f97dfd708a5baca6518`，双平台 runtime `0.18.7-0312635c8bc6`，迁移 `0043_after_sale_phase_evidence`，内部 test 通道/GitHub Prerelease。证据见 `/Users/jeff/Documents/shein-dropshipping-ops/.codex_tmp/release-0187-20260917/deployment-complete.md`；本次未重新探测线上。较早文档中的“候选/未发布”属于开发时点，不能覆盖发布证据。
- 上次检查 Jeff 客户端仍 0.18.5，仅提示升级；新云端不等于员工执行器已升级。当前 `track/resolve` 只查本租户提交/跟踪历史，“匹配 0 单”不代表平台没有退款。
- 推荐实现：独立的只读发现用途 `refund_discovery` + 拟新增能力 `after.sale.refund-discovery.v1`，复用 scan 任务运输；发现真实订单/退款身份后再创建现有固定 items 的 track 任务。平台查找覆盖本次全部输入环境，保留历史快路径，不能放宽跟踪身份白名单或把人工退款伪造为系统提交。
- 同一执行器的物流与售后仍受 BUSINESS 任务互斥；两阶段顺序运行。停止、部分失败、阶段间忙碌、刷新页面均需明确恢复规则，不从历史页面自动接续、不抢占正在使用的环境。上限沿用 300 环境/500 退款单，分页不完整不得报无退款。
- 保持 v0.18.7 五节点及审核未通过/待补充凭证精准识别；回访失败保留最近有效结果。新能力需要配套执行器发布，不能仅热部署页面；本文不代表新功能已获发布验收。
- 本次只增加私有交接和本节记忆，未提交/推送。原记忆备份：`/Users/jeff/Documents/shein-dropshipping-ops/历史备份/20260917/Xynigo平台退款发现交接前_20260917_144737/PROJECT_MEMORY.md`。下文旧流程与版本段落保留其原有历史时点，具体开工以当前正式规约和最新 Git/部署证据为准。

## 当前流程入口

- 交付先按“兼容现有客户端的云端更新 / 需要客户端更新 / 特殊迁移或权限边界变化”分类。兼容云端更新不默认要求完整产品发行；安装包、Git tag 和 Prerelease 等步骤按适用路径执行，不能继续套用下方历史清单。
- v1.1 公开文档修订位于 `codex/shared-test-flow-v1-1`，尚待独立评审后合并；本轮修订入口为[部署与发布手册](/Users/jeff/Documents/shein-dropshipping-ops/.codex_tmp/共享测试部署流程_v1_1_20260910/xynigo-sourcing/docs/部署与发布手册.md)。合并后统一使用正式仓的 `docs/部署与发布手册.md`，开发检查与交接格式分别见 `CONTRIBUTING.md`、`docs/协作与交接.md`，不在记忆中再维护第二套操作命令。
- 既有授权范围内不逐条命令重问；文档修订不授权实际部署，也不表示一键部署、运行时停收、自动版本展示或按需 CI 已实现。当前产品/环境状态以新任务实际核验为准。
- 本文件为本地未跟踪的内部记忆，不纳入公开源码提交；公开流程以已版本化文档为权威入口。

## 20260906 基线（历史）

- 当前共享测试版本：`v0.17.3`；发布标签与远端分支指向 `92e29d9`。
- 当前开发分支：`codex/local-config-desktop-migration`。
- 已发布原始提交：`37aa689`（桌面默认数据源）、`94bf110`（待发货状态）、`5bf30d5`（抽检数量与进度）。
- 双平台运行时：`0.17.3-05f24ca27a7a`；`05f24ca` 还包含本轮 CI 暴露并修复的 Windows 卸载孤立进程问题。
- 云端工作台：`https://xynigo.samforo.icu/`，共享隔离测试环境；不涉及独立生产实例。
- 数据库唯一 Alembic head 仍为 `0030_logistics_auto_site`，本版无迁移，数据库容器未重启。
- Windows/macOS 继续使用内部 `test` 通道，签名与公证状态未提升；GitHub v0.17.3 为 Prerelease，包含 10 个已核验资产。
- 最终提交的 8 项 CI 全通过：https://github.com/wrangler1024/xynigo-sourcing/actions/runs/34024040101
- 本地三份开发记录仍未跟踪，业务代码已提交并推送。

## 系统边界

- 云端负责身份、权限、任务、查询/建环境历史、结果缓存和协作展示；不直接连接员工电脑的 HubStudio。
- 本地执行器主动通过 HTTPS 出站长轮询领取任务；HubStudio Local API、CDP 和 SHEIN 页面解析只在被选择的电脑执行。
- `src/purchase_tool/web/index.html` 是云端与本机旧工作台的唯一 Web UI 源。修改后必须运行：

  ```bash
  python3 cloud/auth-service/deploy/sync_web_workspace.py
  ```

- 本机物流高级设置只在桌面客户端“本机设置”维护；云端查询操作栏不提供编辑或回显按钮。
- 稳定查询 ID、环境创建任务 ID 保留在历史列表和历史详情中，不占用实时执行操作栏。

## 20260905 已验证的新问题

- 墨西哥订单文案 `Esperando ser enviado` 表示等待发货。旧正则只命中其中 `enviado` 会误判“已发货”；本地提交 `94bf110` 已按完整短语修复，兼容大小写和空白，76 项相关测试通过。
- 20260905 建环境抽检故障（20260906 已在源码修复）：桌面保留数值配置，Web 页面却先将其变成勾选开关，再以整批数量生成 `verifySampleCount`。只要开启检测，配置 1、3 或 10 都可能提交 100；后端选样收到 1 时仍只选 1。
- 20260905 IP 检测进度故障（20260906 已在源码修复）：执行器运行中只上报行结果，没有上报 `ipCheckTotal`；云端汇总仍为 0 时，Web 用已返回结果数量作分母，出现检测中“30/30”而顶部仍为“0/0”。
- 6887 的 `Connection timed out` 指向 Windows 执行器读取订单列表阶段的浏览器通信超时。已读到时区、未读到订单号，约耗时 146 秒；缺少具体 CDP 指令/堆栈，代理、页面阻塞、机器负载等底层诱因均未确认。
- 本机 HubStudio 3.58.0 的可选 Chrome 内核列表最高为 148，更新中心无更高版本；官方接口下载 150 返回 `-10018`，SDK 原因 `eTag is nil`。这只是当前客户端观察，不能表述为所有 Mac 永远不支持 150。安全 DNS 调整和重试无效，已恢复原关闭配置（下次重启生效）。

用户于 20260906 已授权补 CI、交付三个提交和部署；现已完成 v0.17.3 发布。现场业务回归及未解决反馈继续见 `task.md`、`handoff.md`。

## 20260906 抽检修复规则

- 抽检数量由执行器本机配置决定。本机正式创建和重试接口忽略请求中的旧 `verifySampleCount`，在受理时读取本机值并固定到本轮任务，运行中改配置不会改变本轮抽检量。
- 云端以最近的执行器配置摘要派发数量；新版执行器开始后回报本轮实际采用值，更新任务 JSON 摘要。没有新增数据库列或迁移。
- Web 创建入口只展示抽检设置，取消“验证全部”的覆盖开关。本机旧设置页保留数值输入，0 表示关闭。
- 单行和批量失败重试仅从本轮重试成功的环境中选样，排除上一轮成功环境；样本不足时按实际成功数封顶。
- 新云端任务显式携带 `ipVerificationProgress=1`，新版执行器才回传扩展的 `ipVerification` 元数据，兼容旧云端严格的 rows-only 进度契约。
- 云端分别维护实际已检测/通过数量及计划总数，Web 显示真实分母。旧记录计划数未知时显示“计划数待确认”；停止或异常结束不伪装成全部检测完成。
- 抽检修复开发使用脱敏样例，未创建真实环境或改写历史业务结果；随后已按用户授权打包部署 v0.17.3。

源码修复阶段验证：本地 694 passed、3 skipped，云端相关 41 项通过。发布阶段重新跑本地全量 692 passed、5 skipped（新包生成前），生成新包后 45 项安装器/发布专项通过；云端全量 183 项、目录/鉴权专项 32 项通过，最终 8 项 GitHub CI 全部通过。

## 20260906 CI 与 Windows 卸载经验

- CI 必须使用 pytest 覆盖 unittest 类与 pytest 函数；本地测试依赖在 `.[test]`，云端使用 Python 3.12 与锁定运行依赖。Web 测试显式提供 Node.js 22。
- Windows 卸载失败不能只延长等待。实测残留 `pythonw.exe` 脱离启动器后仍锁住 DLL，`taskkill /T` 无法保证清理。
- 卸载前只按本安装目录精确识别启动器和 `versions/<revision>/python-embed/python(w).exe`，通过 CIM 清理并确认退出；不按 Python 映像名全局结束进程。
- Windows CI 验证本目录孤立运行时退出、其他 Python 存活、运行目录删除且用户数据保留。
- 部署脚本先传至服务器文件再执行，避免 `bash -s` 与 `docker compose exec -T` 争用标准输入造成脚本被提前消费；必须验证部署完成标记及实际线上版本，不能只看退出码 0。

## HubStudio 与物流查询可靠性约束

### Local API

- 团队 Local API 配额已提升到每分钟 300 次，但现场主要瓶颈是 HubStudio 本地进程和浏览器资源，不是云端配额。
- 执行器继续使用全局错峰和在途请求上限；不能为了追满 300 次额度而放大本地监听器抖动。
- 所有 `127.0.0.1` Local API 请求必须显式绕过系统代理。Clash/TUN 可能把默认 HTTP 客户端的回环请求错误送进代理。
- `browser/start` 和 `browser/stop` 控制请求需要串行提交；不同环境的页面解析可以并行。
- `browser/start` 请求超时不代表启动已取消。必须先按精确 `containerCode` 查询生命周期，确认没有已打开或仍在打开的实例后，才允许受控重试。

### 常见错误码

- `-10005`：上一次 `startBrowser` 尚未结束。不能立即重复 start；应轮询生命周期、必要时清理孤儿窗口。
- `-10008`：系统资源不足。Local API 仍可能可达；应降低压力、等待恢复，不能把它描述为网络断开。
- `E010205`：Local API 频率限制。执行退避，并由全局请求门控统一错峰。
- `code=-1` 且文案类似“需使用‘150’版本内核”：HubStudio 3.58 的本地化缺内核响应。v0.17.2 已将它归类为 `hubstudio_browser_core_missing`。

### 浏览器内核修复

- 内核修复能力从 v0.13.20 起存在，但属于“自动识别 + 管理员确认后下载并验证”，不是查询过程中静默下载。
- 下载必须调用 HubStudio 官方 `/browser/download-core`，完成后启动真实报错环境并通过 CDP 验证内核大版本，再安全关闭。
- 直接运行 `hubstudio-cli start-browser` 不会修改 Xynigo 执行器进程内的修复状态；验证桌面“下载并修复”入口时，要让错误经过 Xynigo 查询/预检链路。
- 当前修复需求保存在执行器内存；执行器重启后提示可能消失。是否持久化为后续增强项，见 `task.md`。

### 跨电脑登录态

- HubStudio 登录态跟环境实例副本走；关闭并完成归档后才同步到云端。
- 同一环境在两台电脑先后或同时打开时，最后关闭归档的一方可能覆盖云端状态。禁止把“支持多用户打开”理解为登录态合并。
- 跨电脑切换环境前，必须等待前一台电脑完全关闭并完成归档；现场排查一次只打开一个环境，检查后关闭并确认无残留。
- v0.17.2 不再仅凭一次 URL 跳转判“登录失效”：需要登录 URL、可见登录界面和二次导航仍然成立。
- 这只能降低瞬时跳转误判，不能替代 HubStudio 的跨设备归档规则。

### 环境与站点解析

- v0.17.2 支持 `AUTO`：按每个环境已打开的 SHEIN host 识别美国站或墨西哥站；清晰的环境名 `US/MX` 仅作空白浏览器降级信号。
- 输入环境序号时在全部 HubStudio 环境中查找，不按网页站点或分组校验。
- HubStudio 分组只用于批量导入序号，不决定查询范围或解析站点。
- 混合或无法识别的站点不能静默猜测，必须返回可重查的明确错误。
- `env-list` 的有效分页参数是 `current + size`，单页上限 200；按序号排查必须翻页后用 `serialNumber` 过滤，不能依赖 `containerCode=<序号>`。

## 数据与安全

- 不将账号密码、Cookie、API Key、访问令牌、真实代理凭证或真实订单内容写入 Git、普通日志、测试夹具和交接文档。
- 本机 `config.json`、运行数据、查询日志和构建产物不得进入提交；提交前运行 `python3 scripts/audit_public_release.py` 和 `git diff --cached` 自查。
- 环境查询截图和 DOM 只用于本机故障排查；输出结论时使用序号和脱敏状态，不复制账号或收件信息。
- 真实平台写入、批量创建、删除环境或自动登录必须有用户明确授权。

## 20260906 测试与发布惯例（历史）

以下保留当时完整发版的执行背景，不是每次共享测试部署的必做清单。当前任务使用上方 v1.1 流程入口；迁移仅在需要且兼容时执行，桌面资产与 Git tag 校验仅在相应客户端/产品发行时适用。

- 本地测试使用仓库 `.venv` 与 `PYTHONPATH=src`。
- 云端服务要求 Python 3.12，使用 `uv run --project cloud/auth-service ...`；不要用 macOS 系统 Python 3.9 跑云端测试。
- `alembic heads/current` 必须在 `cloud/auth-service` 配置上下文执行。
- Windows 交叉验证不能直接运行 Windows 测试二进制；在 macOS 上使用 `GOOS=windows GOARCH=amd64 go test -c` 做编译验证。
- Windows 与 macOS 构建脚本会写同一份更新 manifest，应顺序构建，避免并发覆盖。
- 标准发布采用两段提交：先提交代码和版本号，从该干净提交构建双平台安装包；再把实际 runtime ID、SHA-256 和大小回填 `local_executor_release.py` 并提交，最后在第二个提交上打 tag。
- 云端部署前必须备份 PostgreSQL 和当前代码，先迁移再切换认证服务；不得重建或复制数据库卷。
- 部署后至少验证：公网标题版本、HTTP 200、容器状态、唯一迁移 head、双平台资产 SHA-256、启动错误计数和 Git 远端 tag。

## 不要重复踩坑

- 页面显示“HubStudio Local API 正常”只证明轻量接口可用，不证明浏览器内核齐全或 `browser/start` 可用。
- Local API 错误文案和错误码会随 HubStudio 版本、本地化语言变化；判断应结合错误码、操作类型和经过脱敏的文案模式。
- 查询成功不等于环境已经关闭归档。关闭确认失败需要保留后续清理状态，不能直接复用同一环境。
- 长错误文本不能直接撑开结果表；表格只显示短标签和“详情”，完整错误放弹层。
- 远程服务器可能存在 macOS 扩展属性产生的 tar 警告；打包时排除 AppleDouble 文件，警告本身不等于源码损坏，最终以镜像构建和哈希验证为准。
