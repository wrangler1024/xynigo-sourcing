# Xynigo Sourcing 项目交接

更新时间：2026-08-26

这是新会话和新开发者的第一入口。长期业务规则见 [`docs/项目记忆.md`](docs/项目记忆.md)，滚动技术细节见 [`docs/当前开发交接.md`](docs/当前开发交接.md)，后端路线见 [`docs/后端实施交接.md`](docs/后端实施交接.md)。

## 一页结论

- 内部开发分支：`codex/new-ui-migration`；完整 `v0.12.3` 实现提交为 `a8d3fbd`。接手时仍须先核对 `git status`，不要覆盖用户未纳入代码提交的业务输出。
- 当前源码与共享测试云端版本均为 `0.12.3`；GitHub `v0.12.3` Release 已发布。
- 线上协同测试版：GitHub `v0.12.3`，latest、非 prerelease，包内渠道为 `test`；公开净化提交为 `33f558bd53e3955f41b5f17af9bb27cf233bfb17`。
- 共享测试云端：`https://xynigo.samforo.icu`，2026-08-26 复核 `/healthz` 和 `/readyz` 正常。
- 云端 Web 工作台已在上述域名根路径部署，并与部署前本机工作台共用 `src/purchase_tool/web/index.html` 这一份前端源码；不维护单独的云端版视觉或导航。浏览器可直接完成飞书登录并访问采购任务、采购执行、买家号安全摘要、日志和成员会话；员工电脑不启动 Xynigo 本地服务也可使用这些云端功能。本机执行器离线时只暂停 HubStudio/CDP/SHEIN 等本机操作。
- 浏览器 favicon 固定为原版 X 图标：云端 `/favicon.ico` 必须与 `xynigo-x.ico` 字节一致，页面当前使用 `v=6` 缓存版本；不得再同步为犀牛吉祥物图标。
- 共享测试数据库：当前迁移为 `0013_purchase_task_feishu_mirror`；2026-08-26 独立插件首次真实提交暴露“应用代码已升级、数据库仍停在 0012”的部署错位，导致采购查询/提交读取缺失列并返回 500。已先完成 `pre-0013-purchase-task-feishu-mirror-20260826T184815.dump` 备份及清单校验，再补迁移到 head；失败订单未留下半成品记录，健康/就绪探针正常。
- 本机当前没有 Xynigo 服务监听；`127.0.0.1:8765` 是无关的 `ivyea_agent`。插件独立云端模式不依赖本机服务。此前 8767 报告过 `currentVersion=0.12.3`，但当前已退出，不要把本地文件页误认为服务正在运行。
- 回归基线：内部与公开树本机均为 299 项完成（296 通过、3 项 Windows 条件跳过），云端均为 72 项通过；`v0.12.3` GitHub Actions 六个发布任务通过，旧版 `v0.12.1` 更新器已真实发现新版本。
- 当前不是生产上线。生产服务器、正式域名、正式数据库和正式飞书业务路由仍受上线闸门阻断。

## 里程碑总览

| 日期 | 里程碑 | 状态 | 结果 |
|---|---|---|---|
| 2026-08-24 | 前端产品框架改造 | 完成 | 一级产品导航、二级功能入口、多页签工作区和规划态边界完成 |
| 2026-08-24 | 云端认证服务骨架与测试部署 | 测试已部署 | HTTPS、飞书 OAuth、租户白名单、会话与本地一次性登录桥完成 |
| 2026-08-26 | 云端 Web 工作台 | 共享测试已部署 | 域名根路径直接访问；复用部署前原版 UI，本机服务关闭时仍可登录和使用云端采购、资源、日志与成员功能 |
| 2026-08-24 | 成员手机号邀请闭环 | 测试已部署 | 手机号只用于飞书精确解析，不落库；未知成员保持待审批 |
| 2026-08-25 | 内置管理员、权限与资源中心 P0/P1 | 测试已部署 | RBAC、高敏权限双层裁决、店铺/环境/代理只读对账完成 |
| 2026-08-26 | 代理 IP 三类业务目录 | 本地完成，未部署 | 711 动态住宅用于采购 API 提取；Webshare 静态数据中心用于店铺环境；静态住宅暂无场景 |
| 2026-08-25 | 运营采购助手统一身份与正式提交 | 测试已部署 | 插件经本机安全桥提交 PostgreSQL/Outbox，重复提交幂等 |
| 2026-08-25 | 采购认领、分单和执行队列 | 测试已部署 | 行锁认领、乐观锁分单、个人执行队列完成；真实下单未开放 |
| 2026-08-25 | v0.11.0 合并与 v0.11.1 登录闭环 | 已发布并验收 | 合并本地执行器与云端权限；修复飞书登录轮询竞争 |
| 2026-08-26 | 采购契约 v2 与运营归属 | 测试已部署 | 店铺基础名、运营姓名独立入库；利润/利润率读取和筛选完成 |
| 2026-08-26 | 运营采购助手独立云端登录 | 插件本地完成，待真实验收 | 扩展无需本机服务或 Xynigo 页面，飞书登录后直连云端；失败不切换选项卡、不写备注，首次成功切到“备注信息”并最多预填一条 XYP2，仍由运营保存 |
| 2026-08-26 | 采购提交 500 迁移错位修复 | 共享测试已修复 | 应用已引用 0013 字段但数据库停在 0012；备份后补迁移到 head。随后该测试单云端提交 200，页面因扩展重载丢失回调，已补远端回查和备注恢复入口 |
| 2026-08-26 | 未认领采购单修订 | 共享测试已部署 | 原提交运营可修改仍在公共池的原采购单；认领/分单/执行后拒绝，修订不新增第二条客服备注 |
| 2026-08-26 | 新版采购中心与安全退回 | 测试已部署 | 公共认领池、个人执行工作台、批量认领、快捷下单入口和安全退回完成 |
| 2026-08-26 | HubStudio 请求频率优化 | 代码与测试完成 | 0.3 秒全局错峰、`E010205` 共享冷却、半完成环境接管 |
| 2026-08-26 | 业务操作日志 P0 / 系统运行日志 P1 | 共享测试后端已部署 | 结构化脱敏日志、权限、查询 API、页面和迁移已进入共享测试环境；角色/跨租户专项验收仍待补 |
| 2026-08-26 | 采购下单闭环 P1 后端纵切面 | 后端已部署、前端本地完成 | 买家号资源账本、下单尝试、数量锁、付款批次、失败补偿和物流状态机已部署；快捷绑定安全起始段已本地接通，当前 8767 尚未加载，真实下单执行仍未接入 |
| 2026-08-26 | v0.12.0 全员测试版 | 已发布 | Windows/macOS 双平台包、清单、SHA-256 和线上更新发现通过 |
| 2026-08-26 | 采购日用结果双写与 v0.12.1 | 已部署并发布 | 建环境/物流真实结果写 PostgreSQL 并同步独立飞书测试表；双平台测试包发布 |
| 2026-08-26 | 买家号数据库事实源与旧表解耦 | 共享测试已部署 | 资源中心改查 PostgreSQL；旧表 320 条完整凭证加密迁移；41 字段独立测试 Base 持续单向镜像 |
| 2026-08-26 | v0.12.3 云端工作台与完整买家号测试版 | 已部署并发布 | 云端原版 UI、X favicon、完整买家号权限链路和双平台在线更新包完成；仍非生产上线 |

关键证据文档：

- [`docs/里程碑/20260824-云端认证测试部署.md`](docs/里程碑/20260824-云端认证测试部署.md)
- [`docs/里程碑/20260825-运营采购助手统一身份与正式提交基础.md`](docs/里程碑/20260825-运营采购助手统一身份与正式提交基础.md)
- [`docs/里程碑/20260825-采购执行测试版云端联调.md`](docs/里程碑/20260825-采购执行测试版云端联调.md)
- [`docs/里程碑/20260826-采购契约v2与运营归属测试部署.md`](docs/里程碑/20260826-采购契约v2与运营归属测试部署.md)
- [`docs/里程碑/20260826-v0.12.0采购中心与环境创建优化发布.md`](docs/里程碑/20260826-v0.12.0采购中心与环境创建优化发布.md)
- [`docs/里程碑/20260826-采购下单闭环P1后端纵切面.md`](docs/里程碑/20260826-采购下单闭环P1后端纵切面.md)
- [`docs/里程碑/20260826-采购日用结果双写与v0.12.1发布.md`](docs/里程碑/20260826-采购日用结果双写与v0.12.1发布.md)
- [`docs/里程碑/20260826-买家号数据库事实源与旧表解耦.md`](docs/里程碑/20260826-买家号数据库事实源与旧表解耦.md)
- [`docs/里程碑/20260826-v0.12.3云端工作台与完整买家号发布.md`](docs/里程碑/20260826-v0.12.3云端工作台与完整买家号发布.md)
- [`docs/买家号资源接口-P0.md`](docs/买家号资源接口-P0.md)
- [`docs/飞书买家号安全同步测试表.md`](docs/飞书买家号安全同步测试表.md)

## 当前系统状态

### 本机执行器与发布

- `v0.12.3` 汇总日用结果双写、买家号 PostgreSQL 事实源、完整凭证权限链路以及云端原版 Web 工作台。
- Windows x86_64 与 macOS ARM64 绿色包已发布；`v0.12.1` 更新器能发现 `v0.12.3`，下载后仍需用户在运行窗口明确确认。
- 发布采用独立公开净化分支，不把内部运维文档、真实基础设施信息或凭证带入公开仓库。
- 当前 8767 是已有源码开发进程，最新只读状态已报告 `0.12.3`；本轮没有主动停止或重启，后续更新仍必须避开采购联调并安排正常窗口。

### 云端身份、权限与采购中心

- 飞书登录、成员待审批、手机号邀请、角色、权限、会话撤销和租户隔离已进入共享测试环境。
- 当前权限目录为 29 项；`super_admin` 持有全部权限，内置 `admin` 固定排除三项高敏配置权限，普通 `member` 默认无权限。
- 采购契约同时接受 v1/v2；v2 独立保存 `storeBaseName`、`operatorName`，括号内运营组别暂不解析。
- 采购任务是团队公共待认领池；采购执行只展示当前采购员本人认领的采购单。
- 支持批量认领、店铺/运营筛选、销售金额、采购指导额、预估利润、利润率和采购员归属。
- 未分单、未开始且属于当前采购员的采购单可以填写原因退回任务池；已有分单或已进入后续流程时服务端拒绝。

### 已部署基础与尚未接入的外部执行

- 下单尝试、数量占用、付款结果、正式采购批次、失败补偿和物流回填的后端第一纵切面已部署到共享测试云端，契约见 [`docs/采购下单闭环-P1.md`](docs/采购下单闭环-P1.md)。
- 前端“开始 / 继续本次下单”仍未接新 API；真实 Hub 环境创建/删除、买家号登录、SHEIN 结算付款和不确定结果接管尚未实现。
- 买家号事实账本、完整凭证加密、查询、快照同步契约和事务预占已完成并部署；资源中心、查重、占用和下单统一查询 PostgreSQL。旧表「买家号（统一）」只保留为一次性迁移源，320 条完整测试记录已经迁入数据库。
- 独立飞书 Base「Xynigo 买家号资源同步测试表」与旧台账无关联，买家号表已扩展至 41 字段，完整账号、密码、Cookie、接码资料和业务字段由 Worker 按 outbox 持续单向镜像。Base 不回写数据库，双向同步是明确禁止的边界。
- 业务日志、系统日志和下单闭环代码及迁移已进入共享测试环境；前端真实执行和角色/跨租户专项验收仍未完成。
- 本地执行器仍缺云端设备配对、可撤销设备身份、租约任务、持久化续作与统一任务审计。

## 已确认的核心业务规则

### 认领不等于分单

- 认领只决定采购员归属，认领后采购单应立即进入个人采购执行范围。
- 是否分单、每批包含哪些 SKU 和数量，必须在真实下单与付款阶段结合优惠券、商家门槛和实付情况决定。
- 一单一 SKU 一件可以快捷下单；一 SKU 多件或多 SKU 多件不能在认领阶段固化最终分单。
- 后端必须同时维护需求数量、尝试占用数量、已采购数量和剩余数量，防止重复采购与超买。

### 买家号和 Hub 环境是两种资源

- Hub 环境一旦登录过买家号就不能回公共池，也不能绑定其他买家号。
- 下单失败或主动放弃：若买家号本身无风控，可解除本次占用继续使用；已登录的 Hub 环境必须从 Hub 中删除。
- 付款成功：Hub 环境必须保留并绑定正式采购批次，用于查单、跟单、物流和售后，不能删除或复用。
- 风控或账号异常时，买家号进入人工检查，不得自动恢复可用。
- 成功订单买家号何时可再次使用、成功环境何时归档、尚未登录即取消的环境能否回池仍待业务确认。

### 归属、数据和隐私

- 店铺/运营归属由插件随采购需求提交，采购认领不得覆盖运营归属；后续绩效要支持运营、采购员和团队口径。
- 利润和利润率只使用插件提交的 `estimatedMetrics`，历史数据缺失时显示 `—`，不得自行推算。
- 收件人、国家可在列表按最小必要范围展示；电话、邮编、详细地址和采购链接只在详情按权限读取并审计。
- 未来隐藏店铺、运营、销售金额、利润和利润率必须由后端角色策略裁剪响应，不能只隐藏前端列。
- PostgreSQL 是业务事实源；飞书 Base 是协同镜像，写入必须使用 Outbox、幂等、有限重试和写后回读。

## 过渡期采购协作导入现状（2026-08-26）

- 已新增“小犀助手 → 采购协作导入”，读取店小秘按产品行导出的 xlsx 和完整 XYP2 备注。解析/下载不写外部系统；用户选择普通飞书电子表格、通过只读校验并二次确认后，导入任务才规范表头、追加数据、设置分组底色/采购超链接并补齐订单商品图。
- 新下载 Excel 为 A:AQ 43 列。表头分为采购需求区、采购下单区、系统追踪区，使用三色和表头批注说明；不增加第二表头行。采购需求区按标准顺序规范，采购下单区不校验字段名/数量/顺序，系统追踪区只强制`系统订单键 / 导入批次 / 数据版本`。下单截图、物流截图作为默认下单字段展示，但仍不参与校验。
- 采购下单区默认在`买家号`后提供`付款卡号 / 采购订单号`；旧`平台订单号`只改表头语义，已有单号原值不变。系统追踪区在`系统订单键`后提供`导入操作人`，新 API 导入行自动记录当前 Xynigo 登录用户；该字段表示“谁执行了导入”，不能替代业务责任字段`采购员`，历史行无可靠来源时保持空白。
- A 列为`分单日期`，按北京时间生成并通过飞书/Excel 真实日期类型写入，展示格式固定为`yyyy-mm-dd`。不再保留语义重复的`分单批次`；稳定幂等批次只存系统追踪区`导入批次`。旧表删除`分单批次`前必须逐行确认其非空值均被`导入批次`等值覆盖，否则停止迁移。采购下单区的`下单批次`仍表示优惠券和付款方案确定后的真实结算批次。
- 采购需求区固定前五列为`分单日期 / 采购员 / 销售订单号 / 店铺 / 运营`，工作表冻结首行和前五列；随后依次为`包裹号 / 采购状态 / 优先级`。共享表使用`包裹号`而不是`采购单号`，并必须向采购提供`收货人姓名 / 国家 / 州省 / 城市 / 地址1 / 地址2 / 邮编 / 电话`。旧表在确认导入后规范旧别名、完整收货信息和商品金额，再按标准采购需求顺序移动整列；采购下单区自定义字段不参与校验。
- `销售订单金额`只写每单第一条有效明细，避免 SUM 重复；`商品金额`在每条明细写`产品售价 × 数量`，用于商品口径统计。XYP2 原价、优惠券、采购币种、订单规格、销售产品单价和首行凑单补差继续写入「采购备注」。
- 协作表只需要订单商品图。店小秘导出内嵌“产品图片”在无 Pillow 环境下从 OOXML DrawingML 读取，并转换成 Excel 可复制的单元格图片；采购商品图列及 SHEIN 商品页网络取图流程已全部删除。
- Web 工作台解析预览按需从本机短时解析计划读取订单商品图，展示 62×62 缩略图并支持点击打开原图；图片接口受 `assistant.access` 权限保护，不把图片 Base64 塞入解析 JSON。
- 飞书目标不固定：每个解析计划都可粘贴新的 `/sheets/` 链接，先读取工作簿、人工选择工作表，再按字段名校验采购需求区和系统追踪区。采购下单区完全不校验，采购可自定义扩展与调序。目标只在该本机短时计划中保存，不写项目配置。
- 命名决策（2026-08-26）：企业飞书普通电子表格文件名使用“采购执行协作表”，当前工作表名为“采购执行协作区”。文件名表达收单、分单、下单、付款、物流和跟单的完整采购执行范围；“共享”仅是协作方式，不再作为业务名称。系统不得硬编码文件名、工作表名或 token，固定契约仅为采购需求区、系统追踪区和`数据版本`等表头字段；采购更换表格或改名后仍通过链接选择和字段校验接入。
- 导入批次改为“文件名 + 12 位业务内容摘要”：同一份业务数据重新解析仍使用同一批次号。写入前按导入批次和行签名回读，每 40 行分块追加；中断重试只续传缺失行，检测到超出计划的重复/异常行则停止写入。
- 一体化导入任务先在用户确认后用有序批处理规范旧表字段并插入缺失需求列，回读通过后按真实表头投影追加类型保真数据。整批行签名回读一致后，再设置表头分区、六组高区分订单浅色、52px 行高、采购超链接和订单商品图。图片列、链接列和整行末列都动态定位，不再硬编码 M/N。幂等重试不覆盖采购员后续人工任务色。
- Web 预览和协作表中的“商品金额”是明细口径：`产品售价 × 单个产品数量`，使用销售订单币种显示；它不替代每单仅首行写入的“销售订单金额”。销售订单和包裹号及店铺/运营均使用上下两行展示。
- Web 预览、导出 Excel 与 API 新增行均按“销售订单号 + 包裹号”分组使用蓝、橙、绿、紫、黄、粉六组高区分浅色循环；同组明细同色，新组首行带分隔线。Excel 图片列宽由 14 缩至 10.5、数据行由 70pt 缩至 52.5pt；飞书行高为 52px，适合每天追加数百单。该底色仅用于初始识别，采购员后续按整行颜色分配任务时可直接覆盖。
- 最新 XYP2 实测导出 `order_20260826140050682_945257.xlsx` 已只读验证：4 单/6 明细、6 张订单商品图；当前代码模板升级为 A:AQ 43 列。
- 新增/扩展 `tests/test_procurement_import.py` 与 `tests/test_lark_sheet_sync.py`；已覆盖类型保真追加、稳定批次、部分续传、重复阻断、图片幂等和写后回读。真实飞书结构写入和回读已完成，仍待用新店小秘导出验收首批完整数据、链接和图片追加。
- 2026-08-26 本次字段改造目标测试 15 项全部通过；本机全量回归运行 299 项，296 项通过、3 项 Windows 条件跳过、0 项失败。企业飞书“采购执行协作表”的“采购执行协作区”已从 revision 123 的 A:AR 44 列迁移为 revision 131 的 A:AQ 43 列，共 41 条非空数据行：14 个旧`分单批次`均被`导入批次`等值覆盖后安全删列，14 个`分单时间`转换为真实`分单日期`，前五列与采购需求区顺序已规范并冻结。迁移前后全部 199 个物理数据行按逻辑字段逐项比对为 0 差异；未追加采购数据。

## 当前卡点

1. **外部执行协议缺失**：后端状态机已完成，但没有“云端任务 → 本地执行器 → HubStudio/SHEIN → 脱敏结果回报”的设备身份、租约和幂等协议。
2. **双资源生命周期未完成**：买家号 P0 账本及下单事务预占已完成，但自动抽号/超时释放、Hub 环境独立租约、环境真实创建/删除、风控冻结和成功后复用时点尚未形成完整闭环。
3. **前端尚未接新状态机**：组合、资源、付款结果、失败清理和物流时间线仍缺可操作界面。
4. **最小权限与字段权限未收口**：真实成员此前用管理员角色验收；采购员角色、数据范围与敏感经营字段策略仍待配置和联调。
5. **日志专项验收未完成**：`0005_business_operation_logs_p0`、`0007_system_runtime_logs_p1` 已部署，但最小角色、本人/租户范围和跨租户 404 仍需专项复核。
6. **飞书协同仍有边界**：买家号、建环境和物流结果 Worker、有限重试和写后回读已部署；Webshare 普通表格授权仍未完成。买家号 Base 设计为数据库→Base 单向镜像，不计划开放通用双向同步。
7. **执行器控制面缺失**：多电脑设备在线状态、任务租约、断网续作和云端统一审计还停留在路线设计。
8. **生产闸门未关闭**：主机加固、正式 DNS/TLS、生产密钥、正式数据路由、异机备份、监控告警和灰度方案未验收。
9. **业务待确认项**：成功订单买家号复用时点、成功环境归档/清理、未登录环境取消后的回池规则、运营组别解析均未定。
10. **普通飞书表格直接导入的团队运行依赖未产品化**：当前使用本机 `lark-cli` 用户授权，不硬编码共享表；开发机已具备，采购电脑的安装/授权与后续云端用户 OAuth 代理仍需选型和验收。

## 下一步计划

### P0：完成 v0.12.3 团队验收

1. 在采购电脑按现有绿色包流程从 v0.12.1 更新到 v0.12.3，不覆盖配置、日志和运行数据。
2. 使用运营采购插件验证“提交 → 公共任务池 → 批量认领 → 个人采购执行 → 安全退回”。
3. 重点复测利润/利润率、店铺/运营字段、处理下拉、固定操作列及 HubStudio `E010205` 限流恢复。
4. 只记录脱敏结果；不在本轮顺带开放真实下单或批量外部写入。

### P0：权限与可观测性收口

1. 创建最小权限采购员角色，将测试成员从管理员降权，验证页面可见性与 API 403 一致。
2. 落地字段可见性策略：店铺、运营、销售金额、利润、利润率由后端按角色裁剪。
3. 对已部署的业务日志 P0、系统日志 P1 验证本人范围、租户范围、跨租户 404 和脱敏规则。

### P1：采购执行真实状态机

1. **已完成（共享测试后端）**：`checkout_attempt`、数量占用、版本/幂等、付款正式批次、失败补偿状态和物流写接口。
2. 将采购执行前端接到新 API，先用合成资源完成组合、付款结果、清理和物流时间线验收。
3. 设计并实现本地执行器任务协议，再把 HubStudio 环境创建/删除和 SHEIN 结果采集接入；外部结果不确定时必须转人工或接管，不能自动重试下单。
4. 在现有买家号 P0 账本上补自动抽号/超时释放，并建立 Hub 环境独立账本；确认成功买家号复用、成功环境归档和风控冻结规则。
5. `0005/0007/0008/0009/0010/0011` 已在备份后部署；继续补角色隔离、并发状态机和真实日用结果的只读验收，真实下单写入仍需单独授权。

### P2：云端执行器与生产准备

1. 实现设备配对、心跳、任务租约、幂等领取、可撤销设备凭证和脱敏结果上报。
2. 在已完成的买家号/建环境/物流结果 Worker 基础上，补失败运营视图和冲突人工处理。
3. 关闭生产环境安全、域名、备份、监控、正式数据路由和灰度闸门后，再单独批准生产部署。

## 踩坑与经验

- **公开发布必须走净化分支**：内部开发分支包含运维交接与真实环境信息，不能直接推公开仓库。发布前必须运行 `scripts/audit_public_release.py`。
- **版本发布与生产部署不是一件事**：GitHub latest、测试云端代码、数据库迁移和生产上线必须分别记录状态。
- **Alembic revision 要短**：测试库 `alembic_version.version_num` 是 `varchar(32)`，过长 revision 会在迁移最后一步失败并整体回滚。
- **macOS 打包要排除 AppleDouble**：`._*` 文件进入 Docker/Python 源码会产生 null bytes，`.dockerignore` 和发布包都要排除。
- **新云端测试环境要安装 test extra**：新工作树运行测试使用 `uv run --extra test pytest -q`；裸 `uv run pytest` 可能没有 pytest 可执行文件。
- **飞书 PKCE 有兼容差异**：当前测试应用对 challenge 曾返回 `20049`，测试环境暂时关闭 challenge，但随机 state、短时效、摘要存储和单次消费不能取消。
- **认证轮询必须有门禁**：未认证时轮询受保护接口会收到 401；如果共用清理逻辑，会误停飞书登录轮询。所有受保护后台轮询只在 `authReady` 后启动。
- **localhost 不应走系统代理**：HubStudio Local API 使用强制直连 opener，避免 Clash 等系统代理把 `127.0.0.1` 请求转发后报连接失败。
- **并发限流要全局协调**：单线程短重试会被其他 worker 持续打断。请求起点全局错峰，限流冷却必须由所有线程共享。
- **外部写入结果不确定时先接管再重建**：环境创建成功但查询失败时，重试应先按序号/标签查找并接管已有环境，不能直接再次 create。
- **采购认领阶段不能提前固化分单**：优惠券和商家规则只有结算时才能确认；测试版旧分单表可兼容保留，但不是最终领域模型。
- **买家号与环境生命周期必须分开**：失败时买家号可能释放，但登录过的环境绝不能复用；付款成功环境反而必须留存。
- **列表和详情采用不同隐私口径**：列表只返回最小必要字段，完整收件信息仅详情按权限读取并审计，关闭详情后清空 DOM。
- **字段权限必须后端裁剪**：只隐藏列仍可通过 API、筛选项或派生字段反推敏感经营数据。
- **部署增量要隔离**：只部署采购字段时不能夹带尚未批准的日志迁移；每次先生成明确部署树、备份应用和 PostgreSQL，再验证健康、迁移和数据条数。
- **不要按文档假设端口**：服务会寻找空闲端口。每次先 `lsof`，再读取 `/api/update/status` 确认正在运行的实际版本。

## 接手检查清单

```bash
cd /Users/jeff/Documents/xynigo-sourcing
git status --short
git log -5 --oneline --decorate
lsof -nP -iTCP:8767 -sTCP:LISTEN
curl -fsS http://127.0.0.1:8767/api/update/status
curl -fsS https://xynigo.samforo.icu/healthz
curl -fsS https://xynigo.samforo.icu/readyz
PYTHONPATH=src .venv/bin/python -m unittest discover -s tests
cd cloud/auth-service && uv run --extra test pytest -q
```

继续工作前：

1. 阅读本文件、`AGENTS.md`、`docs/项目记忆.md`、`docs/当前开发交接.md`、`docs/后端实施交接.md`。
2. 不覆盖已有改动，不停止正在使用的 8767 服务，不重建现有采购分单。
3. 部署前确认共享测试库当前迁移，不按本地 head 直接升级。
4. 真实 HubStudio、店小秘、SHEIN、飞书 Base 写入和数据删除必须取得当次明确授权。
5. 密码、Cookie、App Secret、Token、Open ID、代理凭证和完整收件信息不得进入仓库、日志、截图或测试夹具。

## 推荐的新会话开场白

> 继续 Xynigo v0.12.3 采购执行联调。先阅读 `HANDOFF.md`、`AGENTS.md`、`docs/买家号资源接口-P0.md`、`docs/采购日用功能数据双写-P0.md`、`docs/采购下单闭环-P1.md`、`docs/项目记忆.md`、`docs/当前开发交接.md` 和 `docs/后端实施交接.md`，核对 Git、8767 实际版本、共享测试健康、数据库迁移和飞书 Outbox 状态。不要擅自重启正在使用的服务，也不要注入合成业务记录。买家号数据库事实源、完整凭证加密和独立测试 Base 单向镜像已部署；优先安排采购电脑正常更新并做真实结果只读验收，再继续外部执行协议。

## 2026-08-26 采购任务飞书镜像 P0（代码完成、未部署）

- 新增 `0013_purchase_task_feishu_mirror` 和采购专用 Worker，将草稿/提交、认领变化、退回及执行版本通过事务 Outbox 镜像到采购单主表和采购明细表；PostgreSQL 仍是唯一事实源。
- Worker 在领取事件前严格校验 38/59 字段、单选项及明细关联目标；按 `orderKey`/`lineKey` 幂等查重，写后回读，并处理重复键、映射冲突、远端缺失、有限重试和过期版本。
- 飞书 v1 关联字段已按记录 ID 字符串数组写入并兼容嵌套回读；部分失败会尽力标记主表并在重试成功后恢复。完整云端回归 `69 passed`，临时 PostgreSQL 迁移升级/降级往返通过。
- 功能默认关闭。本轮没有升级共享测试数据库、没有重启运行服务、没有写入真实 Base。启用前必须取得明确授权，并在全新脱敏测试记录上执行 canary。
- 实际付款/物流属于一对多采购批次，P0 不覆盖明细表中的单值执行字段与人工附件。契约、闸门和回滚见 [`docs/采购任务飞书镜像-P0.md`](docs/采购任务飞书镜像-P0.md)。
