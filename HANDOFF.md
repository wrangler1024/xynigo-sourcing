# Xynigo Sourcing 项目交接

更新时间：2026-08-28

这是新会话和新开发者的第一入口。长期业务规则见 [`docs/项目记忆.md`](docs/项目记忆.md)，滚动技术细节见 [`docs/当前开发交接.md`](docs/当前开发交接.md)，后端路线见 [`docs/后端实施交接.md`](docs/后端实施交接.md)。

## 一页结论

- 内部开发分支：`codex/new-ui-migration`；`v0.12.6` 发布实现提交为 `d1db2f5`，公开净化提交与标签目标为 `88f68f1e44cfb4ba6517d07c2fe1cb85695d456b`。接手时仍须先核对 `git status`，不要覆盖用户未纳入代码提交的业务输出。
- **三条版本线已经对齐到 `0.12.6`**：内部源码、共享测试页面/OpenAPI 与 GitHub Latest/manifest 均为 `0.12.6`。
- 线上协同测试版：GitHub `v0.12.6`，latest、非 prerelease，包内渠道为 `test`；Release：<https://github.com/wrangler1024/xynigo-sourcing/releases/tag/v0.12.6>。
- 共享测试云端：`https://xynigo.samforo.icu`，2026-08-28 复核 `/healthz`、`/readyz` 正常，运行版本为 `0.12.6`，数据库为 `0015_executor_channel_p1 (head)`。
- 下载入口云端热修复 `5a39be3 + ebbc573` 已部署：标准包与绿色包从 `xynigo.samforo.icu` 同域认证接口直接下载，不再打开 GitHub；四个不可变资产由服务器只读挂载并在返回前校验大小和 SHA-256。该次部署镜像 `sha256:724ba6c7c36552bb25ca0a8dbad3ab875cd6c978b334082298e9efea5b47463c`，备份为 `pre-direct-storage-20260827T163313Z`，数据库未迁移。
- 本地执行器配置边界热修复 `92c11f1` 已部署共享测试：设备页只保留 HubStudio 端口、查询/建环境并发、新建抽检数和安全并行，站点、HubStudio 分组及采购员模板移回业务任务；新云端写请求拒绝这些历史字段，读取仍兼容 `v0.12.6` 旧执行器结果。紧凑卡片增量 `ae30930` 及后续 UI 收口将设备/配置卡保持 220px 等高，设备卡收窄、配置卡扩宽，安全并行改为默认开启且与数字控件等高的开关；四个数字设置现支持加减、范围即时校验和无修改禁用保存，修改后“保存修改（未保存）”以高亮按钮及状态提示显式提醒。下载区改为单列并取消重复安装说明；Windows 默认平台同时提供标准安装包和醒目的绿色包兜底入口，均走同域认证下载，自动回退为绿色包时不重复显示。当前镜像 `sha256:f58a834a4bf9cad1b131362c69ceabaf605c95ae0496c114b2bfd105c45d528a`，最新备份为 `pre-executor-ui-tight-20260827T183200Z`，数据库仍为 `0015`。已有设备明确保存的关闭值继续保留；本机新默认值与 revision 投影需随下一执行器版本发布。
- 云端 Web 工作台已在上述域名根路径部署，并与部署前本机工作台共用 `src/purchase_tool/web/index.html` 这一份前端源码；不维护单独的云端版视觉或导航。浏览器可直接完成飞书登录并访问采购任务、采购执行、买家号安全摘要、日志和成员会话；员工电脑不启动 Xynigo 本地服务也可使用这些云端功能。本机执行器离线时只暂停 HubStudio/CDP/SHEIN 等本机操作。
- 云端 Web ↔ 本地执行器 P1 已随 `v0.12.6` 部署：新增 5 分钟一次性配对码、Keychain/CurrentUser DPAPI 设备凭证、设备撤销、20–25 秒主动 HTTPS 长轮询、45 秒任务租约、在线状态、配置读取/写入、配置 revision 冲突保护和脱敏任务回执。Web 可直接读取并保存所选电脑现有 `config.json` 的非敏感字段，包括“查物流与建环境安全并行”开关；代理链接、Cookie、密码、Token 和飞书凭证不进入云端任务。标准安装包已接 `pairingRequestId + xynigo://pair` 一键配对，只有首次设备长轮询后才显示在线；绿色包继续保留独立配对脚本。真实 Windows/macOS 设备安装、配对和心跳仍待两位使用同事验收。
- Xynigo 规模口径已固定为 50 人以内内部团队系统：当前继续使用单套云端 API、PostgreSQL 与 HTTPS 长轮询，不提前引入 Redis、Kafka 或独立连接网关。未来通过传输适配层按指标增加 WebSocket 通知；多实例后才增加 Redis Pub/Sub，长轮询永久保留为断线降级，PostgreSQL 始终保存任务事实。设计正文与可探索图见 [`云端Web统一入口与本地执行器通信通道改造方案_20260827.md`](docs/云端Web统一入口与本地执行器通信通道改造方案_20260827.md) 和 [`Xynigo未来复杂场景可扩展架构_20260827.html`](docs/Xynigo未来复杂场景可扩展架构_20260827.html)。
- Windows 标准安装包已随 `v0.12.6` 作为内部未签名测试包发布：NSIS 3.12 当前用户安装、图形化绿色包迁移、原生 `Xynigo.exe` 状态中心、托盘、隐藏执行器和 `xynigo://` 均已完成；Windows CI 已真实覆盖安装、升级和安全卸载。当前没有可信 Authenticode 签名，用户必须核对 SHA-256 并手动确认 SmartScreen；稳定渠道仍拒绝未签名包。
- Windows 绿色版兜底已发布并绑定为同版本 `greenFallback`：根目录包含 `Xynigo.exe` 状态中心、托盘和品牌资源，兼容 `启动.bat`、人工配对和旧本机界面；绿色版不注册系统协议。
- macOS Apple Silicon 标准 `.app + .pkg` 已随 `v0.12.6` 作为内部未签名测试包发布，安装到 `/Applications`，用户数据位于 `~/Library/Application Support/XynigoSourcing`，首次启动可迁移绿色包白名单数据。当前应用仅 ad-hoc 签名、安装包无 Developer ID/公证/stapling，用户必须核对 SHA-256 并在系统设置中手动放行；绿色包保留回退。
- 浏览器 favicon 固定为原版 X 图标：云端 `/favicon.ico` 必须与 `xynigo-x.ico` 字节一致，页面当前使用 `v=6` 缓存版本；不得再同步为犀牛吉祥物图标。
- 共享测试数据库已部署 `0015_executor_channel_p1`。v0.12.6 上线前应用、`.env` 和 PostgreSQL 已备份到 `pre-v0.12.6-20260827T155612Z`，dump 恢复验证原 `0014` 与 28 张采购单后再迁移；四张执行器表存在且当前均为空，健康/就绪、公网页面、OpenAPI 和无效配对 fail-closed 冒烟正常。
- 采购协作导入云端 API、加密短时计划、持久化 Worker 和应用身份普通 Sheet 网关已部署到共享测试，`XYNIGO_AUTH_PROCUREMENT_IMPORT_ENABLED=true`。解析与下载不再依赖本地执行器；真实新文件已完成 1 行追加、底色和图片写后回读。最新热修复进一步固定“历史订单不可通过重导修改”、相同历史副本整单跳过和 XYP2 多销售行聚合，部署后目标表仍为 49 条非空行、revision 184，未自动重跑任务。
- 本机当前没有 Xynigo 服务监听；`127.0.0.1:8765` 是无关的 `ivyea_agent`。插件独立云端模式不依赖本机服务。此前 8767 报告过 `currentVersion=0.12.3`，但当前已退出，不要把本地文件页误认为服务正在运行。
- `v0.12.6` 回归：内部桌面端 345 项执行（342 通过、3 跳过），云端 97 项通过；公开净化树和审计通过。分支 CI `33089748125` 与标签 CI `33090027867` 均为七任务全绿，四个安装资产公网匿名下载、回下载哈希和清单一致性均通过。
- 当前不是生产上线。生产服务器、正式域名、正式数据库和正式飞书业务路由仍受上线闸门阻断。

## 里程碑总览

| 日期 | 里程碑 | 状态 | 结果 |
|---|---|---|---|
| 2026-08-24 | 前端产品框架改造 | 完成 | 一级产品导航、二级功能入口、多页签工作区和规划态边界完成 |
| 2026-08-24 | 云端认证服务骨架与测试部署 | 测试已部署 | HTTPS、飞书 OAuth、租户白名单、会话与本地一次性登录桥完成 |
| 2026-08-26 | 云端 Web 工作台 | 共享测试已部署 | 域名根路径直接访问；复用部署前原版 UI，本机服务关闭时仍可登录和使用云端采购、资源、日志与成员功能 |
| 2026-08-24 | 成员手机号邀请闭环 | 测试已部署 | 手机号只用于飞书精确解析，不落库；未知成员保持待审批 |
| 2026-08-25 | 内置管理员、权限与资源中心 P0/P1 | 测试已部署 | RBAC、高敏权限双层裁决、店铺/环境/代理只读对账完成 |
| 2026-08-26 | 代理 IP 三类业务目录 | 本地完成，未部署 | 711 动态住宅用于采购 API 提取；Webshare 静态数据中心用于店铺环境；静态住宅暂无场景 |
| 2026-08-25 | 运营采购助手统一身份与正式提交 | 测试已部署 | 插件经本机安全桥提交 PostgreSQL/Outbox，重复提交幂等 |
| 2026-08-25 | 采购认领、分单和执行队列 | 测试已部署 | 行锁认领、乐观锁分单、个人执行队列完成；真实下单未开放 |
| 2026-08-25 | v0.11.0 合并与 v0.11.1 登录闭环 | 已发布并验收 | 合并本地执行器与云端权限；修复飞书登录轮询竞争 |
| 2026-08-26 | 采购契约 v2 与运营归属 | 测试已部署 | 店铺基础名、运营姓名独立入库；利润/利润率读取和筛选完成 |
| 2026-08-26 | 运营采购助手独立云端登录 | v0.12.1 Prerelease 已发布，待真实验收 | 扩展无需本机服务或 Xynigo 页面，飞书登录后直连云端；失败不切换选项卡、不写备注，首次成功切到“备注信息”并最多预填一条 XYP2，仍由运营保存 |
| 2026-08-26 | 采购提交 500 迁移错位修复 | 共享测试已修复 | 应用已引用 0013 字段但数据库停在 0012；备份后补迁移到 head。随后该测试单云端提交 200，页面因扩展重载丢失回调，已补远端回查和备注恢复入口 |
| 2026-08-26 | 未认领采购单修订 | 共享测试已部署 | 原提交运营可修改仍在公共池的原采购单；认领/分单/执行后拒绝，修订不新增第二条客服备注 |
| 2026-08-26 | 新版采购中心与安全退回 | 测试已部署 | 公共认领池、个人执行工作台、批量认领、快捷下单入口和安全退回完成 |
| 2026-08-26 | HubStudio 请求频率优化 | 代码与测试完成 | 0.3 秒全局错峰、`E010205` 共享冷却、半完成环境接管 |
| 2026-08-26 | 业务操作日志 P0 / 系统运行日志 P1 | 共享测试后端已部署 | 结构化脱敏日志、权限、查询 API、页面和迁移已进入共享测试环境；角色/跨租户专项验收仍待补 |
| 2026-08-26 | 采购下单闭环 P1 后端纵切面 | 后端已部署、前端本地完成 | 买家号资源账本、下单尝试、数量锁、付款批次、失败补偿和物流状态机已部署；快捷绑定安全起始段已本地接通，当前 8767 尚未加载，真实下单执行仍未接入 |
| 2026-08-26 | v0.12.0 全员测试版 | 已发布 | Windows/macOS 双平台包、清单、SHA-256 和线上更新发现通过 |
| 2026-08-26 | 采购日用结果双写与 v0.12.1 | 已部署并发布 | 建环境/物流真实结果写 PostgreSQL 并同步独立飞书测试表；双平台测试包发布 |
| 2026-08-26 | 买家号数据库事实源与旧表解耦 | 共享测试已部署 | 资源中心改查 PostgreSQL；旧表 320 条完整凭证加密迁移；41 字段独立测试 Base 持续单向镜像 |
| 2026-08-26 | v0.12.3 云端工作台与完整买家号测试版 | 已部署并发布 | 云端原版 UI、X favicon、完整买家号权限链路和双平台在线更新包完成；仍非生产上线 |
| 2026-08-26 | v0.12.4 云端导入与默认云端入口 | 已部署并发布 | 云端采购协作导入、桌面默认打开云端、双平台本机兼容入口和在线更新完成 |
| 2026-08-27 | v0.12.5 采购导入 safeguards | 已部署并发布 | 历史订单不可通过重导修改、完整副本整单跳过、XYP2 多销售行聚合；共享测试与 GitHub Latest 已对齐 |
| 2026-08-28 | v0.12.6 本地执行器通道与标准安装包 | 共享测试已部署并发布 | P1 配对/长轮询/配置读写、Windows/macOS 标准包和绿色包已上线；内部未签名手动放行，真实设备验收进行中 |

关键证据文档：

- [`docs/里程碑/20260824-云端认证测试部署.md`](docs/里程碑/20260824-云端认证测试部署.md)
- [`docs/里程碑/20260825-运营采购助手统一身份与正式提交基础.md`](docs/里程碑/20260825-运营采购助手统一身份与正式提交基础.md)
- [`docs/里程碑/20260825-采购执行测试版云端联调.md`](docs/里程碑/20260825-采购执行测试版云端联调.md)
- [`docs/里程碑/20260826-采购契约v2与运营归属测试部署.md`](docs/里程碑/20260826-采购契约v2与运营归属测试部署.md)
- [`docs/里程碑/20260826-v0.12.0采购中心与环境创建优化发布.md`](docs/里程碑/20260826-v0.12.0采购中心与环境创建优化发布.md)
- [`docs/里程碑/20260826-采购下单闭环P1后端纵切面.md`](docs/里程碑/20260826-采购下单闭环P1后端纵切面.md)
- [`docs/里程碑/20260826-采购日用结果双写与v0.12.1发布.md`](docs/里程碑/20260826-采购日用结果双写与v0.12.1发布.md)
- [`docs/里程碑/20260826-买家号数据库事实源与旧表解耦.md`](docs/里程碑/20260826-买家号数据库事实源与旧表解耦.md)
- [`docs/里程碑/20260826-v0.12.3云端工作台与完整买家号发布.md`](docs/里程碑/20260826-v0.12.3云端工作台与完整买家号发布.md)
- [`docs/里程碑/20260827-v0.12.5采购协作导入收口发布.md`](docs/里程碑/20260827-v0.12.5采购协作导入收口发布.md)
- [`docs/里程碑/20260828-v0.12.6标准安装包与本地执行器通道发布.md`](docs/里程碑/20260828-v0.12.6标准安装包与本地执行器通道发布.md)
- [`docs/买家号资源接口-P0.md`](docs/买家号资源接口-P0.md)
- [`docs/飞书买家号安全同步测试表.md`](docs/飞书买家号安全同步测试表.md)

## 当前系统状态

### 本机执行器与发布

- GitHub Latest 为 `v0.12.6`，Windows x86_64 与 macOS ARM64 的标准安装包、绿色包和校验清单均已发布；下载和安装始终需要用户明确操作。
- 正式标签 `v0.12.6` 解引用到公开净化提交 `88f68f1e44cfb4ba6517d07c2fe1cb85695d456b`；分支验证与标签发布两轮 CI 均为七任务全绿。
- 发布采用独立公开净化分支，不把内部运维文档、真实基础设施信息或凭证带入公开仓库。
- 当前 8767–8771 均未监听，本机没有运行中的 Xynigo 服务；若后续启动服务，程序仍会从配置端口向后寻找空闲端口，必须先重新核对监听端口和 `/api/update/status`。

### 云端身份、权限与采购中心

- 飞书登录、成员待审批、手机号邀请、角色、权限、会话撤销和租户隔离已进入共享测试环境。
- 已部署权限目录保持既有基线；本地 P1 目录新增 5 项执行器权限后为 35 项：设备查看、配对、撤销、配置读取和配置写入。`super_admin` 持有全部权限，内置 `admin` 仍只固定排除三项高敏配置权限，普通 `member` 默认无权限。
- 采购契约同时接受 v1/v2；v2 独立保存 `storeBaseName`、`operatorName`，括号内运营组别暂不解析。
- 采购任务是团队公共待认领池；采购执行只展示当前采购员本人认领的采购单。
- 支持批量认领、店铺/运营筛选、销售金额、采购指导额、预估利润、利润率和采购员归属。
- 未分单、未开始且属于当前采购员的采购单可以填写原因退回任务池；已有分单或已进入后续流程时服务端拒绝。

### 已部署基础与尚未接入的外部执行

- 下单尝试、数量占用、付款结果、正式采购批次、失败补偿和物流回填的后端第一纵切面已部署到共享测试云端，契约见 [`docs/采购下单闭环-P1.md`](docs/采购下单闭环-P1.md)。
- 前端“开始 / 继续本次下单”仍未接新 API；真实 Hub 环境创建/删除、买家号登录、SHEIN 结算付款和不确定结果接管尚未实现。
- 买家号事实账本、完整凭证加密、查询、快照同步契约和事务预占已完成并部署；资源中心、查重、占用和下单统一查询 PostgreSQL。旧表「买家号（统一）」只保留为一次性迁移源，320 条完整测试记录已经迁入数据库。
- 独立飞书 Base「Xynigo 买家号资源同步测试表」与旧台账无关联，买家号表已扩展至 41 字段，完整账号、密码、Cookie、接码资料和业务字段由 Worker 按 outbox 持续单向镜像。Base 不回写数据库，双向同步是明确禁止的边界。
- 业务日志、系统日志和下单闭环代码及迁移已进入共享测试环境；前端真实执行和角色/跨租户专项验收仍未完成。
- 本地执行器设备控制面第一纵切面已完成：配对、撤销、心跳、租约、配置任务和任务事件已持久化；物流查询、建环境、打开采购环境等业务任务尚未接入云端调度。运行中断后的配置写任务保守转 `uncertain`，不会自动重复执行。

## 已确认的核心业务规则

### 认领不等于分单

- 认领只决定采购员归属，认领后采购单应立即进入个人采购执行范围。
- 是否分单、每批包含哪些 SKU 和数量，必须在真实下单与付款阶段结合优惠券、商家门槛和实付情况决定。
- 一单一 SKU 一件可以快捷下单；一 SKU 多件或多 SKU 多件不能在认领阶段固化最终分单。
- 后端必须同时维护需求数量、尝试占用数量、已采购数量和剩余数量，防止重复采购与超买。

### 买家号和 Hub 环境是两种资源

- Hub 环境一旦登录过买家号就不能回公共池，也不能绑定其他买家号。
- 下单失败或主动放弃：若买家号本身无风控，可解除本次占用继续使用；已登录的 Hub 环境必须从 Hub 中删除。
- 付款成功：Hub 环境必须保留并绑定正式采购批次，用于查单、跟单、物流和售后，不能删除或复用。
- 风控或账号异常时，买家号进入人工检查，不得自动恢复可用。
- 成功订单买家号何时可再次使用、成功环境何时归档、尚未登录即取消的环境能否回池仍待业务确认。

### 归属、数据和隐私

- 店铺/运营归属由插件随采购需求提交，采购认领不得覆盖运营归属；后续绩效要支持运营、采购员和团队口径。
- 利润和利润率只使用插件提交的 `estimatedMetrics`，历史数据缺失时显示 `—`，不得自行推算。
- 收件人、国家可在列表按最小必要范围展示；电话、邮编、详细地址和采购链接只在详情按权限读取并审计。
- 未来隐藏店铺、运营、销售金额、利润和利润率必须由后端角色策略裁剪响应，不能只隐藏前端列。
- PostgreSQL 是业务事实源；飞书 Base 是协同镜像，写入必须使用 Outbox、幂等、有限重试和写后回读。

## 过渡期采购协作导入现状（2026-08-26）

- 已新增“小犀助手 → 采购协作导入”，读取店小秘按产品行导出的 xlsx 和完整 XYP2 备注。解析/下载不写外部系统；用户选择普通飞书电子表格、通过只读校验并二次确认后，导入任务才规范表头、追加数据、设置分组底色/采购超链接并补齐订单商品图。
- 新下载 Excel 为 A:AQ 43 列。表头分为采购需求区、采购下单区、系统追踪区，使用三色和表头批注说明；不增加第二表头行。采购需求区按标准顺序规范，采购下单区不校验字段名/数量/顺序，系统追踪区只强制`系统订单键 / 导入批次 / 数据版本`。下单截图、物流截图作为默认下单字段展示，但仍不参与校验。
- 采购下单区默认在`买家号`后提供`付款卡号 / 采购订单号`；旧`平台订单号`只改表头语义，已有单号原值不变。系统追踪区在`系统订单键`后提供`导入操作人`，新 API 导入行自动记录当前 Xynigo 登录用户；该字段表示“谁执行了导入”，不能替代业务责任字段`采购员`，历史行无可靠来源时保持空白。
- A 列为`分单日期`，按北京时间生成并通过飞书/Excel 真实日期类型写入，展示格式固定为`yyyy-mm-dd`。不再保留语义重复的`分单批次`；稳定幂等批次只存系统追踪区`导入批次`。旧表删除`分单批次`前必须逐行确认其非空值均被`导入批次`等值覆盖，否则停止迁移。采购下单区的`下单批次`仍表示优惠券和付款方案确定后的真实结算批次。
- 采购需求区固定前五列为`分单日期 / 采购员 / 销售订单号 / 店铺 / 运营`，工作表冻结首行和前五列；随后依次为`包裹号 / 采购状态 / 优先级`。共享表使用`包裹号`而不是`采购单号`，并必须向采购提供`收货人姓名 / 国家 / 州省 / 城市 / 地址1 / 地址2 / 邮编 / 电话`。旧表在确认导入后规范旧别名、完整收货信息和商品金额，再按标准采购需求顺序移动整列；采购下单区自定义字段不参与校验。
- `销售订单金额`只写每单第一条有效明细，避免 SUM 重复；`商品金额`在每条明细写`产品售价 × 数量`，用于商品口径统计。XYP2 原价、优惠券、采购币种、订单规格、销售产品单价和首行凑单补差继续写入「采购备注」。
- 协作表只需要订单商品图。店小秘导出内嵌“产品图片”在无 Pillow 环境下从 OOXML DrawingML 读取，并转换成 Excel 可复制的单元格图片；采购商品图列及 SHEIN 商品页网络取图流程已全部删除。
- Web 工作台解析预览按需从本机短时解析计划读取订单商品图，展示 62×62 缩略图并支持点击打开原图；图片接口受 `assistant.access` 权限保护，不把图片 Base64 塞入解析 JSON。
- 飞书目标不固定：每个解析计划都可粘贴新的 `/sheets/` 链接，先读取工作簿、人工选择工作表，再按字段名校验采购需求区和系统追踪区。采购下单区完全不校验，采购可自定义扩展与调序。目标只在该本机短时计划中保存，不写项目配置。
- 命名决策（2026-08-26）：企业飞书普通电子表格文件名使用“采购执行协作表”，当前工作表名为“采购执行协作区”。文件名表达收单、分单、下单、付款、物流和跟单的完整采购执行范围；“共享”仅是协作方式，不再作为业务名称。系统不得硬编码文件名、工作表名或 token，固定契约仅为采购需求区、系统追踪区和`数据版本`等表头字段；采购更换表格或改名后仍通过链接选择和字段校验接入。
- 导入批次改为“文件名 + 12 位业务内容摘要”：同一份业务数据重新解析仍使用同一批次号。写入前按导入批次和行签名回读，每 40 行分块追加；中断重试只续传缺失行，检测到超出计划的重复/异常行则停止写入。
- 采购协作导入不承担订单修订：目标表中已经导入的订单保持原样，跨批次完全一致时整单跳过，历史存在多份完全相同副本也只视为“已存在”；本次明细与历史不同则在写入前阻断。这里与 PostgreSQL 采购任务的“原提交运营可在认领前修订”是两条独立链路，不能用重新导入修改协作表历史订单。
- 一体化导入任务先在用户确认后用有序批处理规范旧表字段并插入缺失需求列，回读通过后按真实表头投影追加类型保真数据。整批行签名回读一致后，再设置表头分区、六组高区分订单浅色、52px 行高、采购超链接和订单商品图。图片列、链接列和整行末列都动态定位，不再硬编码 M/N。幂等重试不覆盖采购员后续人工任务色。
- Web 预览和协作表中的“商品金额”是明细口径：`产品售价 × 单个产品数量`，使用销售订单币种显示；它不替代每单仅首行写入的“销售订单金额”。销售订单和包裹号及店铺/运营均使用上下两行展示。
- Web 预览、导出 Excel 与 API 新增行均按“销售订单号 + 包裹号”分组使用蓝、橙、绿、紫、黄、粉六组高区分浅色循环；同组明细同色，新组首行带分隔线。Excel 图片列宽由 14 缩至 10.5、数据行由 70pt 缩至 52.5pt；飞书行高为 52px，适合每天追加数百单。该底色仅用于初始识别，采购员后续按整行颜色分配任务时可直接覆盖。
- 最新 XYP2 实测导出 `order_20260826140050682_945257.xlsx` 已只读验证：4 单/6 明细、6 张订单商品图；当前代码模板升级为 A:AQ 43 列。
- 新增/扩展 `tests/test_procurement_import.py` 与 `tests/test_lark_sheet_sync.py`；已覆盖类型保真追加、稳定批次、部分续传、重复阻断、图片幂等和写后回读。真实飞书结构写入和回读已完成，仍待用新店小秘导出验收首批完整数据、链接和图片追加。
- 2026-08-26 本次字段改造目标测试 15 项全部通过；本机全量回归运行 299 项，296 项通过、3 项 Windows 条件跳过、0 项失败。企业飞书“采购执行协作表”的“采购执行协作区”已从 revision 123 的 A:AR 44 列迁移为 revision 131 的 A:AQ 43 列，共 41 条非空数据行：14 个旧`分单批次`均被`导入批次`等值覆盖后安全删列，14 个`分单时间`转换为真实`分单日期`，前五列与采购需求区顺序已规范并冻结。迁移前后全部 199 个物理数据行按逻辑字段逐项比对为 0 差异；未追加采购数据。

## 当前卡点

1. **团队端真实升级尚未闭环**：`v0.12.1` 更新器已经从公网发现、下载并校验 `v0.12.3` 双平台包，但仍需至少一台 Windows 和一台 macOS 采购电脑实际确认更新、自动重启、配置/日志/运行数据保留及回退目录。
2. **云端成员真实页面回归待补**：公网未登录页和本机合成会话已覆盖原版 UI；仍需普通成员在本机服务关闭时完成真实飞书登录、刷新后新版本页面、采购/资源/日志读取和退出验收。
3. **外部业务执行协议待扩展**：设备身份、长轮询、租约、幂等回执和配置任务已经实现；仍需在同一协议上增加“云端任务 → 本地执行器 → HubStudio/SHEIN → 脱敏结果回报”的物流、建环境和下单业务任务契约。
4. **双资源生命周期未完成**：买家号 P0 账本及下单事务预占已完成，但自动抽号/超时释放、Hub 环境独立租约、环境真实创建/删除、风控冻结和成功后复用时点尚未形成完整闭环。
5. **前端尚未接新状态机**：组合、资源、付款结果、失败清理和物流时间线仍缺可操作界面。
6. **最小权限与字段权限未收口**：真实成员此前用管理员角色验收；采购员角色、数据范围与敏感经营字段策略仍待配置和联调。
7. **日志专项验收未完成**：`0005_business_operation_logs_p0`、`0007_system_runtime_logs_p1` 已部署，但最小角色、本人/租户范围和跨租户 404 仍需专项复核。
8. **飞书协同仍有边界**：买家号、建环境和物流结果 Worker、有限重试和写后回读已部署；采购任务迁移 `0013` 已部署，但采购任务 Worker 默认关闭、尚未 canary；Webshare 普通表格授权仍未完成。买家号 Base 设计为数据库→Base 单向镜像，不计划开放通用双向同步。
9. **执行器控制面待真实多机验收**：本地代码已实现多电脑在线状态、配置任务租约、撤销、断网保守接管和脱敏事件；仍需部署 `0015`、发布含配对脚本的新包，并用至少一台 Windows 与一台 macOS 做真实配对/重启/断网/版本冲突验收。
10. **生产闸门未关闭**：主机加固、正式 DNS/TLS、生产密钥、正式数据路由、异机备份、监控告警和灰度方案未验收。
11. **业务待确认项**：成功订单买家号复用时点、成功环境归档/清理、未登录环境取消后的回池规则、运营组别解析均未定。
12. **普通飞书表格直接导入的团队运行依赖未产品化**：当前使用本机 `lark-cli` 用户授权，不硬编码共享表；开发机已具备，采购电脑的安装/授权与后续云端用户 OAuth 代理仍需选型和验收。

## 下一步计划

### P0：完成 v0.12.3 团队验收

1. 各选择一台 Windows 和 macOS 采购电脑，从 v0.12.1 通过现有绿色包更新器升级到 v0.12.3，核对自动重启、版本号、配置/日志/运行数据保留和失败回退目录。
2. 在不启动本机 Xynigo 服务的条件下，使用普通成员完成云端飞书登录、刷新、采购任务、采购执行、买家号安全摘要、日志读取和退出；确认 X favicon、原版 UI 与本机执行器离线提示。
3. 使用运营采购插件验证“提交 → 公共任务池 → 批量认领 → 个人采购执行 → 安全退回”。
4. 重点复测利润/利润率、店铺/运营字段、处理下拉、固定操作列及 HubStudio `E010205` 限流恢复。
5. 只记录脱敏结果；不在本轮顺带开放真实下单、采购任务飞书 Worker 或批量外部写入。

### P0：权限与可观测性收口

1. 创建最小权限采购员角色，将测试成员从管理员降权，验证页面可见性与 API 403 一致。
2. 落地字段可见性策略：店铺、运营、销售金额、利润、利润率由后端按角色裁剪。
3. 对已部署的业务日志 P0、系统日志 P1 验证本人范围、租户范围、跨租户 404 和脱敏规则。

### P1：采购执行真实状态机

1. **已完成（共享测试后端）**：`checkout_attempt`、数量占用、版本/幂等、付款正式批次、失败补偿状态和物流写接口。
2. 将采购执行前端接到新 API，先用合成资源完成组合、付款结果、清理和物流时间线验收。
3. 设计并实现本地执行器任务协议，再把 HubStudio 环境创建/删除和 SHEIN 结果采集接入；外部结果不确定时必须转人工或接管，不能自动重试下单。
4. 在现有买家号 P0 账本上补自动抽号/超时释放，并建立 Hub 环境独立账本；确认成功买家号复用、成功环境归档和风控冻结规则。
5. `0005/0007/0008/0009/0010/0011` 已在备份后部署；继续补角色隔离、并发状态机和真实日用结果的只读验收，真实下单写入仍需单独授权。

### P2：云端执行器与生产准备

1. 实现设备配对、心跳、任务租约、幂等领取、可撤销设备凭证和脱敏结果上报。
2. 在已完成的买家号/建环境/物流结果 Worker 基础上，补失败运营视图和冲突人工处理。
3. 关闭生产环境安全、域名、备份、监控、正式数据路由和灰度闸门后，再单独批准生产部署。

## 踩坑与经验

- **公开发布必须走净化分支**：内部开发分支包含运维交接与真实环境信息，不能直接推公开仓库。发布前必须运行 `scripts/audit_public_release.py`。
- **公开仓库发布不能直接推内部分支**：本轮先提交内部完整树，再从上一个公开标签创建 `codex/release-v0.12.3-public`，只复制可发布源码、测试和发行说明；标签必须指向净化提交，不能指向内部提交。
- **版本发布与生产部署不是一件事**：GitHub latest、测试云端代码、数据库迁移和生产上线必须分别记录状态。
- **云端 Web 更新与本机执行器更新是两条链路**：云端页面部署后刷新即可更新；云端 `/api/update/*` 故意返回 disabled，不能远程替同事升级本机执行器。本机绿色包必须通过 GitHub Release、SHA-256 和用户 `Y` 确认独立升级。
- **原版 UI 必须单一来源同步**：`src/purchase_tool/web/index.html` 是本机和云端唯一 UI 源；只修云端副本会再次产生视觉分叉。发布前运行 `cloud/auth-service/deploy/sync_web_workspace.py` 并比对 HTML 哈希。
- **favicon 不能凭文件名猜内容**：`xynigo-favicon.ico` 实际是犀牛吉祥物，部署前本机 `/favicon.ico` 返回的是 `xynigo-x.ico`。云端同步必须让 `/favicon.ico` 与 `xynigo-x.ico` 字节一致，并提升查询版本号使浏览器旧缓存失效。
- **`set -o pipefail` 下不要用 `curl | grep -q` 做部署探针**：`grep -q` 命中后会提前关闭管道，`curl` 可能返回 23 并触发错误回退。先把响应保存到确定的临时文件，再独立 `grep -q`。
- **部署探针不要匹配会被产品改动删除的文案**：旧云端脚本仍检查重制版提示语，恢复原版 UI 后造成假失败。探针应检查稳定的标题、资源哈希、健康/就绪和必要 API，不依赖营销文案。
- **发布标签只能在 CI 全绿后创建**：先推净化分支并等待 Python 3.9/3.11/3.12、Windows、macOS 和包结构任务全部通过，再创建标签和 Latest Release，避免失败版本进入旧客户端更新通道。
- **Alembic revision 要短**：测试库 `alembic_version.version_num` 是 `varchar(32)`，过长 revision 会在迁移最后一步失败并整体回滚。
- **macOS 打包要排除 AppleDouble**：`._*` 文件进入 Docker/Python 源码会产生 null bytes，`.dockerignore` 和发布包都要排除。
- **新云端测试环境要安装 test extra**：新工作树运行测试使用 `uv run --extra test pytest -q`；裸 `uv run pytest` 可能没有 pytest 可执行文件。
- **飞书 PKCE 有兼容差异**：当前测试应用对 challenge 曾返回 `20049`，测试环境暂时关闭 challenge，但随机 state、短时效、摘要存储和单次消费不能取消。
- **认证轮询必须有门禁**：未认证时轮询受保护接口会收到 401；如果共用清理逻辑，会误停飞书登录轮询。所有受保护后台轮询只在 `authReady` 后启动。
- **localhost 不应走系统代理**：HubStudio Local API 使用强制直连 opener，避免 Clash 等系统代理把 `127.0.0.1` 请求转发后报连接失败。
- **并发限流要全局协调**：单线程短重试会被其他 worker 持续打断。请求起点全局错峰，限流冷却必须由所有线程共享。
- **外部写入结果不确定时先接管再重建**：环境创建成功但查询失败时，重试应先按序号/标签查找并接管已有环境，不能直接再次 create。
- **采购认领阶段不能提前固化分单**：优惠券和商家规则只有结算时才能确认；测试版旧分单表可兼容保留，但不是最终领域模型。
- **买家号与环境生命周期必须分开**：失败时买家号可能释放，但登录过的环境绝不能复用；付款成功环境反而必须留存。
- **列表和详情采用不同隐私口径**：列表只返回最小必要字段，完整收件信息仅详情按权限读取并审计，关闭详情后清空 DOM。
- **字段权限必须后端裁剪**：只隐藏列仍可通过 API、筛选项或派生字段反推敏感经营数据。
- **部署增量要隔离**：只部署采购字段时不能夹带尚未批准的日志迁移；每次先生成明确部署树、备份应用和 PostgreSQL，再验证健康、迁移和数据条数。
- **不要按文档假设端口**：服务会寻找空闲端口。每次先 `lsof`，再读取 `/api/update/status` 确认正在运行的实际版本。

## 接手检查清单

```bash
cd /Users/jeff/Documents/xynigo-sourcing
git status --short
git log -5 --oneline --decorate
lsof -nP -iTCP:8767 -sTCP:LISTEN
# 仅当上一步确认存在 Xynigo 监听时，再请求对应实际端口：
curl -fsS http://127.0.0.1:8767/api/update/status
curl -fsS https://xynigo.samforo.icu/healthz
curl -fsS https://xynigo.samforo.icu/readyz
PYTHONPATH=src .venv/bin/python -m unittest discover -s tests
cd cloud/auth-service && uv run --extra test pytest -q
```

继续工作前：

1. 阅读本文件、`AGENTS.md`、`docs/项目记忆.md`、`docs/当前开发交接.md`、`docs/后端实施交接.md`。
2. 不覆盖已有改动，不停止正在使用的 8767 服务，不重建现有采购分单。
3. 部署前确认共享测试库当前迁移，不按本地 head 直接升级。
4. 真实 HubStudio、店小秘、SHEIN、飞书 Base 写入和数据删除必须取得当次明确授权。
5. 密码、Cookie、App Secret、Token、Open ID、代理凭证和完整收件信息不得进入仓库、日志、截图或测试夹具。

## 推荐的新会话开场白

> 继续 Xynigo v0.12.6 内部真实环境验收。先阅读 `HANDOFF.md`、`AGENTS.md`、`docs/里程碑/20260828-v0.12.6标准安装包与本地执行器通道发布.md`、`docs/当前开发交接.md`、`docs/项目记忆.md` 和 `docs/后端实施交接.md`，核对 Git、8767–8771 是否重新出现监听、共享测试健康、`0015_executor_channel_p1` 和飞书 Outbox 状态。当前本机没有运行中的 Xynigo 服务；不要把 8765 的无关进程当成 Xynigo。优先让一台 Windows 与一台 Apple Silicon Mac 从云端下载未签名标准包，核对 SHA-256、手动放行、迁移旧数据、完成一键配对与真实心跳，再验证配置读取/保存和绿色包回退；不得把本版解释为生产上线或已开放真实业务任务下发。

## 2026-08-26 采购任务飞书镜像 P0（迁移已部署，Worker 未启用）

- 新增 `0013_purchase_task_feishu_mirror` 和采购专用 Worker，将草稿/提交、认领变化、退回及执行版本通过事务 Outbox 镜像到采购单主表和采购明细表；PostgreSQL 仍是唯一事实源。
- Worker 在领取事件前严格校验 38/59 字段、单选项及明细关联目标；按 `orderKey`/`lineKey` 幂等查重，写后回读，并处理重复键、映射冲突、远端缺失、有限重试和过期版本。
- 飞书 v1 关联字段已按记录 ID 字符串数组写入并兼容嵌套回读；部分失败会尽力标记主表并在重试成功后恢复。完整云端回归 `69 passed`，临时 PostgreSQL 迁移升级/降级往返通过。
- `0013_purchase_task_feishu_mirror` 已在迁移错位修复中部署到共享测试数据库；Worker 功能仍默认关闭，尚未对真实采购 Base 执行 canary，也没有因本条记录授权写入真实 Base。启用前必须再次取得明确授权，并在全新脱敏测试记录上执行 canary。
- 实际付款/物流属于一对多采购批次，P0 不覆盖明细表中的单值执行字段与人工附件。契约、闸门和回滚见 [`docs/采购任务飞书镜像-P0.md`](docs/采购任务飞书镜像-P0.md)。

## 2026-08-26 采购协作导入云端化 P0（共享测试已部署）

- “小犀助手 → 采购协作导入”已新增云端原生链路：云端上传解析、图片预览、Excel 下载、普通飞书表格只读校验、二次确认写入及持久化进度，不再把本机执行器作为该功能的技术前置。
- 本地与云端共用 `src/purchase_tool/procurement_import.py` 解析算法；同步脚本只替换飞书网关。本机继续使用 `lark-cli --as user`，云端使用“小犀代采”企业应用 `tenant_access_token`，不保存员工飞书 refresh token。
- 新增 `0014_proc_import_cloud`、`procurement_import_plans` 和 `procurement_import_jobs`。完整收件信息、采购链接和图片只进入租户隔离的 AES-GCM 加密短时计划，默认 30 分钟后清空；持久化任务进度不保存订单号或逐行敏感错误。
- 云端 Worker 支持重启恢复、导入批次/行签名续传、并发目标部分唯一索引、写后回读与二次确认。共享测试当前 `XYNIGO_AUTH_PROCUREMENT_IMPORT_ENABLED=true`；用户仍须选择目标表、通过校验并二次确认才会写入。
- 官方 OpenAPI 无等价背景色读取契约，云端只给本次新追加行设置分组底色，服务启动前已有行全部保留，避免覆盖采购员手工任务色；P0 不新增表头批注。
- 验证：本地 299 项完成（296 通过、3 跳过），云端 77 项通过；临时 PostgreSQL 18 完成空库升级、降回 0013、再升级 0014；共享测试已迁移到 0014，7 条公网接口、1.5 MiB 上传限额、事务回滚式解析/导出和飞书应用身份读取均通过。目标表只读得到 43 个标准表头和 48 条非空行，未写入真实飞书表。
- 上线备份与完整证据见 [`docs/里程碑/20260826-采购协作导入云端上线.md`](docs/里程碑/20260826-采购协作导入云端上线.md)。生产环境仍未部署。
- 详细 API、数据模型、权限、canary 和回滚见 [`docs/采购协作导入云端化-P0.md`](docs/采购协作导入云端化-P0.md)。

## 2026-08-26 桌面默认云端入口（v0.12.4 已发布）

- `python -m purchase_tool` 和绿色包默认启动器仍启动本地 HTTP 执行器，但浏览器改为打开当前 `XYNIGO_AUTH_BASE_URL` 对应的云端工作台；默认值为 `https://xynigo.samforo.icu`。
- `--local-ui` 显式打开实际 `127.0.0.1:<port>` 本机页面。Windows/macOS 绿色包新增“启动-本地执行器”入口，默认启动器与本地入口均保持终端窗口，关闭窗口仍会停止执行器。
- 更新器受管文件、双平台更新 helper、包结构校验和使用说明已纳入新增启动器；默认云端、显式本地及自定义云端 Origin 已有单元测试。
- 云端、OpenAPI、源码与客户端已统一到 `0.12.4`。GitHub Release 为 Latest、非 prerelease，旧 `v0.12.3` 更新器已真实发现 `v0.12.4` `test` 渠道资产；完整证据见 [`docs/里程碑/20260826-v0.12.4云端导入与默认云端入口发布.md`](docs/里程碑/20260826-v0.12.4云端导入与默认云端入口发布.md)。

## 2026-08-26 23:23 收工交接

### 当前事实快照

- 内部分支/HEAD：`codex/new-ui-migration` / `bad70b0`，源码版本 `0.12.5`；桌面端 306 项完成（303 通过、3 跳过），云端 80 项通过。
- 版本分布：内部源码与共享云端页面/OpenAPI 为 `0.12.5`；GitHub Latest 仍是 `0.12.4`。当前公开 Release 为 <https://github.com/wrangler1024/xynigo-sourcing/releases/tag/v0.12.4>，标签提交 `9fc1eae7f212e902f0759919e45e1fa3c825b18b`。
- `v0.12.5` 公开候选：分支 `codex/release-v0.12.5-public`，头 `6ba9ade`；CI `32984758350` 尚未完成，无标签、无 Release。下班后若 CI 自行完成，也不能据此自动创建标签。
- 云端：迁移 `0014_proc_import_cloud (head)`，功能开关启用，容器 running，最近 15 分钟无 `ERROR/CRITICAL/Traceback`。当前短时计划为 `expired=6 / validated=1`，任务为 `completed=2 / failed=7`，没有非终态任务；这些终态记录是联调证据，不得自动删除或重跑。
- 共享测试运行容器七个关键文件已与内部 `bad70b0` 完全一致；宿主机 `/opt/xynigo-auth-test` 的落地源码随后也已同步，备份为 `/opt/xynigo-auth-test/backups/deploy/pre-v0.12.5-host-source-20260826T152937Z/`。同步未重启容器、未改数据库。
- 本机无 Xynigo 服务：8767–8771 均未监听；`127.0.0.1:8765` 为无关 `ivyea_agent`，`127.0.0.1:8001` 也是其他进程。不要停止、复用或误判这两个监听。
- 工作区保留用户/并行任务改动：`docs/品牌VI资产/`、`outputs/`、采购导入云端文档与里程碑尚未统一提交。不要使用 `git add -A` 把业务输出或内部资料带入公开分支。

### 明天第一批动作

1. 先读取本节并重新核对 `git status`、HEAD、`v0.12.5` 公开分支 CI、云端页面/OpenAPI、迁移、容器启动时间和导入任务状态。
2. 重新确认远端容器与宿主机部署源仍和 `bad70b0` 一致：至少检查 `main.py / __init__.py / procurement_import_core.py / procurement_import_sheet.py / procurement_import_service.py / procurement_import_xlsx.py / web/index.html`；防止下班后的并行部署再次造成漂移。
3. 只读汇总 7 个 failed 任务的稳定 `last_error_code` 和时间分布；不得解密计划、读取订单号/收件信息或删除记录。先区分历史测试失败、权限失败、业务冲突与代码缺陷。
4. 若继续发布 `v0.12.5`：云端完整版本已部署，先再次确认健康与哈希，再等待公开分支 CI 全绿，构建双平台 `test` 包，随后按“标签 CI 全绿 → Latest Release → 旧版更新发现”顺序执行。
5. 用一台 Windows 和一台 macOS 做 `v0.12.3/0.12.4 → 新版` 真实更新，核对默认打开云端、独立本机执行器入口、配置/日志/运行数据保留和回滚。当前 GitHub 下载数只证明清单被读取和 Windows 包有一次下载，不等于真实升级闭环。

### 当前卡点

1. **GitHub Latest 尚未推进**：内部和云端已为 0.12.5，Latest 仍为 0.12.4；公开候选 CI 尚未完成，无标签、无 Release。这是发布前最高优先级闸门。
2. **失败任务未归因**：数据库有 7 个 failed 终态任务；没有运行中任务，但必须按稳定错误码做脱敏归因，不能用重跑或删除掩盖。
3. **客户端真实更新未验收**：`v0.12.4` manifest 下载 5 次、Windows 包下载 1 次、macOS 包 0 次；尚无两平台真实升级、默认云端启动与本地兼容入口证据。
4. **云端默认入口不等于本机能力云化**：云端页面仍不能直接调用本机 HubStudio/CDP/SHEIN；本地执行器独立入口只是兼容方案，设备配对/心跳/租约通道仍未实现。
5. **生产闸门仍未关闭**：当前全部动作仍是共享测试，不得迁移到生产服务器或把真实自动下单解释为已开放。

### 重点复盘与踩坑

- **版本号要拆成三张表核对**：源码 `__version__`、云端页面/OpenAPI、GitHub Latest/manifest 是独立事实。只有三者都回读一致，才可写“版本已对齐”；窄热修复必须显式标注代码漂移。
- **并行部署前先比较哈希**：本轮多次出现另一任务先完成远端切换。每次覆盖前先比 `main/web/parser/gateway/service` 哈希和容器启动时间，只替换确实不同的文件，避免回退刚上线的修复。
- **Docker 完整构建可能卡在旧依赖下载**：带宽受限服务器优先使用已验证运行镜像作为基线，`requirements.lock` 再校验，只下载新增包；窄热修复使用专用 Dockerfile，并保留旧镜像标签和应用快照。
- **远端 heredoc 会被 `docker compose exec -T` 消费 stdin**：通过 SSH `bash -s` 执行多行脚本时，所有不需要输入的 compose exec/run 都显式加 `</dev/null`；数据库 dump/restore 另行明确重定向。
- **Shell 引号不要包业务数据**：远端 SQL/正则含管道和 `$()` 时优先使用单引号 heredoc，输出只保留状态/计数；不要让本地 shell 提前展开或把敏感字段打印到日志。
- **公开分支一定使用独立 worktree 和明确工作目录**：`git add -A` 前先确认 `pwd`/`git status`；不得在内部根目录误暂存 `outputs/`、内部交接或品牌资产。若误暂存但未提交，只用 `git restore --staged .` 恢复索引，禁止回滚工作树。
- **标签必须晚于分支 CI，Release 必须晚于标签 CI**：`v0.12.4` 严格执行两轮六任务全绿后才设 Latest；候选分支完成不等于可发布。
- **外部 Sheet 导入必须把“追加”和“修订”分开**：历史订单不可通过重导修改；完全一致的完整副本整单跳过，业务差异写前阻断，已有重复只容错不自动清理。
- **终态失败是证据，不是垃圾**：任务失败只保存稳定错误码与脱敏计数；先归因再决定补偿。不得因为“已经失败”就删除、改 completed 或直接重跑。

## 2026-08-27 v0.12.5 发布收口

- GitHub Actions 官方事故恢复后，事故期损坏队列 `32984758350` 仍无法取消或重跑；使用同一公开提交创建临时验证分支获得全新 CI `33032393401`，六任务全绿后删除临时分支。
- 正式注释标签 `v0.12.5` 指向公开提交 `6ba9ade598c9cf118efc15ce8cf3a3cbe2b7d43e`，标签 CI `33032507750` 六任务全绿；GitHub Latest 已发布且为非 prerelease。
- Windows 包 SHA-256 为 `c34af3acafee5f576cab64c6ff177f02cf4f07323ced12e60e789628b9f0827c`，macOS ARM64 包 SHA-256 为 `b00eab1647507dd9e194e0fb319674716e0aebdce7c8b7480a735f43dc0c89df`。公网下载后再次校验通过。
- 旧 `v0.12.4` 更新客户端已从 GitHub Latest 发现 Windows 与 macOS 的 `v0.12.5` 清单。员工电脑仍须在更新提示中输入 `Y`，此次发布没有自动升级任何电脑。
- 共享测试 `/healthz`、`/readyz` 正常，OpenAPI 为 `0.12.5`。本次没有部署生产环境，也没有自动重跑 7 条历史失败任务。
- 完整证据见 [`docs/里程碑/20260827-v0.12.5采购协作导入收口发布.md`](docs/里程碑/20260827-v0.12.5采购协作导入收口发布.md)。

## 2026-08-28 v0.12.6 标准安装包与设备通道发布

- 正式标签 `v0.12.6` 指向公开净化提交 `88f68f1e44cfb4ba6517d07c2fe1cb85695d456b`；分支 CI `33089748125` 和标签 CI `33090027867` 均为七任务全绿。
- GitHub Release 已设为 Latest、非 prerelease，发布 Windows/macOS 标准包、绿色包和校验清单；标准包按内部测试决策暂未签名，稳定渠道门禁仍拒绝未签名资产。
- 共享测试运行 `0.12.6`，数据库为 `0015_executor_channel_p1 (head)`；部署前 dump 已恢复验证原 `0014` 与 28 张采购单。四张设备通道表存在且为空，内外网健康、公网页面/OpenAPI、无效配对 fail-closed 和匿名下载均通过。
- 飞书开发群已发送 Windows/macOS 安装、手动放行、配对、真实心跳、配置读写、安全并行和脱敏反馈教程。下一步只做两位使用同事的真实设备验收和线上问题修复，不直接扩张到生产或真实物流/建环境/下单任务下发。
- 完整证据见 [`docs/里程碑/20260828-v0.12.6标准安装包与本地执行器通道发布.md`](docs/里程碑/20260828-v0.12.6标准安装包与本地执行器通道发布.md)。
