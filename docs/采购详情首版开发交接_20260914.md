# 采购详情首版候选

状态：已实现待验证候选；未迁移共享数据库、未部署或分发。

执行器工作基线 `6e42c37`，插件工作基线 `10d1137`，分支均为 `codex/purchase-details-v1`。

- 本机 `purchase_details.py`：指定已打开环境 + 页面标记精确关联、MX 已付订单解析、Total 交叉校验、单张长图、前后订单一致性、内存暂存及超时结果查询。
- 云端 `purchase_receipt.py`：团队表目标解析由路由控制，行身份与权限复核、版本冲突、持久幂等、原币金额、单任务多行只记一次、独立图片读回、修订历史与未知结果保护。
- 数据迁移 `0035_purchase_receipts`：凭证、任务提交槽、订单唯一归属三表。不能在未迁移的实例宣称回传可用。
- 插件原有地址服务继续使用云端只读代理，新增接口没有扩宽只读代理。

已知限制：首版 MX；采购员列需与登录名称或ID精确匹配；普通飞书电子表格的人工排序没有数据库级事务保证；uncertain/pending 需人工核验后由维护者处理；原图历史当前保存在数据库中，未提供历史浏览界面。当前开发授权不包含真实协作表写入及上线。

验收时必须使用脱敏任务或明确授权的单笔目标，检查“订单号、金额、单图”三项和旧字段不变；另需 Windows/HubStudio 安装与数据库迁移回退验证。保留旧版本和数据库备份。

## 验证结果

- 本机全量：989 passed / 5 skipped；后续新增与调整再跑采购详情和云端客户端专项：56 passed。
- 云端全量：266 passed；后续新增与加强再跑采购凭证专项：15 passed（含 HTTP 鉴权/团队目标限制、修订与原图保护、重复订单、明确失败补图、未知结果、同名用户、目标改绑、迁移 upgrade/downgrade）。
- 配对插件：47 项通过；真实 Chromium 加载实际内容脚本，以完全拦截网络的脱敏页面验证入口和金额展示，pageErrors 为空。
- 两仓候选执行公开内容扫描及 diff 空白检查。测试数据均为合成数据。
- 本轮没有新的真实订单解析验收：先前浏览器调试端口已失效，未为测试重启用户环境。方案阶段两个真实页面的观察只作为解析结构依据，不计为本候选真实写入验收。
- PostgreSQL 线上迁移、Windows 安装、真实图片写入后的 API 形态和表格权限尚未验收。迁移当前验证为隔离 SQLite 升降级与 ORM 字段一致性。
