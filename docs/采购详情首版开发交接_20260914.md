# 采购详情首版候选

状态：采购详情云端已部署至授权测试机；0.17.22 测试下载及更新目录已发布。

执行器工作基线 `6e42c37`，插件工作基线 `10d1137`，分支均为 `codex/purchase-details-v1`。

- 本机 `purchase_details.py`：指定已打开环境 + 页面标记精确关联、MX 已付订单解析、Total 交叉校验、单张长图、前后订单一致性、内存暂存及超时结果查询。
- 云端 `purchase_receipt.py`：团队表目标解析由路由控制，行身份与权限复核、版本冲突、持久幂等、原币金额、单任务多行只记一次、独立图片读回、修订历史与未知结果保护。
- 数据迁移 `0035_purchase_receipts`：凭证、任务提交槽、订单唯一归属三表。不能在未迁移的实例宣称回传可用。
- 插件原有地址服务继续使用云端只读代理，新增接口没有扩宽只读代理。

已知限制：首版 MX；采购员列不参与回传校验；普通飞书电子表格的人工排序没有数据库级事务保证；uncertain/pending 需人工核验后由维护者处理；原图历史当前保存在数据库中，未提供历史浏览界面。当前开发授权不包含真实协作表写入及上线。

验收时必须使用脱敏任务或明确授权的单笔目标，检查“订单号、金额、单图”三项和旧字段不变；另需 Windows/HubStudio 安装与数据库迁移回退验证。保留旧版本和数据库备份。

## 验证结果

- 本机全量：989 passed / 5 skipped；后续新增与调整再跑采购详情和云端客户端专项：56 passed。
- 云端全量：266 passed；后续新增与加强再跑采购凭证专项：15 passed（含 HTTP 鉴权/团队目标限制、修订与原图保护、重复订单、明确失败补图、未知结果、同名用户、目标改绑、迁移 upgrade/downgrade）。
- 配对插件：47 项通过；真实 Chromium 加载实际内容脚本，以完全拦截网络的脱敏页面验证入口和金额展示，pageErrors 为空。
- 两仓候选执行公开内容扫描及 diff 空白检查。测试数据均为合成数据。
- 本轮没有新的真实订单解析验收：先前浏览器调试端口已失效，未为测试重启用户环境。方案阶段两个真实页面的观察只作为解析结构依据，不计为本候选真实写入验收。
- PostgreSQL 线上迁移、Windows 安装、真实图片写入后的 API 形态和表格权限尚未验收。迁移当前验证为隔离 SQLite 升降级与 ORM 字段一致性。

## 测试机部署准备

独立评审发现的空姓名授权与图片原值读取问题已修复，并补金额原值校验及真实网关请求形态测试。云端接口专用 Nginx 配置见 `cloud/auth-service/deploy/purchase-receipts.nginx.inc`。

按追加发布要求，直接更新现有测试机的 0.17.21 下载/更新目录，在现有采购环境测试。云端应用与目录同步 0.17.21，Windows/macOS 均使用通过 CI 的 55ebc032a3f0 构建；不再要求独立联调环境。旧 0.17.20 资产保留用于回退。

最终运行代码独立评审通过（两项必须改已关闭）。CI run 34821307705 的构建来源 55ebc032a3f085b78b43fd55b91932632ce5f92b 全部通过；Windows 安装器检查曾失败，原提交重跑后通过。发布元数据增量为版本标识、说明和真实资产哈希/大小，业务逻辑未变；云端目录专项 19 通过，本机发布/工作台专项 43 通过。实际部署证据保存在授权私有部署记录。

发布核验完成：0718b31 已部署授权测试机，云端版本/目录为0.17.21；数据库0035，健康与就绪200，远端两平台安装包大小及SHA256一致。独立评审通过。安装包构建来源仍55ebc03，不将后续记录提交当成重建来源。实际采购回写由用户在现有环境测试；本次未自动替换团队浏览器插件。

## 20260914 测试反馈调整

按确认取消采购员列的姓名、ID、空值和重名校验，列不再作为必需表头或任务 fingerprint 输入。保留登录权限、租户和登记团队表边界，以及既有凭证的原操作者/管理员修订与补图限制。仅更新云端，不改变桌面或插件协议。修复提交 `d2f81da` 已部署授权测试机；专项 20 项通过，CI `34824878239` 的 cloud-tests 通过，独立评审通过。沿用现有桌面和插件资产。部署后健康检查、未登录请求拒绝、关闭管理员豁免后的目标任务 preview 均通过；不计为实际回传验收。按用户追加授权对在途物流查询发出取消请求后完成切换，切换后无在途任务。

## 20260914 墨西哥站内部编号兼容修复

详情路由内部ID与采购订单billno不同，不再要求相同；仍保持精确URL/marker、有效订单号、MXN、已付款、Total以及截图/提交前后订单一致性检查。构建来源c0c580f，发布元数据6b49117，runtime为0.17.22-c0c580f10cc2。本机解析/版本专项20通过，目录19通过，发布/Web17通过；构建CI34827039564两平台构建与Windows安装验证通过，发布CI34827657046云端检查通过。两次独立评审通过。测试机已切换0.17.22，实际挂载双平台资产SHA256和大小与目录一致，健康/就绪通过；采购员需更新本机执行器，插件仍兼容0.9.0。未替用户提交真实订单凭证。
