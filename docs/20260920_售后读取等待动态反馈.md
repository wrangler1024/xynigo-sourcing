# 售后读取等待期间的动态反馈

基线 `ba6680d`。用户明确授权快速页面热部署、本轮跳过独立评审、极简 CI 与云端测试；本轮例外不修改长期协作与 CI 规则。

按环境提交时，订单卡片已经返回但订单状态尚为 queued，且本批次及其环境仍在运行，则在原订单行显示动态省略号“环境处理中”，辅助说明“订单已读取，正在自动处理，无需手动确认”。动画表达环境正在工作，不推断此订单已经提交或精确的详情读取阶段。同环境多单仍可能串行等待。

历史展示、不活动环境及已结束订单不启用此提示；指定订单、原始状态、筛选、计数、导出、重试均不变。沿用固定宽度省略号及减少动态效果偏好，避免动画撑宽文字。本次没有新增虚构耗时或百分比，也不修改执行器与后端。

本地真实函数 fixture 覆盖活动提示、历史与终态不播放、单行展示、数据不变性；PASS。Web 接线专项 90 passed（0.29 秒）。云端副本与两份原型已同步；安全审计和 diff 检查通过。云端仅运行同一环境行专项；不运行全量 CI、Python 云端套件或桌面构建。发布提交使用 `[skip ci]`。未发起真实平台提交。

## 实际发布结果

代码候选 `23769bee30fb8bd7bba09f926b44b02a29c5b05a` 已合并推送并于 2026-09-20 完成 Web 热部署。按用户明确要求，本轮未做独立评审；云端 Node v22.23.2 仅运行 `after_sale_environment_rows_ui.cjs`，PASS、exit 0。

首次切换检查发现一个在途只读扫描，未重启应用。用户随后授权先停止云端任务再部署，发布负责人通过现有任务服务发送正常停止请求（cancel_requested），未直接覆写终态。执行器随后以 succeeded / after_sale_scan_completed 收尾；只能确认任务已退出，不能将此记录表述为 cancelled 或可恢复暂停。没有自动重跑任务。

无在途任务后，在维护闸保护下完成应用切换；代码/镜像/数据库备份及归档目录检查完成，沿用当日成功的隔离恢复证据。健康、就绪、CSP、迁移一致性检查通过，数据库未重启，待核对记录指纹未变化，维护闸解除。公网页面 SHA-256：`a959e0d11d17696918fad0c8eee1d0ba5f767452ff9ac1e064f95a5ca434e531`，与候选一致。

动态阶段由合成数据真实函数 fixture 验证；没有新增真实售后提交。执行器/后端未更新，客户端无需重装。全量 CI、云端 Python 套件和桌面构建均未运行，不能记作通过。
