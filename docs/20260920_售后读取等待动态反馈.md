# 售后读取等待期间的动态反馈

基线 `ba6680d`。用户明确授权快速页面热部署、本轮跳过独立评审、极简 CI 与云端测试；本轮例外不修改长期协作与 CI 规则。

按环境提交时，订单卡片已经返回但订单状态尚为 queued，且本批次及其环境仍在运行，则在原订单行显示动态省略号“环境处理中”，辅助说明“订单已读取，正在自动处理，无需手动确认”。动画表达环境正在工作，不推断此订单已经提交或精确的详情读取阶段。同环境多单仍可能串行等待。

历史展示、不活动环境及已结束订单不启用此提示；指定订单、原始状态、筛选、计数、导出、重试均不变。沿用固定宽度省略号及减少动态效果偏好，避免动画撑宽文字。本次没有新增虚构耗时或百分比，也不修改执行器与后端。

本地真实函数 fixture 覆盖活动提示、历史与终态不播放、单行展示、数据不变性；PASS。Web 接线专项 90 passed（0.29 秒）。云端副本与两份原型已同步；安全审计和 diff 检查通过。云端仅运行同一环境行专项；不运行全量 CI、Python 云端套件或桌面构建。发布提交使用 `[skip ci]`。未发起真实平台提交。
