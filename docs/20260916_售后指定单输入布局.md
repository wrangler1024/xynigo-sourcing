# 售后指定单输入布局

状态：本地实现与验证完成，未合入 main、未推送、未部署。
分支：`codex/after-sale-input-alignment`；基线：`d6cf18a`。

直接提交与指定退款单回访原先将长说明、输入框和按钮放在同一 flex 行，说明长度不同导致输入框和按钮边界不一致。

本次统一为可见标签、输入与按钮、说明三层结构。桌面采用相同网格列，按钮宽 128px，输入和按钮均高 44px；600px 以下改为单列。增加 label 和 aria-describedby，格式说明不再依赖占位文本。保留节点 ID、解析规则和点击处理，业务行为未变。

验证：工作台 Web 与售后顺序进度专项 82 passed；Playwright 本地合成原型在 1440/1024/768/480/375px 下核对两组输入与按钮坐标、宽高和控件溢出。桌面同高同列，窄屏堆叠，无控件横向溢出。桌面与窄屏截图保存在工作区忽略目录 output/playwright/。未运行真实提交或回访。

Web 云端副本和两份原型已按生成器同步；diff 检查与公开安全审计通过。合并前按仓库规约独立评审。
