# 全模块并发与运行设置统一

状态：独立分支 `codex/unified-runtime-settings`，基于 `4ef32aa`，本地开发候选；本次已整合至本地 main，未推送、发布或部署。

## 最终行为

- 采购售后与店铺结算查询共用开关/并发按钮样式；“无头模式”开关默认开启，关闭后有头运行。两处均在运行时禁用参数控件，终态恢复。
- 所有用户可配置并发仅允许 2、3、5，默认 2。覆盖售后、店铺结算、物流配置、建环境配置、代理检测、Web 设置和桌面设置。桌面和通用设置改为下拉选择，不能输入其他档位。
- 新请求在本机配置、任务桥接、代理检测及云端契约校验；非法整数、字符串、小数、布尔值和 null 不会被截断为合法档位。
- 本机旧配置中合法的 3、5 保留；其余旧值读取时回退为 2，不因读取而改写配置文件。
- 内部安全上限继续有效：有头物流串行，同环境互斥，注册串行；建环境安全并行开启时上限 2、关闭时上限 3。只读云端摘要仍兼容旧执行器历史取值。
- 未改写真实配置、未操作真实平台。云端/桌面应协调升级；旧客户端向新写入接口提交 1、4 等值会被拒绝。

## 源码与验证

权威页面：`src/purchase_tool/web/index.html`；桌面设置：`src/purchase_tool/web/desktop.js`。本机配置闭集和旧值归一化：`src/purchase_tool/concurrency.py`。云端写入契约：`operation_contract.py`、`executor_contract.py`。共享页面、两份售后原型已重新生成。

本机 340 项回归通过，覆盖配置、设备配置服务、代理资源、结算、任务桥接、售后、桌面、建环境与物流查询。新增闭集测试验证保存/迁移差异和内部安全上限；原停止测试通过注入内部单线程上限维持“第二行尚未启动”的确定条件。

离线 Chrome 在 1440px、480px 验证两处开关尺寸/颜色/状态与并发按钮样式一致，开关可切换，档位仅 2/3/5；桌面下拉默认 2、旧值回退及切换 5 通过。售后三类任务 JS 夹具验证请求参数、能力门禁和参数快照通过。公共源码审计及 diff 空白检查通过。

云端结算、售后、执行器通道、任务结果及新增设置校验共 77 项不同测试通过（66 项主回归，另有 11 项并发设置测试；售后运行参数 12 项亦单独复测通过）。仅出现依赖库已有的 Starlette/httpx 弃用提示。
