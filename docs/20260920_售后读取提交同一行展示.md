# 售后读取与提交在同一订单行展示

状态：已合并主分支、推送并完成现有云端工作台 Web 热部署。部署候选 `714df727a3e703683585a074347b64ceacd725a8`；基线 `31904c9`；分支 `codex/after-sale-inline-progress`。后续发布记录提交不改变部署候选。

前次优化只在环境正常结束后收起环境行。本次将读取、提交、核验和结果统一到订单明细行：尚未返回订单时使用环境占位；订单出现后不再额外显示该环境的独立进度行。多订单仍一单一行，订单身份与实际状态不变。

- 按环境提交的运行中订单显示“读取/提交中”及动态省略号；核验与待核对状态保留原语义。
- 环境异常、未结束、结果数量不足等必要说明合并到首条可见订单的备注中，明确标注“环境进度”，不改变订单成功或失败状态。
- 可见订单已足以说明正常进行中的状态时，不重复追加相同环境说明。
- 筛选后没有可见订单、但环境状态匹配时，仍展示环境解释，避免丢失失败范围；没有订单数据的旧批次同样保留提示。
- 本次提交和历史详情共用规则；统计、导出、回访、补提和后台接口不变，不修改传入数据。

实际验证：环境列表、反馈 UI、任务生命周期、Web 接线、P0 和工作区视图专项 **120 passed**。真实渲染 fixture 覆盖等待、运行、核验、待核对、跳过同一订单行，及多订单、环境异常、缺明细、筛选、转义和数据不变性。三份生成输出同步且重生成无漂移；公开安全审计和 diff 格式检查通过。未提交真实平台任务。


## 本轮快速发布检查范围

用户明确授权快速线上热部署、减少 CI 并在云端跑测试。本次限于 Web 展示，采用独立评审、云端隔离 Node 22 真实前端 fixture、生成一致性、安全检查，以及部署后的健康/CSP/只读页面核验。此次发布提交使用 `[skip ci]`，不触发冗长的云端 Python 全量和桌面构建，不修改仓库长期 CI 配置；未运行项不计为通过。执行器、后端代码与数据库结构不变。若后续扩大到这些组件，恢复对应完整检查。

评审发现的筛选说明已同步为“环境提示随订单显示，无明细时单列”。

## 独立评审与实际发布结果

未参与实现的评审者检查原候选及最终增量，结论为可合并，无必须改；提示文案建议已关闭。独立运行六专项得到 120 passed，另有两份前端 fixture 通过；最终文案增量核对了权威源与云端副本一致性及 diff 格式。

云端对最终候选运行 `tests/fixtures/after_sale*_ui.cjs`：Node **v22.23.2**，**11 份 fixture 全部通过，exit 0**，其中生命周期覆盖 17 个场景。测试使用无网络、只读挂载、受资源限制的隔离容器。此次未运行 Python 全量套件或桌面构建，不将其记为通过。

2026-09-20 完成 Web 热部署。备份现有镜像、页面和数据库，检查数据库备份归档目录，并复用当日此前已成功的隔离恢复证据；本轮未重复恢复演练。维护期间确认无活动任务后，仅替换应用镜像中的 Web 页面与 Web 修订标识。健康、就绪、页面哈希、内联脚本 CSP 检查通过；维护闸已解除，未触发回退。

- 线上页面 SHA-256：`5c615cd34c59dfb746e387e161796932ed6d04daf4305981d8b7f88c1f316a19`，与候选一致。
- 后端修订仍为 `0eb707b6fa25845bc4ab6138cf758c5c9118fe2e`，数据库迁移仍为 `0046_shein_settlement`，数据库未重启；待核对记录指纹不变。
- 登录页面只读复核：既有批次订单正常显示，已有明细时不再显示独立环境进度行，新筛选说明生效。运行中的读取、提交和核验同一行行为由上述云端 fixture 验证，本轮未发起真实售后提交。
- 工作台版本仍为 v0.18.9；现有标签、桌面安装包和执行器不变，无须重新安装客户端。
