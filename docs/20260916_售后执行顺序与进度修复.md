# 售后执行顺序与进度修复

状态：独立评审通过，已整合四模块吸顶进度条并完成组合回归；未推送、未部署、未发布执行器。
分支：`codex/after-sale-order-progress`；基线：`6952647`，包含此前所有订单扫描修复。

## 三项测试反馈

1. 提交和回访的执行顺序必须与列表从上到下一致。
2. 回访不能刚开始就按结果行数显示满进度。
3. 扫描完成后取消自动收起，保留手动收起/展开。

## 实现口径

- 扫描勾选按页面行序生成请求，忽略复选框点击先后；指定单按输入顺序，重提和历史回访按来源行序。
- 执行器仅合并相邻同环境条目；A/A/B/A 执行顺序仍为 A/A/B/A，不重排成 A/A/A/B。
  本地提交快照保留请求插入顺序；云端提交快照、历史详情与导出按请求摘要中的订单顺序排列。
- 当前页发起后立即列出全部等待行，后续结果按请求键回填原位置。回访显示等待、回访中、失败和停止状态。
- 回访复用已有 `executor_tasks.progress_summary` 保存该任务的结果行，无新表或迁移。
  尚未上报、queued、running 和 stopped 行不计入实际已处理数；失败行计入已处理，退款阶段不是执行进度。
  同一退款单在另一回访任务中的旧结果不能计入新任务进度。
- 新任务 GET 与导出均读取任务级行快照，按请求退款单顺序；部署前已结束且没有快照的旧任务，保留旧跟踪表回读兼容。
  未完整回传结果的结束任务显示“结果未齐”，不强行满格。
- Web 消费 `summary.progressCompleted/progressTotal`；旧云端缺少进度字段时保守显示 0，不使用历史结果行数推算。
  扫描进度分母明确按环境数，不被同环境多条订单行放大。
- 轮询不重入，丢弃已切换任务的迟到响应；扫描终态退出轮询，保留手动折叠状态。
  其他阶段既有自动折叠行为不在此次扫描反馈范围内。

## 验证

- 本地全量：1105 passed / 5 skipped。
- 云端全量：316 passed；仅有既有 Starlette/httpx 弃用提示，无失败。
- 本地新增行为测试：交错环境清单的执行顺序、反序勾选、乱序/部分结果回填、回访进度、手动折叠和扫描完成后停止轮询。
- 云端新增集成测试：旧退款结果不计入新任务、乱序上报后按清单返回、停止行不计完成、任务间结果隔离、身份/重复行校验、提交历史顺序。
- Playwright 在隔离合成页面验证实际按钮和表格：提交顺序保持列表顺序，回访从 0/2 到 1/2；扫描完成等待 2.6 秒仍展开，手动收起/展开正常。
  截图位于本工作区忽略目录 `output/playwright/`，不作为公开版本材料。
- Web 双副本与两份生成原型同步；多类型原型的额外包装层也取消扫描自动折叠。
- 本次不连接真实订单，不提交退款，不运行共享数据库迁移。

## 发布与限制

先评审和上线兼容云端/Web，再分发执行器。仅更新云端不能修正旧执行器 A/B/A 的分组执行顺序。
暂无真实退款写入验收，本地执行链使用替身验证；不把代码完成视作线上生效。
旧任务没有历史快照的数据不可凭空还原，回读兼容仍可能显示跟踪表后续更新过的状态。

## 20260916 整合评审与回归

- 合并候选：`fcc18c6`，父提交为售后修复 `76716d5` 与四模块吸顶进度条 `26988ec`；无冲突。
- 独立评审：主会话未参与四模块实现，已评审 `4518999..26988ec`；未参与售后实现的评审代理已评审 `4518999..76716d5`，无必须改项。独立复跑本地售后 61 项、云端售后与进度 13 项均通过。
- 建议改暂缓：`AfterSaleTrackRow` 旧注释仍描述 timeline 跟随导出，与现实现不符。仅注释问题，不影响契约和此次合并，留待后续清理。
- 组合候选本地相关测试：331 passed；云端售后专项：23 passed。云端仅既有 Starlette/httpx 弃用提示。
- Playwright 使用本地原型、合成状态和接口替身：四模块分别在 1440/768/480 宽度验证两处滚动位置吸顶稳定、条内无横向溢出、手动收起/展开；售后验证列表顺序、回访 0% 到 50%、扫描结束等待 2.7 秒仍展开。
- 合成测试移除原型登录遮罩及 inert 状态以测试布局，未连接真实业务服务，不等同于真实平台验收。浏览器和临时 HTTP 服务均已关闭。
- Web 同步及两份原型重新生成无差异；公开安全审计与 diff 检查通过。
- 本次仅本地整合；没有 fetch、推送、部署、发布或真实退款写入。
