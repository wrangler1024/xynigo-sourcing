# 20260920 售后反馈修复独立评审

评审者未参与实现；在两个独立 detached worktree 中检查，仅使用合成数据、临时数据库和模拟浏览器响应，没有修改业务实现、推送、部署或访问真实平台。

## 范围与结论

- 基线：`e649be897068117119561dfbf5ffaddbfe6c80f3`。
- 业务候选：`10c48e2c3f2d2d27ab58334d917ac5f061153af2`。
- 发布源码准备增量：`f55acdefd9b51a2c36c6cba4006c9c13e20dca4f`，没有改动业务逻辑。
- 最终资产元数据增量：`0eb707b6fa25845bc4ab6138cf758c5c9118fe2e`。
- **结论：最终候选可合并，未发现必须修改的业务问题。** 源码评审与本地验证已完成；正式切换服务仍须满足最终候选 CI 和既定发布流程。本记录不表示已经发布或完成真实业务验收。

已读取 `AGENTS.md`、`CONTRIBUTING.md`、`docs/协作与交接.md`、`docs/生态跨仓契约.md` 及本次实现记录。旧交接只作为背景，不据此判断当前部署状态。

## 重点核对

| 项目 | 代码位置（相对仓库根目录） | 结论 |
|---|---|---|
| 无入口订单保留事实 | `src/purchase_tool/after_sale_claim.py:1243`、`:1367` | 动态建立订单行，保留列表及详情中的商品事实；无入口分支不进入 `_claim_one`；已有退款标记为 `existing`，终态为 `skip`，不计新增成功 |
| 停止及异常收尾 | `src/purchase_tool/after_sale_claim.py:1326` | 环境收尾处理已发布的 queued/running 行；保留 verifying/uncertain 的核对语义，批末已有保护仍有效 |
| 上行闭集与旧记录 | `src/purchase_tool/operation_executor.py:679`、`cloud/auth-service/src/xynigo_auth/operation_contract.py:1305`、`executor_service.py:2940` | 环境直提才携带新增商品字段；新字段可选，严格整数数量及数组/字符串上限有效；缺省终态保留已有商品、送达及退款事实 |
| 手动接续 | `src/purchase_tool/web/index.html:6755` | 接续校验结束后释放准备锁，再调用真实回访创建链；代次、发现任务编号与双击保护有效 |
| 自动接续与部分结果 | `src/purchase_tool/web/index.html:6676` | 必须覆盖全部输入环境且环境均成功；停止、缺环境或部分失败不自动接续；过期异步响应不推进其他任务 |
| 字段与展示 | `src/purchase_tool/web/index.html:6552`、`:6887` | 发现与回访保持 10 列；等待/读取中不伪装空结果；商品图按环境、订单、退款号关联，仅补充本页展示，不加入回访身份请求；普通回访重设步骤标记 |
| 版本及资产 | `cloud/auth-service/src/xynigo_auth/local_executor_release.py:17`、`:34`、`:53` | 发布准备中临时沿用的旧资产元数据，已由最终增量替换；最终目录与实际安装包、旁车 JSON/SHA 文件一致 |

新增 UI 回归执行真实 `asContinueDiscovery`、`asFinishDiscovery`、`asTrack`、`asCreateTask` 和 `asPoll`，不会通过空回访函数掩盖任务未派发。HTTP 与执行器选择仍使用合成响应，不能代替真实设备验收。

## 实际运行结果

业务候选使用 Python 3.9.6 / 云端 Python 3.12.13；`PYTHONPATH` 指向独立评审源码。Node 为本机 26.5.0，未将其表述为 CI 要求的 Node 22 验证。

| 检查 | 实际结果 |
|---|---|
| `PYTHONPATH=src python -m pytest tests -q --tb=short`，业务候选，本机 | **1322 passed，5 skipped，116.75 秒**；5 项属于当前环境未运行的安装包/Windows 平台检查，不能计为平台安装验收 |
| 云端目录 `PYTHONPATH=src python -m pytest tests/test_after_sale*.py -q --tb=short`，业务候选 | **104 项通过，退出码 0**；仅既有 Starlette/httpx 弃用提示；不是云端全量套件 |
| `node tests/fixtures/after_sale_feedback_ui.cjs` | 通过：状态与列数、部分结果手动接续、完整自动接续、停止、缺环境、旧响应、双击；真实创建及轮询函数被调用 |
| 独立合成异常注入 | 无入口读取异常时，两条已发布行均收尾 fail；零提交调用；环境关闭一次 |
| 独立云端字段边界验证 | 10 类非法输入被拒绝，包括布尔/字符串/小数数量、数量越界、数组越界、超长图片和未知字段；4 类旧输入/合法数量边界接受 |
| Web 同步器与两份售后原型生成器，业务候选，Python 3.12 | 重生成后零 diff，无新增输出 |
| `tests/test_release_v060.py`，最终候选 | **5 项通过** |
| 云端 `tests/test_local_executor_release.py`，最终候选 | **20 项通过** |
| `scripts/audit_public_release.py`、`git diff --check` | 业务候选和最终发布候选均通过 |

最终发布候选只增加版本、说明与资产目录，业务全量结果对应 `10c48e2`；没有把旧 SHA 全量结果称为 `0eb707b` 全量或 CI 结果。发布相关专项已在 `0eb707b` 实跑。

## 资产核对

构建源码 `f55acdefd9b51a2c36c6cba4006c9c13e20dca4f`，两平台 `runtimeId` 均为 `0.18.9-f55acdefd9b5`。

| 资产 | 实测字节数 | 独立计算 SHA-256 |
|---|---:|---|
| `Xynigo_Sourcing_Windows_Setup_v0.18.9.exe` | 15413016 | `051f60b1665fccd58f30a70f85696163dd6ebc191dbea112916c646f280ca0c0` |
| `Xynigo_Sourcing_macOS_Standard_v0.18.9.pkg` | 11791522 | `0b452a3d46aadd3f78944444d503282a450706f8f0b2a12cf75d84ed60f68a20` |

此处实际读取安装包计算哈希，并核对 JSON、SHA 文件与最终目录。macOS 展开、自检、签名完整性及 TLS 自检由发布负责人报告，本独立评审未重复执行；Windows 原生安装与最终 GitHub CI 不在本地评审已完成范围内。

## 意见与后续边界

- **必须改：无。** 发布准备中的旧资产占位已在 `0eb707b` 闭环。
- **建议改（不阻塞合并）：** 将此次独立执行的异常收尾和云端非法商品字段矩阵纳入永久回归。对应 `tests/test_after_sale_feedback_facts.py:102`、`cloud/auth-service/tests/test_after_sale_feedback_facts.py:6`；目前仓内新增测试重点覆盖停止及有效回执往返，边界补验由本次评审单独执行。
- 发布负责人继续等待最终候选必需 CI，遵守云端兼容接收方先行、执行器后行；回退云端前处理新字段生产方。未执行任何真实退款申请，不将只读及模拟回归替代业务验收。
