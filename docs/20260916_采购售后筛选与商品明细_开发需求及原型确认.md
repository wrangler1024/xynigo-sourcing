# 采购售后筛选与商品明细：开发需求及原型确认

确认日期：20260916。用户已确认交互原型，并要求先记录开发需求和原型设计。

状态：设计已确认，正式功能待开发。本轮只保存文档和原型，不修改正式业务逻辑、不部署、不提交真实售后。不得将原型确认解释为已完成开发或真实退款授权。

## 1. 原型与范围

- 原型：[售后日期与商品件数筛选](prototypes/20260916-after-sale-date-items.html)。独立静态 HTML，可直接打开，无外部依赖；全部为模拟订单与示意商品图，禁止连接真实业务接口。
- 在现有“② 可申请清单”增加日期筛选、件数筛选、单件/多件字段、多商品展示和提交前核对。
- 沿用现有采购售后视觉风格；原型中的示例日期、金额、统计数和快捷日期不作为生产常量。
- 原型聚焦新增交互，未完整重现原页面所有字段与导出入口；不得据此删除原有售后类型、物流号、完整送达时间或 Excel 导出能力。

## 2. 已确认的字段口径

| 字段 | 口径 |
|---|---|
| 订单商品总件数 | 按整单商品实际数量合计，不按图片数、商品种类数或包裹数推断 |
| 单件订单 | 商品总件数 = 1 |
| 多件订单 | 商品总件数 ≥ 2；同款购买 2 件也属于多件 |
| 件数待核对 | 未采集到数量或数据不完整，不能默认算单件或将未知值按 0 处理 |
| 商品明细 | 展示商品图、商品标识/名称、规格及每项数量；商品数量与可退包裹数分开 |
| 送达日期 | 以平台展示的送达日期筛选，含起止日，不擅自转换时区 |
| 送达时间缺失 | 明确标识，不用下单时间、预计送达时间或退货截止时间替代 |
| 订单金额合计 | 仅用于选单核对，不表述为预计或最终退款金额 |

“整单商品数量”与“当前可退包裹内商品数量”不可混用。若现有接口只能返回可退包裹商品，必须补齐整单来源或标记未知，不能据此认定整单单件/多件。跨包裹重复商品须按稳定商品行标识及实际数量处理，不能简单按图片 URL 去重。

## 3. 筛选、勾选与提交

1. 日期：支持起始日、截止日、仅填一端；起止日均包含。起始日晚于截止日时提示无效范围并阻止该范围选单。
2. 件数：全部、单件订单、多件订单、待核对；与日期、申请状态筛选取交集。
3. 日期缺失：有日期范围时不纳入；提供“仅看送达时间缺失”。件数未知不进入单件或多件筛选结果。
4. 申请状态：全部状态、仅可申请、暂不可申请；非可申请订单不能勾选。
5. 展示“扫描总数 / 当前显示数 / 当前可申请数”，区分全部扫描结果与筛选子集。
6. 批量勾选仅作用于当前筛选结果中的可申请订单；表头复选框支持全选及部分选中状态。
7. 修改任何筛选条件时清空勾选，并在有原选择时提示清空数量，避免提交被隐藏的订单；重置筛选恢复全部结果。
8. 底部展示已选订单数及订单金额合计；未选择时禁用提交。
9. “核对并提交售后”打开确认弹层，列出订单、环境、日期、件数，并汇总单件/多件/待核对订单数；支持返回修改。
10. 正式提交前仍使用平台实时资格核验，退款原路退回；日期与件数筛选不能代替资格校验，也不改变现有防重复提交与可靠回传机制。

## 4. 商品图与采集链路

现状已通过源码确认：

- `src/purchase_tool/after_sale_claim.py` 的订单卡片采集使用首个商品图片元素。
- 同文件售后预查询将包裹 `item_list` 投影为首个商品的 `goods_img`，丢弃其余商品及数量。
- `src/purchase_tool/operation_executor.py` 的扫描行投影优先使用首个可退包裹的 `goodsImg`。
- Web 当前每单仅接收一个 `goodsImg`，不能表示多商品订单。

这能解释多商品订单只显示一张图，但不能直接证明跨订单图片重复的原因；相同首商品或图片取错仍需真实样本核对，不能猜测。

目标设计：一行一单，展示多商品缩略图；点击打开明细，明确款数与件数。同款多件不必重复画相同缩略图，但必须展示数量。缩略图数量有限时须标识还有更多商品，完整明细不能丢失。示意原型展示前三款；正式实现需覆盖超过三款的情况。

开发影响面：执行器商品明细采集 → 扫描结果投影 → 云端闭集契约与快照 → Web 筛选/展示。不能只改页面或把可退包裹数当作商品件数。新字段需兼容历史记录和旧执行器，缺失按“待核对”显示；具体字段名、数量来源与能力门槛在开发前核验。

## 5. 同轮已确认及待定位问题

| 项目 | 已确认要求 / 当前结论 |
|---|---|
| 三张结果表高度 | 可申请清单、提交结果、退款跟踪统一约 20 行可见高度；不足时自然增高，超出内部滚动，表头固定；不截断数据、不影响完整导出 |
| 运行状态卡片 | 扫描、提交、回访统一采用物流查询的卡片布局：上方标题/展开收起，下方停止按钮/进度条/进度与耗时；窄屏及吸顶交互一并核对 |
| 平均耗时与累计用时缺失 | 运行中更新，结束后保留；扫描按环境、提交/回访按订单统计。并发下平均耗时算法仍待确认，不能将原型演示值当作已确定公式 |
| 单个订单送达时间缺失 | 原扫描本机结果已为空，非云端或 Web 丢字段。只读复查平台有日期，原读取函数可正常识别；两次观察到卡片先就绪、日期稍后出现，但未复现最终持续漏采。原批次直接原因待确认；评估缺失诊断、有限重试及详情兜底 |

当前正式代码虽使用 `scroll-20`，但缺少相应高度规则；20 行方案是已确认待开发项。具体像素值需依据各表真实标准行高、表头和边框测量，备注换行可能降低可见行数。原型使用 1240px 仅演示 20×60px 行高加表头，不作为正式三表统一像素常量。

## 6. 开发前仍需确定的细节

- 多包裹订单送达日期汇总规则（最早/最晚/全部包裹均送达后才形成订单日期），不能从单包裹示例直接推断。
- 平台真实整单商品数量字段、SKU/规格标识及数量含义，需脱敏证据验证。
- 并发平均耗时、累计用时和重查耗时的统计口径；物流查询的“本轮重查”不可直接照搬到售后。
- Excel 是否增加商品件数/类型/全部图片字段，以及是否新增“导出当前筛选结果”；在明确前保留现有“导出全部扫描结果”语义。

以上细节不推翻已确认原型，但应在对应逻辑实现前确定，禁止用猜测补齐。

## 7. 验收标准

- 单件、同款两件、多款多件、多包裹、数量未知、图片缺失均有样例；字段不混淆且完整明细可查看。
- 日期边界、仅起始/截止日、缺失日期、无效区间、日期×件数×资格组合过滤正确。
- 筛选只影响展示与选择，不删除扫描结果；勾选不跨越隐藏数据，未知字段不会误归类。
- 提交确认列表与最终任务订单集合一致，取消确认不发任务；正式提交仍执行实时资格核验。
- 三表最大高度及表头固定符合 20 行方案，数据和完整导出不截断。
- 新旧执行器/历史快照兼容，异步扫描持续回传时的筛选及勾选行为需回归。
- 本次原型已人工验证：日期与单件联合筛选、筛选后全选和确认、多商品明细、变更筛选清空选择、仅看日期缺失。模拟确认不发送网络请求，不代表正式业务测试通过。

## 8. 交付与下一步

本轮交付为本需求文档和已确认 HTML 原型。正式工程代码、线上服务与安装包均未因本次设计记录而修改；后续先补齐待确认口径，再按已确认原型开展实现、契约测试、独立评审及另行安排发布。
