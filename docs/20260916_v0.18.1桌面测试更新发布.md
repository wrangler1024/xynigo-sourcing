# v0.18.1 桌面测试更新发布

授权：本轮“发布线上更新”。渠道为既有内部 test，未改为正式签名发行。

- 源码提交：`fa610ec`，基于已完成只读真机联调的售后可靠性版本，仅增加版本号、说明及生成页面同步。
- 发布清单提交：`9af85ec`；不可变本地 tag `v0.18.1`。双平台 runtime ID 均为 `0.18.1-fa610ec0f119`。
- Windows 标准安装包 SHA256：`d55afa17b503efd9b4981db07908d616e8deca99cdfc5e6a32bee53385e1307e`，15361682 字节。
- macOS 标准安装包 SHA256：`52d73959885648c0b00dd99d1a809745ed429ecfa475189464c1ae320300ef59`，14362224 字节。打包 Python 3.11.15，包自检、云端 TLS 自检通过。
- 发布及执行器专项 36 项通过，云端安装包清单 19 项通过，公开安全审计和源/云端 Web 一致性通过。独立审核源码、资产实际哈希与发布脚本通过。
- 共享测试环境已切换至 `9af85ec`。切换前完成备份、隔离整库恢复验证及任务排空；仅重建应用服务，数据库仍为 `0040_after_sale_refunds`，数据库容器未重启。旧版安装包保留，定位与回退证据见私有运维记录。
- 当前旧客户端已通过在线检查发现 `0.18.1-fa610ec0f119`，状态为 available；发布更新不等于本机已完成安装，本轮未触发用户系统安装器。
- 本轮未执行 Windows 真机安装验收。两平台仍为内部未签名测试包，macOS 未公证；没有真实退款写入。
- 构建固定候选后，侧会话将紧凑输入区布局合入 main（`d9416a2`）；该增量不在本次安装包及线上云端候选中，需后续版本独立验证发布。
- 本轮没有推送 Git 远端或创建 GitHub Release；线上升级通过系统内认证下载入口提供。
