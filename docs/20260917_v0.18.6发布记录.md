# v0.18.6 补丁发布记录

用户确认发布按环境提交及按环境回访。本文件记录固定发布候选，实际切换、备份和健康核验保存在私有发布记录中。

- 业务候选 `4078d410b512a09e48b27a915cd404de7370ce5d` 已独立评审通过，相关137项测试通过；回访匹配另经PostgreSQL18验证。
- 安装包源码 `9b9fd67e1152c407ddf20b90d5bf798cb7f7cfe4`；双平台 runtime ID `0.18.6-9b9fd67e1152`。版本、大小和哈希来自实际安装包，已回填更新目录。
- 云端和双平台目录统一为v0.18.6，维持test及原内部测试签名状态；没有新执行器能力，v0.18.5执行器协议兼容。
- 迁移保持0042，无在线数据迁移。按环境提交必须实时扫描并确认，按环境回访仅匹配已有历史记录。
- 发布验证包含停收/排空、数据库备份与隔离恢复、应用单服务切换、实际HTML/CSP、认证下载及新匹配接口只读查询；不创建真实扫描、回访或退款提交任务。
