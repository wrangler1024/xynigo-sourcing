# 售后订单与环境提示展示优化

状态：已合并并部署既有工作台 Web；沿用 v0.18.9，执行器安装包不变。基线 `dc9a1cd`，分支 `codex/after-sale-row-display`。

环境直提返回真实订单后，旧页面仍固定在每组订单前展示环境进度行，单订单环境因此看起来有两条订单。现在优先展示订单明细：环境正常结束、已返回订单全部终态且数量不低于环境已报告计数时，收起冗余环境行。

- 一单一条明细，多单各自展示；环境序号、商品、退款凭证及原因保留。
- 环境仍在处理、失败、停止、待核对、带错误信息或没有明细时，保留环境提示。
- 环境报告计数超过已返回明细数量时保留提示，避免掩盖回传不完整。
- 筛选使用全部关联明细判断是否收尾；筛选未显示订单但匹配环境状态时仍保留环境提示。
- 本次提交和历史详情共用渲染规则。只调整展示，不改变订单数据、数量统计、导出、回访或补提范围。

实际验证：真实环境/订单表格 fixture 通过；环境列表、售后反馈、任务生命周期及 Web 接线专项共 **93 passed**。覆盖正常单单/多单、跳过、处理中、异常、明细不足、筛选及旧空结果。云端 Web 与两份原型同步，重复生成无漂移；公开安全审计与 diff 格式检查通过。以上为开发阶段验证；部署核验见下。


## 部署与只读核验

- Web 部署提交：`86aa5e2c2696df6b1a7828f4b8bdebae68ce2909`；已合并 main 并推送。后续归档提交只改文档。
- [独立评审](20260920_售后展示优化独立评审.md)：可合并，无必须改；Node 22 专项及额外 25 个边界检查通过。
- [候选 CI](https://github.com/wrangler1024/xynigo-sourcing/actions/runs/35484641925)：全部 8 项成功，包含云端全量及双平台检查。
- 备份实际代码、旧镜像和数据库；新数据库归档目录验证通过，复用同日已完成的隔离恢复演练证据。维护停入并多次确认无在途任务后切换 auth，数据库未重启或覆盖。
- 健康、就绪、内联脚本 CSP 检查通过；启动无错误，重启计数 0，维护已解除；待核对售后记录指纹保持不变。
- 公网 HTML SHA-256：`5e5016695affae7a9123e2a1b4b3c66775ce995e1e3d64c7951300e47a2b6870`，与候选完全一致。
- 登录后的“本次提交”和同一批次“历史详情”均只读核验：订单身份及数量保持不变，正常结束的环境占位行全部收起。原始业务证据保存在私有空间，不进入公开仓。
- 本次未新建售后、回访或退款写入任务。异常及未完成场景由合成回归验证，不为验收制造真实失败。

刷新工作台即可取得展示更新。本次不更改已发布的 v0.18.9 标签或安装包；后端运行源码仍沿用该版本，仅 Web 部署标记更新。
