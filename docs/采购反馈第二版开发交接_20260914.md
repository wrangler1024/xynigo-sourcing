# 采购反馈第二版开发交接

状态：开发候选，未发布或切换测试机。分支 codex/purchase-feedback-v2；本机候选0.17.24、插件候选0.10.0。现有发布目录仍按已验证0.17.23资产解释，不把候选源代码当作已安装版本。

## 实现范围

- 性能：共享HTTP连接、租户及凭证隔离的token缓存，去除重复元信息查询，格式化身份字段保持不变，原始金额与图片按目标行批量读取；每个写入阶段仍重新定位核验。脱敏冷缓存单商品链路31→18次飞书请求；选择填色含必要复核为22次。多商品仍使用批量读取；真实耗时未作提速百分比承诺。
- 本机：轻量订单复核不重复计算截图边界；短期CDP端口缓存仍逐次验证URL和document marker；按凭证锁定，最多4个后台提交，status只查询不重放。认证使用调用时的成员和token快照，网络等待不持公共认证锁。
- 填色：默认不填色，每次新操作重置；七种浅色，仅当前任务行A～AR。凭证完成先持久化，填色独立锁定当前revision并核对实时任务与原图。pending/failed可仅重试颜色，旧revision不覆盖新颜色，补色与查询使用相同活动requestId。
- 体验：面板120ms淡出、140ms淡入，保留输入和原visibility优先级，减少动态效果时不动画。10秒看门狗恢复面板，过期截图不得使用。

## 验证

两端全量回归已运行通过；后续恢复与兼容修正逐项追加回归。专项覆盖默认不填色、连续/不连续商品行、进程中断、跨Session新revision、格式化前导零与数量、补色超时、后台重复提交、身份变化、任务切换迟到结果。真实Chromium验证隐藏/恢复、保留输入、减少动态效果和默认不填色的8选项界面。

只读验证了现有飞书接口支持不指定终止行的范围读取，没有写入实际订单或颜色。实际Windows/HubStudio耗时基线、真实任务填色和截图验收仍待发布前验证。

## 发布顺序与限制

先部署新云端代码，再更新本机执行器和插件。新字段通过features协商；旧客户端仍同步提交，不发送填色参数。无新增数据库迁移，颜色元数据复用既有JSON列。安装包、云端目录与团队扩展分发本轮尚未更新。代码完成、发布完成、业务验收分别记录。
