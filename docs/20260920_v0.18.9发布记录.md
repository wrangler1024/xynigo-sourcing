# v0.18.9 测试渠道发布记录

状态：20260920 已合并主分支、推送，并部署既有测试环境。未扩大到生产环境；未新增真实退款申请、真实回访或业务批量写入。

## 源码与评审

- 业务修复：`10c48e2c3f2d2d27ab58334d917ac5f061153af2`。
- 双平台构建源码：`f55acdefd9b51a2c36c6cba4006c9c13e20dca4f`。
- 部署及资产目录提交：`0eb707b6fa25845bc4ab6138cf758c5c9118fe2e`，不可变标签 `v0.18.9`。
- 分支 `codex/after-sale-feedback-fixes` 已快进合并到 main 并推送。后续归档提交仅更新文档，不改变以上运行代码或安装包。
- [独立评审](20260920_售后反馈修复独立评审.md)结论可合并、无必须改。
- [最终候选 CI](https://github.com/wrangler1024/xynigo-sourcing/actions/runs/35483127564) 全部 8 项成功，包括云端全量、三个 Python 版本、Windows 更新器、Windows 安装升级卸载、macOS 打包及安装包检查。

## 交付内容

修复环境直提结果丢失、跳过原因笼统、环境状态提示、回访字段列对应、两阶段提示和部分结果手动接续；改善团队环境定位错误及倒计时解析。具体范围及限制见 [实现记录](20260920_售后反馈修复与回访衔接.md)。

云端先升级可选字段接收方，再开放桌面新版本下载；旧执行器继续兼容。数据字段采集修复需要用户将实际执行任务的电脑升级到 v0.18.9，网页版本更新不会替代本机升级，历史批次也不会自动补写。

## 安装包

两平台 runtimeId 均为 `0.18.9-f55acdefd9b5`，继续沿用 `test` 渠道及内部未签名测试标记，未将其标记为稳定可信发行。安装包通过已登录工作台提供下载。

| 资产 | 字节数 | SHA-256 |
|---|---:|---|
| Windows 标准安装包 | 15413016 | `051f60b1665fccd58f30a70f85696163dd6ebc191dbea112916c646f280ca0c0` |
| macOS ARM64 标准安装包 | 11791522 | `0b452a3d46aadd3f78944444d503282a450706f8f0b2a12cf75d84ed60f68a20` |

本机构建文件、旁车 JSON/SHA、云端目录以及部署容器内实际挂载文件的哈希和大小一致。macOS 发布包展开后签名完整性、版本、原生自检及 TLS 自检通过。CI 的 Windows 原生验收对应同源重新构建包，不混同为对本机发布二进制逐字节的安装验收。

## 部署核验

- 旧应用镜像、实际源码、配置与 PostgreSQL 均保留回退材料；备份目录检查及隔离 PostgreSQL 恢复验证通过，原始定位仅存私有运维记录。
- 采用既有维护入口停止新请求，多次确认执行器、导入及采购凭证在途数为零后切换 auth；未并行启动第二个业务 worker。
- 迁移 head 前后均为 `0046_shein_settlement`，无新增迁移；数据库容器启动时间保持不变。
- 健康与就绪检查成功，应用重启计数为零；启动日志无 ERROR/Traceback，维护入口已恢复。
- 公网 HTML SHA-256 为 `9a12c6f6570251d861007f912cbbe7b6bb47b3908108e45a33e40e5ccbe006ae`，与候选完全一致；内联脚本 CSP 校验通过。
- 切换前后待核对售后记录的状态指纹一致；没有修改或重派未知写入记录。
- 登录工作台显示 v0.18.9，两个安装包下载请求均返回 HTTP 200，设备心跳与只读配置摘要正常。浏览器工具不允许访问下载记录页，下载后本机文件哈希尚待取得实际保存路径；没有把 HTTP 200 当作该项哈希复验完成。

应用切换时自动回退脚本已备妥，本次未触发回退。后续回退云端前须先处理发送新增字段的执行器；不能直接把旧云端切回而保留新生产方。当前电脑尚未完成安装升级，真实业务功能验收另行执行。
