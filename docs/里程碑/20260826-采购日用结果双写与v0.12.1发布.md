# 采购日用结果双写与 v0.12.1 发布

日期：2026-08-26

状态：共享测试部署和全员测试版发布均已完成。

## 结果

- 买家号建环境和物流单号查询的真实工作结果现在先写本机持久化待发队列，再幂等上报共享服务。
- PostgreSQL 同一事务保存任务、逐条结果和 `operational_sync_outbox`；飞书失败不会丢失数据库事实。
- 云端 Worker 按稳定同步键创建或更新飞书记录，写后回读成功才标记完成，失败有限重试。
- 成功建环境结果同时补充不含凭证的 `buyer_accounts` 安全资源元数据，为后续下单绑定买家号做准备。
- 密码、Cookie、接码 Key、Token、完整账号及原始外部响应均禁止进入接口、数据库、日志和飞书。

## 共享测试部署

- 服务健康：`/healthz` 正常，`/readyz` 为 ready。
- 数据库迁移：`0010_operation_result_sync`。
- 新表：`environment_creation_runs`、`environment_creation_results`、`logistics_query_runs`、`logistics_query_results`、`operational_sync_outbox`。
- 部署验收时五张表均为 0 条，未注入合成业务记录。
- 两个服务端入口已进入 OpenAPI：`PUT /v1/operations/environment-creation-runs`、`PUT /v1/operations/logistics-query-runs`。
- Worker 已启用，启动日志无错误；应用身份能读取两张目标结果表字段。

部署前备份：

- 应用：`/opt/xynigo-auth-test/backups/deploy/pre-operation-sync-20260826T042630Z/app.tar.gz`
  - SHA-256：`1d0a7d26c9be73c073e72703a7a4c6e6461e2db5225c9e856135a20dd96ba0a0`
- PostgreSQL：`/opt/xynigo-auth-test/backups/postgres/pre-operation-sync-20260826T042630Z.dump`
  - SHA-256：`126f97cf26a690dcc88d24d10066da6d7dca6031cac5ba3843a7b518d03542b1`

本地迁移演练备份：`.codex_tmp/db-backups/pre-operation-sync-20260826.dump`，SHA-256 为 `e49f5e089a6cc6768b61defd1187a3deed06e7b24bfc9fdc97939321fc26e865`。

## 独立飞书多维表

- 当前承接持续同步的 Base：[`Xynigo 买家号资源同步测试表`](https://ccn3ygojej8v.feishu.cn/base/TZDabK44kaxVKZsDK2ycxb8qnBe)
- `买家号安全同步测试表`：16 个字段。
- `买家号建环境结果测试表`：26 个字段。
- `物流单号查询结果测试表`：30 个字段。

三张表位于全新 Base，不包含关联字段，也不连接或替换现有“希音采购买家号台账”。本里程碑初次只读复核时三张表均为 0 条；后续 `0.12.2`/`0011` 已将旧表 320 条白名单安全元数据迁入 PostgreSQL，并由 outbox 镜像到当前 Base，详见买家号数据库事实源里程碑。

## v0.12.1 发布

- 发布页：<https://github.com/wrangler1024/xynigo-sourcing/releases/tag/v0.12.1>
- 公开净化分支：`codex/release-v0.12.1-public`
- 发布提交：`6829b1b5b60d0d3bb14db2a68a5fe5a26c69306f`
- 标签：`v0.12.1`；latest、非 prerelease，包内渠道为 `test`。
- Windows SHA-256：`73b608a6f2fb7bdab70d1f2206cefaf0e9a961e64b8135d699b643b142093032`
- macOS ARM64 SHA-256：`2f44137d25041c5d1d676914e0cb6933ecaae8930e04a2dbddbc6723e40562d6`
- GitHub Actions 的 Python 3.9/3.11/3.12、Windows 更新器、Windows 包冒烟和 macOS 打包自检均通过；`v0.12.0` 更新器已实际发现 `v0.12.1`。

## 验证与边界

- 内部完整工作区：本机 279 项完成（276 通过、3 项 Windows 条件跳过）；云端 60 项通过。
- 公开发布树：278 项完成（275 通过、3 项跳过），公开源码审计和双平台包结构检查通过。
- 当前仍是团队共享测试环境，不代表正式生产上线。其他功能继续按测试边界运行；真实下单、生产环境和正式数据路由没有因本里程碑自动开放。
