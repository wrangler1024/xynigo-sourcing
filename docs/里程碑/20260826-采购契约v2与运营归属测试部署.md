# 采购契约 v2 与运营归属测试部署

日期：2026-08-26

状态：已部署到广州共享测试环境；业务操作日志未随本次上线。

## 部署范围

- 云端采购契约从仅接受 v1 调整为同时接受 v1/v2。
- v2 显式接收并校验 `storeBaseName`、`operatorName`；组别暂不解析。
- `purchase_orders` 增加 `store_name`、`store_base_name`、`operator_name` 和对应索引。
- 采购概览、任务范围、本人已认领采购单、站点/关键词筛选，以及列表/详情的运营、采购员、件数、进度和采购批次摘要进入共享测试环境。
- 旧 v1 规范化摘要保持不增加新字段，避免历史内容哈希发生无意义变化。

## 迁移与隔离

- 测试库从 `0004_procurement_execution_test` 升级到 `0006_purchase_operator`。
- 业务日志迁移 `0005_business_operation_logs_p0` 改为承接 `0006`，本次未执行；共享测试环境不存在新增日志列，`/v1/business-logs` 未开放。
- 首次迁移因原迁移 ID 超过 `alembic_version.version_num varchar(32)` 被 PostgreSQL 拒绝，事务整体回滚，旧认证容器持续在线；将 revision 缩短为 `0006_purchase_operator` 后重新验证并成功迁移。

## 备份与回滚

- 应用、环境与清单：`/opt/xynigo-auth-test/backups/deploy/pre-purchase-contract-v2-20260825T163931Z/`。
- PostgreSQL：`/opt/xynigo-auth-test/backups/postgres/pre-purchase-contract-v2-20260825T163931Z.dump`。
- 备份均为 `root:root 0600`；SHA-256 和 `pg_restore -l` 通过。
- dump 已恢复到临时数据库验证，恢复版本为 `0004_procurement_execution_test`，采购单数量与部署前一致；临时验证库随后删除。
- 旧认证与迁移镜像保留 `pre-purchase-contract-v2-20260825T163931Z` 标签。

## 验证结果

- 隔离部署树云端测试 38 项通过；完整工作区云端测试 45 项通过。
- 公网 `/healthz`、`/readyz` 正常；OpenAPI 的 `PurchaseDraft.schemaVersion` 为 `[1, 2]`，并包含两个运营归属字段。
- 合法 v2 未认证提交到达鉴权层并返回 `401 authentication_required`，不再出现旧云端契约导致的 422。
- 部署前后采购单、明细、Outbox 和分单数量一致；既有采购单的店铺原名/基础名全部回填，现有可识别店铺的运营姓名全部解析成功。
- 未创建测试采购单，未触发 HubStudio、SHEIN、店小秘审核或飞书 Base 写入。

## 采购列表利润字段追加部署

2026-08-26 对采购同事刚提交的真实测试单做只读链路核对，确认插件已经把
`estimatedMetrics.estimatedProfit` 和 `estimatedMetrics.profitMargin` 传入云端，
云端采购单明细也已原样保存。列表显示为空的根因是共享测试环境仍使用旧列表
序列化逻辑，没有把两个嵌套指标展开到列表摘要，并非插件漏传。

- 共享测试环境追加上线列表字段 `profitCurrency`、`estimatedProfit`、`profitMargin`，同时开放店铺、运营精确筛选和筛选选项读取。
- 本次只替换 `main.py` 与 `purchase_service.py`，未部署业务日志模块，未执行数据库迁移；迁移版本保持 `0006_purchase_operator (head)`。
- 部署前应用和环境快照：`/opt/xynigo-auth-test/backups/deploy/pre-procurement-list-profit-20260825T171840Z/`。
- 部署前数据库：`/opt/xynigo-auth-test/backups/postgres/pre-procurement-list-profit-20260825T171840Z.dump`。
- 备份均为 `root:root 0600`，SHA-256 与容器内 `pg_restore -l` 校验通过；旧镜像保留 `pre-procurement-list-profit-20260825T171840Z` 标签。
- 隔离云端测试 38 项、插件测试 29 项通过；公网 `/healthz`、`/readyz` 正常。
- 当前登录会话回读验证：最新测试单列表摘要返回 `profitCurrency=MXN`、`estimatedProfit=108.27`、`profitMargin=51.99`，与云端明细中的 `estimatedMetrics` 一致。
- 未新增、修改或认领采购业务单，未触发 HubStudio、SHEIN、店小秘审核或飞书写入。

## 当前边界

本次只解决运营采购助手正式提交契约和采购列表读取。真实资源绑定、打开环境、下单尝试、付款结果、成功环境留存、失败环境删除、查单跟单和飞书同步仍未开放，必须继续经过单独业务设计与授权。
