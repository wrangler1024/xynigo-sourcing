# 采购执行测试版云端联调

日期：2026-08-25

状态：已部署到隔离测试实例并完成首轮受控写入联调；真实资源绑定、下单和飞书同步未开放。

## 部署范围

- 云端迁移升级到 `0004_procurement_execution_test`。
- 新增 `procurement.execution.manage`，目录同步后为 `super_admin=29/29`、`admin=26/29`、`member=0/29`。
- 部署 Web 采购中心概览、列表、详情读取，以及认领、分单保存、采购执行队列接口。
- 本机 `127.0.0.1:8771` 加载当前工作区，复用系统安全存储中的既有云端会话；`8767` 未停止。

## 备份与回滚

- 部署前应用和 `.env` 快照：`/opt/xynigo-auth-test/backups/deploy/pre-procurement-execution-20260825T071332Z/`。
- 部署前数据库：`/opt/xynigo-auth-test/backups/postgres/pre-procurement-execution-20260825T071332Z.dump`。
- 应用、环境、校验清单和 dump 均为 `root:root 0600`；SHA-256、归档清单和 `pg_restore -l` 检查通过。
- dump 已恢复到临时数据库验证，迁移为 `0003_purchase_request_foundation`，主单/明细/Outbox 为 `1/1/1`；临时验证库随后删除。
- 部署前运行镜像保留为 `xynigo-auth-auth:pre-procurement-execution-20260825T071332Z` 和 `xynigo-auth-migrate:pre-procurement-execution-20260825T071332Z`。

## 构建说明

服务器首次拉取新 Python 基础镜像时访问 PyPI 极慢。运行约 30 分钟后仍停留在依赖下载，因此只终止了未完成的构建任务，旧认证容器始终在线。核对新旧 `requirements.lock` SHA-256 完全一致后，以部署前运行镜像为依赖基线，仅覆盖 `alembic.ini`、`migrations/` 和 `src/` 完成增量镜像；随后 Compose 迁移容器退出 0，认证容器正常替换。

## 验证结果

- `/healthz`、`/readyz` 均正常；有效载荷的未登录认领请求返回 401。
- 云端迁移为 `0004_procurement_execution_test (head)`，`purchase_splits` 和 `purchase_split_lines` 已创建。
- 迁移后原有主单、明细和 Outbox 保持 `1/1/1`，迁移本身未创建分单。
- 当前超级管理员真实会话完成 1 次认领和 1 次分单保存：订单 `execution_revision=1`，明细状态为 `claimed`，分单状态为 `waiting_binding`，分配数量为 1。
- 分单的 Hub 环境、买家号和平台采购单号均为空，没有保存模拟资源引用；采购执行队列显示 1 张待绑定分单，店铺销售订单号搜索命中。
- `purchase_order.lines.claim` 和 `purchase_order.split_plan.save` 各有 1 条成功审计。
- 浏览器控制台无 error/warning；详情关闭后敏感收件信息从 DOM 清空。
- 本地 233 项测试完成（230 通过、3 跳过），云端服务 37 项通过。

## 未触发的外部动作

- 未调用 HubStudio 环境打开或下单。
- 未向 SHEIN 提交订单或回填平台采购单号。
- 未触发店小秘原生审核。
- 未写入飞书 Base；原 Outbox 仍为待处理。

## 下一步边界

先共同确认同一明细拆数量、跨采购单资源占用、资源解绑、开始下单、失败回滚、退回和异常状态规则，再开发真实资源绑定和后续执行写接口。买家号密码、Cookie、接码 Key 和飞书凭证继续只存在受信系统，不进入前端或采购数据库。
