# 2026-08-28 v0.12.6 发布后完整开发周期、v0.12.7 统一入口与执行器收口

记录时间：2026-08-28 22:55 CST

复盘范围：从 `v0.12.6` 标准安装包与设备通道发布后开始，覆盖云端统一下载入口、Windows/macOS 真实安装、设备配对与心跳、状态展示、设备配置边界、`v0.12.7` 候选、`workspace.rpc.v1` 业务通道、Windows hotfix1–11、macOS hotfix6–7、在线更新和 US 建环境闭环。没有生成 hotfix 资产、但在开发或真实使用中暴露的问题同样纳入。

## 里程碑结论

Xynigo 已从“云端页面 + 旧本机页面并存”推进到“云端 Web 统一入口 + 本地执行器安全执行”的共享测试阶段。`workspace.rpc.v1` 已把物流查询、买家号文件解析与入库、账号注册、HubStudio 建环境、店铺/代理读取和文件导出交给当前用户配对的本地执行器；PostgreSQL 保存任务、租约、进度与脱敏回执，本机敏感正文使用独立域 AES-GCM 后才进入任务存储。

这次切换不是一次干净的新版本发布，而是在两位采购同事真实电脑上连续暴露问题后形成的 `v0.12.7` 热修复列车。当前 Windows 已推进到 hotfix11，macOS 已推进到 hotfix7。功能已可供共享测试团队使用，但版本治理、平台签名、真实设备验收和主工作区收口尚未完成，不能称为生产上线。

## 当前权威状态

### Git 与发布

- 内部主工作区：`codex/new-ui-migration` / `f34b26d`，存在未提交的系统订单键、采购助手、HubStudio、采购契约、文档、原型和输出目录改动。
- 完整热修复主线：`codex/release-v0.12.7-executor-bridge-hotfix` / `0d5ed6e`。
- Windows 状态中心 v2 阶段分支：`codex/windows-status-center-v2` / `7d7b1b3`，CI `33139625838` 全绿；该分支基于较早热修复点，只可作 UI 设计与测试证据，不可作为当前发布基线。
- `v0.12.7` 标签固定在 `38c3aca29401f05f9875c42ca0169e7c31011241`，Release 为 prerelease；GitHub Latest 仍为 `v0.12.6`。
- hotfix11 提交 `0d5ed6e` 的 CI `33181243218` 全绿。

### 共享测试部署

- 地址：`https://xynigo.samforo.icu`。
- `/healthz`：`ok`；`/readyz`：`ready`。
- OpenAPI：`0.12.7`。
- 数据库：`0017_executor_workspace_rpc (head)`。
- 当前镜像：`sha256:284dfc60abbd3d92764fc6723f1b3683ccc19921488efe023022ddcac3f81f6b`。
- 当前容器启动时间：2026-08-28 22:39 CST。
- Windows 默认下载：`Xynigo_Sourcing_Windows_Setup_v0.12.7_hotfix11.exe`，15,055,297 字节，SHA-256 `54320a6d475dd681d247468b568cd1479942109e6a84fc428b8666064aea17ac`。
- macOS 默认下载：`Xynigo_Sourcing_macOS_Standard_v0.12.7_hotfix7.pkg`，19,399,200 字节，SHA-256 `35cc3a77a27090ed00726ea26ea40fcdf503b7e69178ac4144620ac7146166b0`。
- 服务器资产、云端目录和 GitHub Release 资产哈希一致；下载接口继续要求同域登录，匿名请求返回 401。
- 这是共享测试部署；生产服务器、正式域名、正式数据库和正式飞书路由均未部署。

## 第一阶段：v0.12.6 发布后、v0.12.7 候选前的完整问题复盘

| 现场问题 | 直接原因 | 更深层根因 | 修复过程 | 防复发门禁 |
|---|---|---|---|---|
| 云端下载按钮跳转 GitHub，采购同事看到外部站点、信任和下载警告 | Release catalog 直接暴露 GitHub asset URL | 只完成“可下载”，没有把统一入口、认证和浏览器信任作为一条产品链路验收 | 增加同域认证下载接口；服务器只读保存审核过的资产；返回前校验大小和 SHA | Web 测试断言下载地址只允许同域相对路径；匿名下载必须 401；部署后回下载并复算 SHA |
| 下载页默认选择 macOS，团队大多数同事却使用 Windows | 默认值跟随开发机/浏览器平台 | 产品默认值由开发环境推导，没有按真实用户结构决策 | 团队入口固定默认 Windows，Mac 由唯一使用者手动切换 | 新默认值必须记录目标用户占比和产品决策；不能只在开发机上验收 |
| Windows 浏览器提示“请打开前确保信任”，Smart App Control 直接阻止 | 标准包未做 Authenticode；Smart App Control 不支持单应用例外 | 把“内部未签名测试例外”当作普通 SmartScreen 提示，低估不同 Windows 安全层级 | 明确内部手动处理教程；保持 SHA 校验；稳定渠道继续拒绝未签名包 | 签名状态必须进入 release metadata 和稳定渠道门禁；真实 Windows 至少覆盖 SmartScreen 与 Smart App Control 两类设备 |
| macOS 安装包无法打开 | 无 Developer ID Installer、公证和 stapling | 标准包结构完成不等于平台信任链完成 | 通过系统设置手动放行内部包；保留 SHA 和绿色版回退 | Mac 稳定版必须在最终 `.pkg` 上验证签名、公证、stapling；内部例外必须显式标记 |
| Mac 显示“配对成功”，云端仍长期离线 | 执行器启动时只加载一次设备凭证；启动后再配对不会重新进入轮询 | 把配对看成安装前置，而没有测试“先启动、后配对”的真实生命周期 | 运行中的 worker 自动发现新凭证并开始心跳，无需重启 | 生命周期测试固定覆盖：未配对启动 → 生成码 → 配对 → 不重启进入在线 |
| Mac 周期性弹出 Python 读取钥匙串授权 | 每次长轮询/状态读取都重新访问 Keychain；飞书 App Secret 也重复读取 | 安全存储被当成普通配置读取，没有设计进程内缓存和失败缓存 | 设备凭证在单次运行复用；飞书凭证增加进程内缓存；拒绝结果在当前进程内短路 | 测试注入安全存储计数器，断言正常轮询只读取一次；拒绝后不得形成弹窗风暴 |
| 退出 Xynigo 后，云端短时间仍显示“本地执行器在线” | 云端只能等心跳 TTL 过期，没有实时断开事件 | UI 把租约状态展示成即时进程状态，没有说明最终一致窗口 | 所有页面按统一 TTL 刷新；在线口径改为真实心跳而不是安装/配对结果 | 使用假时钟测试在线 → 停心跳 → 过期离线；页面必须显示最后心跳时间，不能宣称即时断开 |
| HubStudio 主窗口退出后云端仍显示已连接 | HubStudio Local API 子进程仍存活，父窗口退出不等于 API 退出 | 用“客户端窗口/进程”代替实际依赖能力作为状态真相 | HubStudio 状态改以 Local API 可达、认证和协议兼容为依据；进程探测只作失败诊断 | 状态模型分开 `clientRunning / localApiEnabled / authenticated`；测试覆盖主窗口关、API 子进程仍在的组合 |
| 顶部同时出现“执行器 1 台在线”和“本机执行器离线”，退出后数量还残留 | 设备列表计数、所选设备状态和本机探针由不同轮询维护 | 同一概念有多个前端事实源，刷新频率与 TTL 不一致 | 移除没有操作意义的设备数量；所有业务页面复用统一执行器状态轮询 | 顶栏只消费单一状态模型；端到端测试模拟列表在线但当前设备离线，禁止互相矛盾文案 |
| “1 台在线”让用户误解为必须关心设备数量 | 后端计数直接暴露到用户界面 | 没有先定义顶部状态要回答的用户问题 | 顶部只显示“本地执行器在线/离线”，设备数量留在管理页 | UI 验收先写问题口径：顶部回答“当前能否执行”，管理页才回答“有多少设备” |
| 执行器与 HubStudio 的绿点、胶囊和间距不一致 | 两套组件分别开发 | 缺少全局状态组件和颜色语义规范 | 统一状态 pill、信号点、文本和连接态颜色 | 所有连接状态必须引用同一 CSS 组件/颜色 token；视觉回归检查三个状态并排截图 |
| 本地设备配置仍包含站点、HubStudio 分组和采购员模板 | 旧 `config.json` 同时保存运行时参数和业务策略 | 没有明确“设备能力”和“业务任务”的领域边界 | 云端设备配置只保留端口、并发、抽检和安全并行；业务字段移回任务请求，读取兼容旧字段 | 设备配置使用后端 allowlist；新增字段必须回答“更换电脑后是否仍应保留”，否则不得进入设备配置 |
| “保存本地配置”迁到云端后同事误以为会写云端公共配置 | 页面入口云化，但数据所有权说明仍沿用本机时代 | 入口位置、存储位置和执行位置三个概念混淆 | 由云端通过所选设备读取/保存本机 `config.json`；页面明确业务字段不固化到电脑 | 每个设置项标注事实源和作用域；契约测试禁止云端返回代理、Cookie、Token 和业务分组 |
| 旧绿色包迁移提示目录无效，必须包含 `VERSION.json` | 用户选择了下载目录或安装器文件，不是绿色包根目录 | 安装器只做技术校验，没有在选择前解释目录契约 | 允许不迁移直接安装；文档明确仅已有绿色包用户选择含 `VERSION.json` 的根目录 | 目录选择页提前显示示例路径并即时校验；CI 覆盖不迁移、有效迁移、无效目录三条路径 |
| Windows 状态中心实际效果是原生白框和系统按钮，与高保真稿差异大 | Walk 不支持原型中的圆角、阴影和自定义托盘头图；Grid 只有两列且未按可用宽度设计 | 原型没有绑定实现技术栈，代码验收又只检查能启动 | 重做可落地原型；四卡等宽、配对按状态显隐、托盘回归 Windows 原生菜单；真实 CI 创建窗口 | 原型评审必须写明平台控件边界；Windows 100%/125%/150% DPI 真实截图纳入发版检查 |
| 配对入口在“在线”原型状态下隐藏，首次用户以为没有设备配对 | 原型默认展示成功态 | 原型默认状态没有按首次安装主路径选择 | 默认切到“首次使用/待配对”；已配对后才收起输入框 | 所有安装类原型默认展示首次关键任务；成功态作为可切换状态，不作为唯一首屏 |

## 已实现能力

1. 云端 Web 是员工统一业务入口；本机旧页面只保留诊断和回滚。
2. 设备使用 5 分钟一次性配对码，凭证进入 Keychain / CurrentUser DPAPI；云端设备接口同时按组织和当前用户隔离。
3. 本地执行器通过 20–25 秒 HTTPS 长轮询领取任务，使用租约、幂等键、进度与不确定态避免重复外部写入。
4. `workspace.rpc.v1` 只接受服务端白名单路径和方法；敏感买家号文件与结果单独加密。
5. Windows 标准安装包具备状态中心、托盘、协议启动、覆盖安装、构建修订运行时目录和在线更新基础。
6. macOS 标准包已带可信 CA 包，冻结运行时可以验证云端 TLS。
7. HubStudio Local API 使用有限端口、回环直连、认证状态和明确错误码；不把 `hubstudio-cli` 作为采购员依赖。
8. 状态中心定版 UI 已合入完整热修复主线：总体状态、云端/HubStudio/任务/版本四卡、动态配对区和原生托盘菜单。

## 第二阶段：v0.12.7 业务通道与热修复问题

下表记录候选发布后的数据库、安装器、执行器、业务 RPC 和在线更新问题。hotfix 编号只是资产顺序，不是复盘范围边界。

### 热修复与故障时间线

| 阶段 | 现场问题 | 修复与经验 |
|---|---|---|
| 云端首发后 | 历史建环境/物流结果上传时，父任务未 flush，PostgreSQL 外键拒绝子记录 | 在写子结果前显式 flush 父记录；SQLite 测试启用外键，避免测试库掩盖生产数据库约束 |
| Windows hotfix1 | Walk 把绝对图标路径再次拼接安装目录，状态中心启动即崩溃 | Declarative 图片使用相对资源名；直接文件 API 才使用安装目录绝对路径 |
| Windows hotfix2 | 连续点击配对使一次性码被重复消费；Python 中文错误输出乱码 | 配对做 single-flight，进行中锁定输入和按钮；子进程固定 UTF-8 |
| 云端授权修复 | 同组织成员能看到或操作其他成员的设备 | 设备列表、摘要、配置、撤销、任务查询/取消统一增加 owner 校验，越权返回 404 |
| Windows hotfix3 | Web 已看到在线设备，但业务模块仍走旧本机路径并提示未启动 | 引入 `workspace.rpc.v1`、迁移 `0017` 和执行器短期业务会话，正式切到云端调度 |
| Windows hotfix4 | Win32 实际 UI 与高保真原型差异大，状态卡挤在中央，配对入口不清楚 | 原型回归 Walk 可落地边界；四卡等宽、配对区按状态显隐、托盘使用原生菜单 |
| Windows hotfix5 | 同为 `0.12.7` 的覆盖安装只替换 `Xynigo.exe`，`SetOverwrite off` 继续复用旧 Python 运行时 | 运行时目录必须包含构建修订号；覆盖安装要切换不可变运行时，版本号相同不代表二进制相同 |
| 双平台 hotfix6 | HubStudio 显示已连接但分组读不到；回环请求被系统代理转发；并发短读取互相报忙；旧 Python 残留 | localhost 强制直连；短读取可安全排队；只按验证过的 PID 接管旧执行器；Walk 资源根固定为安装目录 |
| macOS hotfix7 | 冻结 Python 缺少系统可信 CA，HubStudio 正常但云端 TLS 失败 | 随包提供 certifi CA，并在最终冻结程序上直接做 `/healthz` TLS 自检 |
| Web 轮询修复 | 空闲页面每 1.5 秒仍创建进度 RPC，挤占执行器队列 | 只初始化当前模块；分组请求防重入；队列繁忙只有限重试 |
| Windows hotfix7 | 状态中心驻留托盘时，HubStudio 探针周期性闪黑框 | Local API 可达时不调用 `tasklist.exe`；诊断进程强制 `CREATE_NO_WINDOW` / `SW_HIDE` |
| Windows hotfix8 | 买家号文件解析未在写环境前充分校验 Cookie；失败重试后页面行状态不刷新 | 文件解析阶段做 Cookie 站点/结构预检；重试后强制回读目标行，不靠旧前端快照 |
| Windows hotfix9 | 不同来源的有效买家号 Cookie 组合被过度严格规则误拒绝 | 区分可验证站点 Cookie 与允许的混合登录 Cookie，保持安全校验但不以单一模板判断全部账号 |
| Windows hotfix10 | 标准安装包缺少从云端受控更新的完整链路；CI 错把父子安装进程生命周期当成单一进程 | 增加认证下载、SHA/大小核验、用户确认和安装器交接；测试分别等待安装器并模拟执行器主动退出 |
| Windows hotfix11 | 美国站建环境的站点配置在前端、RPC 与本机配置之间没有闭环 | 将 US 站点字段纳入白名单、配置解析、执行参数、页面回读和测试，避免只改 UI 文案 |

## 关键踩坑经验

### 版本与安装包

- 同一 SemVer 下不断上传新资产会让标签、Release、云端目录和员工已安装运行时失去一一对应关系。`v0.12.7` 已证明这种做法难以审计；下一步必须提升版本号。
- NSIS 的根启动器覆盖成功，不代表版本目录中的 Python 执行器已更新。安装回归必须预置旧同版本运行时，再验证 `current-version`/构建修订真正切换。
- 在线更新涉及执行器、状态中心、安装器和新执行器四个生命周期，不能用“等待一个 PID 退出”作为成功条件。
- 未签名不是普通提示问题。Smart App Control 不支持单应用例外；内部测试可以人工关闭，团队稳定使用必须做 Authenticode/Developer ID。

### Windows 原生 UI

- Walk Declarative 的字符串图片由资源管理器相对可执行目录解析；绝对路径会被二次拼接。
- 原型必须承认 Win32/Walk 的真实边界。复杂圆角品牌菜单不会自动变成原生托盘菜单；先画可落地结构，再写代码。
- 后台诊断命令也会影响用户体验。任何 `tasklist`、Python 或辅助命令都要显式隐藏控制台并限制调用频率。

### 云端与本机通道

- “页面在线”“云端有设备”“本机能执行白名单业务”是三种不同状态，必须分别检查心跳、能力版本和 RPC 支持。
- 回环流量必须绕过系统代理；否则 localhost 可能被代理软件转发，形成难以识别的连接错误。
- 轮询不是越频繁越实时。空闲轮询也会生成任务、租约和数据库负担，必须防重入、合并读取并设置退避。
- 设备授权不能只按租户；共享电脑场景下还要按当前用户 owner 隔离。

### 数据与外部写入

- PostgreSQL 外键、事务 flush 和 SQLite 默认行为不同；测试库必须主动开启等价约束。
- Cookie 校验要验证安全属性与目标站点，不能把“结构不同”直接等同于无效；真实账号会包含多来源登录 Cookie。
- 外部任务失败后页面必须重新读取服务端状态，不得靠本地行快照猜测是否成功。
- 不确定结果不得自动重试。物流、建环境、买家号、飞书和采购外部写入都必须使用幂等键、接管和人工复核边界。

## 根因模式与防复发总矩阵

| 根因模式 | 本周期表现 | 以后必须执行的工程门禁 |
|---|---|---|
| 只测开发机，不测目标平台 | Mac 默认平台、Windows Walk UI 偏差、Smart App Control、黑框 | 每次标准包发布前至少一台真实 Windows 与一台 Apple Silicon Mac；保留 DPI、平台安全提示、启动、托盘、退出截图与结果 |
| 把“能构建”当成“能升级” | 同版本运行时未替换、安装器父子进程判断错误 | CI 先安装旧包并写入数据哨兵，再升级新包；验证运行时构建修订、进程交接、配置/日志/配对保留 |
| 一个概念有多个事实源 | 顶部在线数量、所选设备、本机离线互相矛盾 | 后端定义唯一状态机；前端所有页面复用同一 selector/轮询；假时钟覆盖心跳过期和恢复 |
| 用父进程或窗口判断能力 | HubStudio 窗口关闭但 Local API 子进程仍在 | 状态必须直接探测依赖 API；进程存在只作诊断，不能作为“已连接”的最终依据 |
| 安全存储按普通配置高频读取 | Keychain 连续弹窗 | 进程内缓存成功值和拒绝结果；测试统计读取次数；日志不得打印服务名对应的秘密 |
| 领域边界未收口 | 站点/分组/采购员写入设备配置；US 参数链路漏一段 | 设备配置、业务任务和组织策略分别使用 allowlist；新增字段必须做 Web → 云端 RPC → 本机 → 回读全链路矩阵 |
| 本机回环仍受系统环境影响 | localhost 被代理转发、辅助命令弹窗、shell 参数解析错误 | 回环客户端强制无代理；Windows 子进程默认 `CREATE_NO_WINDOW`；PID/端口使用原生 API，不拼 shell 命令 |
| 测试数据库弱于线上数据库 | SQLite 未启外键，PostgreSQL 才暴露父子写入失败 | SQLite 测试显式开启外键；涉及迁移/事务的功能必须在临时 PostgreSQL 做 upgrade/downgrade 和关键写入 |
| 对真实业务数据做单模板假设 | 混合登录 Cookie 被误判，US 站点配置漏传 | 脱敏样例必须覆盖 MX/US、单来源/混合 Cookie、重试/接管；不以一条成功样例定义全部合法数据 |
| 版本和资产没有一一对应 | v0.12.7 标签固定，云端已到 hotfix11，Latest 仍是 v0.12.6 | 一个版本只发布一组最终资产；修复必须升版本；分支 CI → 标签 CI → Release → 云端目录 → 客户端发现逐级回读 |
| 部署探针依赖表面文案 | UI 文案变化造成假失败或漏检 | 探针只检查健康、OpenAPI、迁移、能力声明、文件哈希和必要 API；营销文案与布局由独立视觉测试负责 |

### 下一版本必须新增或保留的回归场景

1. 未配对启动后完成配对，无需重启进入在线；安全存储只读一次。
2. 停止执行器后按 TTL 离线，HubStudio 父窗口和 Local API 子进程组合状态不矛盾。
3. Windows 从旧同版本运行时覆盖升级，真正切换构建修订；配置、日志、运行数据和配对不变。
4. Windows 在线更新完整模拟：确认下载、SHA 校验、执行器退出、安装器结束、新运行时就绪、失败回滚。
5. Windows 状态中心在 100%/125%/150% DPI 创建成功，四卡不挤压，首次配对入口可见，托盘不闪黑框。
6. macOS 最终冻结程序执行真实 TLS `/healthz`，不是只用系统 Python 测试。
7. 当前用户不能列表、读取、撤销、配置或取消其他用户的设备与任务。
8. MX/US、单来源/混合 Cookie、HubStudio 端口回退、系统代理开启场景全部进入脱敏矩阵。
9. 外部写入重试后强制回读行状态；不确定结果保持 `uncertain`，不自动重复创建。
10. 云端目录的文件名、大小、SHA、服务器文件和 GitHub Release 资产逐项一致；匿名下载保持 401。

## 当前卡点

1. `v0.12.7` Release 已积累 Windows hotfix1–11、macOS hotfix6–7，Release Notes 只完整记录到 Windows hotfix7。
2. GitHub Latest 仍为 v0.12.6，而共享测试和员工入口已使用 v0.12.7 hotfix11/hotfix7。
3. Windows/macOS 安装包未获得平台可信签名。
4. 两位使用同事承担真实测试，没有独立测试人员；部分流程只有 CI 和开发机证据。
5. 主工作区和完整热修复分支明显漂移，且主工作区有未提交业务输出，不能直接合并。
6. 生产上线仍受签名、权限专项、真实外部写入、备份恢复和回滚演练闸门阻断。

## 下一步计划

1. 冻结 `v0.12.7`，不再创建 hotfix12；从 `0d5ed6e` 建立新版本候选。
2. 用采购同事 Windows 电脑完整验证 hotfix11：覆盖安装、配对保留、分组、物流、买家号、MX/US 建环境、进度和在线更新。
3. 用 Apple Silicon Mac 验证 hotfix7 TLS、心跳、分组与 RPC。
4. 逐文件收口主工作区与热修复主线，禁止整树覆盖和 `git add -A`。
5. 补齐 v0.12.7 hotfix8–11 Release Notes，保留全部旧资产和不可变标签。
6. 新版本完成分支 CI、标签 CI、云端部署、目录 SHA 回读和真实客户端更新后，再决定是否推进 Latest。
7. 稳定渠道前完成 Windows/macOS 平台签名与生产回滚演练。

## 接手注意

- 当前线上正确基线是 hotfix11/hotfix7，不是独立状态中心 v2 包。
- 部署前必须重新读取容器启动时间、镜像、OpenAPI、Alembic、目录资产和 CI；并行任务可能再次推进热修复。
- 不删除历史 Release 资产、不移动 `v0.12.7` 标签、不自动清理失败任务。
- 完整任务顺序见项目根目录 [`task.md`](../../task.md)。
