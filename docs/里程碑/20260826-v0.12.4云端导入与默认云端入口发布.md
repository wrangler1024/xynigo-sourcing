# v0.12.4 云端采购协作导入与默认云端入口发布

日期：2026-08-26

状态：共享测试云端已部署，GitHub 双平台线上协同测试版已发布。

## 版本与入口

- 云端页面、OpenAPI、源码、更新清单和执行器统一为 `0.12.4`。
- Release：<https://github.com/wrangler1024/xynigo-sourcing/releases/tag/v0.12.4>，Latest、非 prerelease，包内渠道为 `test`。
- 公开净化分支：`codex/release-v0.12.4-public`；公开提交与标签目标均为 `9fc1eae7f212e902f0759919e45e1fa3c825b18b`。
- 默认桌面启动器仍启动本地执行器，但浏览器打开云端工作台；双平台包各自新增独立“启动-本地执行器”入口。

## 发布资产

- Windows x86_64：`Xynigo_Sourcing_Windows_20260826_v0.12.4.zip`
  - SHA-256：`2908fd37246e141d1866ed4181cce42d2e869b6dbff7582160de97fed99b60f1`
- macOS ARM64：`Xynigo_Sourcing_macOS_arm64_20260826_v0.12.4.zip`
  - SHA-256：`b49c67ea0322d87404c7f5b0c5e7c358cbd2f1b2b4c1b624feb394cf39346998`
- 更新清单：`Xynigo_Sourcing_v0.12.4_update.json`；校验文件：`Xynigo_Sourcing_v0.12.4_SHA256SUMS.txt`。

## 云端切换

- 版本切换前逐字比较远端与本地源码，除版本标识外一致；保留旧镜像标签和 `/opt/xynigo-auth-test/backups/deploy/pre-v0.12.4-20260826T141639Z/` 应用快照。
- 本次没有数据库迁移或 Nginx 变更；数据库保持 `0014_proc_import_cloud`。
- 发布前后 `/healthz`、`/readyz`、`/v1/auth/web/status` 均为 200；页面标题和 OpenAPI 均回读 `0.12.4`。
- 切换时已有 5 个采购协作导入任务处于失败终态，没有运行中任务；未删除或修改这些记录。

## 验证

- 内部工作树与公开净化树：桌面端 302 项完成（299 通过、3 项平台条件跳过），云端 79 项通过。
- 公开源码安全审计、Ruff、前端 JavaScript、Shell 脚本、双平台包结构、SHA-256、macOS 自包含运行时和包内启动自检全部通过。
- 分支 CI `32979601505` 与标签 CI `32979815495` 的 Python 3.9/3.11/3.12、Windows 更新器、Windows 包冒烟和 macOS 打包六项任务全部通过。
- 旧 `v0.12.3` 更新器已从 GitHub Latest 真实发现 `v0.12.4`、`test` 渠道及当前平台资产。

## 边界与下一步

- 本次发布没有静默更新同事电脑；用户仍需在旧版运行窗口输入 `Y` 确认安装。
- 默认云端入口不等于云端已经获得 HubStudio/CDP/SHEIN 本机执行通道；需要这些功能时使用独立本地执行器入口。
- 下一步至少使用一台 Windows 和一台 macOS 真实完成 `v0.12.3 → v0.12.4` 在线更新，核对默认云端启动、兼容本机入口、配置/日志/运行数据保留和失败回滚。
