# 20260826 采购下单闭环 P1 后端纵切面

状态：后端已部署到共享测试环境；快捷绑定安全起始段已在本地完成，当前 8767 尚未加载。

## 完成结果

- 新增买家号安全元数据、下单尝试、数量占用、正式采购批次、批次明细和供应商物流六个持久化模型，以及 `0008_checkout_flow`、`0009_buyer_account_resource` 两个迁移。
- 完成“创建/修改尝试 → 开始结算 → 付款结果 → 失败补偿或正式批次 → 物流完成”的纯数据库状态机。
- 对采购数量、尝试版本、采购单执行修订、创建幂等、物流版本和资源占用实施并发保护。
- 付款成功资源保守留存；失败或放弃且已登录环境时，必须等环境删除确认后才释放数量。
- 新增七个采购执行写接口，并接入桌面端同源代理白名单和 `procurement.execution.manage` 权限。
- 采购详情新增需求、已购、占用、剩余数量以及尝试/正式批次时间线数据。
- 审计继续脱敏，不记录资源引用、买家号标签、平台订单号、物流单号和凭证。

## 安全边界

初次实现时没有调用 HubStudio、SHEIN 或飞书，没有创建/删除真实环境，没有提交或付款真实订单。后端迁移后来已随共享测试部署推进到 `0011_buyer_db_base_mirror`；本轮前端联调仍未停止或重启 8767，也未写入真实外部系统或共享业务数据。

## 验证

- 云端测试：60 项通过。
- 桌面端测试：284 项完成（281 通过、3 项条件跳过）。
- Alembic：当前共享测试迁移为 `0011_buyer_db_base_mirror`，包含本里程碑的 `0008`、`0009`。
- 页面：隔离 8768 + Playwright 网络桩已验证安全资源查询、创建下单尝试、开始结算和详情刷新；控制台无错误。

详细契约、状态规则和部署约束见 [`../采购下单闭环-P1.md`](../采购下单闭环-P1.md)。
