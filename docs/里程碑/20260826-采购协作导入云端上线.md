# 采购协作导入云端上线

日期：2026-08-26

状态：共享测试已完成日期格式、跨批次查重和动态执行状态优化；真实新文件已成功追加 1 行并补图，目标表当前 49 条非空行，生产环境未部署。

## 上线范围

- “小犀助手 → 采购协作导入”在云端支持 xlsx/XYP2 解析、图片预览、Excel 下载、普通飞书表格读取/校验、二次确认异步导入及进度查询。
- 云端使用“小犀代采”企业应用租户身份访问普通 Sheet，不依赖员工电脑、本地执行器或本地 `lark-cli`。
- 新增迁移 `0014_proc_import_cloud`，短时计划采用租户/计划绑定的 AES-GCM 密文；任务进度和审计不保存订单号、收件信息、采购链接或图片。
- 共享测试开关 `XYNIGO_AUTH_PROCUREMENT_IMPORT_ENABLED=true`。这只开放功能入口，任何 Sheet 写入仍要求用户选择目标、通过字段校验并二次确认。

## 备份与构建

- 应用、环境和 Nginx：`/opt/xynigo-auth-test/backups/deploy/pre-procurement-import-cloud-20260826T132407Z/`。
- PostgreSQL：`/opt/xynigo-auth-test/backups/postgres/pre-procurement-import-cloud-20260826T132407Z.dump`。
- dump 清单和 SHA-256 已生成；恢复到临时数据库确认版本为 `0013_purchase_task_feishu_mirror`、采购单 26 条，随后删除临时库。
- 旧 auth/migrate 镜像保留 `pre-procurement-import-20260826T132407Z` 标签。
- 完整镜像构建因服务器下载既有大依赖长时间阻塞而终止，线上旧容器始终正常。随后以旧镜像为锁定基线，仅下载锁文件中新加的 `openpyxl 3.1.5` 与 `et-xmlfile 2.0.0`，覆盖已验证源码和迁移；新镜像以非 root `xynigo` 用户运行。

## 切换与验证

- Alembic 已事务性升级到 `0014_proc_import_cloud (head)`，新计划表和任务表上线后均为 0 条。
- 原业务计数前后保持：租户 1、用户 4、采购单 26、采购明细 29。
- Nginx 仅对 `/v1/assistant/procurement-import/parse` 放宽到 30 MiB；全局仍为 1 MiB。配置语法检查和热加载通过。
- 公网健康、就绪、Web 登录状态均为 200；OpenAPI 包含全部 7 条新接口。1.5 MiB 合成请求到达应用鉴权层并返回 401，证明未被 Nginx 旧限额拦截。
- 企业应用获取 tenant token 成功。真实容器对合成 xlsx 完成解析和 Excel 导出后事务回滚，数据库仍无导入计划或任务。
- 首次目标读取返回 `99991672`，原因为“小犀代采”缺少 Sheet 应用身份 scope；开通并发布 `sheets:spreadsheet` 后重建 auth 容器，云端成功读取“采购执行协作区”43 个标准表头和 48 条非空行。该 canary 全程只读，没有改动数据、结构、样式或图片。
- 新容器最近日志没有 `ERROR`、`CRITICAL` 或 Traceback。

## 同日 90204 修复与二次部署

- 用户对“采购执行协作区”发起首次真实导入后，任务在飞书返回 `90204 WrongRequestBody` 时停止：计划含 7 条明细，追加 0 行、补图 0 张。目标表回读没有本批签名，7 条均保持待补状态；没有出现重复行。
- 网关现在只对表头样式、日期格式、行高和冻结窗格等可逐字重复的幂等写入，在 `90204` 时等待后重试一次。数据行 `values_append` 明确不做盲重试，仍由下一次任务先按“导入批次 + 行签名”回读查重，再只补缺失行。
- 同订单六色分组整体提亮，且相邻颜色 RGB 距离仍不低于 45；云端只给本次新追加行着色，已有采购任务色继续保留。
- 页面“共享表兼容规则”由 7 段开发说明收敛为 3 段现行规则；工作表下拉框与右侧按钮统一为 42px 高、11px 圆角和相同字重。
- 回归结果：桌面端 302 项完成（299 通过、3 项平台条件跳过），云端 79 项通过；新增测试证明幂等样式会对 90204 重试一次，而数据追加请求只调用一次。
- 二次部署备份位于 `/opt/xynigo-auth-test/backups/deploy/pre-procurement-import-fix-20260826T140807Z/`，应用、环境、Compose、宝塔 Nginx 与 PostgreSQL dump 校验通过；dump 已实际恢复到临时库并确认 `0014_proc_import_cloud`。回滚镜像为 `xynigo-auth-auth:rollback-20260826T140807Z`。
- 新镜像为 `sha256:70e17dea9ef69fe6bbccbe4b338b076c12d351120c29e5fd6ce45b686f4c4e41`。公网 `/healthz`、`/readyz` 均为 200，容器重启计数为 0；线上首页 SHA-256 为 `be2f99689cedb3f25f774ffb357eadc9f08de52a9ebe49e41af554f899a0ffea`，与本地构建一致。
- 发布后云端应用身份只读复核目标表：1 个工作表、A:AQ 43 个表头、48 条非空行。部署过程没有自动重跑失败任务，也没有追加真实采购数据或图片。

## 同日日期格式根因热修复

- 用户改用全新文件 `order_20260826221646564_945257.xlsx` 后仍得到 90204。云端记录确认该文件解析为 1 个订单、1 条明细、1 张图、0 警告、0 错误，新批次连续两次均为追加 0 行，排除订单内容和查重冲突。
- 飞书 changeset 显示失败任务已成功把 A29:A49 写为真实日期序列，随后没有形成日期样式 revision。根因为共享解析器使用 Excel 格式 `yyyy-mm-dd`，云端网关直接传给 Sheets v2；飞书原生格式要求月份为大写 `M`，即 `yyyy-MM-dd`，错误字符串被接口以 `90204 WrongRequestBody` 拒绝。
- 修复仅位于云端网关边界：`yyyy-mm-dd → yyyy-MM-dd`、`yyyy/mm/dd → yyyy/MM/dd`。本地 Excel 导出和本机 `lark-cli` 路径继续使用 Excel 格式，不改变既有工作簿显示。
- 新增回归测试断言 Sheets v2 请求体使用 `yyyy-MM-dd`；桌面端 302 项完成（299 通过、3 跳过），云端 80 项通过。用户提供的新文件只读回归得到 1 行/1 单/1 图和稳定批次，与云端任务一致。
- 热修复发现共享测试站已切到另一任务发布的 v0.12.4，因此以当时线上镜像为基线只替换一个网关文件，没有覆盖页面或其它源码。部署后页面仍为 v0.12.4，Web SHA-256 保持 `121fae22b6b5d12389380bf4fce9dfd1484f518e23a2c084e6dade6e560e84c8`。
- 备份位于 `/opt/xynigo-auth-test/backups/deploy/pre-sheet-date-format-20260826T142117Z/`；应用、环境、Compose、宝塔 Nginx 与 PostgreSQL dump 校验通过，dump 已恢复到临时库并确认 `0014_proc_import_cloud`。回滚镜像为 `xynigo-auth-auth:rollback-sheet-date-format-20260826T142117Z`。
- 热修复镜像为 `sha256:d6fa70128bc38fd2d5e664952373310d600d58d1874258680ff7642ecf534896`。公网健康/就绪为 200，功能开关开启，容器零重启；只读复核目标表为 43 个表头、48 条非空行、revision 163，没有自动重跑或追加数据。

## 同日跨批次查重与动态执行状态

- 日期格式热修复后，用户的新文件真实导入完成：新追加 1 行、已存在 0 行、新设底色 1 行、采购超链接 0 个、新补图 1 张、已有图片跳过 0 张；目标表随后为 49 条非空行，证明 90204 根因修复生效。
- 查重升级为两层：同一导入批次继续按完整行签名续传；不同批次按“系统订单键 + 销售订单号 + 包裹号 + 主/次规格 + 数量 + 指导价 + 采购备注 + 数据版本”比较订单完整明细集合。历史订单完全一致则整单计为已存在，只追加真正缺失的新订单。
- 历史订单存在但明细集合不同，或当前批次仅部分写入且同时发现旧批次同订单时，在任何追加和补图前停止，避免把变更订单误判为完成或形成跨批次重复。
- 页面执行状态新增阶段脉冲、流动进度条、百分比、计数变化反馈和完成勾选；状态会依次显示跨批次校验、表头规范、数据追加、回读、底色、链接和图片。完成态使用“导入完成：新追加…关键数据均已回读确认”统一文案，并支持 `prefers-reduced-motion`。
- 浏览器在 1280px 和 900px 宽度完成视觉检查，窄屏无横向溢出；桌面端 304 项完成（301 通过、3 跳过），云端 80 项通过，JavaScript 语法与本地/云端页面同步校验通过。
- 部署前备份位于 `/opt/xynigo-auth-test/backups/deploy/pre-cross-batch-progress-20260826T143725Z/`，数据库 dump 已恢复到临时库并确认 `0014_proc_import_cloud`。回滚镜像为 `xynigo-auth-auth:rollback-cross-batch-progress-20260826T143725Z`。
- 新镜像为 `sha256:186b13c2c3ccd26fd8288e0ed8a7a2400f776897ec595a64d0fd69aa49d76133`，基于现有 v0.12.4 只替换采购导入核心、飞书网关和 Web 页面。公网健康/就绪为 200，容器零重启；页面 SHA-256 为 `6f7c8ff500863bd1482afcae109076584ccc1134b47b0aba96875fe576e455fb`。

## 同日历史订单不可修改边界与 XYP2 聚合修复

- 产品边界固定为：采购协作导入只能追加、查重和续传，不能修改已经进入普通采购协作表的订单。跨批次完全一致的历史订单整单跳过；历史已有多个完整相同副本时仍只算一份“已存在”，不修改其业务字段、底色、链接或图片。明细不同则在任何追加前阻断，并明确提示“采购任务认领前修改”属于独立修订流程。
- 店小秘同 SKU/规格的多行现在全部聚合到对应 XYP2 明细，不再只选一行并把其余行误报为“没有对应采购明细”。聚合销售数量与 XYP2 数量一致时不产生黄色关注；商品金额按全部来源行的`产品售价 × 单个产品数量`求和。
- 用户实测文件只读复验为 15 个导出产品行、3 单、6 条采购明细、0 警告、0 错误；其中一单的 S/M/L 各由 4 个销售行聚合，需求数量均为 4，商品金额均由错误的 170.55 修正为 682.20。
- 对 revision 184 的目标表只读复验得到 3 条历史明细锁定、3 条新明细待追加；原先两张订单各有 7 份内容完全相同的历史副本，不再误报为“历史订单明细不一致”。本次验证没有触发真实导入，未删除或整理已有重复副本。
- 页面“共享表兼容规则”、导入说明、确认弹窗和失败态都增加了边界文案；真实历史冲突不再提示无意义的“可安全重试”，而是说明重复导入不会修改历史订单。
- 回归：本地 306 项完成（303 通过、3 项平台条件跳过），云端 80 项通过；新增“全部订单均为跨批次历史订单时不调用行样式、链接或图片写入”的零写入测试。真实文件解析、目标表只读查重、JavaScript 语法、生成核心同步和 `git diff --check` 均通过。
- 最终切换前回滚点为 `/opt/xynigo-auth-test/backups/deploy/pre-proc-import-readonly-20260826T150545Z/`，PostgreSQL dump 为 `/opt/xynigo-auth-test/backups/postgres/pre-proc-import-readonly-20260826T150545Z.dump`；dump 已恢复到临时库，确认 `0014_proc_import_cloud` 和 28 张采购单后删除临时库。回滚镜像为 `xynigo-auth-auth:rollback-proc-import-readonly-20260826T150545Z`。
- 最终镜像为 `sha256:9cb9d495331596852d44b18f77669ffa55c7024f69d9a1077d94c912216ad4df`；采购导入核心 SHA-256 为 `41cf68aaa6dc5157f8d0f9e9d7151cc2d5280018200828db34e7f4c8c98942a0`，页面 SHA-256 为 `e8954385c0ca0bd92d4b0ad471339eb625d4341b6f5a769a0d82728834ab140f`，公网返回与本地一致。部署后健康/就绪正常、容器零重启、迁移仍为 0014、最近日志无错误。
- 云端应用身份部署后只读回读目标表仍为 43 个表头、49 条非空行、revision 184；任务状态仍为 1 个 completed、7 个 failed，证明部署没有自动重跑任务或改动飞书数据。

## 未执行事项与下一步

- 二次部署没有自动重跑原失败任务。用户可重新解析、读取并校验目标工作表后再次确认导入；若表内已存在部分成功数据，系统会按稳定批次和行签名跳过，只补缺失行和缺失图片。
- 生产服务器、正式域名和正式数据路由继续受独立上线闸门约束，本次授权只覆盖共享测试站。

## 回滚

- 快速停用：将 `XYNIGO_AUTH_PROCUREMENT_IMPORT_ENABLED=false` 并重建 auth 容器。
- 应用回滚：把 auth/migrate 标签切回 `xynigo-auth-auth:rollback-cross-batch-progress-20260826T143725Z`；旧应用可兼容数据库保留的新增表，不需要立即降级。
- 只有确认两张新表没有任务或计划且已备份时，才可考虑降回 0013；飞书外部写入不能通过数据库降级撤销。
