# v0.18.4 发布记录

用户授权开始部署，本次统一升级云端与双平台执行器。业务基线为已独立评审的 P0 修复 `4505922` 与订单处理双视图 `e9164ac`；不增加真实退款提交或历史业务回填。

沿用两阶段发布：固定版本与源码提交构建双平台安装包，再回填实际 runtime ID、SHA-256 和大小。第一阶段的旧发行目录故意保留，不能作为云端运行版本；目录回填后才推送最终候选并部署。

发布通道保持内部 test。共享环境在备份和隔离恢复验证后，使用既有维护入口停收排空，再升级 `0041 → 0042`，只切换应用服务。迁移新增商品快照、完成时间及诊断列，旧值保持未知；保留旧代码、资产和备份，禁止自动恢复或降级业务库。

敏感部署定位、备份路径与运维证据仅保存在私有记录。

## 已核验安装资产

构建来源：`a84df3b6f3db2de626707ca156e15999136723d9`；两平台 runtime ID：`0.18.4-a84df3b6f3db`。

- Windows x86_64：15378766 字节，SHA-256 `c9b9f9acd73541fcee08fbfc03819171fca3d90b5b420a7e4ca8a7bad84f0d3a`。
- macOS arm64：11737125 字节，SHA-256 `80f013bb8fc87423479ccc87da3b9c5dc2ef5052176ffa9e7a281c4ebc61e4c9`。

Windows 未正式签名；macOS 使用 ad-hoc 应用签名，未 Developer ID 签名或公证。保持内部测试渠道，不标记为正式可信发行。macOS 实际安装包已展开验证、检查签名并运行包自检及云端 TLS 自检。

本地安装器、更新及版本专项 67 项通过，云端发行目录专项 20 项通过。业务基线的本机全量 1220 通过、3 项 Windows 专属测试跳过；云端售后兼容专项 32 项通过。发布前须由最终提交对应的完整 CI 和独立评审放行；此记录中的本地通过结果不代替部署成功证据。
