# v0.18.2 统一版本发布

用户要求本次统一安装包与云端版本。源码提交 `7d1c7bd`，发布清单提交 `17fa1da`；两平台 runtime ID 均为 `0.18.2-7d1c7bde9f67`。

- Windows 标准安装包：15364231 字节，SHA-256 `e905780c11e98b0cfc82460280597e5c3de3c8c46f962c487e7598cf52f148cb`。
- macOS 标准安装包：14362281 字节，SHA-256 `77c8a5bdf27ff4f53f1c023f25304be616081328823ed26c1ae7eecd577f672e`。
- 源码固定后顺序构建，保留真实包元数据；包结构、安全检查、macOS 包自检与云端 TLS 自检通过。版本/公开安全专项 8 项、云端安装包清单专项 20 项通过。
- 独立评审逐项核对真实资产哈希、大小、runtime ID、渠道和签名声明，通过且无必须改。
- 恢复云端版本与安装包版本相等的导入守卫，移除 v0.18.2 云端独立发布例外。后续同版本发布须同步评审云端、Web 与双平台安装包。
- 仍使用既有内部 test 渠道：两平台未正式签名，macOS 未公证，未执行 Windows 真机安装。更新包发布不等于用户机器已完成安装。

共享测试环境已部署 `17fa1da`，切换前完成代码与数据库备份、归档校验和活动任务排空；仅重建应用服务，数据库未重启、迁移仍为 0040。双平台资产在容器挂载目录的 SHA-256 与候选一致，readiness 正常、重启计数 0。本机旧客户端强制检查更新后返回 available，latestVersion=0.18.2、runtime ID 与候选一致；当前仍运行 v0.18.0，未触发系统安装器。旧版资产保留。没有推送远端或创建 GitHub Release。

此前云端独立发布记录是历史快照，以本记录的统一发布结果为准。
