# 采购售后：按环境单遍直提（实现记录）

日期：20260917。需求由 Jeff 当日确认（「按环境提交兼容单遍直提，前端提示提交环境上限，直接负责代码落地」）。
候选分支 `codex/after-sale-claim-env-direct`，已 rebase 到含并行线「SHEIN 店铺授权」合并的 `origin/main`
（迁移因此由 0044 顺延为 0045，接在店铺授权的 0044 之后）。本记录描述本地开发结果，
**不代表云端或安装包已更新**。

## 1. 问题与口径变更

旧行为（v0.18.6 `4078d41` 引入）：③「按环境提交」= 先跑一遍**只读扫描**（逐环境翻全部订单、逐单读详情、
有入口的单再调 pre_info），跑完弹「核对按环境提交」清单，人工确认后才建提交 Run **第二次打开环境**逐单提交。
真机实测扫描阶段约 **55 秒/环境**，11 个环境光扫描就要 6 分钟起步，与「快速单任务操作」的设计意图不符。

新口径（本次实现）：**按环境提交 = 单遍直提**。点一次确认后直接进环境：读到带售后入口的订单就现场
核验并提交；**没有入口的环境跳过并回报原因**，不再先出全量清单、不再人工二次核对。

| 维度 | 旧（扫描→核对→提交） | 新（单遍直提） |
|---|---|---|
| 环境打开次数 | 2 次（扫描 1 + 提交 1） | **1 次** |
| 写操作前人工确认 | 扫描结果清单逐单核对 | 发起前一次性确认（写操作 + 上限提示） |
| 无入口环境 | 扫描行「暂不可申请」，不进提交 | 环境级结果 `skip` + 原因，订单行不产生 |
| 取消 | 扫描阶段取消则整批不写 | 提交 Run 取消（`/cancel`），在途单完成后停 |
| 幂等 | 扫描清单 + 提交前 pre_info | **提交前 pre_info 体检**（唯一闸门；平台侧已退包裹不可再退） |

按订单号（完整信息）的「直接提交」入口不受影响，照旧逐单核验、逐单提交。

## 2. 四层改动清单

| 层 | 文件 | 改动 |
|---|---|---|
| 执行器本地 | `src/purchase_tool/after_sale_claim.py` | 新增 `start_claim_environments` / `_run_claim_environments` / `_claim_env_one`（环境只开一遍：读订单列表 → 有入口的订单逐个 `_claim_one`）；环境级行 `claimEnvRows` 随 `snapshot()` 输出；`_publish_claim` 补 `orderNo` 身份字段；`_claim_env` 的 per-item 异常隔离抽成 `_claim_item_guarded` 共用 |
| 执行器本地路由 | `src/purchase_tool/main.py` | 新增 `POST /api/after-sale/claim-environments`（≤300 环境）+ 权限表 `assistant.access` |
| 桥接 | `src/purchase_tool/operation_executor.py` | 同一 `after.sale.claim.v1` 任务类型按报文体分流：带 `environmentSerials` → `_execute_after_sale_claim_environments`（轮询进度、上报环境级快照、终态 summary）；新增 `_after_sale_env_rows` 投影与 `_after_sale_environment_summary`（进度按环境计、成败计数按订单计） |
| 能力位 | `src/purchase_tool/executor_channel.py`、`cloud/auth-service/src/xynigo_auth/executor_contract.py` | 新执行器声明 `after.sale.claim-environment.v1`（进 SUPPORTED 与 MODERN_ONLY 两份清单、云端枚举） |
| 云端契约 | `cloud/auth-service/src/xynigo_auth/operation_contract.py` | `AfterSaleClaimRunCreateBody`：`items` 与 `environmentSerials` **二选一**（model_validator），环境序号去重校验、≤300；新增 `AfterSaleClaimEnvironmentRow` 闭集（extra=forbid） |
| 云端服务 | `cloud/auth-service/src/xynigo_auth/operation_service.py` | 建 Run 环境分支（`total_count`＝环境数，`request_summary.mode='environments'`）；快照输出 `submitMode` + `environments`；历史项输出 `submitMode`；`_payload_hash` 对 None 的新字段剔除，**保持按单请求的历史哈希不变**（老幂等键重放不会被判成不同请求） |
| 云端任务 | `cloud/auth-service/src/xynigo_auth/executor_service.py` | 派发前能力位检查（缺 `after.sale.claim-environment.v1` → 409 `executor_after_sale_environment_upgrade_required`）；终态 summary 白名单加 `environments`；进度快照放宽为「**环境序号必须在计划清单内**、订单号允许执行中动态发现」，环境行数上限 `AFTER_SALE_ENV_ROW_CAP=2000`；环境级结果整体落 `run.environment_results`；环境模式下成功/失败等计数**不再被 total_count（环境数）截断** |
| 云端路由 | `cloud/auth-service/src/xynigo_auth/main.py` | 同一 `/v1/operation-runs/after-sale-claim` 按报文体下发 `environmentSerials`；审计 `change_summary.submitMode` |
| 迁移 | `cloud/auth-service/migrations/versions/0045_after_sale_env_results.py` | `after_sale_claim_runs.environment_results` JSON `server_default='[]'`（按单批次恒为 `[]`） |
| 云端模型 | `cloud/auth-service/src/xynigo_auth/models.py` | `AfterSaleClaimRun.environment_results` |
| Web | `src/purchase_tool/web/index.html`（同步 `cloud/.../web/index.html`，`sync_web_workspace.py`） | `asDirectSubmit` 按环境分支改直提（confirm 一次性确认，含 300 环境上限提示）；新增 `asSubmitByEnvironment`（能力位选执行器 → 建 Run → 提交视图轮询）与 `asRenderEnvOutcomes`（环境结果条）；进度单位按 `个环境`；删除扫描优先路径（`directSubmitScan` / `asFinishEnvironmentSubmit` / `asConfirmEnvironmentClaims` / `asEnvironmentClaimPreview` / 「提交前扫描中」文案）；帮助文案改为单遍直提说明 |

## 3. 计数与状态口径

- **进度** = 环境（`progressCompleted` / `progressTotal` = 已完成环境 / 计划环境）。
- **成功 / 失败 / 跳过 / 停止** = 订单（与环境数解耦；success 可能 > total，这是刻意的）。
- **环境级结果**（`environments`）：`ok` 已提交、`blocked` 入口订单均不可申请、`skip` 无售后入口、
  `fail`/`login`/`inuse`/`stopped` 故障类；每行带 `entryCount` / `submittedCount` / `blockedCount` /
  `failedCount` / `note` / `errorSummary` / `durationSeconds`，落库 `after_sale_claim_runs.environment_results`。
- 整批「全部环境无入口」= `completed`（跳过不是故障，与按单提交的 `blocked` 口径一致）；环境或订单未终态
  → `uncertain`。

## 4. 兼容与发布顺序

- **老执行器不会收到直提任务**：云端在派发前按能力位拦截（409），Web 侧也要求该能力位才允许选设备；
  失败文案明确要求升级执行器。这解决了「新云端 + 老执行器」的协议不兼容问题（老执行器的桥接层不认识
  `environmentSerials` 报文）。
- **发布顺序**：先云端（含迁移 0045（并行线 0044 店铺授权已先合入 main，本迁移顺延接在其后），`docker compose build migrate` 后再 `run --rm migrate`），
  后执行器（需要新桌面包才具备能力位与本地 `claim-environments` 路由）。云端先上不影响既有四个入口。
- 按单提交的报文与哈希口径完全未变（`items` 分支的 payload 规范化与旧版一致）。

## 5. 测试与验证

```bash
cd ~/Documents/xynigo-worktrees/as-env-direct
PYTHONPATH=$PWD/src ~/Documents/xynigo-sourcing/.venv/bin/python -m pytest tests/ -q            # 本地全量
cd cloud/auth-service
PYTHONPATH=$PWD/src ~/Documents/xynigo-sourcing/cloud/auth-service/.venv/bin/python -m pytest tests/ -q -p no:warnings   # 云端全量
```

新增/改写的用例：

- **执行器本地**（`tests/test_after_sale_claim.py`）：`ClaimEnvironmentBatchTests`（去重与清洗、环境行
  三种结局、真跑 `_claim_env_one` 只对有入口订单提交、全 blocked 环境记 blocked 不算失败、停止落
  `stopped`）、`BridgeClaimEnvironmentTests`（上行报文、环境行投影闭集、进度按环境 / 计数按订单、
  分流到直提 handler、缺失/超限报错、未知状态回落）。
- **Web 行为**（`tests/test_after_sale_submit_environments.py` + 夹具重写）：一次确认后**只发一次**建 Run、
  **不再调扫描**、能力位为 `after.sale.claim-environment.v1`、拒绝确认/非法输入/超 300/规划类型均不写、
  起跑互斥、停止走提交 Run 的 cancel、帮助文案带 300 环境上限；按订单号入口回归。
- **云端**（`cloud/auth-service/tests/test_after_sale_claim.py`）：直提 Run 全链路（建单幂等 → 进度含
  环境行 → 环境外订单 422 且不落脏行 → 终态 success 5 > total 4 不被截断 → 历史/详情 `submitMode` 与
  环境级结果）、二选一与去重契约 422、缺能力位 409、迁移 0045 默认值；另在 `test_after_sale_p0.py`
  补「按环境范围哈希只吃 environmentSerials」一条。
- **运行选项**（`tests/test_after_sale_runtime_controls.py`）：直提经真实本地 HTTP 路由透传
  `browserMode` / `concurrency`。
- **对齐**（`tests/test_executor_workspace_web.py`）：建 Run 报文键 ⊆ 云端契约且直提分支必含
  `environmentSerials`、桥接环境行投影字段 ⊆ 云端 `AfterSaleClaimEnvironmentRow`、Web 读的
  `data.environments` / `submitMode` 云端快照确实返回、建 Run 请求仍只有一处（`asSubmitItems`）。

两份原型（`docs/prototypes/20260915-*.html`）已按新 Web 重跑。

**本轮验证结果**（rebase 到新 main 后重跑）：本地全量 **1271 passed / 5 skipped**（0 失败）；云端全量 **417 项 0 失败**
（连续两次全量运行 exit 0；本机曾与另一条工作线的测试并发，未取到汇总行，故以收集数与零失败为准）。
**CI（push 触发）**：rebase 前两次运行为全绿（run 35210559884 / 35212173699）；店铺授权线合入 main 后
其文档触发公开仓审计、连带所有分支变红，该线已脱敏修复（main `8f3f6f9`，CI 转绿），本分支已 rebase 到
该点之上并本地通过 `audit_public_release.py`。评审整改后的数字见 §7。

## 6. 已知限制（v1）

1. **无按单前置防护**：直提不预知订单号，`create_after_sale_claim_run` 里针对 `uncertain/verifying` 历史
   行的逐单拦截只覆盖按单提交；直提的幂等依赖提交前 pre_info 体检（平台侧已退包裹不可再退）。
   已回传过的结果行仍参与历史防护。
2. **环境级失败没有截图**：截图按订单号挂结果行，环境整体失败（如订单列表翻页失败）只有 `errorSummary` 文本。
3. **导出不含环境级结果**：`build_after_sale_claim_export` 仍只导订单行；环境级跳过只在页面与历史详情展示。
4. **uncertain 批次无法枚举未上报订单**：执行器中途死亡时，未上报订单不在请求清单里，重新发起不会被
   逐单拦截（同为按单提交的历史风险面）。
5. 单批上限 300 环境、订单行上限 2000（云端常数），超限报 422 由执行器/云端拦截。
6. 未做真机验收（需部署云端 + 新桌面包后按 §5 流程在测试机跑一单）。
7. **环境级失败没有订单行，因此不进「补提失败单」**：补提只针对有行的可恢复失败单；环境读取失败/未登录
   需要人工重跑该环境（批次终态会如实报 `failed`/`partial_failure`，环境结果条给出原因）。
8. 同环境多个别名（序号/环境ID/名称指向同一环境）不会去重：本地按 `containerCode` 串行但会跑两遍。
   Web 入口只收数字序号，实际只可能由直接调 API 构造；幂等仍由 pre_info 兜住。

## 7. 评审与整改（20260917）

首轮评审：Cursor **本地直评**（只读），对象＝本分支功能提交 `07de7c6`，结论 **改后合并**。
四项必修已全部闭合，建议项逐条处置：

| # | 必修项（评审） | 整改 |
|---|---|---|
| 1 | 停止打断列表读取时，残缺列表被当成「无入口」写成 `skip` | `_claim_env_one` 读完列表立即查 `_stop_event`：已停止则环境落 `stopped`、注明「订单列表未确认；本次未提交」，不再进候选判断，也不在列表不完整时新开提交（在途单允许做完） |
| 2 | 环境内还有候选未处理时，环境仍会被标成 `ok` | 候选循环记录 `remaining`；因停止提前离开时环境一律落 `stopped`（已提交计数保留），note 写「已停止，剩余 N 单未处理」。直提没有剩余订单行，环境级状态是唯一剩余信号 |
| 3 | 环境级 `fail`/`login`/`inuse` 不产生订单行，整批会被报成 `completed` | `_after_sale_environment_summary` 引入 `env_bad` 并与订单失败合并为 `hard`：**全环境失败→`failed`、与成功订单并存→`partial_failure`**；全 `skip`/全 `blocked` 仍是 `completed`（跳过不是故障） |
| 4 | 无订单行的环境结果在「恢复批次」与「历史详情」几乎不可见 | ① `asLoadLatestClaim` 的恢复条件加入 `environments.length`——全 skip/全未登录批次刷新后不再消失；② 历史详情新增环境结果条（`asHistoryEnvOutcomes`，与页面共用 `asEnvOutcomePills`），meta 补「读取失败/已停止」计数，空态写明「无可申请订单，环境级结果见上方」 |

建议项处置：

- **采纳**：`executor_after_sale_environment_upgrade_required` 进 `cloudApiError` 文案表；`_run_claim_environments`
  批末补齐 `verifying→uncertain` / `queued,running→stopped` 安全网（与按单一致）；环境行 `durationSeconds: None`
  不再被收成 0 秒；历史列表环境数改为「直提批次＝计划环境数」（全 skip 不再显示 0）；同环境订单间停顿改用
  `_stop_event.wait` 可被停止打断；订单列表读取失败的错误文案去掉「扫描」字样（直提也在用）；
  迁移测试 docstring 更正为 0045；`expectedExecutorId` 已成死参数（旧扫描确认路径删除后无调用方）——
  整块移除而不是只改文案。
- **暂缓（记录理由）**：同环境别名去重。Web 入口只收数字序号，别名重复只可能由直接调 API 构造；
  锁已按 `containerCode` 串行、`pre_info` 已防重复写入。列入 §6 已知限制，下一轮按需补。

**复评结论（20260917 第二轮，Cursor 本地直评）：可合并，无必须改**。复评另提三条「不挡合并」的未闭合项，
已在合并前收口：① 横幅标题补 `partial_failure` 映射（环境失败＋订单成功不再写「完成」，与历史一致）；
② 新增 `AS_STATE.envMode` 模式标志，进度单位与环境失败句不再依赖 `environments` 是否已回传
（运行中恢复时不会退回「单」）；③ 订单页数超限文案去掉「扫描」字样。并补两条静态护栏测试。

**整改后验证**：本地全量 **1277 passed / 5 skipped**、云端全量 **417 项 0 失败**、四个 Web 夹具全 PASS；
两份原型与云端副本按最终源码重跑/重同步。复评范围＝相对 `07de7c6` 的增量（执行器本地、桥接、云端服务、
Web、测试）。

