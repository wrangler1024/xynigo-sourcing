# 20260915 采购售后（丢件退款）模块 · 交接

交接时间：20260915（首版完成，待未参与方评审）
分支/提交：`codex/after-sale-claim-v1`，基线 `origin/main` `6e42c37`（v0.17.20）
worktree：`~/Documents/xynigo-worktrees/after-sale-claim`
状态：**已自测全绿、已提交到分支，未推送、未合并、未部署**。可交互原型已出，等界面确认后再谈部署。

## 1. 需求与形态

采购部对已下单买家号的「包裹已送达但没收到货」订单，需要在 SHEIN 买家端发起退款申请（本次类型＝**丢件退款**）。人工动线：开环境 → 进订单列表 → 找到带售后入口的卡片 → 点进去 → 勾包裹 → 选退款路径 → 提交。

产品化形态（Jeff 拍板）：Xynigo 采购中心新增二级模块「**采购售后**」，两步式「扫描 → 勾选 → 提交」，走云端 Run 链路；首版一次做完执行器/云端/Web 三层。

## 2. 改动范围（相对 origin/main）

**执行器（本地）**
- 新增 `src/purchase_tool/after_sale_claim.py`（`AfterSaleClaimer`）：扫描（只读）/提交（写）两条批次链、环境占用四层策略、异常截图、单间随机停 5~15 秒、环境间串行。
- `src/purchase_tool/main.py`：`/api/after-sale/{scan,submit,progress,stop,screenshot}` 五个路由（`assistant.access`）+ 实例化（`afterSaleHeadless` 默认 false＝可见窗口）。
- `src/purchase_tool/operation_executor.py`：`after.sale.scan.v1` / `after.sale.claim.v1` 两个业务任务，含行投影闭集与汇总口径（`blocked/skip/empty` 计 skipped 不计 failed）。
- `src/purchase_tool/executor_channel.py`：两个新能力登记进 `SUPPORTED_CAPABILITIES` 与 `MODERN_ONLY_CAPABILITIES`。

**云端（auth-service）**
| 文件 | 内容 |
|---|---|
| `operation_contract.py` | +132：扫描行/提交行/建单体的闭集契约（`extra=forbid`） |
| `models.py` | +134：`AfterSaleClaimRun`/`AfterSaleClaimResult`；task_type 约束加两个新类型 |
| `operation_service.py` | +154：建 Run（幂等键+payload hash）、快照、lastRuns |
| `executor_service.py` | +402：结果校验、扫描摘要、Run 回写、进度 upsert（含截图 350KB/7 天） |
| `executor_contract.py` | +2：能力 Literal |
| `main.py` | +323：7 条路由（扫描建任务不建 Run；提交建 Run；两条 cancel） |
| `migrations/versions/0035_after_sale_claim.py` | +159：两张表 + `executor_tasks.progress_summary` + task_type 约束 |
| `tests/test_after_sale_claim.py` | +652：6 条端到端 |

**Web（权威源）**
- `src/purchase_tool/web/index.html`：二级 tab「采购售后」（`data-parent="procurement"`）、`FEATURE_MODULES.aftersale`、`afterSalePanel` 三卡、`as*` 前缀 JS、`setFeaturePanel` 接线。
- 已跑 `python cloud/auth-service/deploy/sync_web_workspace.py`，双副本字节一致（有契约测试守着）。

**文档/原型**
- `docs/prototypes/20260915-procurement-after-sale-v1.{html,md,png×2}` + 生成脚本 `build_procurement_after_sale_prototype.py`。
- 业务侧经验沉淀到私有业务仓：`自动化工具/HubStudio采购环境自动化/06-已送达未收到售后申请.md`（含页面选择器与四个坑）。

## 3. 接口契约（全部 `assistant.access`，无新权限码）

- `POST /v1/after-sale/scan` →202 `{taskId,status,executorId}`；只建任务不建 Run。
- `GET /v1/after-sale/scan/{task_id}` →`{taskId,status,executorId,summary:{rows,totalCount,claimableCount},lastRuns}`；**运行中也带 summary**（部分行）。
- `POST /v1/after-sale/scan/{task_id}/cancel`（无 body）→ 同形状。
- `POST /v1/operation-runs/after-sale-claim` →202 `<快照>`；幂等键同 key 同 payload 复用原 Run、不同 payload 409；任务 payload `{runId,runKey,browserMode,items}`。
- `GET /v1/operation-runs/after-sale-claim/{run_id}` / `/latest` → 快照 / null。
- `POST /v1/operation-runs/after-sale-claim/{run_id}/cancel` → 快照。
- **快照为扁平键**（逐键镜像 `store_finance_snapshot()`）：`runId,status,phase,browserMode,progressCompleted,progressTotal,totalCount,successCount,failedCount,skippedCount,stoppedCount,stopRequested,startedAt,completedAt,rows[]`。

## 4. 验证结果

| 范围 | 命令 | 结果 |
|---|---|---|
| 本地 | `PYTHONPATH=src python -m pytest tests -q` | **1013 passed / 5 skipped** |
| 云端 | `cloud/auth-service/.venv/bin/python -m pytest tests -p no:warnings` | **263 passed**（基线 257 + 新增 6） |
| 原型交互 | 无头浏览器实跑扫描→勾选→提交 | 全通，控制台零报错 |
| 网页语法 | 内联脚本 `node --check` | rc=0 |
| 副本一致 | 字节比对 + 契约测试 | 一致 |
| 迁移（隔离 PG） | 临时库 `alembic upgrade head` → `downgrade 0034` → `upgrade head` | 通过，表/列/约束/索引与 ORM 反射一致 |
| 公开仓审计 | `scripts/audit_public_release.py` | passed |
| `git diff --check` | — | 干净 |

**真机验证（HubStudio + 真实买家号，只读）**
- 扫描路径在环境 4902/4903/4904/4905 跑通；4904、4905 命中可申请订单；脚本自开的环境扫完自动关闭，手动开着的不动。
- 提交路径做**只读演练**：在 4904 的订单上走完「进申请页 → 勾包裹 → Confirmar → 切原路退回 → Presentar 可点」，**点击前停住**，零提交。

## 5. 需要留意的实现决策（评审重点）

1. **退款路径固定「原路退回」**：页面默认是 SHEIN 钱包；界面单选项与提交报文的 `path` 不是同一套取值（映射 `{1:2,2:3,3:1}`），原路退回＝`path:3`。业务口径变化需改 `REFUND_PATH_LABEL`。
2. **已提交过不可勾选**：`canPick = orderNo && claimable && status==='ok'`，这是幂等第一道闸门。
3. **skipped 与 failed 分开统计**：`blocked/skip/empty` 归 skipped，避免幂等重跑把整批误报失败。
4. **云端主动偏差 4 项**（为满足前端与本地投影，可回退但会掉功能）：
   - `executor_tasks` 新增 `progress_summary JSON` 列——扫描不建 Run，但前端要求运行中也有部分行；回退则运行中 rows 恒空。
   - Run 表新增 `skipped_count`/`stopped_count`——本地终态 summary 会回报，否则丢两个计数。
   - 提交进度行额外接纳 `packageCount`——本地扫描/提交共用字段清单；不接纳则真机提交进度 422。
   - `BUSINESS_RESULT_KEYS` 增加 `skippedCount`——否则提交终态回执 422。

## 6. 限制与未做

1. **模块的提交落库未做真机实跑**（演练止于 Presentar 前一步）。手动流程同日已在 4902 真机提交成功（退款单号 2390765181147136），模块用的是同一套选择器与步骤。
2. 多包裹订单的「循环再提」分支无样本可验（现有样本均单包裹）。
3. 未加导出与「补提失败」重跑，未做 `failed_retry` 合并视图。
4. 扫描未加客户端最低版本门禁（沿用能力位判断：老执行器不会 advertise `after.sale.*`）。
5. 未推送、未合并、未部署；线上/测试服务器均未涉及，实例迁移未执行（仅隔离临时库验证）。
6. 既有测试波动：`tests/test_updater.py::test_network_or_github_failure_does_not_block_startup` 在本机全套连跑时偶发失败（单跑与基线全量均通过），与本次改动无因果，未修。

## 7. 下一步

1. 界面确认（可交互原型已出）→ 若需调整，改 `src/purchase_tool/web/index.html` 后重跑原型生成脚本与 `sync_web_workspace.py`。
2. 未参与方评审（重点见第 5 节）。
3. 评审通过后：推分支 → CI → 部署测试服务器 → 真机跑一单坐实提交落库。

## 8. 20260916 更新（已合并 / 已上线 / ④ 与导出）

第 1~7 节写于 20260915，以下是其后的实际进展，读的时候以本节为准。

**已合并、已上线**
- 三轮未参与方评审闭环：第一轮三项必修（④ 路由误写 `authorize()` 致 500、`after.sale.track.v1` 漏进云端 `BUSINESS_TASK_TYPES`、③ 表头 11 列 vs 行 12 格）、第二轮两项（`phaseCounts` 被当整型校验致 finish 422、③ 状态列未移到位）、第三轮判定可合并。
- 迁移与另一条线抢同一编号，重编号为 `0036_after_sale_claim` → `0037_after_sale_claim_fields` → `0038_after_sale_refund_tracking`（服务器原 head 是 `0035_purchase_receipts`）。
- 测试服务器已部署（公网 v0.18.0）；v0.18.0 双平台安装包已发布（tag + Release + release-assets，哈希逐字节核对）。
- 上线后补的两处：① 售后类型选择改成与「环境创建」同款的三段式卡片；② ④ 增加「回访指定单」入口，回访对象不再绑死「本页 ③ 提交过的单」。

**本次新增：④ 导出 Excel**
- 路由 `GET /v1/after-sale/track/{task_id}/export`（`assistant.access`，审计动作 `assistant.after_sale.track.export`），返回 `退款跟踪结果_YYYYMMDD.xlsx`。
- 导出的是**该回访任务清单内**的行（不回访、不重跑），列与工作台 ④ 表头逐列一致，由测试钉死列序（`test_track_export_columns_match_workbench_table`）。
- 「商品图」列写 CDN 链接而不是内嵌图片：导出不依赖服务端外网取图，链接在 Excel 里可点开；要内嵌需另做（服务端下载图片，多一层失败面）。
- 空列落成真正的空单元格（不写「—」占位符），保证 Excel 里能直接筛选求和。

**运维教训（造成过一次「执行器不可用」误判）**
- 桌面端升级后**必须重启执行器进程**：本次安装包已升到 0.18.0，但旧进程仍在跑，云端 `local_executors` 里该执行器仍报 0.17.19、能力位无 `after.sale.*`，工作台报「当前执行器版本不支持阶段 2 正式任务」。重启后同一 executor id 立刻变成 0.18.0 + 20 个能力（含三个 `after.sale.*`）。
- 排查手法：`local_executors` 里看 `client_version` 与 `capabilities`；本机可用 `~/Library/Application Support/XynigoSourcing/运行数据/executor-channel.json` 的 `lastPollAt` 判断进程是否真的在轮询。

**仍未做**
- 「补提失败」重跑与 `failed_retry` 合并视图。
- 多类型（催促发货 / 取消订单）的业务规则：判定条件、幂等与冷却、权限拆分、执行器侧动作都未定，界面上这两类仍是规划态（点选给横幅、禁用扫描）。
