# Xynigo 生态跨仓契约

## 售后直提结果与回访衔接（20260920，开发候选，未部署）

环境单遍直提继续使用既有 `after.sale.claim.v1` 环境清单入口；不会增加扫描前置。执行器保留已读取但无申请入口的订单，以 `skip` 返回订单资料及具体原因。已有退款凭证的 `source` 为 `existing`，不能计为本次新增受理。订单统计包含本批已读取的跳过订单，环境进度仍单独按环境计数；未知字段保持空值。

执行器 → 云端 `AfterSaleClaimProgressRow` 新增可选 `goodsImages`（至多 100 项、单项 300 字符）、`goodsItems`（至多 100 项，沿用商品明细闭集）、`itemCount`（严格整数 1～100000 或 null）。仅动态发现的环境直提结果发送这三项；按订单清单提交仍以请求快照为商品事实来源。云端沿用既有商品字段，无新增迁移；后续缺省或空快照不得擦除先前已保存的商品及送达事实。

Web 的发现与回访仍是两阶段只读任务，回访请求身份闭集不变。发现结果先校验归属，完整覆盖输入环境才允许同页面自动接续；失败、停止或缺少环境结果时保留手动继续。手动继续在解除本次准备锁后创建回访任务，且检查任务代次和发现任务编号，过期响应不得推进其他任务。两阶段之间可用商品图只作为本页展示补充，不加入回访请求身份字段；没有已保存商品资料的历史回访行仍可能缺图。

兼容与发布顺序：先发布云端可选字段接收和 Web，再发布新执行器。旧执行器、旧历史行缺新字段合法；只更新 Web 不会补采旧执行器丢弃的订单资料，也不会追补既有批次。新执行器向旧闭集云端发送新增字段会被拒，不能倒序发布；若需回退云端，须先停止新字段生产方或回退执行器。插件不受影响，本节不代表部署授权。

实现与验证见 [售后反馈修复与回访衔接](20260920_售后反馈修复与回访衔接.md)。下节 20260917 的“先扫描再确认提交”为当时设计记录，环境直提现状以单遍直提实现及本节为准。

## 售后按环境回访匹配（20260917，开发候选）

Web → 云端新增只读匹配接口 POST `/v1/after-sale/track/resolve`，闭集请求为 `environmentSerials`（1～300个数字字符串，单项最多64字符，去重保序）。返回 `items`（环境、订单、退款单、店铺名）、`environments`（每环境匹配数、状态及原因）和计数；仅查询当前租户已有提交与跟踪记录。匹配不派发任务，确认后沿用原创建回访接口与 `after.sale.track.v1`。最多500个退款单、所选环境最多5000条历史记录，超限整体返回422；候选退款单归属额外跨本租户全部历史核验，只投影身份并小批读取。旧三字段入口保留，无迁移或新执行器能力；云端与Web配套部署，本条不代表已上线。详见《20260917_售后按环境回访.md》。

按环境指定提交复用原扫描任务接口，实时取得可申请订单后由用户确认，再调用原售后提交Run接口；不新增请求字段或执行器能力。前端意图只绑定本次扫描任务及执行器，不持久化、不因普通扫描/刷新恢复而自动提交；停止或扫描失败即解除。详见《20260917_售后按环境提交.md》，本条同为未部署候选。

状态：v1 定稿 · 20260910，经 Jeff 确认。本文维护共同接口的归属、兼容要求与验证入口，不宣称尚未完成的集成已经上线。较新功能和测试按 [源码核对基线](../CONTRIBUTING.md#源码核对基线) 解释；下文主仓源码/测试路径相对此基线，代码链接固定到该公开提交。版本、实现与部署状态分别核验。

插件仓为 [crossborder-userscripts](https://github.com/wrangler1024/crossborder-userscripts)。以下“插件路径”均相对该仓根目录，主仓路径相对本仓根目录。普通克隆和 worktree 均按仓库身份定位邻库，不依赖固定父目录。

## 契约地图

| 契约 | 生产者 → 消费者 | 当前边界 |
|---|---|---|
| XYP2 备注 | 提单助手 → 主仓导入与 XYP2 解析器 | 已实现 |
| 云端采购与身份 | 提单助手 ↔ 云端认证/采购服务 | 已实现：登录、续期、草稿、提交、读取及修改冲突 |
| 本机采购助手 API | 采购助手 ↔ 本地执行器 | 已实现：配对、任务/地址读取、能力协商和环境增强 |
| 精准采购链接 | 型号助手、主仓导入、提单助手 XYP2 重建 → 提单助手、售价对比 | 已实现：URL 查询与 fragment 元数据；选品器普通商品链接不在此契约内 |
| 物流标识与接入 | 采购/物流数据 → 物流助手 | 当前手工/文件输入；尚未直接接入云端物流查询结果 |

## XYP2

插件源：`extensions/xynigo-dxm-purchase-assistant/src/core.js`。主仓解析源：[procurement_import.py](https://github.com/wrangler1024/xynigo-sourcing/blob/e10e8a271b3316194a1f6a29a49e65f9f9adcf3a/src/purchase_tool/procurement_import.py)，云端副本由同步脚本生成；[XYP2 解析器说明](https://github.com/wrangler1024/xynigo-sourcing/blob/e10e8a271b3316194a1f6a29a49e65f9f9adcf3a/docs/xyp2-parser.md)复用同一解析规则。

变更须核对 `[XYP2]` 包围格式、明细顺序、字段类型、数量/价格单位、站点/币种/mallCode、必填与可选字段以及备注长度约束。不得以改变字段含义的方式复用旧格式；对旧备注缺失可选字段的行为须明确。

至少用合成样例验证：现有备注、新备注、截断/损坏备注、重复输入、多采购明细和多账号修订。有效历史备注不能仅因客户端升级失去可读性；需要停止支持时另行设计迁移或明确拒绝说明。不能把解码成功当作可重复写入的证明，导入幂等与修订语义也需回归。

验证入口：插件 `extensions/xynigo-dxm-purchase-assistant/tests/core.test.js`、主仓 `tests/test_xyp2_parser.py`、`cloud/auth-service/tests/test_procurement_import_cloud.py` 与 `cloud/auth-service/tests/test_xyp2_parser.py`；按变更扩大覆盖。

## 云端采购与身份

插件调用源：`extensions/xynigo-dxm-purchase-assistant/src/background.js`。草稿、提交、读取分别调用 `/v1/purchase-orders/draft`、`/submit`、`/get`（后两者沿用相同前缀）。还依赖登录、会话续期、退出及权限判断，不能只检查备注输出。

权威校验见 [purchase_contract.py](https://github.com/wrangler1024/xynigo-sourcing/blob/e10e8a271b3316194a1f6a29a49e65f9f9adcf3a/cloud/auth-service/src/xynigo_auth/purchase_contract.py)。变更须检查 `schemaVersion`、`orderKey`/系统订单键、来源销售行、金额归属、`expectedDraftRevision` 及冲突响应。请求只用于并发控制的字段不能悄然进入稳定哈希，跨账号修订不能覆盖他人较新的数据。

身份与权限是服务端边界；不能以隐藏前端入口替代鉴权，也不能让插件持有服务端密钥。增加字段前检查服务端严格校验，不能假设旧服务会忽略未知字段。

验证入口：插件 `tests/background.test.js`、`tests/source-associations.test.js`（均在提单助手扩展目录）；主仓 `cloud/auth-service/tests/test_purchase_api.py`、`test_purchase_source_associations.py`、`test_purchase_shared_revision.py`。

## 本机采购助手 API

插件源：`extensions/xynigo-shein-purchase-assistant/src/background.js`；协议前缀为 `/api/purchase-assistant/v1`，包含短时会话配对和能力探测。主仓验证入口：[test_purchase_assistant.py](https://github.com/wrangler1024/xynigo-sourcing/blob/e10e8a271b3316194a1f6a29a49e65f9f9adcf3a/tests/test_purchase_assistant.py)、[test_purchase_assistant_suborders.py](https://github.com/wrangler1024/xynigo-sourcing/blob/e10e8a271b3316194a1f6a29a49e65f9f9adcf3a/tests/test_purchase_assistant_suborders.py)。

声明最低能力和不支持时的行为，不要求插件与执行器版本号相等。能力声明、真实端点和错误码必须一致；环境增强不可用时能否保留核心地址读取，以已验证降级策略为准。

收件信息只在明确选中的任务范围返回；任务身份、地址冲突、字段映射、数据源选择与站点变更需要回归。回环来源、扩展配对、响应不缓存和敏感数据不持久化等边界不可因兼容旧版本而放宽。云端部署不能更新员工电脑中的本机执行器代码。

业务工作空间可能存在比正式插件仓更新的测试源码。发现这种差异时先记录来源、版本、功能与测试证据，经确认后归并到正式仓；不能用版本号较低的副本直接覆盖，也不能凭版本号较高就宣布其已正式验收。

## 精准采购链接

当前元数据生产者为型号助手 `scripts/shein-product-variant-helper/shein_product_variant_helper.user.js` 的 `buildPurchaseLink`、主仓 `procurement_import.py` 的 `_purchase_link`，以及提单助手 `extensions/xynigo-dxm-purchase-assistant/src/core.js` 的 `parseXyp2Remark`（从备注重建链接）。当前解析消费者为同一提单助手核心的 `parsePreciseLink` 和 `extensions/xynigo-shein-price-compare/core.js`；对应生成油猴脚本和打包副本不另算独立实现。

选品器 `scripts/shein-globalship-selector/shein_globalship_selector.user.js` 的 `formatProductLinks` 使用 `canonicalizeProductUrl` 清除 query 与 fragment，仅复制普通商品页面链接，不携带 `xv`，不属于本元数据契约的生产者或消费者。未来若增加精准采购链接功能，须先纳入本契约并补兼容验证。协作表及链接单元格负责完整保留 URL，不据此成为元数据的语义解析者。

当前链接含商品、SKU、mallCode 查询字段，以及 fragment 中的 `xv`、`p`、`s`、`op`、`cr`、`gp`、`c`、可选 `pt`。具体缺失值和接受条件以各消费者测试为准；不把审单原价、指导价和实际付款混成一个金额。

变更需验证生成、读取、导入重建后的往返结果，尤其是币种、规格、优惠比例、URL 编码和旧链接没有采价时间的情况。不要因调整一个插件而破坏另一插件的读取；售价对比构建还嵌入型号助手源码，需同时检查其包内容。

## 物流标识与未来接入

现有物流助手以手工粘贴或 Excel/CSV 接收订单号、采购子单号、物流单号和物流商渠道。它不直接读取云端采购或物流查询 API，也没有已经建成的云端幂等审计链路。

订单号、包裹 ID、采购子单号和系统订单键不是可互换字段。不得按子单序号、列表顺序或接口返回顺序猜测商品/包裹关系；平台受理不等于最终发货成功，超时或响应未知不等于失败可重试。

未来接入前需单独确认：字段和身份映射、来源与时效、权限范围、重复提交与结果未知策略、人工确认边界和云端审计。该设计与实现通过评审后再把本文状态改为“已接入”。

## 契约变更与发布顺序

每次变更记录：受影响契约、生产者/消费者、各仓候选 SHA、旧/新输入兼容结果、最低能力、发布顺序与回退条件。修改权威正文和受影响仓入口摘要，不在两仓复制整份规范。

默认先部署能兼容新旧输入的接收方，再发布产生新输入的一方；验证历史数据和旧客户端后，才考虑移除兼容。不能固定为“主仓永远先发”。插件自动更新、云端发布和桌面安装有不同生效时间，需考虑新旧版本共存。

跨仓 CI 不依赖作者机器的邻库目录。后续共同合成样例采用明确版本/提交固定引用，并在两仓验证；禁止从邻库默认分支实时获取数据而不固定来源。共享夹具与兼容矩阵的机器门禁仍待实施。

## 采购详情凭证（基础协议）

生产者：采购助手 `src/purchase-details.js` → 本机 `/api/purchase-assistant/v1/purchase-details` → 云端 `/v1/assistant/purchase-receipts`。本机健康接口增加 `features.purchaseDetailsV1`，不改变旧地址读取协议。云端凭证要求迁移 `0035_purchase_receipts`；只增加协议，不将旧只读代理改成任意写入代理。

本机 action：`read`、`submit`、`status`、`retry-image`。read 输入任务键、当前标签页 URL、随机 document 标记、用户确认的环境序号。通过云端查验数据源与任务，再连接已打开环境的唯一匹配标签页。返回短时 captureId、只读订单信息、JPEG 预览、目标信息与版本。submit 仅接受 captureId、confirmed 和修订原因，不接受客户端替换金额/图片。captureId 按登录成员隔离，30 分钟失效。

云端 action：`preview`、`submit`、`status`、`retry-image`。来源由云端登记的团队表 sourceId 解析。以 assistant.access 权限、租户和已登记团队表限制写入；采购员字段不参与回传校验（20260914 经确认调整）；个人速填表拒绝。身份键沿用 PT1 的 `[系统订单键或fallback,店铺,完整销售订单号,包裹号]`，保留人工拆单后缀，不按行号或截断订单号定位。

一任务一采购订单。多商品行只记一次金额和截图，订单号同步到同任务商品行；不更新采购状态。原币 MXN 保存在凭证记录；现有“实际付款”单元格保持数值，不混入币种文本，不新增表头。首版拒绝其他币种。

提交使用 requestId 幂等键、expectedRevision、任务 fingerprint。数据库持久化 slot 认领与每次不可变凭证，记录操作者、原因、时间、订单号、金额、币种、付款时间和原图。原图为业务凭证保存在租户数据库，不进入公开仓、日志或扩展持久存储。同采购订单不能分配给其他任务。修订需原操作者或管理员、有原因、明确版本，旧凭证通过 previous_id 留存。

状态为 pending → text_done → complete；明确图片权限/参数拒绝为 image_failed，可补图；任何无法确认的外部结果为 uncertain，不自动重试。进程在写入中终止保留 pending/text_done，后续仅查询，不盲目回放。截图验证绕过网关 image cache，读原单元格并比对原/后图指纹。人工已有值或图不被静默覆盖。

飞书不提供本方案已验证的事务式行身份 CAS。读回可检测并发变更，但不能消除人工排序与写入间的竞态；受控验收期间避免修改目标任务结构。未知结果仍需管理员核对，尚未实现自动恢复门禁。上线前必须验证登录权限、图像写入读回形态，以及新旧客户端共存。

发布顺序：云端迁移和兼容接口 → 本机执行器 → 插件候选。现有云端部署不会更新本机执行器。独立评审、真实写入验收和分发授权分别记录。

## 采购反馈第二版扩展（开发候选，未部署）

执行器0.17.24 / 插件0.10.0候选。（20260916 更新：执行器候选升到 **0.18.0**，新增能力 `after.sale.scan.v1` / `after.sale.claim.v1` / `after.sale.track.v1`——采购售后模块。**发布顺序：先云端（已部署测试机，迁移 head 0038），再执行器**；老执行器不具备这三个能力，新任务下不去，但既有能力与协议不受影响，可分批升级。插件侧本次不变。）云端preview新增features.fillColorV1；本机read新增features.fillColorV1和asyncSubmitV1。旧插件忽略新增返回字段，继续同步提交；新插件仅在对应能力为true时发送扩展参数。旧云端不接收填色参数，旧执行器不显示补色入口。

本机submit、retry-image、retry-color可选async:true，立即返回processing表示处理中，不表示已完成；status查询本机任务，完成后返回云端最终状态。最多4个后台操作，同凭证串行；轮询不会重新提交。执行器重启后通过重新读取任务恢复云端记录，未知写入不自动重放。读取请求超时105秒，后台提交不再让插件等待整个云端请求；仍保留结果未知保护。

新增fillColor字段默认空字符串，允许浅绿#E2F0D9、浅蓝#DDEBF7、浅黄#FFF2CC、浅橙#FCE4D6、浅粉#F4DCE6、浅紫#E4DFEC、浅青#DDF2EF。每次新操作默认不填色，不沿用上次颜色。不填色不改变已有底色。填色只覆盖当前任务匹配行的A～AR，不包括表头及夹在商品行之间的其他任务。连续行合并成范围，仅设置backColor。

选择的颜色是提交内容的一部分，同一requestId不可换色。颜色状态保存在既有凭证image_cells.color中（selected/state），无新增迁移。凭证complete先持久化，再单独锁定任务提交槽并核对当前revision、实时任务和原图后填色；颜色pending/failed可用retry-color单独恢复，complete不因颜色失败退回未完成。旧revision的颜色操作被superseded跳过，补色和status始终使用同一活动requestId。

身份字段仍按ToString读取，原始金额和图片分开批量读取；每次写入前后实时核验，不缓存任务行。共享HTTP连接和按租户/凭证隔离的有效token缓存，返回不含业务内容的performance计时与请求统计。脱敏冷缓存单商品流程18次飞书请求（旧路径31次）；选色并成功填色22次，包含独立填色前的必要复核。实际耗时需Windows场景测量，不将请求数下降视为同等比例的耗时下降。

截图边界仅计算一次，后续读取轻量订单字段。面板淡出120毫秒、恢复淡入140毫秒；减少动态效果模式不播放动画，10秒看门狗恢复面板并使过期截图无效。窗口、任务和输入状态保留。

## 采购售后运行参数（开发候选，未发布）

售后扫描、提交、回访请求增加 `concurrency`（严格整数 2 / 3 / 5，默认 2），`browserMode` 默认改为 `headless`，可显式选择 `visible`。三个既有 `after.sale.*.v1` 任务类型不变，新增执行器能力 `after.sale.runtime-controls.v1`。Web 及云端均拒绝将新运行参数交给缺少该能力的执行器，避免设置被旧桥接层忽略。提交 Run 将并发数保存至既有 request_summary，不新增数据库列；并发设置变化参与请求哈希，旧幂等键不得复用于不同运行参数。

并发单位是环境，同环境订单串行；环境序号/名称别名解析到同一 containerCode 时使用同一批次锁。停止后排队环境不再启动，已经进入的订单沿用原收尾逻辑。复用已经打开的浏览器不会重启它来改变可见性，模式设置用于本批新开的环境。

兼容上线顺序：先云端接受新能力与字段，再发布支持该能力的本地执行器。仅改云端页面不能使现有桌面进程获得无头/并发支持。本候选未进行部署、桌面分发或真实平台写入验收。


## 全模块并发档位（20260916 开发候选，未发布）

所有用户可配置并发统一为严格整数 `2 / 3 / 5`，默认 `2`：物流查询配置、店铺结算请求、售后三类请求、环境创建配置、代理检测，以及 Web/桌面设备设置。API 显式提交其他值（包括布尔值、字符串和小数）拒绝；读取本机旧配置中的非法档位回退到 2，读取过程不改写配置文件。合法的 3、5 保留。

内部有效并发仍受现有业务限制：物流有头查询为 1，同环境任务互斥，注册串行；环境创建在安全并行开启时封顶 2、关闭时封顶 3。这些是执行安全上限，不是新增配置档位。云端只读运行摘要继续兼容旧执行器上报的历史范围，写入契约采用新闭集。旧客户端提交 1、4 或其他档位会收到校验失败，因此 Web、云端契约和桌面执行器需协调升级。本地开发候选不等于已上线。

## 售后完整退款记录与最终回执（20260916 开发候选，未发布）

执行器新增 `after.sale.reliable-results.v1` 能力。云端对新建提交、回访任务要求该能力；扫描仍使用原能力门槛。新云端继续接受旧任务不含扩展字段的历史回执，但新任务不分发给旧执行器。

提交结果行新增可选 `refunds` 数组，元素闭集为 `refundBillId / packageNo / refundPath / refundAccount / submittedAt`，每单最多 100 条。旧单号字段保留兼容；新记录按退款号增量合并，已受理包裹不会因后续失败被清空。迁移 `0040_after_sale_refunds` 在提交结果表新增 JSON 列，旧记录默认空数组；空数组由页面和导出回退至旧单号。历史、结果表仍一行一订单，单元格展示全部退款号；回访按退款号展开成多条请求。

提交及回访的 finish.resultSummary 新增可选 `rows`，只允许这两类任务使用，行契约与进度相同；结果入库与任务终态在同一事务内完成，相同回执重试幂等。最终进度上传失败不丢弃结束回执中的结果。失败/排队回访不会清除跟踪表中已有的有效平台阶段。

上线顺序：云端备份和迁移 0040 → 兼容云端服务/Web → 支持新能力的执行器；不可先向旧云端发布新回执。回滚旧云端前须停止分发新任务并让在途任务完成；不删除已有退款 JSON 数据。本候选未部署、未发布，也未进行真实退款写入验收。结束回执沿用执行器既有重试机制，进程崩溃或平台受理后尚未取得退款号的情况仍需人工核对，不能盲目重放外部写入。
# 售后扫描原因证据增量（20260916）

`AfterSaleScanRow` 增加四个可选字符串字段，默认空：`reasonCode`（最大64字符）、`platformStatus`（240）、`reasonSource`（32）、`checkedAt`（40，执行器读取时的带时区时间）。具体原因继续放在兼容的 `errorSummary` 中；证据不得改变 `claimable` 的平台实时核验口径。`platformStatus` 是订单列表原文，`reasonSource` 标记资格判断来源（`order_list` / `pre_info`），二者不同源时前端必须分别说明。

发布顺序为云端闭集及 Web 先升级，再发布执行器。旧执行器/历史行缺少新增字段合法；新执行器直接连接旧闭集云端会导致快照被拒，不能倒序发布。无新增数据库列。Excel 在扫描清单末尾追加三列证据信息，原有列序保持。

## 售后本地筛选与计时草案（20260916，未发布）

扫描行拟增加可选 `durationSeconds`、`itemCount`、`itemCountSource`、`goodsImages`、`goodsItems`、`deliveredDate`、`deliveryDateStatus`、`deliveryNote`；数量缺失为 null，日期完整性缺省 unknown，图片/明细缺省空数组。商品明细闭集包含 goodsImg、name、specification、quantity，未知值不得由图片数推断。扫描/回访任务响应增加 startedAt/completedAt，采用带UTC时区时间。旧客户端行保持合法；新字段执行器须在云端闭集升级后发布。

真实整单包裹与逐项商品来源仍待验证，详见《20260916_售后本地调优阶段记录.md》。仅当整单包裹集合完整、逐包全部送达且日期齐备才形成 deliveredDate；不完整时空值且unknown，不能用于日期范围选单。此草案与本地实现不代表已上线。


## 售后回执核对扩展（开发候选，20260916）

本次仅涉及主仓云端、执行器和工作台，不改变插件协议。提交行新增 `verifying`（仍在核验、不计完成）与 `uncertain`（结果待核对）；退款 JSON 保留来源、实际阶段、平台申请原文时间和时区，已有记录不能被当成本次新增成功。终态售后提交汇总接收非负整数 `uncertainCount`。

云端先部署迁移 `0041_after_sale_uncertain` 和兼容字段闭集，再发布声明 `after.sale.receipt-recovery.v1` 的执行器；旧执行器提交被能力门禁拦截，历史记录缺新增字段仍可读取。工作台热更新不能替代执行器升级。未知写结果不可按失败直接重放，环境别名及订单号空白、大小写不能绕过同一订单保护。

具体字段、测试和回退条件见 [售后反馈修复实现与验证](20260916_售后反馈修复实现与验证.md)。本节不代表已发布或完成真实退款写入验收。

### 售后 P0 补充（20260917 开发候选）

提交请求新增 `goodsImages`、`goodsItems`、`itemCount`，作为云端批次商品快照保存，任务下发与进度不重复携带完整数组。结果新增 `operationCompletedAt`、`submissionError`、`recoveryError`，与退款凭证的申请时间和来源分别保存；迁移 head 增至 `0042_after_sale_result_facts`。执行器新增 `after.sale.claim-evidence.v1`，售后提交须具备该能力。旧结果缺失字段保持未知，云端先升级兼容接收方再升级执行器。详见 [P0 修复记录](20260917_售后P0多图与受理凭证修复.md)。插件协议不变，本候选尚未发布。

## 退款当前节点与审核补证证据（20260917 开发候选，未部署）

回访/提交退款凭证新增可空 phaseEvidence，闭集为 source（timeline_nodes_v1）、step（严格整数1~5）、title（240）、detail（800）、canSupplement（严格布尔）、reason（300）、reasonSource（空/current_node/negotiation_history）。阶段与节点必须一致，旧回执无此字段保持合法。整页及完整时间轴仍不上传。

新增阶段 shein_refunded、bank_processed、review_failed、evidence_required；旧 refunded/rejected 保留为未重新核验的历史状态，不能按第五节点解释。迁移0043新增跟踪当前节点证据及上次成功说明；失败保留原阶段和原检查时间，任务级快照固定上次观察，重新审核成功后清掉旧理由。

新回访要求能力 after.sale.phase-evidence.v1，防止向旧执行器分发仍会按全文误判的任务。先升级云端兼容接收方及迁移，再分发新执行器；旧安装包不被同版本替换。新执行器不可先向旧云端发送新字段。插件不受影响。具体行为与验证见《20260917_退款五节点与审核补证修复.md》。
