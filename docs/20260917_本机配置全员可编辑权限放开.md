# 本机配置向所有登录成员放开

分支 `codex/local-config-open-members`，提交 `d9b9049`（基于 origin/main `ab4c65a`）。按 Jeff 20260917 决策执行。

## 背景与诊断

志恒、康德在云端工作台是管理员，但桌面客户端「本机配置」显示"普通采购员仅可查看设备级设置"，运行参数与物流查询高级设置只读。原因是两条闸门叠加加一处硬编码文案：

- 桌面客户端 `canConfigure()` 要求 `super_admin` 角色且持有 `system.integration.manage`；该权限在云端属超管专属（`SUPER_ADMIN_ONLY_PERMISSIONS`），管理员结构性拿不到。
- 执行器本地写入端点同样要求 `super_admin`：POST `/api/config` 内联校验，`/api/hub-api-key` 经 `AUTH_PERMISSION_BY_PATH` 表驱动命中超管专属。
- 锁定态文案写死"采购员"，管理员看到后误以为角色未生效。侧栏头像旁角色字样才是真实角色。

## 决策

Jeff 拍板方案 B 并扩大范围：本机运行参数不设角色门槛，所有登录成员可配置。这些设置仅存本机、影响本机执行器行为，「发布本机配置到组织」仍为管理员流程，未改动。

## 改动（4 文件，+60/−15）

- `src/purchase_tool/main.py`：POST `/api/config` 改为仅登录校验；从 `AUTH_PERMISSION_BY_PATH` 移除 `/api/hub-api-key`，同样落到仅登录校验。`hub-core-repair` 保持超管专属不变。
- `src/purchase_tool/web/desktop.js`：`canConfigure()` 改为登录即可，本机配置页对全部成员解锁，锁定文案不再渲染。
- 测试：反转原"成员不可写"用例为"仅需登录"；`test_env_config.py` 新增成员真实保存运行参数与 HubStudio Key 两个成功用例。

## 评审后补（Cursor 评审结论：可合并，无必须改）

评审指出兼容页仍锁同一写入，核实为真实用户路径后一并修复：

- **兼容页修复**：Windows 状态中心「本机设置」与 macOS 客户端打开的都是 `/?view=localsettings`（`packaging/windows/launcher/main.go` `localSettingsURL`、`packaging/macos/desktop_client.swift` `desktopSettingsQuery`），对应 `src/purchase_tool/web/index.html` 的本机设置视图。该视图三处旧权限判定（提示条、渲染禁用、保存守卫）改为登录即可；已跑 `sync_web_workspace.py` 同步云端副本。
- **共享说明**：desktop.js 设置页常驻“本机设置由这台电脑上的所有登录成员共享”提示；index.html 提示条同步改为“所有登录成员均可修改”。
- **防回归**：新增 `test_device_config_paths_stay_out_of_permission_maps`（路径不入权限表，回加即红）与两条 UI 契约测试；删除评审认为偏松的裸 STATE 用例（真实保存由 `test_env_config.py` 两条用例覆盖）。
- **暂缓项**：`/api/hub-api-key` 未加乐观锁（覆盖式语义、失败面小，加锁需改安全存储契约，另立小单）；诊断页内核修复 UI 用 `admin` 而接口仍超管专属（`desktop.js:796` vs `main.py:306-307`）属既有不一致，本单不引入，登记待办。

## 验证

worktree 本地全量 `python -m pytest tests -q`：1257 passed，5 skipped（本轮删 1 条弱用例、加 3 条新用例，总数持平）。受影响专项（`test_cloud_auth` / `test_env_config` / `test_desktop_ui` / `test_executor_workspace_web`）173 项通过。

云端套件（`cloud/auth-service`，收集 397 项）全绿（exit 0），在 `index.html` 云端副本同步后重跑。`sync_web_workspace.py` 已运行：`index.html` 与云端副本一致；`desktop.js` 不在清单、无云端副本。

## 边界与未完成

- 未部署、未发版、未真机验收；桌面客户端与兼容页需随执行器版本更新后由真机确认。
- 云端工作台远程下发执行器配置的权限（`executor.config.write`）保持管理员限，不受本次影响。
- 待办（本单不引入、未修）：诊断页内核修复按钮按 `roleInfo().admin` 显示，而接口 `/api/hub-core-repair/*` 仍超管专属，管理员可能看到按钮但被 403 拒绝（`desktop.js:796` vs `main.py:306-307`）。
- 待办：`/api/hub-api-key` 无乐观锁（后写覆盖），如需与 `/api/config` 的 revision 对齐另立小单。
- 评审（Cursor，20260917）：结论可合并、无必须改；6 条建议中已采纳兼容页修复、权限表防回归断言、弱用例删除、共享提示与死代码标注，暂缓项见上。
