# 交接：Xynigo v0.17.19 发布收尾（20260911，ZCode → Cursor）

状态：**已完成（20260911）**。CI `34584017764` 全绿后已创建 GitHub Prerelease v0.17.19（2 资产），飞书开发群通知 `om_x100b6508fce630a0c2acc1f77f621fb`。tag 仍为 `5a16678`，未重建。main 含 CI 修复 `da00490`。

## 当前状态（已完成，勿重做）

- 两段发布提交已推 main：e717be1（升版 0.17.19）→ 5a16678（回填发布元数据）；不可变 tag `v0.17.19` 已推（勿移动/重建）。
- 双平台安装包 = CI run 34580782999 产物，已下载到 `.codex_tmp/release-v01719/`（gitignored，勿删）：
  - win: `Xynigo_Sourcing_Windows_Setup_v0.17.19.exe`（15,375,285B，sha256 3e289a3a8a4244f077204bbcfb9e0c0295554c9d8c00016e17225fdfad82e2f9）
  - mac: `Xynigo_Sourcing_macOS_Standard_v0.17.19.pkg`（20,266,999B，sha256 cf241b29cf5501a6d7c74dad33cc89c20327932a1b92a06873a16649757e047a）
  - 两包已上传服务器 `/opt/xynigo-auth-test/release-assets/`（sha256sum 复核一致）；云端目录 `local_executor_release.py` 已部署，容器内 0.17.19 校验通过，healthz/readyz 200。自动更新通道已就绪。
- 若 `.codex_tmp/release-v01719/` 缺失：`gh run download 34580782999 -n xynigo-sourcing-windows-standard-installer -D .codex_tmp/release-v01719/win` 与 `-n xynigo-sourcing-macos-arm64 -D .codex_tmp/release-v01719/macos` 重新下载（须在仓库根目录执行）。
- CI 在 5a16678（最终候选）上的 run 编号 34581942050，应已/将变绿。

## 剩余任务一：GitHub Release（等 CI 绿后执行）

```bash
cd /Users/jeff/Documents/xynigo-sourcing
gh run list --branch main --limit 1        # 确认 34581942050 conclusion=success
gh release create v0.17.19 \
  --title "Xynigo Sourcing v0.17.19（测试通道）" \
  --prerelease \
  --notes-file release/v0.17.19.zh-CN.md \
  .codex_tmp/release-v01719/win/Xynigo_Sourcing_Windows_Setup_v0.17.19.exe \
  .codex_tmp/release-v01719/macos/Xynigo_Sourcing_macOS_Standard_v0.17.19.pkg
gh release view v0.17.19   # 核验资产数=2、哈希与上文一致
```

注意：公开仓、测试通道（internalUnsignedTest），务必 `--prerelease`；tag 已存在故不要重建。

## 剩余任务二：飞书「小犀代采 Xynigo - 系统开发群」上线通知

群 chat_id：`oc_9df1bfce33a5d3a7e45ee9a803ce7350`（lark-cli 默认企业 profile 已配置凭证，无需索取）。先 `lark-cli im +message-send --help` 确认参数名，然后发送以下文案（建议按富文本/纯文本分段）：

---
【店铺结算查询】功能上线 🎉

Xynigo 执行器客户端 v0.17.19 已发布，「小犀助手 › 店铺结算查询」正式可用：

功能：批量自动登录 SHEIN 店铺后台，采集在途订单/累计未结算/下次结算/已完成结算/不可提现金额，一键导出《店铺结算汇总表》直接交财务，全程只读。

操作步骤（运营同事请按此操作）：
1. 下载安装执行器客户端（GitHub Release 或云端工作台下载入口，Windows 装 Setup、Mac 装 pkg）——此前只用过提单助手插件的同事注意：执行器客户端是另一个程序，需要新装。
2. 打开客户端，飞书扫码登录（与工作台同一账号）。
3. 底部「运行环境」确认本机执行器在线（HubStudio 已登录）。
4. 进入「小犀助手 › 店铺结算查询」：粘贴店铺名单（支持店铺名/环境序号/环境ID）→ 查询环境 → 勾选 → 一键巡检。
5. 结果页实时出数；失败店铺可点「补采失败」只重跑失败的；完成后「导出 Excel」发财务。

如有疑问或故障，随时在群里反馈。
---

## 验证标准

- Release 页面：v0.17.19 prerelease，2 个资产，下载后 sha256 与上文一致。
- 飞书群：消息已发送且正文完整（含下载入口与 5 步操作）。
- 完成后：更新本项目记忆（`~/.zcode/cli/memories/projects/shein-dropshipping-ops-4ba42e9454862df3/memory/shein-store-finance-inspection-v1.md` 的「待办」段标记发布完成），并把本文件改名加「_已完成」。

## 关键坑（务必遵守）

- 部署云端任何发布目录改动时，**三个版本承载文件必须一起 rsync**：`cloud/auth-service/src/xynigo_auth/{__init__.py,main.py,local_executor_release.py}`，漏一个容器 import 守卫直接崩（本次已踩：只传目录文件导致 auth 崩溃重启，补传后恢复）。
- 每次改提交前跑 `python3 scripts/audit_public_release.py`（公开仓审计；本地 `.codex_tmp` 报 IP 属干扰可忽略，提交文件必须零发现）。
- 服务器访问：`ssh -i ~/Documents/网络运维/servers/tencentcloud/guangzhou/tencentcloud-gz-01/credentials/tencentcloud-gz-01_ed25519 ubuntu@43.136.40.39`；部署用 `--rsync-path="sudo rsync"`；多文件 rsync 勿用 `--relative`。
- 执行器源码进程在 8765（`XYNIGO_DATA_DIR=~/Library/Application Support/XynigoSourcing` 启动），发布期间勿动。
