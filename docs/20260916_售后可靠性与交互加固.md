# 售后可靠性与交互加固

基线：main `e723f7f`。分支：`codex/after-sale-reliability-ux`。
范围：按模块评估修复可靠回传、多包裹记录、任务控制和前端交互；未推送、未提交真实退款。
状态：实现完成、独立复审通过；`aac30a4` 已合入本地 main 并部署共享测试环境。最终功能与样式提交为 `e7d2cf9`，部署提交包含验证记录。

## 实现

- 每个包裹生成退款号后立即保留凭证；后续资格/账户读取或第二包失败仍保留已受理记录。部分失败保持失败订单口径，并在页面标明已有受理包裹，历史与导出显示全部退款号，回访展开全部包裹。
- finish 携带完整结果行，与云端终态同事务入库；上传失败的进度不记为已确认。结果行不完整时标记 uncertain，不能把缺失行解释为完成。
- 新任务通过可靠结果能力门槛；迁移 0040、闭集字段和升级顺序见《生态跨仓契约》。
- 提交资格复用严格只读校验；无法识别的回访页面返回失败，明确已受理节点仍支持全文兜底，失败读取不覆盖之前的有效平台阶段。
- 回访增加独立取消路由；运行中禁止清空结果；三入口在首个 await 前锁定发起状态，失败重试同一报文复用幂等键。
- 终态标题区分取消、失败、待核对及部分失败。扫描发起时重置进度。移除没有实际保障的“终态冻结”承诺，明确可重复只读回访。
- 手工输入改为多行文本，支持中英文分号和换行；回访错误组、重复组明确提示，存在错误时不发送部分清单。
- 窄屏运行条换行，停止按钮保持可见；输入区沿用统一网格。补齐本机提交路由对商品图、送达时间的透传。

## 验证

- 最终本地全量 1168 passed / 5 skipped；云端全量 342 项通过。仅既有 Python 转义与 Starlette/httpx 弃用提示。后续定稿的样式增量 Web 82 项复跑通过。
- 云端新增端到端测试覆盖无进度上传的最终结果入库、结束回执重复发送、无效结果事务回滚、多包裹历史/导出、失败回访保留旧阶段、回访取消路径及旧能力拒绝。
- SQLite 与独立临时 PostgreSQL 16 均验证 0040 升级、新旧记录默认值及回退保留原字段数据。临时 PostgreSQL 容器已销毁，无共享数据库操作。
- Playwright 合成页面在 1440/768/375px 验证运行条无横向溢出、停止按钮可见、多行输入对齐和格式错误阻断。截图位于忽略目录 output/playwright/。
- 源/云端 Web 与两份原型同步，JavaScript 语法、公开安全审计和 diff 检查通过。
- 未参与实现的评审者复审 `e723f7f..e7d2cf9` 通过，无剩余必须改；评审发现的时间轴全文兜底问题已修复并补回归。独立验证本地后端 50 项、云端可靠性与迁移 2 项及最终补审专项 12 项通过。
- 上述合成验证使用的浏览器、临时 HTTP 服务与 PostgreSQL 容器均已关闭。

## 共享测试部署与真机联调（20260916）

- 按本次合并、部署及真机联调授权，本地 main 快进至 `aac30a4`；云端镜像来源固定为该提交，标签与容器内版本标记一致。没有推送远端、发布 Release 或修改公共桌面更新资产。
- 切换前受控停止入口，确认执行器及导入任务排空；备份代码、配置和数据库，并在独立 PostgreSQL 18 容器完成整库恢复验证。随后升级 `0039_tracking_timestamp_defaults → 0040_after_sale_refunds`，仅切换应用服务，数据库容器未重启。
- 部署后 readiness 正常，迁移位于唯一 head，应用重启计数为 0；联调结束无活动执行器任务，导入任务状态计数未改变。备份定位、归档哈希、镜像及操作日志保留于私有运维记录。
- 本机执行器临时运行同一提交的不可变源码归档，runtime ID 为 `0.18.0-aac30a4840d2-dev`，复用既有配对身份并保持在线；安装目录未替换。桌面正常重启可能回到旧运行时，新提交/回访任务仍受可靠结果能力门槛保护。
- 通过已登录工作台真实扫描两个环境：分别返回运输中、暂不可申请，以及已送达、可申请的一个包裹；进度从 0/2 到 2/2，扫描完成卡片保持展开。
- 指定一个既有退款单只读回访成功，返回审核中、金额及原卡掩码。另以中文分号输入两个既有退款单并点击停止，最终两行均为已停止，已处理保持 0/2，控件恢复可用。
- 提交历史可打开，环境数与批次记录正常展示。联调创建的 HubStudio 浏览器已关闭，用户原先打开的环境未动。

## 限制

真实平台验收覆盖只读扫描、退款回访和停止，不包含新退款提交；多包裹受理及异常回传以合成测试验证。平台受理与本地进程并非一个事务；进程崩溃或尚未取得退款号时仍需人工核对。旧记录过去已经丢失的多包裹退款号不能凭空恢复。桌面安装包尚未发布，本机当前为临时源码联调运行时。

## 输入区紧凑调整（部署后反馈）

- 独立分支 `codex/after-sale-compact-inputs`：指定订单提交、指定退款单回访的输入框默认高度从 72px 收紧为 44px，保留多行输入与手动纵向拉伸；按钮固定 44px，顶端对齐，不再跟随文本框拉高。
- 已同步云端 Web 及两份原型；现有 Web/桌面界面测试 99 项通过，diff 检查通过。此样式增量已通过独立评审，并以 `d9416a2` 合入 main（保留并行完成的 0.18.1 发布改动）；合并后 99 项测试复跑通过。

- 样式增量已部署至共享测试站：公网 HTML 与合并后权威源 SHA-256 一致（`dae1226eb185efeefecfcf8b38394ee143e855144f4714be6c2e862208889db9`），readiness 正常。0.18.1 容器根文件系统为只读，因此原地热替换未成功且未改变线上页面；改为备份后受控停入、确认活动任务为零，再切换仅含 HTML 差异的应用镜像。未迁移或重启数据库，未改客户端安装包。
