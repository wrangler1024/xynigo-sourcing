# 售后导出入口

分支：codex/sticky-task-progress。当前提交结果新增“导出提交结果 Excel”；退款跟踪原按钮明确为“导出退款跟踪 Excel”。

当前结果与历史详情共享既有批次 Excel 导出接口，列序与提交结果表一致。不新增云端接口或改动数据。无批次或无结果时禁用；运行中可导出该批次已回传结果。刷新恢复最近结果时同步恢复批次 ID，并阻止迟到的恢复请求覆盖新批次。

验证：81 项 Web 测试通过（含真实导出函数的合成 DOM/API 用例），云端 10 项 Excel 导出测试通过；内联脚本语法与源码安全审计通过。覆盖当前/历史批次隔离、空结果、恢复身份、下载失败、重复点击及迟到响应。未触达真实订单，未合并或部署。
