# 任务：接手 Xynigo 采购售后（丢件退款）模块，继续完善与打磨

你是这个仓库的新会话。上一会话已完成四链落地并全部上线测试机，交接文档已写好——**先读文档再动手**。

## 一、仓库与基线

- 仓库 `wrangler1024/xynigo-sourcing`（私有）。**基线以 `origin/main` 为准**：先 `git fetch origin`，当前 `origin/main` = `54d32a9`。
- 建议自建 worktree：
  ```
  git worktree add ~/Documents/xynigo-worktrees/after-sale-polish -b codex/after-sale-polish origin/main
  ```
  ⚠️ 不要把本机 `~/Documents/xynigo-sourcing` 的本地 `main` 当基线（它停在另一条线上，且被一个遗留 worktree 占着）。

## 二、先读这三份（按顺序）

1. `docs/20260915_采购售后模块交接.md` —— 模块总交接。**§10＝线上事实**（已上线清单、两个字段断点与云端兜底、0039 迁移坑、一次性数据回填的说明）；**§11＝交给你的待办 + 接手须知 + 关键文件地图**。
2. `handoff.md`（仓库根）—— 一页索引：一句话现状、本轮提交一览、Codex 入口、三条最容易踩的坑。
3. 需求原文（三份都已改成「已实现」，改前先看口径）：`docs/20260916_售后需求排期与暂缓.md`、`docs/20260916_需求_售后提交历史与从历史跟进.md`、`docs/20260916_需求_售后直接提交指定单.md`；原型定版 `docs/prototypes/20260915-procurement-after-sale-v2-multitype.md`。

## 三、现状（一句话）

采购售后四条链——② 扫描可申请 / ③ 提交售后（含**补提失败**、**直接提交指定单**）/ ④ 退款跟踪回访（含**导出**）/ ③ **提交历史**（列表·详情·回访本批·重提·批次导出）——**已全部上测试机 `xynigo.samforo.icu` 并核验**；本地 `1082 passed / 5 skipped`，云端套件全绿；迁移头 `0039_tracking_timestamp_defaults`。

**唯一未上线＝执行器 0.18.1**：桥接层透传（`deliveredAt`/`goodsImg`）已在 main，但桌面端没发版，线上执行器仍是 0.18.0（云端兜底已让这两列显示出来，所以**不是阻塞项**）。

## 四、要做的事（优先级见模块文档 §11.1）

1. **执行器 0.18.1 发版**（唯一欠账）。两段式：版本号+发布说明提交 → CI 构建双平台候选 → 回填 `local_executor_release.py`（runtimeId/SHA/大小/notesZh）→ 打不可变 tag → 上传测试机 `release-assets` 并核对实际文件哈希。**分发资产与发 Release 需 Jeff 明确授权：先出方案，再执行。**
2. 历史列表补「**环境数**」列（该批次结果行去重 `environment_serial` 计数，不加表、不加列）。
3. 指定单粘贴的「**一行多组**」拆组（偶数个字段拆成多组、奇数报错），补边界用例。
4. 打磨项（优先级更低）：状态条窄屏实测；历史弹层接 `Escape`；全量历史导出（要另立项，口径与批次导出一致）。
5. **多类型框架（催促发货／取消订单）仍暂缓**：界面上保持规划态，不要提前设计判定条件/幂等冷却/权限拆分。

## 五、工程规矩（必须遵守）

- **一改动一分支**（`codex/` 前缀）+ 未参与方评审后再合并；评审提示词照抄 `review-request-after-sale-export.md` / `review-request-after-sale-three-requirements.md` 的四段式（口径要点 / 重点审查项 / 已知取舍 / 怎么跑）。
- 改 Web **必跑** `python cloud/auth-service/deploy/sync_web_workspace.py`（权威源与云端副本必须逐字节一致，有测试守着）；改 `index.html` 后**重跑两份原型生成脚本**（`docs/prototypes/build_procurement_after_sale_prototype.py`、`build_procurement_after_sale_multitype_prototype.py`）——原型是生成时点的快照，忘了重跑就会「在浏览器里验的是旧代码」（上一会话踩过）。
- 结构断言要**钉列序**（只数单元格个数会让整列错位合法化）；**新增跨边界字段必须在 Web / 云端契约 / 执行器三处对齐并各配一条测试**——本项目失误的主形态就是「跨边界名字/字段对不上」，`WebCloudContractAlignmentTests` 已常驻机械比对。
- 测试跑法（`PYTHONPATH` 坑：借用的 venv 里包是 editable、指向主检出，不指 `PYTHONPATH` 会跑到别的源码）：
  ```
  cd <worktree>
  PYTHONPATH=$PWD/src ~/Documents/xynigo-sourcing/.venv/bin/python -m pytest tests/ -q
  cd cloud/auth-service
  PYTHONPATH=$PWD/src ~/Documents/xynigo-sourcing/cloud/auth-service/.venv/bin/python -m pytest tests/ -q
  ```
- **部署测试机等 Jeff 点头**：预检 `df -h /` → 在途任务排空（**连续两次为 0**；共享机上有同事的真机任务，别去杀）→ 回退锚点 + `pg_dump -Fc` → rsync → **`build migrate` + `run --rm migrate`**（不 build 会报 `Can't locate revision`）→ `build auth` + `up -d auth` → 核验 healthz/readyz/页面/RestartCount/日志 error/新增 uncertain=0。

## 六、交付期望

每个改动：分支 + 提交（说明改了什么 / 怎么验 / 已知限制 / 后续事项）+ 两套测试全绿 +（涉及界面时）浏览器实测证据 + 评审提示词文本。不要只改交付包或原型就宣称正式源码已更新。

**开工第一步**：读完 §10、§11 与 `handoff.md` 后，先给我一份简短计划——「本次要做的 3 项 + 每项验收标准 + 哪些需要 Jeff 授权」，确认后再动手。
