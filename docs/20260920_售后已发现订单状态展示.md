# 按环境提交的已发现订单状态

基线 `0b8ab2b`。用户授权本次前端热部署，精简 CI 与云端测试流程。

订单卡片返回时执行器先发布 queued，读取详情后才发布 running；图片出现不代表已进入提交。按环境模式中，已有订单号但仍为 queued 的行改显示“已发现，待核对”，说明“订单已读取，详情及售后资格待核对”。不根据图片或环境 running 推断某笔订单已经提交，不增加虚假的阶段精度。不添加动态动画，以免把同环境尚未轮到的订单误报为正在执行。

原始状态、等待分类、统计、导出和重试规则不变。指定订单队列仍显示等待；运行、核验、待核对及终态保持原有展示。持续使用同一订单行。

本地真实渲染 fixture 已通过：新增发现提示、等待分类保留、未变为处理中、指定订单保持原语义，沿用单行生命周期及数据不变性断言。生成输出同步，公开安全审计及 diff 检查通过。

本次仅运行受影响前端 fixture 与 Web 接线检查；云端使用现有 Node 22 隔离容器，不安装 Python 依赖、不跑全量套件或桌面构建。发布提交使用 `[skip ci]`，不修改长期 CI 配置。独立评审与云端实际结果、部署验证另行回填。未发起真实平台写入。

## 发布验证结果

候选 `edf5773aaf6a8eb1075d922b33c52377ace8f4b9` 已合并 main、推送并于 2026-09-20 完成 Web 热部署。独立评审结论可合并，无必须改或建议改；评审者独立运行环境行 fixture 与 Web 接线专项，结果 PASS / 90 passed。

本地 Web 接线专项最初因状态 HTML 抽出变量而有 1 项列序标记断言失败；将断言标记同步为模板的 `${statusHtml}` 后，90 项全部通过（0.29 秒），仍校验完整列序。

云端 Node v22.23.2 在无网络、只读挂载的隔离容器实际运行以下三份 fixture，全部通过、exit 0：

- `after_sale_environment_rows_ui.cjs`：订单/环境行及新增已发现状态展示。
- `after_sale_task_lifecycle_ui.cjs`：17 场景生命周期回归。
- `after_sale_feedback_ui.cjs`：状态、字段、部分结果及停止等回归。

已完成镜像/代码/数据库备份、归档目录检查，复用当日成功的隔离恢复证据。维护闸下重复核验无在途任务后仅切换 auth 页面镜像；数据库未重启、迁移不变，待核对记录指纹不变，维护闸已解除。健康、就绪与内联脚本 CSP 通过。线上页面 SHA-256 与候选一致：`b3f8386a918e09bb195ef8ca12d03416f49e7ff40dfefacacf1ae0294943fc08`。

浏览器只读核验：现有已结束批次订单明细正常展示。线上无在途批次，本轮新提示通过云端合成场景验证，未重新发起真实提交。未运行全量 CI、Python 云端套件或桌面构建，未更改后端与执行器，客户端无需重装。
