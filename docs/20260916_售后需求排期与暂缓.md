# 20260916 售后需求排期与暂缓

状态：Jeff 20260916 口头拍板（本文件是排期口径，具体规格见两份需求文档）
关联：`docs/20260916_需求_售后直接提交指定单.md`、`docs/20260916_需求_售后提交历史与从历史跟进.md`

## 1. 先做：补提失败

从需求二（提交历史）里**拆出来独立交付**，不依赖提交历史：

- 入口在 ③ 提交结果（针对**当前这批**，不需要先做历史）。
- 范围＝**可恢复失败**：`fail` / `login` / `inuse` / `stopped`；
  **排除 `blocked`**——平台判定「已无可退包裹」，重提只会白跑一遍写操作，界面上这类行不给入口。
- 动作＝把这些行重建成**新一批**提交（同一套建 Run + 执行器提交链，提交前执行器的 `pre_info` 体检保证幂等）。
- 写操作，需二次确认。

交付定义：③ 上能对当前批次的失败行一键重提，重提结果按新批次独立展示。

## 2. 已交付（20260916）

三条一次做完，分支 `codex/after-sale-three-requirements`（待评审合并）：

1. 补提失败（§1）——③ 当前批次一键重提，范围＝可恢复失败，排除 `blocked`。
2. 解除售后页面前置锁 + 直接提交指定单（需求一）——③ 粘贴 `环境序号 订单号` 即可提交。
3. 提交历史 + 从历史跟进退款（需求二）——批次详情里的「重提失败单」
   **复用第 1 节的同一个动作**，没有第二条提交路径。

## 3. 暂缓：多类型框架

**（催促发货 / 取消订单）短期不做——没有业务场景。** 界面上这两类保持规划态
（点选给横幅、禁用扫描），不投入判定条件/幂等冷却/权限拆分/执行器动作的设计与开发。
重新评估的前提＝出现真实业务场景。

## 4. 与模块交接文档既有欠账的对应关系

- 交接文档里的「补提失败重跑」＝本排期第 1 节（现在正式排上）。
- 交接文档里的 `failed_retry 合并视图`（把重提批次与源批次合成一个视图）**暂不做**：
  先按单批次看，等批次链变长再评估。
