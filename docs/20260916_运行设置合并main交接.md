# 运行设置合并 main

范围：按用户指令将 `codex/unified-runtime-settings`（`af14737`）合入本地 `main`（合并前 `d6cf18a`）。包含该分支的提示卡间距、提交结果导出、售后无头与并发设置，以及全模块 2／3／5 档位统一。保留 main 的所有订单扫描、送达详情补齐、任务级回访进度、稳定行序与扫描卡片手动折叠行为。

## 冲突裁决

`after_sale_claim.py` 中旧版严格全局串行与新版跨环境并发冲突。用户明确选择：提交／回访也使用 2／3／5 并发；列表顺序固定，完成顺序允许不同。

最终按环境分组并发，同一环境内逐单处理；列表和导出保留请求行序。旧执行顺序测试改为验证环境内行序与展示行序，并新增强制 B 先于 A 完成的测试，确认两类任务的展示仍为 A/B。前端参数夹具补充合并后依赖，并对意外“发起失败”直接报错，避免只检查请求发送而掩盖后续渲染错误。

## 验证与边界

本机专项主回归 444 项通过；新增乱序完成测试及受影响专项 39 项通过（共 445 项不同测试）。覆盖售后扫描、任务级进度、运行参数、配置、代理、结算、任务桥接、桌面、环境创建与物流查询。当前提交结果及历史导出 JS 夹具通过。

共享 Web 与两份售后原型由权威源重新生成。未 fetch、推送、部署、重启服务或操作真实订单。本次完成合并自查与回归，未新增独立评审或线上业务验收。

云端售后、任务结果、导出、运行参数、配置闭集和执行器通道共 80 项通过。离线 Chrome 在 1440px／480px 验证统一开关和并发档位、扫描完成等待 2.4 秒仍保持展开。公共源码审计、生成物一致性、JavaScript 语法及 diff 空白检查通过。仅出现既有 Python 转义与 Starlette/httpx 弃用提示。
