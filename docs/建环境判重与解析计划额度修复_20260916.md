# 建环境判重与解析计划额度修复（20260916）

## 背景

20260916 测试机（xynigo.samforo.icu）运营建环境时连续被拒：截图错误是「短时解析计划过多」（HTTP 429，`environment_plan_limit`），但它只是次生故障。

实测时间线（UTC）：

| 时间 | 动作 | 结果 |
|---|---|---|
| 06:31–06:49 | 连传 5 个号商交付文件（30/20/60/59/30 行） | 每次提交都 409 `environment_account_already_bound` |
| 06:51–07:01 | 又点 5 次 | 全部 429，组织被卡约 30 分钟 |

用服务端密钥解密当时的计划并逐行比对环境清单后确认：这 5 个文件里分别只有 8、5、1、1、8 行是已建环境的账号，**绝大多数是全新行**。真正的问题是两条叠加：

1. `reserve_environment_names` 的判重**整批拒绝**，且错误里不说是哪几行，运营只能改文件重传；
2. 每次重传生成一个新的短时解析计划（30 分钟 TTL），额度按**租户**统计、上限 5，且**没有释放入口**，于是重传把全组织拖停。

当天该租户 21 次上传、8 个批次、17 次判重拦截；历史累计判重拦截 72 次，配额耗尽仅 2 次——说明运营痛点在判重，配额是放大器。

## 交付范围

分支 `codex/env-create-ops-hardening`（基于 20260916 的 `origin/main` b7799e1），提交：

- `1e355dc` fix(cloud)：建环境判重给出可操作明细，解析计划额度可释放
- `39cb17e` feat(web)：建环境冲突行清单、跳过开关与放弃解析按钮

服务端（`cloud/auth-service/src/xynigo_auth/`）：

- `purchase_service.py`：`PurchaseServiceError` 增加可选 `diagnostics`；`main.purchase_error` 把它放进 HTTP `detail`。
- `operation_service.py`：新增 `environment_plan_conflicts()`（按提交路径的两条判重分支逐行产出脱敏明细）、`conflict_diagnostics()`、`collapse_assignments()`；`reserve_environment_names` 的两种拒绝都带上明细；批次标签解析改为一次查询。
- `operation_contract.py`：`EnvironmentCreateRunCreateBody` 新增 `skipConflictingRows`（默认 false）。
- `main.py`：新增 `POST /v1/environment-plans/{cloud_plan_id}/release`；创建 Run 时按 `skipConflictingRows` 裁剪计划行与采购员分配、收敛 `totalCount`/`progressTotal`/抽检数并把跳过行写入 Run 摘要；计划错误统一走 `plan_error_detail`；429 带 `Retry-After`。
- `environment_plan_service.py`：`release()`（幂等）、`_supersede_live_plans()`（同一用户+站点+分组的新上传自动结束更早计划，`releasedBy: superseded`）、额度耗尽的 429 带可重试分钟数与诊断；已释放/已取代的计划给出 `environment_plan_released` / `environment_plan_superseded` 稳定错误码。

前端（`src/purchase_tool/web/index.html`，并经 `sync_web_workspace.py` 同步到云端副本）：

- 判重被拒时展示冲突行清单（行号、邮箱掩码、单号掩码、已建环境名、原批次短码）+「跳过这些行，只创建其余账号」勾选；
- 提交成功提示本次跳过行数；新增「放弃本次解析」按钮；补齐新错误码文案；上传或站点/分组变化时清空冲突明细。

文档：`docs/阶段2云端运行链路_20260901.md` 更新接口清单、额度语义与现场问题覆盖。

## 验证

环境：独立 worktree `/Users/jeff/Documents/xynigo-env-create-ops-hardening`（不动他人使用中的检出）。

- 云端包（`.venv` Python 3.12，`cloud/auth-service`）：`python -m pytest tests -q --tb=short` → 全绿（详见提交前后两次运行记录）。
- 新增用例：`tests/test_environment_conflict_skip.py`（明细字段、跳过裁剪、全冲突、HubStudio 观察命中）、`tests/test_environment_plan_lifecycle.py`（释放幂等、额度释放后可再解析、429 诊断与 `Retry-After`、取代后不可提交、审计行保留）。
- 仓根包（`.venv` Python 3.11）：`python -m pytest tests -q --tb=short` → **1056 passed, 5 skipped**，含本地/云端副本逐字节一致与按钮文案断言。
- 生成物一致性：`sync_web_workspace.py` 重跑后 `git diff --exit-code -- cloud/auth-service/src/xynigo_auth/web/` 为 0，无未跟踪生成输出。

## 限制与未覆盖

- **未部署**：测试机仍是修复前版本，需按部署手册执行（本单无数据库迁移）。
- 跳过开关只在**云端工作台**生效；桌面端本地链路不经过该接口。
- 配额语义仍是**租户级**（上限 5、TTL 30 分钟）。取代+释放使正常流程不再累计，但若真有 5 个不同站点/分组的计划同时存活，仍会触发 429——此时页面会给出可重试分钟数。
- 「跳过已绑定行」按行号裁剪采购员分配；同一采购员在文件中不连续时，分配会被拆成多段（服务端 `collapse_assignments` 重新合并）。
- 未做真机验证（需先部署）。

## 评审与后续修改（20260916）

Cursor 作为未参与实现方评审候选 `4ea4b4f`：**结论通过，无必须改**（评审在独立 worktree `/tmp/xynigo-review` detached 检出，未使用实现者目录），并提出 8 条不阻断建议。已实施的 7 条：

1. 裁剪后回写 `request_summary.verifySampleCount`（原值按未裁剪批次冻结，会让快照显示偏大的抽检数）。
2. 额度计数先扣除本次上传将要取代的同站点分组计划：配额已满时，同组重传不再被自己占用的槽位拒绝；被取代的计划仍只在解析成功后释放，坏文件不会提前丢掉旧计划。
3. `release()` 拒绝已 `submitted` 的计划（返回 `environment_plan_consumed`），避免双标签页把已产生 Run 的计划改写成“用户放弃”而污染审计。
4. 正式执行确认框按裁剪后的剩余行数报数，并注明排除了几个已有环境的账号。
5. 冲突行超过 50 行时在清单末尾提示已截断、跳过按整体清单生效。
6. 根测试新增 `EnvironmentConflictSkipWiringTests`，钉住冲突面板/跳过开关/放弃按钮/三个新错误码/确认框文案。
7. `_validation_log_target` 补 `/release` 路径（仅影响畸形请求的校验日志）。

未实施（评审列为可选）：切回旧标签页时用 `GET /v1/environment-plans/latest` 提前发现计划已被取代，留作后续体验优化。

补充验证（在上述修改之后）：云端套件 3.12 全绿、仓根套件 **1060 passed / 5 skipped**、web 生成物一致性通过。

## 部署与回退

**合并提示（评审补充）**：本单基线是 `b7799e1`，`origin/main` 其后已有售后等 16 个提交（评审时 `b482e1f`）——**不要部署 `4ea4b4f` 本身**。先把当前 `origin/main` 合入本分支（`main.py` 与两份 `index.html` 会两边都改但无冲突标记），合并 SHA 上重跑 `sync_web_workspace.py` 确认本地/云端副本仍逐字节一致，并重跑云端与仓根两套 pytest，CI 也以合并后 SHA 为准。合并树含 0039 迁移，新库/生产首装必须跑到 0039。

按 `docs/部署与发布手册.md`：预检磁盘 → 等 `executor_tasks` 排空（两次采样）→ 旧镜像打 tag → `pg_dump` → rsync（`src migrations alembic.ini requirements.lock pyproject.toml Dockerfile compose.yaml`）→ `build migrate` + `run --rm migrate` → `build auth && up -d auth` → 核验 `/healthz`、`/readyz`、公网版本号、RestartCount、日志 error 数。本单自身不带迁移，但**合并后的候选含 0039**，因此 migrate 仍要跑（对已有手工 DDL 的库是幂等的 SET DEFAULT）；回退即 `docker tag` 回旧镜像并 `up -d`。

## 评审要点

1. `skipConflictingRows` 的语义：默认关闭、只在运营显式勾选后生效；裁剪是否遗漏别的按行对齐量（当前处理 `totalCount`/`progressTotal`/`verifySampleCount`/`assignments`/计划行）。
2. 取代语义：同一用户+站点+分组的旧计划被立即结束；两个标签页同时准备不同文件时，旧标签页会收到 `environment_plan_superseded`，而不是静默失效。
3. 错误响应新增 `detail.diagnostics`，与既有 `error.diagnostics` 前端契约一致；未改任何既有错误码的取值。

## 顺带发现（不属于本单，未改）

1. **生成物核对方法有版本歧义**：`CONTRIBUTING.md` 要求用 Python 3.12 跑生成器后 `git diff --exit-code`，但基线里的 `environment_plan_core.py` 是 Python 3.9 生成的——3.12 的 `ast.unparse` 会去掉 `for (a, b) in ...` 的冗余括号，因此该检查在未改动任何源码时也会报差异。本次已还原该文件，未把无关差异并入本单；需要确认生成器基准解释器（改文档或统一重生成一次）。
2. **`codex/after-sale-tracking-defaults`（0039）已核验可合并**：相对 `origin/main` 仅 1 个提交、可快进、`git merge-tree` 无冲突；把它的迁移与测试临时取入当前 main 后 `test_migration_timestamp_defaults.py` + `test_after_sale_claim.py` 全部通过。测试机数据库的 `after_sale_refund_tracking.created_at/updated_at` 已有 `now()` 默认值（手工 DDL 生效，④回访不再 500），但**新库/生产首装仍会复现 0038 漏默认值的问题**，合并前这条修复不完整。
3. 平台侧：本地/云端副本、错误码文案、`detail.diagnostics` 通道均未新增对外字段名以外的契约变化。
