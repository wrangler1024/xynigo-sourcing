# v0.18.3 发布记录

用户授权推送 main 并部署新版本，云端与双平台安装包统一升级。业务基线为已独立评审的 `09691ba`；本次不新增退款业务写入或历史数据库回填。

采用两阶段发布：先固定版本与发布说明并构建安装包，再回填云端发行目录的真实 SHA-256、大小与 runtime ID。第一阶段仍保留旧发行目录，版本一致性守卫会拒绝将中间态作为云端服务启动；资产回填后才推送最终发布提交和部署。

渠道保持内部 test，签名/公证状态保持真实。共享环境先备份与停收排空，再迁移 `0040 → 0041`、切换应用服务、验证健康及安装资产；数据库服务不重启。旧代码、资产和备份保留供回退。具体部署定位和业务证据只保存在私有记录中。

## 已核验安装资产

构建来源：`0b66449181a79ac11d58bce69107dfa174791994`；两平台 runtime ID：`0.18.3-0b66449181a7`。

- windows-x86_64：15380141 字节，SHA-256 `5f45b09d79c6723288ae52f1b204efae85400b52b08b85374c8ad1cfde63a95c`。
- macos-arm64：14402136 字节，SHA-256 `9cc7f6d91da9f04354ad1429db29dc000b3af78a3e157ce6d0782da7bdc0fd0b`。

两平台仍为内部未正式签名测试包；macOS 未公证，未将其标记为正式可信发行。最终分发以本次核验的安装包为准，不用 CI 另一次重构建的不同 runtime ID 替换。
