# 交接 · 让 Codex「只熟悉模块代码、不动手开发」

> 用途：Jeff 20260916 深夜指令——打磨批次暂停，先让 Codex **熟悉采购售后模块的代码**，不写代码。
> 下面第一节起**整段可复制**发给 Codex。文档本身是记录，不要在熟悉阶段修改它。

## 会话框精简版（推荐发这一份，完整要求就在本文件里）

```text
# 任务：只读熟悉 Xynigo「采购售后（丢件退款）」模块代码（不动手开发）

熟悉素材＝本机检出 + 线上部署那一版，不用 git fetch、不用新建 worktree：
- 本机模块代码：~/Documents/xynigo-worktrees/after-sale-polish（打磨 worktree，基线 origin/main，
  模块代码最全）；另有 ~/Documents/xynigo-sourcing（主检出，⚠️ 停在 codex/store-finance-inspect-v1
  分支，个别行与线上不同）
- 线上：测试机 xynigo.samorfo.icu 上部署的那一版（云端服务 + 工作台页面；SSH 进去只读看源码即可，
  不方便就以本机检出为准）

完整要求已存盘（就是 origin/main 上的那份文档），先读它再动手，本机路径：
~/Documents/xynigo-worktrees/xynigo-main/docs/20260916_交接_售后模块熟悉任务_Codex提示词.md
（注意：这份交接/提示词文档只在本机这个 xynigo-main 检出里，after-sale-polish 那条分支上没有。）

一句话：这是熟悉任务，不是开发任务。全程零文件改动——不改文件、不建/切分支、不 commit、不 push、
不发 Release、不部署、不重启线上、不写数据库、不跑任何写操作；读文件、grep、git log、跑只读 pytest、
只读查看线上部署都可以。发现该改的地方只记录，不要顺手修。

按那份文档「四、要熟悉的代码」的顺序读：执行器（after_sale_claim.py / main.py / operation_executor.py）
→ 云端（operation_contract.py / models.py / operation_service.py / executor_service.py / main.py /
after_sale_export.py）→ Web（src/purchase_tool/web/index.html 的 as* 与 #asRunStrip / #asHistory*）
→ 迁移 0036~0039 → 两套测试（跑法见文档）。

产出写进你的回复（不要写成文件），固定五段：① 模块地图（每条带 file:line）② 四条链数据流（并指出
哪几跳会丢字段或改名字）③ 跨边界字段清单（Web 发什么 / 云端契约要什么 / 执行器投影带什么）④ 关键
口径与不变量 ⑤ 可疑点（只列不改，不确定的标「待确认」，不要猜也不要顺手修）。
```

---

# 任务：熟悉 Xynigo「采购售后（丢件退款）」模块的代码（**只读，不动手开发**）

## 一、本次任务的性质（先看这条，它约束后面全部动作）

**这是熟悉任务，不是开发任务。** 你的产出是一份**代码地图报告**（写在你的回复里），不是代码改动。

- **禁止**：修改/新增/删除任何文件（包括文档）、创建或切换分支、`git commit` / `git push`、
  打 tag、发 Release、部署、重启线上服务、写数据库、在线上跑任何写操作。
- **允许**：读文件、`grep` / `rg`、`git log` / `git show` / `git diff`、`ls`、跑只读的测试命令
  （`pytest`，它不写仓库文件）、在 `docs/` 里**读**资料，以及**只读查看线上部署那一版**。
- 如果你在阅读过程中发现「这里该改」——**只记录到报告第 5 节，不要动手**。Jeff 会在你熟悉完之后
  再决定做什么、由谁做。

## 二、熟悉素材：本机检出 + 线上部署那一版

**不用 `git fetch`，也不用新建 worktree。** 直接读本机已有检出，需要时对照线上部署的那一版。

- **文档**（本提示词、模块交接、`handoff.md`）都在 `~/Documents/xynigo-worktrees/xynigo-main` ——
  这是 `origin/main` 的本机检出（detached）。下面的 `docs/...` 路径都相对它；
  `after-sale-polish` 那条分支**没有**这几份新文档，别在那儿找。
- `~/Documents/xynigo-worktrees/after-sale-polish` —— 打磨用的 worktree（基线 `origin/main` `e33d196`），
  **模块代码最全，读代码优先读这里**。
- `~/Documents/xynigo-sourcing` —— 主检出。⚠️ 它当前停在 `codex/store-finance-inspect-v1` 分支（不是
  main），模块文件都在，但个别行与线上可能有差；以 `after-sale-polish` 为准。
- 线上部署的那一版 —— 测试机 `xynigo.samorfo.icu`：云端服务与工作台页面都在那儿（部署目录的历史记载
  是 `/opt/xynigo-auth-test`，见 `release/v0.11.0.zh-CN.md`）。**只读看**，不要改文件、不要重启容器、
  不要跑迁移；那台是**多项目共享**测试机，上面有同事的真机任务，不要动。
- 现状提醒：有一条**在飞分支** `codex/after-sale-history-env-count`（@ `5dce475`，给历史列表加「环境数」
  列，未合并、未部署）——它就是 `after-sale-polish` 当前检出的分支；**线上没有这一项**，报告里如提到
  这一列，请注明「分支上、未上线」。


## 三、先读文档（按顺序，读完再读代码）

1. `docs/20260915_采购售后模块交接.md` —— 模块总交接。重点：**§5 实现决策**（哪些是刻意取舍）、
   **§10 线上事实**、**§11 待办与工程规矩**、**§12 本次暂停点 + 执行器 0.18.1 发版流程调研**。
2. `handoff.md`（仓库根）—— 一页索引：一句话现状、打磨批次暂停点、三条最容易踩的坑。
3. 需求原文（三份都已标「已实现」，口径都在里面）：
   `docs/20260916_售后需求排期与暂缓.md`、`docs/20260916_需求_售后提交历史与从历史跟进.md`、
   `docs/20260916_需求_售后直接提交指定单.md`。
4. 原型定版：`docs/prototypes/20260915-procurement-after-sale-v2-multitype.md`（界面形态与状态条）。

## 四、要熟悉的代码（三层 + 测试 + 迁移）

按「一层一层读」的顺序，不要跳着读：

| # | 关注点 | 文件 |
|---|---|---|
| 1 | 执行器业务链（扫描 / 提交 / 回访） | `src/purchase_tool/after_sale_claim.py`（`AfterSaleClaimer`：`scan` / `claim` / `track` 三条链、环境占用策略、异常截图、`pre_info` 体检） |
| 2 | 执行器本地 API 五个路由 + 权限表 | `src/purchase_tool/main.py`（`/api/after-sale/*`） |
| 3 | 桌面 → 云端的任务桥接（三个任务类型、行投影闭集） | `src/purchase_tool/operation_executor.py`（`after.sale.scan.v1` / `after.sale.claim.v1` / `after.sale.track.v1`，注意 `_execute_after_sale_claim` 里重建条目的那段——**跨边界字段就是在这里最容易丢**） |
| 4 | 云端契约（三套行闭集 + 建单体 / 回访体，`extra=forbid`） | `cloud/auth-service/src/xynigo_auth/operation_contract.py` |
| 5 | 云端数据模型 | `cloud/auth-service/src/xynigo_auth/models.py`（`AfterSaleClaimRun` / `AfterSaleClaimResult`） |
| 6 | 云端服务（建 Run / 快照 / 历史 / 结果回写 / 进度 upsert / 云端兜底） | `cloud/auth-service/src/xynigo_auth/operation_service.py`、`executor_service.py` |
| 7 | 云端路由 | `cloud/auth-service/src/xynigo_auth/main.py`（`/v1/after-sale/*`、`/v1/operation-runs/after-sale-claim/*`，注意 `/history` 注册必须在 `/{run_id}` 之前） |
| 8 | 导出（④ 回访导出 + ③ 批次导出同一套口径） | `cloud/auth-service/src/xynigo_auth/after_sale_export.py` |
| 9 | Web 权威源（三张表 + 状态条 + 历史弹层；`as*` 前缀 JS） | `src/purchase_tool/web/index.html`（`asScanRows` / `asClaimRows` / `asTrackTable`、`#asRunStrip`、`#asHistory*`、`asSubmitItems` 唯一建 Run 入口） |
| 10 | 迁移 | `cloud/auth-service/migrations/versions/0036~0039_*`（含 `alembic_version.version_num` 只有 32 字符这个坑） |
| 11 | 测试（两套，跑法见下） | `tests/test_executor_workspace_web.py`（Web 接线与契约对齐）、`tests/test_after_sale_claim.py`（本地）、`cloud/auth-service/tests/test_after_sale_claim.py`、`test_after_sale_export.py` |

**测试怎么跑**（只读；注意 `PYTHONPATH` 这个坑：借用的 venv 里包是 editable、指向主检出，不指
`PYTHONPATH` 会跑到别的源码）：
```
cd ~/Documents/xynigo-worktrees/after-sale-polish
PYTHONPATH=$PWD/src ~/Documents/xynigo-sourcing/.venv/bin/python -m pytest tests/ -q
cd cloud/auth-service
PYTHONPATH=$PWD/src ~/Documents/xynigo-sourcing/cloud/auth-service/.venv/bin/python -m pytest tests/ -q
```
（只想看用例清单不跑的话，加 `--collect-only -q`。）

## 五、产出格式（固定五段，写进你的回复；**不要写成文件**）

1. **模块地图**：文件 → 职责 → 关键函数/路由（**每条都要带 `file:line`**）。
2. **四条链的数据流**：扫描 / 提交（③）/ 回访（④）/ 历史，每条写清
   「Web 入口函数 → 云端路由 → 云端服务函数 → 执行器任务类型 → 执行器实现 → 回传落库」，
   并指出该链上**哪几跳会丢字段或改名字**。
3. **跨边界字段清单**：把提交行、扫描行、跟踪行三套字段，按「Web 发什么 / 云端契约要什么 /
   执行器投影带什么」三列对齐列出，标注必填与可选；点名你认为最可能对不上的 2~3 处。
4. **关键口径与不变量**：幂等（同 key 同 payload 复用、不同 payload 409）、`blocked`/`skip`/`empty`
   计入「已跳过」而不是失败、退款路径固定「原路退回」与界面取值映射、历史可见范围（**租户内互相
   可见**，刻意偏离物流/建环境的「本人+管理员」）、导出列序与 ③ 表头一致、状态文案（处理中 vs
   已退款）。
5. **可疑点 / 风险 / 待确认**：只列不改。每条写「现象 + 依据（`file:line`）+ 你的判断 + 建议下一步」；
   不确定的一律标「**待确认**」，不要猜，也不要顺手修。

## 六、纪律（再强调一次）

- 全程**零文件改动**（本机与线上都一样）。发现要改的地方，写进报告第 5 节。
- 报告里的每个结论都要有 `file:line` 依据；没有依据的推断要显式标注。
- 线上那台是**多项目共享**测试机：只读看，不要改文件、不要重启容器、不要跑迁移、不要动数据库，
  也不要碰同事正在跑的真机任务；本任务不需要任何真实凭证。
- 你有疑问就列成「待确认」问题，不要自行决定实现方案。
