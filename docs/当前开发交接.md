# Xynigo Sourcing 当前开发交接

更新时间：2026-08-25

当前阶段：采购中心读取、认领、分单与采购执行队列测试版已部署到云端测试实例。云端迁移为 `0004_procurement_execution_test`，真实目录为超级管理员 29/29、管理员 26/29、成员 0/29；现有测试采购单已由超级管理员认领并生成 1 张未绑定资源的 `waiting_binding` 采购分单。普通成员尚未实际新增/批准/登录，Webshare 普通表格尚未授权，飞书同步 Worker、真实下单状态写入和正式采购绿色包尚未发布。

版本合并状态：GitHub 稳定版 `v0.10.0` 的安全并行、本地任务协调与 HubStudio 调用闸门已经与当前云端认证、RBAC、采购中心和插件桥合并；统一协同测试候选版本为 `v0.11.0`。该版本保持 `v0.10.0` 本地执行器能力，不替换正在运行的 8767/8771；完成回归、固定 Git 提交和测试包灰度前不得标记为稳定版。

2026-08-25 完整回归已通过本地 248 项、云端 37 项、插件 28 项、插件浏览器合成回归、macOS 包自检、Windows 包结构检查、Docker 镜像构建和采购中心真实只读浏览器联调。公共源码安全审计仍因内部架构/项目记忆文档包含基础设施 IP 而阻断公开发布；在完成文档迁移或脱敏前，只能作为共享测试环境灰度候选。完整记录见 [`里程碑/20260825-v0.11.0合并与完整回归.md`](里程碑/20260825-v0.11.0合并与完整回归.md)。

测试使用方向：将 `43.136.40.39` 上的 Xynigo 作为采购团队持续使用的共享测试环境，不在生产准备期间停用。采购同事可以继续使用已开放的云端功能和现有本地 Xynigo 服务，并配合认领、分单、权限和采购执行交互测试。2026-08-25 最新复核为云端 `/healthz`、`/readyz` 正常，本机 `127.0.0.1:8767` 与 `127.0.0.1:8771` 均在监听并返回 HTTP 200；未经明确授权不得停止、替换或重启这些本地服务。真实平台下单、审核、正式飞书写入和批量业务变更仍需单独授权。

架构边界（2026-08-25 生效）：广州多项目共享测试服务器 `shared-test-gz-01`（实例 ID `lhins-b6nu398t`）和 `/opt/xynigo-auth-test` 只保留 Xynigo 测试环境，严禁部署生产容器、生产数据库或生产密钥。历史测试 n8n 已按用户明确指令停止，容器/数据卷保留且自动重启关闭；其他既有服务不得受影响。生产只能独立部署到 `xynigo-prod-gz-01` 的 `/opt/xynigo-auth-prod`，Compose 项目名固定为 `xynigo-auth-prod`，域名为 `app.xynigo.com`，并从空 PostgreSQL 执行已批准迁移。测试与生产不得共用数据库、数据卷、账号、会话、Cookie、日志、备份或飞书业务表。完整决策与当前只读盘点见 [`架构决策-20260825-广州腾讯轻量服务器测试与生产双环境隔离.md`](架构决策-20260825-广州腾讯轻量服务器测试与生产双环境隔离.md)。

生产部署当前未放行：生产云防火墙已调整为管理员单一来源 SSH 和公网 `80/443`，独立密钥登录及主机运行态已核验；但 UFW 未启用，SSH 仍允许密码认证/root 登录，Docker/Nginx/生产目录尚不存在。`0003/0004` 尚未进入获批 Git 提交，DNSPod 仍只有 NS 记录，备案/TLS、生产密钥与 Base 路由、异机备份和监控均未验收。关闭全部闸门前不得执行生产部署。

2026-08-25 已部署增量：运营采购助手采用“插件 → Xynigo 本机安全桥 → 云端采购 API → PostgreSQL/Outbox”。云端已部署采购基础、Web 采购中心读取、认领、分单保存和采购执行队列接口；本机 8771 当前加载最新工作区并复用现有登录，8767 保持原样。首次联调使用的扩展本地版为 `0.1.51`；合并后的协同测试候选包统一为 `0.11.0`，已区分保存草稿与正式提交。首次云端联调已完成“采购列表 → 认领 → 保存待绑定资源分单 → 采购执行队列/销售订单号搜索”，没有触发店小秘审核、HubStudio 下单、SHEIN 写入或飞书同步。契约见 [`采购中心接口-P0.md`](采购中心接口-P0.md)，部署记录见 [`里程碑/20260825-采购执行测试版云端联调.md`](里程碑/20260825-采购执行测试版云端联调.md)。

## 新会话先做什么

1. 完整阅读本文件、[`项目记忆.md`](项目记忆.md) 和 [`后端实施交接.md`](后端实施交接.md)。
2. 执行 `git status --short`、`git log -5 --oneline` 和本机监听端口检查，不覆盖已有改动。
3. 不要未经用户确认停止当前正在使用的采购服务；本地服务会从配置端口开始自动寻找后续空闲端口。
4. `0004` 与采购执行测试接口已经部署；不要回退或重建现有测试分单。飞书同步 Worker、真实资源绑定、HubStudio/SHEIN 下单或任何 Base 写入仍需单独授权。

## 代码与版本基线

- 项目目录：`/Users/jeff/Documents/xynigo-sourcing`
- 开发分支：`codex/new-ui-migration`
- 本批开始时 HEAD：`83743a9 docs: hand off Feishu auth and permissions phase`
- 当前成员/角色/会话改动尚在工作区、未提交，但云端 `main.py` 已部署到隔离测试实例；继续时以实际 `git status` 和远端哈希为准。
- 本次交接前代码 HEAD：`7ee0b17 fix(ui): refine authenticated user menu`
- 登录主链路提交：`023f212 feat(auth): connect local UI to Feishu cloud login`
- 飞书应用切换提交：`4045dae feat(auth): complete Xiaoxi Feishu OAuth switch`
- 云端部署基线提交：`167e54c chore: deploy cloud auth test instance`

交接时文档仍在更新，实际继续开发前以 `git status` 和最新 HEAD 为准。

## 当前已经完成

### 云端认证服务

- `cloud/auth-service/` 使用 FastAPI、SQLAlchemy/Alembic 和 PostgreSQL，独立于本地 Python 3.9 采购应用。
- 腾讯轻量服务器隔离测试入口为 `https://xynigo.samforo.icu`；交接时 `/healthz` 和 `/readyz` 均通过 HTTPS 返回正常。
- 已切换为“小犀代采”飞书应用并完成真实 OAuth 回调，不再使用仅个人可用的 `samforo-cli` 应用。
- 精确回调地址已经在飞书应用后台登记：`https://xynigo.samforo.icu/v1/auth/feishu/callback`。
- 已实现租户白名单、新成员待审批、显式超级管理员、RBAC 权限摘要、短期可撤销会话、登录审计和本地一次性登录桥。
- 当前迁移版本包含 `0001_identity_foundation`、`0002_local_login_bridge` 和 `0003_purchase_request_foundation`。业务表共 13 张，加 `alembic_version` 共 14 张。
- PostgreSQL 不发布宿主机端口，认证服务只绑定服务器回环端口，由 Nginx 提供 HTTPS；已有 root-only 同机备份和恢复演练。
- 2026-08-24 已将 `/v1/admin/*` 管理员 API 部署到隔离测试实例；部署前保存应用、`.env` 和 PostgreSQL 备份，部署后迁移仍为 `0002_local_login_bridge`、public 仍为 11 张表。

### 本地应用接入

- 本地页面已经接入飞书扫码/授权登录、等待轮询、登录门禁、当前成员和退出。
- 云端会话由本地 Python 服务换取；macOS 保存到 Keychain，Windows 保存到 CurrentUser DPAPI。网页拿不到轮询令牌和最终会话令牌。
- 本地主要业务 API 已增加后端权限校验；所有浏览器 POST 请求增加同源校验。
- 本地 Python 服务已增加 `/api/admin/*` 安全代理；云端 Bearer 会话继续只由本地服务从系统安全存储读取，浏览器不会拿到会话令牌。
- 普通成员只能通过 `/api/lark/status` 读取脱敏就绪状态；飞书连接配置接口仍要求 `system.lark_connection.manage` 且角色为 `super_admin`。
- `system.lark_connection.manage`、`system.integration.manage` 和 `resource.ip.credential.manage` 已按超级管理员专属高敏权限部署。部署后临时误授管理员飞书连接权限，8771 配置接口仍因缺少 `super_admin` 角色返回 403；代理凭证误授权会被云端目录同步移除。
- 前端菜单按权限摘要屏蔽；账号入口已调整到顶部状态区最右侧，宽屏显示身份摘要，窄屏显示头像并通过下拉菜单展示姓名、角色和退出。
- 飞书头像加载成功时只显示图片，加载失败才回退姓名缩写，已修复文字与图片上下叠加问题。
- 飞书本地登录使用命名授权窗口；云端 `/v1/auth/local/complete` 在授权成功后自行尝试关闭，本地主页面领取会话后再次主动关闭该窗口。正常登录不会遗留完成页 Tab；若用户手动打开完成页、浏览器拒绝脚本关闭，只显示“可手动关闭”的兼容提示。完成页 CSP 只放行固定脚本的 SHA-256，不使用 `unsafe-inline`。
- “系统管理”已将“成员管理、角色管理、权限策略、数据范围”收拢到二级入口“组织与权限”；前三项使用真实云端 API，“数据范围”仅显示规划态且不请求接口。登录会话保留为独立二级入口。审批与角色分配保持分离，停用会提示并由云端撤销有效会话。
- 成员管理已增加“新增成员”：管理员输入飞书绑定手机号，后端用应用身份精确匹配“小犀代采”可见的飞书成员，确认后固定创建为 `pending`，可在具备 `system.role.manage` 时预分配角色，但仍必须另行批准。手机号只用于当次飞书查询，不写入 Xynigo 数据库、响应或审计；浏览器不会获得飞书 Open ID。
- “小犀代采”已配置手机号查找所需的应用身份权限、通讯录数据权限范围和应用可用范围；真实查找成功返回指定普通企业成员的姓名、头像与已激活状态。权限生效不需要重启 Xynigo，每次查找都会重新获取应用身份令牌。
- 权限策略页已按“一级菜单 → 二级功能 → 后端权限项”展示工作区当前 25 项权限，包含六个一级模块入口和 8 项 `resource.store.*`、`resource.ip.*` 资源权限；一级和二级复选框支持批量选择，共用权限码的入口会同步勾选。页面只保存角色与权限码关联，不修改成员状态、成员角色、会话或数据范围；配置、克隆、分配等未实现写能力不会因权限码存在而自动开放。
- 权限策略采用“左侧可搜索、可滚动角色列表 + 右侧单角色权限详情”的主从布局，不再按角色数量纵向重复整套权限表。一级模块默认以紧凑折叠行展示，只在需要时展开二级功能与权限码，并提供“展开全部/收起全部”；窄屏角色列表改为横向滚动。角色切换保留系统锁定和权限计数；当前角色有未保存调整时，切换或刷新会提示，恢复到服务端原值后未保存状态自动清除。
- 本地静态首页与图片响应统一增加 `Cache-Control: no-store`，避免源码开发实例更新后浏览器继续显示旧菜单；对已运行的旧进程可使用带版本查询参数的首页地址立即绕过缓存。

### 资源中心店铺与代理 IP（P0/P1 已部署）

- 资源中心顺序已合并为买家号库、号商入库、账号注册、环境创建、店铺管理、代理IP。
- 店铺管理只读汇总安全店铺字段、代理台账与 HubStudio 全量环境；只把台账明确的浏览器/窗口序号关系视为绑定，名称一致仅标记候选，不自动建立关联。
- 代理IP普通列表跳过代理账号/密码列；只有用户明确发起本机检测时才按所选资产临时读取凭证。检测默认并发 10、超时 8 秒、失败复测 1 次，同端点只检测一次；凭证不进入前端、缓存、历史、日志或导出。
- 真实只读验收读到 342 家店铺、2495 个 HubStudio 环境和 500 条 Webshare 资产。普通代理台账尚未授权给企业应用，所以占用关系统一标记“占用待授权”，不误算未使用；未执行代理检测或任何写入。
- 店铺配置/凭证更新/克隆、代理分配/凭证管理写接口均未开放，前端和 API 返回 `writesEnabled=false`。详见 [`里程碑/20260825-资源中心店铺与代理IP-P0P1.md`](里程碑/20260825-资源中心店铺与代理IP-P0P1.md)。

### 阶段 2 管理 API（内置管理员增量已部署）

- 成员：列表/筛选、详情、手机号精确解析与待审批成员新增、批准、停用、恢复；停用使用数据库行锁并同时撤销有效会话。新增接口会在后端重新解析手机号，防止前端伪造飞书成员身份。
- 角色与策略：工作区后端固定维护 `super_admin`、`admin` 与默认无权限的 `member` 三个系统角色。`admin` 固定获得 22 项日常业务与常规管理权限，明确排除 `system.lark_connection.manage`、`system.integration.manage` 与 `resource.ip.credential.manage`；`super_admin` 获得全部 25 项。`super_admin` 与 `admin` 的权限集不可编辑，系统角色不可改名/删除；自定义角色仍由后端生成代码。
- 会话：当前租户有效会话摘要、撤销单会话、按成员撤销全部会话；响应不包含令牌或令牌摘要。
- 管理 API 分别执行 `system.member.manage`、`system.role.manage` 后端校验；跨租户目标按不可见处理并记录拒绝审计。
- `super_admin` 与 `admin` 权限集不可通过页面改写；三项高敏权限不能授予任何非 `super_admin` 角色。非超级管理员的角色管理员不能授予自身不具备的权限，也不能借角色分配升级为超级管理员。
- 所有成功状态/授权/撤销变更以及权限拒绝、跨租户拒绝都写入 `audit_events`。

### 采购执行测试版本（云端已部署并完成首轮联调）

- `procurement.execution.manage` 已同步到云端，权限目录为 29 项；`super_admin=29/29`、`admin=26/29`、`member=0/29`。
- `0004_procurement_execution_test` 已迁移云端测试库，新增采购明细认领字段、采购分单与分单明细表；既有主单、明细和 Outbox 均完整保留。
- 新增认领、分单保存和采购执行队列接口；认领支持整单/批量/单条、行锁冲突与重复请求幂等，分单保存使用 `executionRevision` 乐观锁并校验当前采购员归属、数量完整和资源站点一致。
- 数据模型允许同一采购明细按数量分配到多张采购分单；当前前端测试交互先支持整条明细在分单间移动，下一轮再共同设计“同一行拆数量”的编辑方式。
- Web 采购任务页已从 DOM 模拟切换为测试写接口；采购执行页已删除静态分单种子，改为读取 PostgreSQL 测试接口。队列与详情均展示店铺销售订单号，并支持销售订单号关键词搜索。
- 测试资源只保存 Hub 环境安全标识/名称和脱敏买家号标签；不接受或保存密码、Cookie、接码 Key、飞书凭证。真实下单、平台订单号回填、跨采购单资源占用、异常回滚和飞书同步仍未开放。
- 首轮真实测试库联调将既有 1 条明细认领给当前超级管理员，生成 1 张 `waiting_binding` 分单并分配数量 1；Hub 环境、买家号和平台采购单号均为空。认领和分单保存各产生 1 条成功审计，采购执行页面按销售订单号搜索命中，控制台无 error/warning。
- 独立 8792 脱敏服务完成浏览器闭环：批量认领 2 单 5 条、将首单 3 条明细拆为 3 张分单并绑定三组测试资源、采购执行页显示 3 张待下单分单，销售订单号 `GSH202608250001` 搜索命中 3 张；控制台无 error/warning。未访问真实 HubStudio、飞书或 SHEIN。

### 已验证结果

- 本地测试：`.venv/bin/python -m unittest discover -s tests`，共 233 项完成（230 项通过、3 项按既有条件跳过）。
- 云端认证测试：`cd cloud/auth-service && uv run pytest -q`，37 项通过；存在一条 Starlette `TestClient`/`httpx` 弃用预警，不影响当前结果。
- 内置管理员验收覆盖：系统角色锁定、22/25 默认权限、三项高敏权限缺失、权限策略拒绝和本地配置接口角色二次校验。部署后数据库与浏览器均确认三个系统角色；临时误授权 403/自动清除验证后已删除全部测试数据。
- 店铺与代理资源中心通过服务层、API 权限、脱敏/导出、凭证按需读取、重试/去重及浏览器页面合成验收；真实数据源检查为店铺/Hub/代理资产可读、普通代理台账未授权，整个过程保持只读。
- 管理前端已通过临时合成服务与真实浏览器检查；“组织与权限”的已实现子页、独立登录会话页、权限导航、动态表格和工作区页签均正常。数据范围保持禁用规划态，该检查没有访问或修改真实成员、角色、会话及采购数据。
- 部署后 `/healthz`、`/readyz` 返回 200；OpenAPI 保留旧登录与管理员路由并新增 3 条采购接口。现有超级管理员 Bearer 会话在容器重建后继续有效，迁移为 `0003_purchase_request_foundation`、public schema 为 14 张表。
- 新增 `127.0.0.1:8769` 开发实例加载完整本地代理，真实读取到 1 名 active 成员、2 个系统角色、11 项权限和 1 个有效会话；浏览器已验证成员管理、角色管理、权限策略和登录会话均不再出现 `not found`，未执行状态或授权写入。
- 新增 `127.0.0.1:8770` 角色配置开发实例；浏览器已验证“新建角色”编辑器、系统角色锁定、成员数量和角色表格均读取真实云端数据，控制台无 error。验收只打开并取消新建表单，没有写入真实角色。
- 同一 8770 实例已完成分级权限策略只读验收：共享权限同步、一级菜单半选状态和唯一权限计数正常；交互后恢复原值且未点击保存，没有修改真实角色权限。
- 8770 已进一步完成权限主从布局验收：角色搜索得到正确的过滤数量，任意时刻右侧只渲染一个角色；自定义/系统角色切换、锁定状态和未保存状态均正常，控制台无 error。
- `127.0.0.1:8771` 曾用于手机号新增成员联调：先验证权限缺失错误与表单清理，权限和范围发布生效后又成功读取指定普通企业成员的姓名、头像和已激活状态。只读查找后点击取消，没有确认新增，控制台无 error。
- 云端运行目录已扩展到 25 项，新增 8 项店铺/代理资源权限；`super_admin` 实际为 25 项、`admin` 为 22 项、`member` 为 0 项。
- 真实超级管理员已完成本地页面登录和退出闭环；权限摘要和账号区通过浏览器检查，控制台无 error。
- 2026-08-25 已用临时本地测试页模拟 8771 的脚本打开方式：云端完成页在约 1 秒内自动关闭，只保留主页面，控制台无 error；临时页面、标签和 8788 服务随后全部清理。真实登录会话未退出或重做。
- 最新核对时 `127.0.0.1:8767` 旧进程与 `127.0.0.1:8771` 当前鉴权整合版均在监听；8767 缺少鉴权路由但保持原样，8771 已加载当前工作区并保持超级管理员登录。新会话必须重新检查，不能凭文档直接停止任一进程。
- 2026-08-25 重新部署前备份位于服务器 `/opt/xynigo-auth-test/backups/deploy/pre-resource-admin-20260825T023928Z/` 和 `/opt/xynigo-auth-test/backups/postgres/pre-resource-admin-20260825T023928Z.dump`，均为 root-only 0600 且 SHA-256/pg_restore 清单通过。8771 独立配置备份为 `.codex_tmp/invite-ui/config.pre-resource-admin-20260825T023928Z.json`。
- 运营采购部署前备份为 `/opt/xynigo-auth-test/backups/deploy/pre-procurement-foundation-20260825T040549Z/` 和 `/opt/xynigo-auth-test/backups/postgres/pre-procurement-foundation-20260825T040549Z.dump`；应用、`.env`、清单和 dump 均为 `root:root 0600`，SHA-256 与 `pg_restore` 清单校验通过。
- 采购基础部署后首次权限目录同步暴露旧库升级缺陷：同一请求内两次同步会重复排队角色权限。现已在目录同步末尾显式 `flush` 并新增升级回归；当时目录为超级管理员 28/28、管理员 25/28、成员 0/28，0004 部署后已同步为 29/29、26/29、0/29。
- 首次手工提交已形成 1 张 `submitted` 主单、1 条 `unclaimed` 明细和 1 条 `order.submitted/pending` Outbox；用户页面确认未触发店小秘原生审核。日志记录 4 次成功提交调用但数据库仅 1 份，幂等有效；若确认只点击一次，需继续排查前端重复请求。
- 部署时 macOS tar 扩展属性生成 `._*` AppleDouble 文件，首次迁移因此报 null bytes；已删除本批生成文件并在 `.dockerignore` 增加双层 `._*` 排除，重建后迁移容器退出 0。该问题未修改数据库迁移版本，仍为 `0002_local_login_bridge`。
- 8771 的统一买家号台账目标已通过超级管理员配置接口恢复；买家号库返回 320 条统计，浏览器复验控制台无 error。

## 成员准入规则

“能在飞书看到授权页”和“能进入 Xynigo”是两个阶段：

1. 成员必须位于“小犀代采”应用的飞书可用范围内，才能正常发起授权。
2. OAuth 返回的 `tenant_key` 必须在 Xynigo 云端租户白名单中。
3. 未知成员首次授权只创建 `users.status=pending`，不会签发系统会话。
4. 只有 `active` 成员才能登录；`disabled` 成员必须被拒绝。
5. 新成员自动激活默认关闭；只有环境变量中明确指定的超级管理员可在首次登录时激活并初始化超级管理员权限。
6. 成员激活后仍必须分配角色和权限，模块显示与业务 API 均按权限裁决。

云端测试实例现已具备成员审批、角色分配和会话管理 API；在普通成员完成管理员批准与角色授权前，其他企业成员首次完成飞书授权后仍会按设计停留在待审批状态。

## 下一步开发任务

按以下顺序推进，不要同时展开云端任务调度或采购数据迁移：

1. 与业务方共同确认同一明细拆数量、跨采购单资源占用、开始下单、失败回滚、退回和异常规则，再开放后续状态写接口。
2. 将分单资源选择从当前脱敏模拟列表对接到安全的 Hub 环境/买家号元数据接口；买家号密码、Cookie 和接码 Key 仍不得进入浏览器或 PostgreSQL。
3. 为采购员创建最小权限角色，完成普通成员的认领冲突、越权 403、撤权后即时失效和跨租户隔离真实联调。
4. 实现飞书同步 Worker 前先完成 Base 字段/选项预检和全新脱敏订单幂等回读；任何 Base 写入仍需单独授权。
5. 开通 `sheets:spreadsheet:readonly` 并授予 Webshare IP 总表访问；完成普通成员新增、批准、登录、停用和会话撤销闭环。
6. 独立开发并授权飞书同步 Worker；在新脱敏 `orderKey` 上验证幂等写入与回读，不能直接拿现有正式提交记录试写。
7. 提交当前工作区、使用新版本号构建 Windows/macOS 绿色包，优先由新刚和志恒小范围灰度后再考虑扩大范围。

## 当前卡点

- 手机号查找已成功，但普通企业成员尚未确认新增、批准或登录；云端已有三个系统角色，但仍只有 1 名已启用超级管理员，尚未创建普通业务角色或给新刚、志恒做真实授权。
- “小犀代采”企业应用当前无权读取 Webshare IP 普通表格；需开通 `sheets:spreadsheet:readonly` 并授予该文档访问，否则只能展示 500 条代理资产，不能判断占用或执行按需凭证检测。
- 飞书令牌端对当前应用的 PKCE challenge 返回 `20049`。测试实例暂时将 `XYNIGO_AUTH_FEISHU_PKCE_METHOD` 设为 `disabled`；随机 state、五分钟有效期、摘要存储和单次消费仍启用。飞书侧兼容后优先恢复 S256。
- 云端仍是单机测试实例：备份只有同机副本，缺少异机备份、保留周期、失败告警和高可用。
- 正式绿色包、Windows Keychain/DPAPI 实机联调和现有采购员灰度尚未完成，不能把当前测试登录直接视为正式上线。
- 交接时观察到一个长期未退出的远程 `docker compose up -d --build` SSH 终端会话，但云端健康和就绪检查正常。新会话先只读确认终端/进程状态，不要根据这一条记录直接终止它。

## 已知踩坑与处理经验

- 飞书回调 URL 必须逐字符一致；未登记精确地址会出现 `20029`。
- 当前应用的 S256 和 plain PKCE 令牌交换均出现 `20049`；兼容开关只作为受控临时措施，代码默认值仍保持 S256。
- 飞书登录必须使用“小犀代采”应用，不能误切回仅个人使用的 `samforo-cli`。
- 飞书 OAuth 用户身份与多维表格 OpenAPI 企业凭证不能混用；前者识别用户，后者代表服务访问业务表。
- 本地登录不能把云端会话交给网页保存；必须由本地服务通过一次性轮询桥换取并写入系统安全存储。
- 只隐藏前端菜单不构成权限控制；本地业务接口、云端管理员接口和飞书配置接口都必须各自校验权限。
- `python -m purchase_tool --help` 不会显示帮助，而会按普通入口启动一个新服务；当前入口只识别 `--no-browser`。检查端口前不要运行 `--help`。
- 本地端口被占用时会从 `config.json` 的 `serverPort` 起自动尝试后续 9 个端口。页面和测试必须使用启动窗口实际打印的 URL，不能硬编码认为一定是 8767。
- 多开本地服务时，停止前先用 `lsof -nP -iTCP:<port> -sTCP:LISTEN` 精确确认 PID；不要批量结束所有 Python 进程。
- 直接展示云端 OAuth 回调页面曾受应用内浏览器客户端策略影响；应以本地一次性登录桥、云端审计/会话记录和本地页面状态共同验收。
- 服务器从 PyPI 首次构建镜像较慢；依赖没有变化时优先复用镜像缓存，不要因等待时间长重复执行多个构建。

## 关键代码入口

- 云端认证路由和 RBAC 初始化：`cloud/auth-service/src/xynigo_auth/main.py`
- 云端管理员 API 合成测试：`cloud/auth-service/tests/test_admin_api.py`
- 飞书通讯录手机号解析：`cloud/auth-service/src/xynigo_auth/feishu.py`
- 飞书通讯录客户端合成测试：`cloud/auth-service/tests/test_feishu_directory.py`
- 云端设置与租户/成员激活开关：`cloud/auth-service/src/xynigo_auth/config.py`
- 云端数据模型与迁移：`cloud/auth-service/src/xynigo_auth/models.py`、`cloud/auth-service/migrations/`
- 本地登录桥和系统安全存储：`src/purchase_tool/cloud_auth.py`
- 本地 API 权限入口：`src/purchase_tool/main.py`
- 资源中心只读对账与本机代理检测：`src/purchase_tool/resource_center.py`
- 登录门禁、权限菜单和账号区：`src/purchase_tool/web/index.html`
- 云端部署记录：[`里程碑/20260824-云端认证测试部署.md`](里程碑/20260824-云端认证测试部署.md)
- 手机号邀请里程碑：[`里程碑/20260824-成员手机号邀请闭环.md`](里程碑/20260824-成员手机号邀请闭环.md)
- 店铺与代理 IP 里程碑：[`里程碑/20260825-资源中心店铺与代理IP-P0P1.md`](里程碑/20260825-资源中心店铺与代理IP-P0P1.md)
- 本地接入记录：[`里程碑/20260824-本地前端接入云端登录.md`](里程碑/20260824-本地前端接入云端登录.md)

## 本地启动与检查

启动开发服务：

```bash
PYTHONPATH=src .venv/bin/python -m purchase_tool --no-browser
```

启动后以终端打印的 URL 为准。只读检查示例：

```bash
lsof -nP -iTCP:8767 -sTCP:LISTEN
lsof -nP -iTCP:8769 -sTCP:LISTEN
lsof -nP -iTCP:8770 -sTCP:LISTEN
lsof -nP -iTCP:8771 -sTCP:LISTEN
curl -fsS https://xynigo.samforo.icu/healthz
curl -fsS https://xynigo.samforo.icu/readyz
```

## 简短新会话开场白

> 继续 Xynigo 权限与资源中心阶段。请先阅读 `AGENTS.md`、`docs/当前开发交接.md`、`docs/里程碑/20260825-内置管理员与云端配置隔离.md` 和 `docs/里程碑/20260825-资源中心店铺与代理IP-P0P1.md`，核对分支、HEAD、工作区与本地服务，勿擅自停止正在使用的采购实例。内置 `admin`、25 项权限目录和店铺/代理 IP P0/P1 已部署到 8771；下一步补飞书普通表格授权并完成真实成员准入和新刚/志恒灰度。保持凭证不出本机、不落前端/缓存/日志/导出。
