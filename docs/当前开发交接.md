# Xynigo Sourcing 当前开发交接

更新时间：2026-08-28

当前阶段：内部源码、共享测试页面/OpenAPI 和 GitHub Latest/manifest 已对齐为 `0.12.6`。云端 Web ↔ 本地执行器 P1 已部署，数据库为 `0015_executor_channel_p1 (head)`；Windows/macOS 标准安装包和绿色包已发布。P1 已完成设备配对/撤销、独立设备凭证、主动长轮询、任务租约、真实在线状态和本地配置读写首个纵切面；物流、建环境和下单业务任务仍未进入云端调度。资源中心买家号列表、查重、入库、占用和下单继续统一以 PostgreSQL 为事实源。当前本机没有 Xynigo 正式服务监听，`8765` 为无关服务；生产上线仍未完成。

2026-08-28 下载入口热修复已部署：标准包与绿色包通过 `xynigo.samforo.icu` 同域认证接口直接下载，页面不再打开 GitHub；四个 `v0.12.6` 文件由服务器 `release-assets` 只读挂载，返回前校验大小和 SHA-256。提交为 `5a39be3 + ebbc573`，备份为 `pre-direct-storage-20260827T163313Z`，当前镜像为 `sha256:724ba6c7c36552bb25ca0a8dbad3ab875cd6c978b334082298e9efea5b47463c`。这是云端窄热修复，GitHub `v0.12.6` 包内容和标签保持不可变；数据库仍为 0015。

Windows 标准安装包已随 `v0.12.6` 发布：NSIS 3.12 当前用户安装器使用 Xynigo 定制欢迎图/页眉和图形化绿色包迁移页；原生 `Xynigo.exe` 提供状态中心与托盘菜单，隐藏启动 Python 执行器，并显示云端通道、HubStudio、本机任务、版本、配对和心跳。当前约 15 MiB 产物仍未做可信 Authenticode 签名，只允许 `test + internalUnsignedTest` 渠道由用户核对 SHA-256 后手动放行；稳定渠道仍拒绝。

Windows 绿色版兜底已随 `v0.12.6` 发布：绿色包根目录包含可直接运行的 `Xynigo.exe`、品牌图片和托盘状态中心，启动器可同时识别标准版 `versions/<version>` 与绿色版根目录运行时；`启动.bat` 保留并转交状态中心，旧本机界面和人工配对脚本继续存在。云端发布清单以同版本 `greenFallback` 展示真实文件、SHA-256 与启动步骤。

macOS Apple Silicon 标准安装包已随 `v0.12.6` 发布：Swift 原生启动器、`.app + .pkg`、首次绿色包白名单迁移、独立 Application Support 数据目录、`xynigo://`、POSIX 单实例锁及 macOS CI 展开校验均已完成。约 11 MiB `.pkg` 的应用仅 ad-hoc 签名，安装包无 Developer ID 签名且未公证；内部测试用户必须核对 SHA-256 并在系统设置中手动放行。

Mac 标准版云端一键配对已上线：创建配对码同时返回 `pairingRequestId`，标准安装包页面可调用 `xynigo://pair` 并精确轮询本次请求；配对接口不写 `last_seen_at`，只有设备首次带 Keychain 凭证发起 HTTPS 长轮询才标记在线。绿色包继续显示命令/配对脚本回退；未签名标准包只在内部 test 渠道显式放行。

2026-08-28 本地执行器配置边界优化已部署共享测试、执行器新包尚未发布：云端设备页只保留 HubStudio 端口、查询/建环境并发、新建抽检数和安全并行开关；默认站点、MX/US 分组、采购员分配模板从设备配置移出。设备 revision 只覆盖上述运行与安全投影，旧业务字段继续保留在历史 `config.json` 和环境创建页面兼容使用；云端读取仍接受 `v0.12.6` 的旧安全结果，新的配置写请求拒绝业务分配字段。边界提交为 `92c11f1`；紧凑 UI 提交 `ae30930` 与后续页面收口把“已绑定设备/设备运行配置”保持为 220px 等高，设备卡收窄、配置卡扩宽，端口标题保持单行，安全并行使用默认开启的开关；下载区改为全宽单列并取消重复安装说明。Windows 默认平台同时显示标准安装包与绿色包兜底入口，绿色包使用发布清单内同域地址、文件名和 SHA-256，主下载已自动回退为绿色包时隐藏重复入口。当前镜像 `sha256:a5b2a4f491c19e7cf9bebdf59f3a0300855d4961a08b3c1ee40d53392933f2ca`，最新备份 `pre-executor-ui-tight-20260827T181754Z`，数据库仍为 `0015_executor_channel_p1`。已有设备明确保存的关闭值继续保留；本机新默认值需随下一执行器版本发布。

## 2026-08-28 v0.12.6 已部署并发布

- Release：<https://github.com/wrangler1024/xynigo-sourcing/releases/tag/v0.12.6>，Latest、非 prerelease；正式标签解引用到 `88f68f1e44cfb4ba6517d07c2fe1cb85695d456b`。
- 分支 CI `33089748125` 与标签 CI `33090027867` 均为七任务全绿，覆盖 Python 3.9/3.11/3.12、Windows 更新器、绿色包、Windows 标准安装/升级/卸载和 macOS 标准包。
- 共享测试运行 `0.12.6`，数据库为 `0015_executor_channel_p1 (head)`；部署前应用/环境/dump 备份为 `pre-v0.12.6-20260827T155612Z`，dump 已恢复验证原 `0014` 和 28 张采购单。
- 四个安装资产从公网 Release 回下载后哈希一致，匿名下载均返回 200；公网健康/就绪、页面/OpenAPI、四张空设备表和无效配对 fail-closed 冒烟通过。
- 飞书开发群已发送未签名手动安装、配对真实心跳、配置读写、安全并行和脱敏反馈教程。下一步是一台 Windows 与一台 Apple Silicon Mac 的真实安装/升级/迁移/配对验收。
- 完整证据见 [`里程碑/20260828-v0.12.6标准安装包与本地执行器通道发布.md`](里程碑/20260828-v0.12.6标准安装包与本地执行器通道发布.md)。

## 2026-08-27 v0.12.5 已发布

- Release：<https://github.com/wrangler1024/xynigo-sourcing/releases/tag/v0.12.5>，Latest、非 prerelease；正式标签解引用到 `6ba9ade598c9cf118efc15ce8cf3a3cbe2b7d43e`。
- 分支验证 CI `33032393401` 和标签 CI `33032507750` 均为 Python 3.9/3.11/3.12、Windows 更新器、通用打包冒烟和 macOS 打包六任务全绿。
- Windows ZIP SHA-256：`c34af3acafee5f576cab64c6ff177f02cf4f07323ced12e60e789628b9f0827c`；macOS ARM64 ZIP SHA-256：`b00eab1647507dd9e194e0fb319674716e0aebdce7c8b7480a735f43dc0c89df`。公网重新下载后校验通过。
- 旧 `v0.12.4` 更新客户端能发现两个平台的 `v0.12.5` 资产；用户仍须输入 `Y` 才会下载并切换。共享测试健康/就绪正常，OpenAPI 为 `0.12.5`；没有生产部署。
- 完整证据见 [`里程碑/20260827-v0.12.5采购协作导入收口发布.md`](里程碑/20260827-v0.12.5采购协作导入收口发布.md)。

## 2026-08-27 云端 Web ↔ 本地执行器 P1（已随 v0.12.6 部署）

- 定版入口已进入正式共用 Web：顶部栏按真实心跳显示待连接/离线/在线数量，点击后打开“系统管理 → 本地执行器”；云端模式隐藏旧“检查更新”，首次下载安装与执行器已运行后的在线更新保持两条独立流程。
- 云端认证只读接口 `GET /v1/local-executor/releases/latest` 与应用版本强绑定，当前返回 GitHub 不可变标签 `v0.12.6` 的 Windows/macOS 标准包和同版本绿色包 `greenFallback`，包含真实大小、完整 SHA-256、发布页与直接下载地址；版本升级未同步清单时服务会在导入阶段失败。
- Web 自动识别 Windows/macOS 并允许手动切换，读取云端清单后才启用下载、复制 SHA-256 和版本说明；下载由用户明确点击触发，页面不自动运行文件，不要求关闭 Gatekeeper/SmartScreen，也不把下载动作显示成安装或连接成功。
- `0015_executor_channel_p1` 新增 `local_executors`、`executor_pairing_codes`、`executor_tasks` 和 `executor_task_events`。配对码 5 分钟有效且单次消费，数据库只存配对码/设备凭证/租约令牌摘要；设备接口只接受独立 Bearer 并拒绝浏览器 Cookie。
- 本地执行器只主动访问云端 HTTPS 443。首期使用 20–25 秒长轮询；心跳 60 秒内视为在线，领取任务后获得 45 秒租约并每 15 秒续租。配置任务为 `config.read.v1` / `config.write.v1`，写入前同时校验云端记录 revision 和本地实时 revision；已开始的写任务租约中断后转 `uncertain`，不自动重放。
- 本地设备凭证分别保存到 macOS Keychain 和 Windows CurrentUser DPAPI，`运行数据/executor-channel.json` 只保存设备 ID、平台、状态、最近轮询和配置摘要。配置仍以本机 `config.json` 为事实源，保存使用原子替换并经过 `LocalTaskCoordinator` 独占闸门。
- Web 已接设备列表、生成配对码、撤销、配置读取/保存和任务取消。可编辑 Hub 端口、查询并发、建环境并发、抽检数、默认站点、采购分组、采购员模板及“查物流与建环境安全并行”；代理提取链接、账号密码、Cookie、Token 和飞书凭证不进入请求、任务、结果或日志。
- Windows/macOS 绿色包构建脚本新增独立“配对本地执行器”入口；Windows 绿色版进一步加入 `Xynigo.exe` 状态中心、托盘和品牌资源。更新器把启动器、品牌资源与配对脚本纳入受管路径；配置、日志、运行数据仍为保留路径。
- `src/purchase_tool/web/index.html` 已通过同步脚本复制到云端包内。内部桌面端 345 项执行（342 通过、3 跳过），云端 97 项通过；公开树、审计、分支 CI 和标签 CI 通过。共享测试已部署 0015，四张执行器表当前为空，等待真实设备配对验收。

## 2026-08-27 Windows 标准安装包（已随 v0.12.6 发布）

- 构建入口为 `组装Windows标准安装包.sh`，以已经过校验的绿色包作为唯一 payload，再用 NSIS 3.12 编译当前用户安装器。
- 默认目录 `%LOCALAPPDATA%\Programs\Xynigo Sourcing`，不请求 UAC、不注册自启动或系统服务。应用版本放在 `versions/<version>`，配置/日志/运行数据放安装根目录；升级切换版本标记，不覆盖运行中旧版本。
- 显式 `/MIGRATEDIR` 只从含 `VERSION.json` 的旧绿色包读取配置与八类数据目录，源目录不修改，目标已有文件不覆盖。卸载删除应用版本、协议和快捷方式，但保留用户数据。
- 当前允许 `xynigo://start`、`wake` 和带五分钟一次性配对码的 `pair`；协议拒绝长期凭证和业务动作。云端仅在 Windows 浏览器上由用户明确点击“启动 Xynigo”后调用，并继续以设备心跳确认结果。
- 品牌向导、图形化迁移、`Xynigo.exe` 状态中心、托盘菜单和回环脱敏状态接口已进入代码；开始菜单、桌面快捷方式和 `xynigo://` 均指向原生状态中心，普通用户不再依赖黑色命令窗口。
- `v0.12.6` 内部测试安装器 SHA-256 为 `e45a5030290e5b2e0fca9bf3eafd9d857114128025d3fa7fadb2b57aae3628e4`。签名脚本和手动签名发布工作流已经实现，但本机与仓库目前没有可信 Authenticode 证书；本次通过 `test + internalUnsignedTest` 明确例外发布，用户必须核对 SHA-256 并手动确认 SmartScreen。Windows Runner 已完成构建、安装、升级和卸载；可信签名与采购电脑实测仍待完成。详见 [`Windows标准安装包实现_20260827.md`](Windows标准安装包实现_20260827.md)。

## 2026-08-27 macOS 标准安装包（已随 v0.12.6 发布）

- 构建入口为 `组装macOS标准安装包.sh`，复用已校验 `macos-arm64` 绿色包运行时，编译最低 macOS 13 的 Swift 启动器，再由 `pkgbuild + productbuild` 生成 `/Applications/Xynigo Sourcing.app` 安装包。
- 标准数据目录为 `~/Library/Application Support/XynigoSourcing`，与应用包分离；首次启动可以选择绿色包目录，迁移器只复制配置/日志/运行数据/导入文件白名单且不覆盖已有文件。
- 原生应用注册 `xynigo://`，Swift 与 Python 双层限制为 `start / wake / pair`；协议通过当前用户 `0600` 临时文件进入固定脚本，不承载业务命令。本地执行器增加 `executor.lock` 非阻塞文件锁，避免多个 Terminal 进程并行操作 HubStudio。
- 不安装 LaunchAgent、LaunchDaemon、Login Item 或系统服务；标准模式关闭绿色包自更新器。`.pkg` 写入系统 `/Applications`，安装时预期请求管理员确认。
- `v0.12.6` 内部测试 `.pkg` SHA-256 为 `39df44182bde84fd8c576d73a014a3dee1017943225c244b7b453399bd3ee8e4`；应用仅 ad-hoc 签名，安装包 `no signature`、未公证。本次通过 `test + internalUnsignedTest` 明确例外发布，用户必须核对 SHA-256 并在系统设置中手动放行。macOS CI 已完成构建、展开和载荷校验；Developer ID、公证/stapling 与真实采购 Mac 安装/升级仍待完成。详见 [`macOS标准安装包与本地执行器实现_20260827.md`](macOS标准安装包与本地执行器实现_20260827.md)。

## 2026-08-26 23:23 收工补充

- `v0.12.5` 公开候选分支为 `codex/release-v0.12.5-public`、头 `6ba9ade`，CI `32984758350` 仍排队；无标签、无 Release，不能自动发布。
- 当前内部回归为桌面端 306 项完成（303 通过、3 跳过）、云端 80 项通过。云端短时计划 `expired=6 / validated=1`，任务 `completed=2 / failed=7`，没有非终态任务，最近 15 分钟无服务错误日志。
- 完整 `v0.12.5` 容器和宿主机部署源已与 HEAD 七个关键文件哈希一致。明天先复核是否再次漂移，并对 failed 任务做稳定错误码脱敏归因，再决定是否发布 `v0.12.5`。详细快照、卡点和踩坑统一以根目录 [`HANDOFF.md`](../HANDOFF.md) 的“2026-08-26 23:23 收工交接”为准。

## 2026-08-26 桌面默认打开云端（v0.12.4 已发布）

- 桌面默认启动不再打开 `127.0.0.1`，而是打开本地认证客户端实际配置的云端 Origin；本地 HTTP 执行器仍照常启动和监听，原 HubStudio/CDP/SHEIN 逻辑未删除。
- `--local-ui` 保留本机界面。Windows 包新增“启动-本地执行器.bat”，macOS 包新增“启动-本地执行器-Mac.command”；更新器和回滚 helper 把新入口作为受管文件。
- `v0.12.4` 已发布为 GitHub Latest、非 prerelease；旧 `v0.12.3` 更新器已从线上清单发现新版本和当前平台 `test` 资产。更新仍需用户在运行窗口输入 `Y` 确认。

## 2026-08-26 v0.12.3 线上协同测试版发布

- 完整内部实现提交：`a8d3fbd`；PUBLIC 仓库只推送公开净化分支 `codex/release-v0.12.3-public`，发布提交为 `33f558bd53e3955f41b5f17af9bb27cf233bfb17`。
- GitHub `v0.12.3` 已发布为 Latest、非 prerelease，包内渠道继续为 `test`；Release：<https://github.com/wrangler1024/xynigo-sourcing/releases/tag/v0.12.3>。
- Windows x86_64 包 SHA-256：`6507514c6eb5d6f964cf9747d835866bca4149459156215b97af6a1a74592982`；macOS ARM64 包 SHA-256：`d797cfdf8f661d643685a6f09153b5dfff4d2a16b73a0ec7b6f0a94eb439fbce`。
- 内部与公开树本机均为 299 项完成（296 通过、3 项平台条件跳过），云端均为 72 项通过；公开源码安全审计、双平台包结构、SHA-256、macOS 自包含运行时自检全部通过。
- GitHub Actions 运行 `32966981964` 的 Python 3.9/3.11/3.12、Windows 更新器、Windows 包冒烟和 macOS 打包六项任务全部通过；旧 `v0.12.1` 更新器已从公网 Latest API 发现 `v0.12.3` 和对应平台资产。
- 共享测试 Web 根路径、`/healthz`、`/readyz`、`/v1/auth/web/status` 继续返回 200，原版 UI 与 X favicon 保持不变。该发布只开放团队线上协同测试更新，不等同于生产服务器、正式数据路由或真实自动下单上线。
- 本轮本地开发收口后的完整当前状态、卡点、下一步和部署/发布踩坑统一见 [`里程碑/20260826-v0.12.3云端工作台与完整买家号发布.md`](里程碑/20260826-v0.12.3云端工作台与完整买家号发布.md)；当前 8767–8771 均未监听，团队端两平台真实升级与普通成员云端独立使用仍是首要验收项。

## 2026-08-26 云端 Web 工作台部署

- `https://xynigo.samforo.icu/` 已从 API 根路径 404 切换为 Xynigo 云端 Web 工作台。云端不维护另一套前端：`src/purchase_tool/web/index.html` 是本机与云端的唯一 UI 源，`cloud/auth-service/deploy/sync_web_workspace.py` 将原页面和品牌资源原样同步到认证服务。原有布局、导航、页签、配色和品牌保持不变，仅在云端运行时切换 API 适配及“云端已连接/本机执行器离线”状态文案。
- 地址栏/标签页图标沿用部署前的 X 图标。同步脚本将 `xynigo-x.ico` 同时发布为 `/xynigo-x.ico` 和 `/favicon.ico`，页面图标缓存版本为 `v=6`；2026-08-26 公网浏览器已直接打开资源核对为 256×256 X 图标。
- 飞书浏览器登录成功后使用 Host-only、Secure、HttpOnly 会话 Cookie 回跳 `/`；公开登录壳通过 `GET /v1/auth/web/status` 安静检查会话，未登录不再产生预期 401 控制台错误。
- 云端页面已接采购概览、采购任务、本人采购执行、买家号安全摘要、业务日志、系统日志和成员/会话读取；认领继续调用现有云端行锁接口。账号密码、Cookie、接码资料和会话令牌不进入普通页面数据。
- Cookie 鉴权的 POST/PUT/PATCH/DELETE 新增 `X-Xynigo-Web-CSRF: same-origin` 保护；Bearer 本地执行器和浏览器插件链路保持兼容。
- 页面明确显示本机执行器离线边界：本地服务关闭时仍可登录、查看与操作云端功能；HubStudio、CDP、SHEIN 浏览器和本机截图等能力暂停，直到后续设备心跳/任务租约通道完成。
- 原版 UI 恢复部署前备份位于 `/opt/xynigo-auth-test/backups/deploy/pre-cloud-ui-compat-20260826T114652Z/`，数据库备份为 `/opt/xynigo-auth-test/backups/postgres/pre-cloud-ui-compat-20260826T114652Z.dump`；应用、环境与 dump 的 SHA-256、`pg_restore` 清单和临时库恢复均通过，恢复库包含原 23 张采购单后已删除。镜像切换后 `alembic current == heads == 0013_purchase_task_feishu_mirror`。
- 回归：云端 72 项通过，本机 299 项完成（296 通过、3 项 Windows 条件跳过）；本机合成云端会话覆盖原版采购任务、采购执行、买家号和成员页面，公网浏览器再次核对原版未登录页，控制台均为 0 error/0 warning。部署后公网根路径、原品牌静态资源、`/healthz`、`/readyz` 与 `/v1/auth/web/status` 均返回 200；本机 8767–8771 均未监听。

## 2026-08-26 独立插件首次真实提交 500 修复

- 测试包裹 `XMWU39A60390` 的飞书登录成功，但 `POST /v1/purchase-orders/submit` 连续返回非 JSON 500；插件按新边界没有写入客服备注。
- 云端日志确认根因是 `psycopg.errors.UndefinedColumn: purchase_orders.feishu_record_id`。同时采购任务列表也受影响，说明不是订单字段或 XYP2 内容问题，而是应用/数据库版本错位。
- 云端应用镜像包含 `0013_purchase_task_feishu_mirror` 模型和迁移，但数据库与旧 migrate 容器仍停在 `0012_buyer_account_credentials`。`docker compose` 只重建 auth 时没有重新执行已经成功退出的 migrate 服务。
- 修复前创建并校验 `/opt/xynigo-auth-test/backups/postgres/pre-0013-purchase-task-feishu-mirror-20260826T184815.dump` 及 SHA-256；随后使用当前 auth 镜像执行 `alembic upgrade head`，数据库现为 0013，三个采购主单镜像字段已存在，健康/就绪探针正常。
- 三次失败事务对该包裹号的 `purchase_orders` 计数为 0，没有半成品主单或客服备注。未替用户重新提交真实订单，需由用户在刷新/重载最新插件后人工重试。
- 用户迁移后再次点击时，云端日志确认 `POST /v1/purchase-orders/submit` 返回 200；数据库现有 1 张 `submitted` 主单、版本 1、1 条有效明细和 1 条 Outbox。页面随后报 `extension context invalidated`，属于扩展重载后旧内容脚本丢失成功回调，不能再按“提交失败”重试。该单客服备注尚未自动预填，应使用已提交版本的 XYP2 恢复入口补写一次。
- 插件已增加远端状态回查：打开采购明细先调用 `/v1/purchase-orders/get`，把云端 draft/submitted 状态恢复到脱敏本地缓存；已提交状态显示“复制已提交XYP2”，只复制云端版本供人工检查后粘贴，不自动新建/保存备注。扩展上下文失效也改为明确提示刷新，不再误报云端失败。
- 部署经验固定：只更新 auth 镜像不足以保证已退出的 migrate 服务重跑；以后应用切换前必须显式执行并回读 `alembic current == alembic heads`，否则禁止开放流量。

## 2026-08-26 独立插件登录 403 修复

- 云端日志确认 `POST /v1/auth/local/start` 连续返回 403，系统日志来源为 `extension_direct`，请求在路由匹配前被 CSRF 中间件拒绝；不是飞书权限、成员审批或采购 RBAC 问题。
- 根因是扩展云端请求携带了浏览器已有的 Xynigo 网页 Cookie，同时没有网页专用 `X-Xynigo-Web-CSRF` 头。云端正确地把它视为 Cookie 鉴权写请求并 fail closed。
- 插件后台现对登录、轮询、身份查询和采购接口统一强制 `credentials: "omit"`；独立插件只使用 `chrome.storage.session` 中的短期 Bearer，不再混入网页会话。云端 CSRF 中间件未放宽。
- 扩展测试 34 项和构建通过，登录发起与采购提交测试均明确断言 `credentials === "omit"`。需要重载开发扩展并刷新店小秘页面后重新登录。

## 2026-08-26 已提交采购单修订交互

- 原交互把 `submitted` 永久映射为前端双按钮禁用，且 CSS 对所有 disabled 按钮使用 `cursor: wait`，造成悬停像持续加载，也不给运营纠错入口。
- 新规则：正式提交后，只要全部有效明细仍为 `unclaimed`、没有采购员占用且没有分单，原提交运营可以修改并再次提交；云端更新同一采购单 `draftRevision`、重算明细并产生新 `order.submitted` Outbox 版本。
- 若采购单已被认领、存在分单或进入采购执行，云端返回 `purchase_order_in_progress`；非原提交运营返回 `purchase_revision_forbidden`。不能靠前端解禁绕过状态锁。
- 插件重新打开已提交单时隐藏“保存采购单”，主按钮改为“提交修改”；普通禁用态改为 `not-allowed`，只有真实网络处理中才使用等待光标。
- 首次正式提交仍只预填一条 XYP2。修订成功不得新增第二条客服备注：插件已按真实店小秘结构适配备注列表与 `.remark-modal` 编辑器，仅在唯一 XYP2 客服备注存在时替换结构块、自动提交并回读；人工文字保留。0 条、多条、内容竞态、超长或回读失败时停止并使用剪贴板兜底。
- 云端全量测试、插件 34 项测试、Tampermonkey 3 项生成契约和浏览器冒烟通过；冒烟覆盖唯一备注自动编辑和多条备注 fail-closed。共享测试 auth 容器已于 19:10 重建，部署前代码/镜像备份为 `/opt/xynigo-auth-test/backups/deploy/pre-purchase-revision-20260826T190956`。
- 实测包裹 `XMWU39A60390` 当前为 `submitted / unclaimed / 1 条有效明细`，满足修订条件；系统没有代用户执行真实修订，需重载插件并刷新店小秘页面后人工验证。
- 展示取舍已确认：店小秘采购明细前端取消 ROI，只显示采购总额、利润、利润率；核心计算和 `estimatedMetrics.roi` 提交字段保留，不做数据库删列或契约降级。

## 2026-08-26 买家号数据库事实源与测试 Base 持续同步

- 新增并部署 `0011_buyer_db_base_mirror` 与 `0012_buyer_account_credentials`；资源中心页面和采购下单都改从 `buyer_accounts` 查询，不再运行时读取或写入旧表。
- 一次性迁移读取旧表既有 23 个字段。dry-run 与正式迁移均确认 320 条；数据库最终 320 条，凭证使用 Fernet 认证加密，Outbox 和日志不含明文。
- 全新 Base Token 为 `TZDabK44kaxVKZsDK2ycxb8qnBe`，买家号表为 `tbl4pO3O7309blVA`；建环境和物流结果表也位于同一独立 Base，但没有关联旧台账。
- “小犀代采”仅获得这张测试 Base 的可编辑文档应用权限。买家号表为 41 字段，完整账号、密码、Cookie、接码资料及业务字段由 Worker 使用事务 outbox、幂等 upsert、有限重试和写后回读持续镜像。当前 v2 outbox、账号同步状态和 Base 均为 320/320 completed，旧表→数据库→新 Base 全字段差异为 0。Base 不回写数据库，也不承担查重、锁或状态裁决。
- 共享测试应用和 PostgreSQL 已在部署前备份并校验；本轮部署没有主动停止或重启本机 8767，最新只读状态与共享云端均为 `0.12.3`。

## 2026-08-26 采购日用结果双写与 v0.12.1

- 新增 `PUT /v1/operations/environment-creation-runs` 和 `PUT /v1/operations/logistics-query-runs`，服务端以租户 + `runKey` 幂等保存任务、结果和 Outbox。
- `0010_operation_result_sync` 新增五张 PostgreSQL 表；成功建环境结果同时安全合并到 `buyer_accounts`，不保存或回显凭证，也不静默覆盖冲突绑定。
- 本机使用独立的持久化待发文件，网络或会话异常时指数退避重试；服务端确认接收后才移除本地项。
- 云端 Worker 已部署并启用，按“同步键”写入/更新飞书并写后回读；失败有限重试，事实仍以 PostgreSQL 为准。
- 全新 Base「Xynigo 买家号资源同步测试表」初建时包含 16 字段的买家号表、26 字段的建环境结果表和 30 字段的物流结果表，无关联字段且与旧台账无关；当时三表均为 0 条。后续 `0011/0012` 已把买家号表扩展为 41 字段，并将旧表 320 条完整数据迁入加密数据库及测试 Base。
- 该阶段共享服务迁移为 `0010_operation_result_sync`；后续已升级到 `0012_buyer_account_credentials`，以本文顶部状态为准。
- `v0.12.1` 公开发布提交为 `6829b1b5b60d0d3bb14db2a68a5fe5a26c69306f`，双平台包、清单、SHA-256、更新发现和六项 GitHub Actions 作业均通过。详见 [`里程碑/20260826-采购日用结果双写与v0.12.1发布.md`](里程碑/20260826-采购日用结果双写与v0.12.1发布.md)。

## 2026-08-26 采购下单闭环 P1 本地实现

- 新状态机不再把旧测试分单当最终事实：付款前使用可修改 `checkout_attempt` 占用明细数量，付款成功后原子形成正式 `purchase_batch`，物流按包裹回填。
- 同一 Hub 环境或买家号不能被两个非终态尝试并发占用；付款成功资源在复用规则确认前保守留存。失败/放弃且已登录环境时必须先确认环境删除，删除失败继续锁量并转人工。
- 新增安全买家号快照/只读列表、七个闭环写接口、本地安全代理白名单、执行权限和脱敏审计；采购详情返回需求、已购、占用、剩余数量及尝试/批次时间线。
- 企业飞书空间已新建独立 Base「Xynigo 买家号资源同步测试表」和「买家号安全同步测试表」，16 个无凭证字段读回通过。该条记录是 P1 本地实现时的空表状态；后续已更换为当前独立 Base、增加两张日用结果表并启用数据库→Base 单向 Worker。通用双向同步明确不开放。
- 合成回归：云端 60 项通过；本地 284 项完成（281 通过、3 项条件跳过）。快捷绑定页面已在隔离 8768 + Playwright 网络桩验证资源查询、创建尝试和开始结算，随后已关闭隔离服务；现有 8767 进程没有停止或重启，真实 HubStudio、SHEIN 和共享数据库均未写入；飞书只创建了上述独立空表结构，没有业务记录写入。
- 买家号接口字段与安全边界见 [`买家号资源接口-P0.md`](买家号资源接口-P0.md)；完整闭环见 [`采购下单闭环-P1.md`](采购下单闭环-P1.md)，里程碑见 [`里程碑/20260826-采购下单闭环P1后端纵切面.md`](里程碑/20260826-采购下单闭环P1后端纵切面.md)。

版本合并状态：当前源码、共享测试云端和 GitHub Latest 均为 `0.12.4`，增加云端采购协作导入、桌面默认云端入口与独立本机兼容启动器；包内渠道为 `test`，不代表生产稳定开放。

2026-08-25 v0.11.1 回归通过本地 254 项（251 通过、3 项按 Windows 环境条件跳过）、云端 37 项、公开源码安全审计、登录恢复合成浏览器回归、Windows/macOS 包结构与 SHA-256 校验，以及两个平台旧版更新器的远端 latest 发现检查。内部修复提交为 `9123da7`，干净公开发布提交为 `231dce8`。最终由新刚、志恒在各自电脑完成真实飞书授权和工作台登录验收。完整记录见 [`里程碑/20260825-v0.11.1成员登录闭环.md`](里程碑/20260825-v0.11.1成员登录闭环.md)。

测试使用方向：将共享测试云端作为采购团队持续使用的联调环境，不在生产准备期间停用。采购同事可以继续使用已开放的云端功能和现有本地 Xynigo 服务。2026-08-26 最新复核为云端 `/healthz`、`/readyz` 正常，数据库迁移为 `0014_proc_import_cloud`，页面与 OpenAPI 均为 `0.12.4`；本机当前没有 Xynigo 服务监听。未经明确授权不得停止、替换或重启重新出现的联调服务。用户已授权买家号完整凭证及业务资料、建环境和物流真实结果写入共享 PostgreSQL 并持续镜像到本轮独立飞书测试 Base；真实平台下单、审核、其他飞书表写入和批量业务变更仍需单独授权。

架构边界（2026-08-25 生效）：广州多项目共享测试服务器 `shared-test-gz-01`（实例 ID `lhins-b6nu398t`）和 `/opt/xynigo-auth-test` 只保留 Xynigo 测试环境，严禁部署生产容器、生产数据库或生产密钥。历史测试 n8n 已按用户明确指令停止，容器/数据卷保留且自动重启关闭；其他既有服务不得受影响。生产只能独立部署到 `xynigo-prod-gz-01` 的 `/opt/xynigo-auth-prod`，Compose 项目名固定为 `xynigo-auth-prod`，域名为 `app.xynigo.com`，并从空 PostgreSQL 执行已批准迁移。测试与生产不得共用数据库、数据卷、账号、会话、Cookie、日志、备份或飞书业务表。完整决策与当前只读盘点见 [`架构决策-20260825-广州腾讯轻量服务器测试与生产双环境隔离.md`](架构决策-20260825-广州腾讯轻量服务器测试与生产双环境隔离.md)。

生产部署当前未放行：生产云防火墙已调整为管理员单一来源 SSH 和公网 `80/443`，独立密钥登录及主机运行态已核验；但 UFW 未启用，SSH 仍允许密码认证/root 登录，Docker/Nginx/生产目录尚不存在。`0003/0004` 尚未进入获批 Git 提交，DNSPod 仍只有 NS 记录，备案/TLS、生产密钥与 Base 路由、异机备份和监控均未验收。关闭全部闸门前不得执行生产部署。

2026-08-25 已部署增量：运营采购助手采用“插件 → Xynigo 本机安全桥 → 云端采购 API → PostgreSQL/Outbox”。云端已部署采购基础、Web 采购中心读取、认领、分单保存和采购执行队列接口；首次联调时使用独立本机服务复用现有登录，不影响当时的 8767 稳定实例。首次云端联调已完成“采购列表 → 认领 → 保存待绑定资源分单 → 采购执行队列/销售订单号搜索”，没有触发店小秘审核、HubStudio 下单、SHEIN 写入或飞书同步。契约见 [`采购中心接口-P0.md`](采购中心接口-P0.md)，部署记录见 [`里程碑/20260825-采购执行测试版云端联调.md`](里程碑/20260825-采购执行测试版云端联调.md)。

2026-08-26 最新插件增量（`v0.12.1` GitHub Prerelease 已发布、待团队真实验收）：运营使用边界调整为“店小秘扩展 → Xynigo 云端采购 API → PostgreSQL/Outbox”。扩展版本为 `0.12.1-standalone-cloud-login-test`，运营电脑只安装扩展即可，不运行本机服务，也不需要先登录 Xynigo 页面。扩展自有页面通过现有云端 `/v1/auth/local/start`、`/v1/auth/local/poll` 完成飞书登录，短期 Xynigo 会话只保存于 Manifest V3 Service Worker 的 `chrome.storage.session`；飞书密钥/令牌不进入扩展，内容脚本不可读取会话。正式提交失败不打开、不填写客服备注；成功后只预填一条 XYP2 且不自动保存；未认领时可修订同一采购单和唯一 XYP2，已认领后拒绝直接修改。等待备注处理时显示持续进度，已认领修改失败的详细提示保留 8 秒。发布提交、`main` 与标签统一为 `e8a03ef`，线上 ZIP SHA-256 为 `470a7b918a63a875b4b0ce4deb8fd6588264dd79c0fb3ba6a1af61269d7a65b3`；仍需在不启动本机服务的条件下完成真实飞书 OAuth、授权订单提交和单备注验收。

2026-08-26 已部署增量：根据采购同事确认的真实结算场景，新版采购中心 UI 与读取契约完成第一阶段测试部署。采购任务页默认公共待认领池，列表增加店铺/运营、收件人/国家、采购单金额、采购员和快捷筛选；采购执行改为当前采购员已认领采购单的宽屏列表，并通过 `claimedByMe=true` 保证未提前分单的采购单也能进入；每行仅保留“详情 / 处理”两个按钮，处理下拉包含继续采购与查单跟单；详情改为独立工作区，采购明细展示采购备注主/次规格，已付款批次纵向展示查单环境。采购契约 v2 新增 `storeBaseName`、`operatorName`，云端采购单新增独立店铺/运营列并完成历史回填。真实下单会话、付款结果或 Hub 环境删除/留存写入仍未开放。

2026-08-26 已部署交互补充：采购执行筛选框已统一现行 UI，主列表使用“采购单状态 / 跟单摘要”，一单一 SKU 一件且尚未采购、未生成采购批次时保留“快捷下单（绑定资源）”入口；物流单号、物流商和物流状态按付款批次放在采购单详情，当前只预留“待回填”展示，持久化字段和回填 API 尚未实现。新增 `POST /v1/procurement/orders/{purchaseOrderId}/return`，退回原因必填；服务端只允许当前采购员本人认领、尚未开始且没有分单的明细退回公共待认领池，已有分单或已进入下单、付款、跟单流程时拒绝退回。该次部署没有新增迁移，当时共享测试库为 `0006_purchase_operator`；后续已推进到 `0010_operation_result_sync`。

2026-08-26 历史增量（后续已部署）：业务操作日志 P0 完成时，`audit_events` 由统一 `BusinessLogService` 追加，新增操作人/角色快照、模块、操作类型、业务对象、明确结果口径、失败原因、字段级变更摘要、来源、客户端版本和 traceId。迁移依赖顺序为 `0004_procurement_execution_test -> 0006_purchase_operator -> 0005_business_operation_logs_p0`；该迁移和日志只读 API 后续已随 `0010` 进入共享测试环境。完整契约见 [`日志中心接口-P0.md`](日志中心接口-P0.md)。

2026-08-26 历史增量（后续已部署）：系统运行日志 P1 完成时新增独立 `system_log_events` 与 `SystemLogService`，采集云端认证服务 HTTP 运行、4xx 警告、5xx 和未处理异常；只保存路由模板、状态、耗时、异常类型/错误码/指纹和脱敏摘要，不保存查询串、请求/响应正文、异常消息或堆栈。默认保留 30 天、每租户 100,000 条，成功读取请求支持确定性采样，写请求和错误始终采集。新增 `system.runtime_log.read`、当前租户只读 API、本机安全 GET 代理，以及“系统管理 → 系统运行日志”筛选列表和详情；该迁移和 API 后续已随 `0010` 进入共享测试环境。详细契约见 [`系统运行日志接口-P1.md`](系统运行日志接口-P1.md)。

该轮历史验证：隔离部署树 38 项云端测试通过；完整工作区云端 52 项通过；本地 271 项完成（268 通过、3 项按 Windows 条件跳过）。当时共享测试环境 `/healthz`、`/readyz` 正常，OpenAPI 接受采购契约 v1/v2 与采购单退回接口，历史 6 张采购单全部完成店铺基础名与运营归属回填，数据条数保持不变；当时迁移 head 为 `0007_system_runtime_logs_p1`、共享库为 `0006_purchase_operator`。当前状态以本文顶部 `0010` 记录为准。

2026-08-26 v0.12.0 已发布：公开净化分支 `codex/release-v0.12.0-public` 提交为 `3ea1ce6`，标签 `v0.12.0` 已发布为 GitHub latest，包含 Windows x86_64 与 macOS ARM64 绿色包、双平台更新清单和 SHA-256 文件。GitHub Actions 的 Python 3.9/3.11/3.12、Windows 更新器、Windows 包冒烟和 macOS 打包自检全部通过，线上更新器已实际解析两个平台清单。该发布只更新本机受管程序，不部署生产服务器，也不把尚未部署的业务/系统日志迁移带入共享测试数据库。详见 [`里程碑/20260826-v0.12.0采购中心与环境创建优化发布.md`](里程碑/20260826-v0.12.0采购中心与环境创建优化发布.md)。

## 新会话先做什么

1. 先阅读根目录 [`HANDOFF.md`](../HANDOFF.md)，再完整阅读本文件、[`项目记忆.md`](项目记忆.md) 和 [`后端实施交接.md`](后端实施交接.md)。
2. 执行 `git status --short`、`git log -5 --oneline` 和本机监听端口检查，不覆盖已有改动。
3. 不要未经用户确认停止当前正在使用的采购服务；本地服务会从配置端口开始自动寻找后续空闲端口。
4. 共享测试库已经到 `0011_buyer_db_base_mirror`；不要回退迁移或重建现有测试分单。买家号/建环境/物流结果 Worker 和目标测试 Base 写入已授权并启用；HubStudio/SHEIN 真实下单、其他 Base 写入或数据删除仍需单独授权。

## 代码与版本基线

- 项目目录：`/Users/jeff/Documents/xynigo-sourcing`
- 开发分支：`codex/new-ui-migration`
- v0.12.3 内部完整实现提交：`a8d3fbd feat: complete v0.12.3 cloud sourcing workflow`
- v0.12.3 公开发布提交：`33f558bd53e3955f41b5f17af9bb27cf233bfb17 release: publish v0.12.3 cloud workspace test update`
- v0.12.1 公开发布提交：`6829b1b5b60d0d3bb14db2a68a5fe5a26c69306f release: prepare v0.12.1 test update`
- v0.12.0 公开发布提交：`3ea1ce6 release: prepare v0.12.0 test update`
- v0.11.1 登录修复基线：`9123da7 fix(auth): keep Feishu login polling alive`
- v0.11.1 干净公开发布提交：`231dce8 fix(auth): keep Feishu login polling alive`
- v0.11.0 合并基线：`922e90f merge: integrate v0.10.0 safe parallel runtime`
- 登录主链路提交：`023f212 feat(auth): connect local UI to Feishu cloud login`
- 飞书应用切换提交：`4045dae feat(auth): complete Xiaoxi Feishu OAuth switch`
- 云端部署基线提交：`167e54c chore: deploy cloud auth test instance`

交接时文档仍在更新，实际继续开发前以 `git status` 和最新 HEAD 为准。

## 当前已经完成

### 云端认证服务

- `cloud/auth-service/` 使用 FastAPI、SQLAlchemy/Alembic 和 PostgreSQL，独立于本地 Python 3.9 采购应用。
- 腾讯轻量服务器隔离测试入口为 `https://xynigo.samforo.icu`；交接时 `/healthz` 和 `/readyz` 均通过 HTTPS 返回正常。
- 已切换为“小犀代采”飞书应用并完成真实 OAuth 回调，不再使用仅个人可用的 `samforo-cli` 应用。
- 精确回调地址已经在飞书应用后台登记：`https://xynigo.samforo.icu/v1/auth/feishu/callback`。
- 已实现租户白名单、新成员待审批、显式超级管理员、RBAC 权限摘要、短期可撤销会话、登录审计和本地一次性登录桥。
- 当前测试环境迁移为 `0015_executor_channel_p1`，包含身份、采购单基础、认领/分单、结构化日志、下单闭环、买家号数据库事实源、完整凭证加密、采购日用结果双写、采购协作导入云端任务和本地执行器设备通道；生产环境仍未部署。
- PostgreSQL 不发布宿主机端口，认证服务只绑定服务器回环端口，由 Nginx 提供 HTTPS；已有 root-only 同机备份和恢复演练。
- 2026-08-24 已将 `/v1/admin/*` 管理员 API 部署到隔离测试实例；部署前保存应用、`.env` 和 PostgreSQL 备份，部署后迁移仍为 `0002_local_login_bridge`、public 仍为 11 张表。

### 本地应用接入

- 本地页面已经接入飞书扫码/授权登录、等待轮询、登录门禁、当前成员和退出。
- v0.11.1 在未认证期间停止受保护的采购任务轮询，并支持恢复尚未过期的登录请求；这解决了飞书回调成功后仍回到登录页的问题。新刚、志恒已在各自电脑完成真实登录验收。
- 云端会话由本地 Python 服务换取；macOS 保存到 Keychain，Windows 保存到 CurrentUser DPAPI。网页拿不到轮询令牌和最终会话令牌。
- 本地主要业务 API 已增加后端权限校验；所有浏览器 POST 请求增加同源校验。
- 本地 Python 服务已增加 `/api/admin/*` 安全代理；云端 Bearer 会话继续只由本地服务从系统安全存储读取，浏览器不会拿到会话令牌。
- 普通成员只能通过 `/api/lark/status` 读取脱敏就绪状态；飞书连接配置接口仍要求 `system.lark_connection.manage` 且角色为 `super_admin`。
- `system.lark_connection.manage`、`system.integration.manage` 和 `resource.ip.credential.manage` 已按超级管理员专属高敏权限部署。部署后临时误授管理员飞书连接权限，8771 配置接口仍因缺少 `super_admin` 角色返回 403；代理凭证误授权会被云端目录同步移除。
- 前端菜单按权限摘要屏蔽；账号入口已调整到顶部状态区最右侧，宽屏显示身份摘要，窄屏显示头像并通过下拉菜单展示姓名、角色和退出。
- 飞书头像加载成功时只显示图片，加载失败才回退姓名缩写，已修复文字与图片上下叠加问题。
- 飞书本地登录使用命名授权窗口；云端 `/v1/auth/local/complete` 在授权成功后自行尝试关闭，本地主页面领取会话后再次主动关闭该窗口。正常登录不会遗留完成页 Tab；若用户手动打开完成页、浏览器拒绝脚本关闭，只显示“可手动关闭”的兼容提示。完成页 CSP 只放行固定脚本的 SHA-256，不使用 `unsafe-inline`。
- “系统管理”已将“成员管理、角色管理、权限策略、数据范围”收拢到二级入口“组织与权限”；前三项使用真实云端 API，“数据范围”仅显示规划态且不请求接口。登录会话保留为独立二级入口。审批与角色分配保持分离，停用会提示并由云端撤销有效会话。
- 成员管理已增加“新增成员”：管理员输入飞书绑定手机号，后端用应用身份精确匹配“小犀代采”可见的飞书成员，确认后固定创建为 `pending`，可在具备 `system.role.manage` 时预分配角色，但仍必须另行批准。手机号只用于当次飞书查询，不写入 Xynigo 数据库、响应或审计；浏览器不会获得飞书 Open ID。
- “小犀代采”已配置手机号查找所需的应用身份权限、通讯录数据权限范围和应用可用范围；真实查找成功返回指定普通企业成员的姓名、头像与已激活状态。权限生效不需要重启 Xynigo，每次查找都会重新获取应用身份令牌。
- 权限策略页已按“一级菜单 → 二级功能 → 后端权限项”展示工作区当前 29 项权限；一级和二级复选框支持批量选择，共用权限码的入口会同步勾选。页面只保存角色与权限码关联，不修改成员状态、成员角色、会话或数据范围；配置、克隆、分配等未实现写能力不会因权限码存在而自动开放。
- 权限策略采用“左侧可搜索、可滚动角色列表 + 右侧单角色权限详情”的主从布局，不再按角色数量纵向重复整套权限表。一级模块默认以紧凑折叠行展示，只在需要时展开二级功能与权限码，并提供“展开全部/收起全部”；窄屏角色列表改为横向滚动。角色切换保留系统锁定和权限计数；当前角色有未保存调整时，切换或刷新会提示，恢复到服务端原值后未保存状态自动清除。
- 本地静态首页与图片响应统一增加 `Cache-Control: no-store`，避免源码开发实例更新后浏览器继续显示旧菜单；对已运行的旧进程可使用带版本查询参数的首页地址立即绕过缓存。

### 资源中心店铺与代理 IP（P0/P1 已部署）

- 资源中心顺序已合并为买家号库、号商入库、账号注册、环境创建、店铺管理、代理IP。
- 店铺管理只读汇总安全店铺字段、代理台账与 HubStudio 全量环境；只把台账明确的浏览器/窗口序号关系视为绑定，名称一致仅标记候选，不自动建立关联。
- 代理IP普通列表跳过代理账号/密码列；只有用户明确发起本机检测时才按所选资产临时读取凭证。检测默认并发 10、超时 8 秒、失败复测 1 次，同端点只检测一次；凭证不进入前端、缓存、历史、日志或导出。
- 真实只读验收读到 342 家店铺、2495 个 HubStudio 环境和 500 条 Webshare 资产。普通代理台账尚未授权给企业应用，所以占用关系统一标记“占用待授权”，不误算未使用；未执行代理检测或任何写入。
- 店铺配置/凭证更新/克隆、代理分配/凭证管理写接口均未开放，前端和 API 返回 `writesEnabled=false`。详见 [`里程碑/20260825-资源中心店铺与代理IP-P0P1.md`](里程碑/20260825-资源中心店铺与代理IP-P0P1.md)。
- 2026-08-26 本地源码增加三类代理的正式业务目录与页面筛选：711 动态住宅代理用于采购、按 API 动态提取并要求本机网络出口 IP 加白；Webshare 静态数据中心 IP 用于绑定店铺环境；静态住宅 IP 暂无使用场景。动态代理不伪装成固定资产行，静态住宅无来源时不伪造 0 库存；只有 Webshare 固定资产允许进入现有本机检测。该增量未部署，完整本地 276 项完成（273 通过、3 跳过）、云端 58 项通过，详见 [`代理IP分类与数据源-P0.md`](代理IP分类与数据源-P0.md)。

### 阶段 2 管理 API（内置管理员增量已部署）

- 成员：列表/筛选、详情、手机号精确解析与待审批成员新增、批准、停用、恢复；停用使用数据库行锁并同时撤销有效会话。新增接口会在后端重新解析手机号，防止前端伪造飞书成员身份。
- 角色与策略：工作区后端固定维护 `super_admin`、`admin` 与默认无权限的 `member` 三个系统角色。当前 29 项权限目录下，`admin` 固定获得 26 项日常业务与常规管理权限，明确排除 `system.lark_connection.manage`、`system.integration.manage` 与 `resource.ip.credential.manage`；`super_admin` 获得全部 29 项。`super_admin` 与 `admin` 的权限集不可编辑，系统角色不可改名/删除；自定义角色仍由后端生成代码。
- 会话：当前租户有效会话摘要、撤销单会话、按成员撤销全部会话；响应不包含令牌或令牌摘要。
- 管理 API 分别执行 `system.member.manage`、`system.role.manage` 后端校验；跨租户目标按不可见处理并记录拒绝审计。
- `super_admin` 与 `admin` 权限集不可通过页面改写；三项高敏权限不能授予任何非 `super_admin` 角色。非超级管理员的角色管理员不能授予自身不具备的权限，也不能借角色分配升级为超级管理员。
- 所有成功状态/授权/撤销变更以及权限拒绝、跨租户拒绝都写入 `audit_events`。

### 采购执行测试版本（云端已部署并完成首轮联调）

- `procurement.execution.manage` 已同步到云端，权限目录为 29 项；`super_admin=29/29`、`admin=26/29`、`member=0/29`。
- `0004_procurement_execution_test` 已迁移云端测试库，新增采购明细认领字段、采购分单与分单明细表；既有主单、明细和 Outbox 均完整保留。
- 新增认领、分单保存和采购执行队列接口；认领支持整单/批量/单条、行锁冲突与重复请求幂等，分单保存使用 `executionRevision` 乐观锁并校验当前采购员归属、数量完整和资源站点一致。
- 数据模型允许同一采购明细按数量分配到多张采购分单；当前前端测试交互先支持整条明细在分单间移动，下一轮再共同设计“同一行拆数量”的编辑方式。
- Web 采购任务页已从 DOM 模拟切换为测试写接口；采购执行页已删除静态分单种子，改为读取 PostgreSQL 测试接口。队列与详情均展示店铺销售订单号，并支持销售订单号关键词搜索。
- 测试资源只保存 Hub 环境安全标识/名称和脱敏买家号标签；不接受或保存密码、Cookie、接码 Key、飞书凭证。真实下单、平台订单号回填、跨采购单资源占用、异常回滚和飞书同步仍未开放。
- 首轮真实测试库联调将既有 1 条明细认领给当前超级管理员，生成 1 张 `waiting_binding` 分单并分配数量 1；Hub 环境、买家号和平台采购单号均为空。认领和分单保存各产生 1 条成功审计，采购执行页面按销售订单号搜索命中，控制台无 error/warning。
- 独立 8792 脱敏服务完成浏览器闭环：批量认领 2 单 5 条、将首单 3 条明细拆为 3 张分单并绑定三组测试资源、采购执行页显示 3 张待下单分单，销售订单号 `GSH202608250001` 搜索命中 3 张；控制台无 error/warning。未访问真实 HubStudio、飞书或 SHEIN。

### 已验证结果

- 当前本地基线：`.venv/bin/python -m unittest discover -s tests`，共 257 项完成（254 项通过、3 项仅 Windows 环境集成测试按条件跳过）；其中包含新版采购中心 UI 静态契约。本轮云端认证/采购 API 仍为 37 项通过。
- 云端认证测试：`cd cloud/auth-service && uv run pytest -q`，37 项通过；存在一条 Starlette `TestClient`/`httpx` 弃用预警，不影响当前结果。
- 内置管理员验收覆盖：系统角色锁定、当前 26/29 默认权限、三项高敏权限缺失、权限策略拒绝和本地配置接口角色二次校验。部署后数据库与浏览器均确认三个系统角色；临时误授权 403/自动清除验证后已删除全部测试数据。
- 店铺与代理资源中心通过服务层、API 权限、脱敏/导出、凭证按需读取、重试/去重及浏览器页面合成验收；真实数据源检查为店铺/Hub/代理资产可读、普通代理台账未授权，整个过程保持只读。
- 管理前端已通过临时合成服务与真实浏览器检查；“组织与权限”的已实现子页、独立登录会话页、权限导航、动态表格和工作区页签均正常。数据范围保持禁用规划态，该检查没有访问或修改真实成员、角色、会话及采购数据。
- 采购基础部署后 `/healthz`、`/readyz` 返回 200；OpenAPI 保留旧登录与管理员路由并新增 3 条采购接口。现有超级管理员 Bearer 会话在容器重建后继续有效；该次部署时迁移为 `0003_purchase_request_foundation`、public schema 为 14 张表，随后已经升级到 0004。
- 新增 `127.0.0.1:8769` 开发实例加载完整本地代理，真实读取到 1 名 active 成员、2 个系统角色、11 项权限和 1 个有效会话；浏览器已验证成员管理、角色管理、权限策略和登录会话均不再出现 `not found`，未执行状态或授权写入。
- 新增 `127.0.0.1:8770` 角色配置开发实例；浏览器已验证“新建角色”编辑器、系统角色锁定、成员数量和角色表格均读取真实云端数据，控制台无 error。验收只打开并取消新建表单，没有写入真实角色。
- 同一 8770 实例已完成分级权限策略只读验收：共享权限同步、一级菜单半选状态和唯一权限计数正常；交互后恢复原值且未点击保存，没有修改真实角色权限。
- 8770 已进一步完成权限主从布局验收：角色搜索得到正确的过滤数量，任意时刻右侧只渲染一个角色；自定义/系统角色切换、锁定状态和未保存状态均正常，控制台无 error。
- `127.0.0.1:8771` 曾用于手机号新增成员联调：先验证权限缺失错误与表单清理，权限和范围发布生效后又成功读取指定普通企业成员的姓名、头像和已激活状态。只读查找后点击取消，没有确认新增，控制台无 error。
- 店铺/代理资源增量部署时目录曾扩展到 25 项，`super_admin=25/25`、`admin=22/25`、`member=0/25`；采购权限部署后当前已经是 29/29、26/29、0/29。
- 真实超级管理员已完成本地页面登录和退出闭环；权限摘要和账号区通过浏览器检查，控制台无 error。
- 新刚、志恒已完成真实成员新增/审批、管理员角色分配、v0.11.1 安装、飞书授权回调和 Xynigo 工作台登录。该证据关闭“普通成员尚未登录”和“逐台安装待验证”两个卡点；最小权限角色、停用撤权和跨租户拒绝仍待验证。
- 2026-08-25 已用临时本地测试页模拟 8771 的脚本打开方式：云端完成页在约 1 秒内自动关闭，只保留主页面，控制台无 error；临时页面、标签和 8788 服务随后全部清理。真实登录会话未退出或重做。
- 2026-08-25 曾核对到 8767 旧进程和 8771 鉴权整合版同时监听；该记录已经被 2026-08-26 的只读复核取代，当前仅 8767 监听且报告 `0.12.3`。新会话必须重新检查，不能依据历史端口记录停止或启动进程。
- 2026-08-25 重新部署前备份位于服务器 `/opt/xynigo-auth-test/backups/deploy/pre-resource-admin-20260825T023928Z/` 和 `/opt/xynigo-auth-test/backups/postgres/pre-resource-admin-20260825T023928Z.dump`，均为 root-only 0600 且 SHA-256/pg_restore 清单通过。8771 独立配置备份为 `.codex_tmp/invite-ui/config.pre-resource-admin-20260825T023928Z.json`。
- 运营采购部署前备份为 `/opt/xynigo-auth-test/backups/deploy/pre-procurement-foundation-20260825T040549Z/` 和 `/opt/xynigo-auth-test/backups/postgres/pre-procurement-foundation-20260825T040549Z.dump`；应用、`.env`、清单和 dump 均为 `root:root 0600`，SHA-256 与 `pg_restore` 清单校验通过。
- 采购基础部署后首次权限目录同步暴露旧库升级缺陷：同一请求内两次同步会重复排队角色权限。现已在目录同步末尾显式 `flush` 并新增升级回归；当时目录为超级管理员 28/28、管理员 25/28、成员 0/28，0004 部署后已同步为 29/29、26/29、0/29。
- 首次手工提交已形成 1 张 `submitted` 主单、1 条 `unclaimed` 明细和 1 条 `order.submitted/pending` Outbox；用户页面确认未触发店小秘原生审核。日志记录 4 次成功提交调用但数据库仅 1 份，幂等有效；若确认只点击一次，需继续排查前端重复请求。
- 部署时 macOS tar 扩展属性生成 `._*` AppleDouble 文件，首次迁移因此报 null bytes；已删除本批生成文件并在 `.dockerignore` 增加双层 `._*` 排除，重建后迁移容器退出 0。该问题未修改数据库迁移版本，仍为 `0002_local_login_bridge`。
- 历史 8771 曾从统一买家号台账返回 320 条统计；该运行时链路现已停用。当前资源中心从 PostgreSQL 返回同批 320 条安全记录，旧表只作一次性迁移源。

## 成员准入规则

“能在飞书看到授权页”和“能进入 Xynigo”是两个阶段：

1. 成员必须位于“小犀代采”应用的飞书可用范围内，才能正常发起授权。
2. OAuth 返回的 `tenant_key` 必须在 Xynigo 云端租户白名单中。
3. 未知成员首次授权只创建 `users.status=pending`，不会签发系统会话。
4. 只有 `active` 成员才能登录；`disabled` 成员必须被拒绝。
5. 新成员自动激活默认关闭；只有环境变量中明确指定的超级管理员可在首次登录时激活并初始化超级管理员权限。
6. 成员激活后仍必须分配角色和权限，模块显示与业务 API 均按权限裁决。

云端测试实例已具备成员审批、角色分配和会话管理 API；新刚、志恒已经完成批准、角色授权和真实登录。其他未知企业成员首次完成飞书授权后仍会按设计停留在待审批状态，不得因为本次验收而开启全员自动激活。

## 下一步开发任务

按以下顺序推进，不要同时展开云端任务调度或采购数据迁移：

1. 将采购执行前端接到本地已完成的“下单尝试 → 结算确认本批方案 → 付款结果 → 正式采购批次”写接口；不得继续沿用认领后预分单作为前置条件。
2. 使用已完成的买家号安全查询/预占接口接资源选择，再补 Hub 环境独立元数据与租约；落实失败/放弃时删除已登录 Hub 环境、付款成功后保留环境跟单的真实执行。买家号密码、Cookie 和接码 Key 仍不得进入浏览器或 PostgreSQL。
3. 将新刚、志恒当前用于登录验收的管理员角色收敛到业务确认后的最小权限采购员角色，完成认领冲突、越权 403、撤权后即时失效和跨租户隔离真实联调。
4. 实现飞书同步 Worker 前先完成 Base 字段/选项预检和全新脱敏订单幂等回读；任何 Base 写入仍需单独授权。
5. 开通 `sheets:spreadsheet:readonly` 并授予 Webshare IP 总表访问；补齐成员停用、会话撤销和重新登录拒绝的真实闭环。
6. 独立开发并授权飞书同步 Worker；在新脱敏 `orderKey` 上验证幂等写入与回读，不能直接拿现有正式提交记录试写。
7. 将“登录门禁前无法使用工作台更新入口”列为升级体验缺口；后续提供登录前安全检查更新或启动期明确提示，同时保留绿色包手动安装和旧目录回退方案。

## 当前卡点

- 新刚、志恒已真实新增、批准并登录，但当前用于验收的是内置管理员角色；最小权限采购员角色及降权后的菜单/API 验收尚未完成。
- “小犀代采”企业应用当前无权读取 Webshare IP 普通表格；需开通 `sheets:spreadsheet:readonly` 并授予该文档访问，否则只能展示 500 条代理资产，不能判断占用或执行按需凭证检测。
- 飞书令牌端对当前应用的 PKCE challenge 返回 `20049`。测试实例暂时将 `XYNIGO_AUTH_FEISHU_PKCE_METHOD` 设为 `disabled`；随机 state、五分钟有效期、摘要存储和单次消费仍启用。飞书侧兼容后优先恢复 S256。
- 云端仍是单机测试实例：备份只有同机副本，缺少异机备份、保留周期、失败告警和高可用。
- v0.11.1 已由新刚、志恒逐台安装并完成真实登录；但未登录页面目前不能触发工作台内更新流程，登录故障用户仍需手动安装绿色包。该体验缺口不影响本次登录闭环，也不能把共享测试验收解释为生产上线。
- 交接时观察到一个长期未退出的远程 `docker compose up -d --build` SSH 终端会话，但云端健康和就绪检查正常。新会话先只读确认终端/进程状态，不要根据这一条记录直接终止它。

## 已知踩坑与处理经验

- 飞书回调 URL 必须逐字符一致；未登记精确地址会出现 `20029`。
- 当前应用的 S256 和 plain PKCE 令牌交换均出现 `20049`；兼容开关只作为受控临时措施，代码默认值仍保持 S256。
- 飞书登录必须使用“小犀代采”应用，不能误切回仅个人使用的 `samforo-cli`。
- 飞书 OAuth 用户身份与多维表格 OpenAPI 企业凭证不能混用；前者识别用户，后者代表服务访问业务表。
- 本地登录不能把云端会话交给网页保存；必须由本地服务通过一次性轮询桥换取并写入系统安全存储。
- 只隐藏前端菜单不构成权限控制；本地业务接口、云端管理员接口和飞书配置接口都必须各自校验权限。
- `python -m purchase_tool --help` 不会显示帮助，而会按普通入口启动一个新服务；当前入口只识别 `--no-browser`。检查端口前不要运行 `--help`。
- 本地端口被占用时会从 `config.json` 的 `serverPort` 起自动尝试后续 9 个端口。页面和测试必须使用启动窗口实际打印的 URL，不能硬编码认为一定是 8767。
- 多开本地服务时，停止前先用 `lsof -nP -iTCP:<port> -sTCP:LISTEN` 精确确认 PID；不要批量结束所有 Python 进程。
- 直接展示云端 OAuth 回调页面曾受应用内浏览器客户端策略影响；应以本地一次性登录桥、云端审计/会话记录和本地页面状态共同验收。
- 服务器从 PyPI 首次构建镜像较慢；依赖没有变化时优先复用镜像缓存，不要因等待时间长重复执行多个构建。

## 关键代码入口

- 云端认证路由和 RBAC 初始化：`cloud/auth-service/src/xynigo_auth/main.py`
- 云端管理员 API 合成测试：`cloud/auth-service/tests/test_admin_api.py`
- 飞书通讯录手机号解析：`cloud/auth-service/src/xynigo_auth/feishu.py`
- 飞书通讯录客户端合成测试：`cloud/auth-service/tests/test_feishu_directory.py`
- 云端设置与租户/成员激活开关：`cloud/auth-service/src/xynigo_auth/config.py`
- 云端数据模型与迁移：`cloud/auth-service/src/xynigo_auth/models.py`、`cloud/auth-service/migrations/`
- 本地登录桥和系统安全存储：`src/purchase_tool/cloud_auth.py`
- 本地 API 权限入口：`src/purchase_tool/main.py`
- 资源中心只读对账与本机代理检测：`src/purchase_tool/resource_center.py`
- 登录门禁、权限菜单和账号区：`src/purchase_tool/web/index.html`
- 云端部署记录：[`里程碑/20260824-云端认证测试部署.md`](里程碑/20260824-云端认证测试部署.md)
- 手机号邀请里程碑：[`里程碑/20260824-成员手机号邀请闭环.md`](里程碑/20260824-成员手机号邀请闭环.md)
- 店铺与代理 IP 里程碑：[`里程碑/20260825-资源中心店铺与代理IP-P0P1.md`](里程碑/20260825-资源中心店铺与代理IP-P0P1.md)
- 本地接入记录：[`里程碑/20260824-本地前端接入云端登录.md`](里程碑/20260824-本地前端接入云端登录.md)
- v0.11.1 成员登录闭环：[`里程碑/20260825-v0.11.1成员登录闭环.md`](里程碑/20260825-v0.11.1成员登录闭环.md)

## 本地启动与检查

启动开发服务：

```bash
PYTHONPATH=src .venv/bin/python -m purchase_tool --no-browser
```

启动后以终端打印的 URL 为准。只读检查示例：

```bash
lsof -nP -iTCP:8767 -sTCP:LISTEN
lsof -nP -iTCP:8769 -sTCP:LISTEN
lsof -nP -iTCP:8770 -sTCP:LISTEN
lsof -nP -iTCP:8771 -sTCP:LISTEN
curl -fsS https://xynigo.samforo.icu/healthz
curl -fsS https://xynigo.samforo.icu/readyz
```

## 简短新会话开场白

> 继续 Xynigo v0.12.2 采购执行联调。先阅读 `HANDOFF.md`、`AGENTS.md`、`docs/买家号资源接口-P0.md`、`docs/采购日用功能数据双写-P0.md`、`docs/项目记忆.md`、`docs/当前开发交接.md` 和 `docs/后端实施交接.md`，核对 Git、8767 实际版本、共享测试健康、数据库迁移和 Outbox 状态，勿擅自停止正在使用的采购实例或注入合成记录。买家号数据库事实源和独立 Base 单向镜像已部署；继续收口最小权限与前端下单闭环。

## 2026-08-26 采购任务批量认领与经营字段（共享测试已部署）

- 采购单列表已接通当前页逐单勾选、全选和批量认领；复用既有 `/v1/procurement/claims` 事务、行锁、幂等与并发冲突规则，不新增第二套认领接口。切页或改变筛选后只保留当前页仍可认领的选择。
- 筛选区增加店铺、运营精确筛选。云端概览返回当前租户去重后的店铺/运营选项，列表 API 增加 `store`、`operator` 精确条件；任务范围、站点、店铺、运营、搜索、每页数量统一为六列同风格控件。
- 列表将原“采购单金额”明确为“销售金额”，增加插件 `estimatedMetrics` 中的预估利润、利润率及口径提示；未提交指标的历史单显示 `—`，不补造数据。
- 为后续字段数据权限增加服务端 `fieldVisibility` 投影契约，范围为 `store/operator/salesAmount/profit/profitMargin`。默认全部可见；未来角色策略必须由服务端注入并在列表、详情和筛选选项中裁剪，客户端请求不能自行开启字段。当前尚未提供权限配置页面，也未把默认值改为隐藏。
- 隐藏运营时不能回传包含运营姓名的完整店铺展示名或 `orderKey`；隐藏利润时详情同时移除可反推利润的预估成本相关字段。该约束已有合成回归。
- 验证：完整工作区云端 52 项通过、本地 271 项完成（268 通过、3 项跳过）；内联 JavaScript 语法与真实浏览器只读交互已验证。店铺/运营下拉和利润字段契约现已部署到共享测试云端。
- 部署后没有自动提交真实认领，也没有开放真实下单。下一步使用 v0.12.3 客户端和插件进行团队端到端验收，并继续保持真实外部写入单独授权。

## 2026-08-26 采购任务飞书镜像 P0（待 canary）

- `cloud/auth-service` 已完成迁移 `0013_purchase_task_feishu_mirror`、采购镜像客户端和后台 Worker；草稿/提交、认领、退回及执行版本均进入事务 Outbox。
- 启动预检要求采购单主表 38 字段、采购明细表 59 字段完全匹配，并核对单选项和「关联采购单」目标。同步按 `orderKey`/`lineKey` 幂等，采用主表保存中 → 明细 → 主表完成的顺序和写后回读。
- 云端完整测试现为 70 项通过；临时 PostgreSQL `upgrade head → downgrade 0012_buyer_account_credentials → upgrade head` 通过。桌面端完整回归现为 297 项完成（294 通过、3 项 Windows 条件跳过）。

## 2026-08-26 普通飞书采购协作表直接导入增量

- “采购协作导入”已升级为“动态绑定普通飞书表格 → 一次确认 → 按字段名规范结构与追加数据 → 补齐动态图片列”。下载 Excel 仍保留为离线备份/手工兜底；当前标准模板为 A:AQ 43 列。
- `LarkCliSheetsGateway.append_table_rows` 使用已授权的 `lark-cli --as user` 和 `+table-put mode=append` 写入。金额/数量保持数值类型，订单号等保持文本类型；业务行放在私有临时 JSON 文件，不进入进程命令行。
- 导入批次使用文件名 + 12 位业务内容摘要，相同数据重新解析仍得到同一批次。每 40 行追加；重试会回读已有行签名并只续传缺失行，超出计划的重复/异常行会在写入前阻断。
- 跨批次查重边界已明确：普通采购协作表中已导入订单不支持通过重新导入修改。完全相同的一个或多个历史副本按“已存在”整单跳过，不设置其底色、链接或图片；明细不同则在任何追加前阻断并提示走独立修订流程。采购任务在无人认领、无分单时允许原提交运营修订，是 PostgreSQL 采购任务链路的状态能力，与本页面无关。
- 数据整批回读数量和关键字段通过后才补动态定位的单元格图片；已有数据/图片幂等跳过，中断后可从页面安全重试。工作表写入前按 sheet ID 重新解析当前名称，避免使用过期名称误创子表。
- 新增 `/api/assistant/procurement-import/sheet-sync` 及状态路由，保留旧 `image-sync` 别名供服务热更新短时兼容。前端确认框明确说明分单日期、安全删列、采购需求区顺序、冻结列及自定义下单区边界，并分别展示数据行和图片进度。
- 2026-08-26 字段迁移已完成：`分单时间`改为北京时间真实日期`分单日期`，取消被`导入批次`等值覆盖的`分单批次`，固定并冻结`分单日期 / 采购员 / 销售订单号 / 店铺 / 运营`五列，`包裹号 / 采购状态 / 优先级`紧随其后。真实“采购执行协作区”当前为 revision 131、A:AQ 43 列、41 条非空数据行；迁移前后全部 199 个物理数据行逻辑字段比对 0 差异。前端 JavaScript 语法及本机全量 299 项测试完成（296 通过、3 项 Windows 条件跳过）。
- 当前开关仍为关闭，未部署 `0012`，未触碰运行中的 8767/共享测试服务，也未写真实飞书。下一步只允许在明确授权后，用全新脱敏 `orderKey` 做独立测试 Base canary。
- 详细字段边界、配置、回滚与一对多采购批次限制见 [`采购任务飞书镜像-P0.md`](采购任务飞书镜像-P0.md)。

## 2026-08-26 采购协作导入云端化 P0（共享测试已部署）

- 云端认证服务新增 `/v1/assistant/procurement-import/*` 七条接口，覆盖 xlsx/XYP2 解析、图片、Excel、目标表读取/校验、二次确认导入和状态轮询。原 Web 源按运行环境自动选择本地 `/api` 或云端 `/v1`，图片与下载保持同域 Cookie 鉴权。
- `deploy/sync_procurement_import_core.py` 保证本地/云端共用解析器与 Excel 图片算法；云端增加应用身份普通 Sheets OpenAPI 网关，不读取开发机 `lark-cli` 配置，也不持久化员工飞书 token。
- `0014_proc_import_cloud` 新增加密短时计划和持久化任务。计划以部署密钥派生的 AES-GCM 加密，默认 30 分钟清空；Worker 重启后恢复非终态任务，继续依赖稳定批次、行签名和回读完成幂等续传。
- 同一租户/目标/批次的非终态任务有数据库部分唯一索引，防止并发双击重复执行。任务进度和审计只保存计数、状态和稳定错误码，不保存订单号、收件信息、采购链接或图片。
- 共享测试已在完整备份和临时恢复验证后迁移到 `0014`，功能开关已设为 `true`，Nginx 仅对 parse 路由放宽到 30 MiB；7 条公网契约、1.5 MiB 请求、飞书租户身份及事务回滚式解析/导出通过。随后“小犀代采”开通 Sheet 应用身份权限，并在用户二次确认后完成真实新文件 1 行追加、底色和图片写后回读。最新历史不可修改与聚合修复部署后只读回读为 43 个标准表头、49 条非空行、revision 184，没有自动重跑任务或修改目标表。
- 回归：本地 299 项完成（296 通过、3 跳过），云端 77 项通过；临时 PostgreSQL 18 `upgrade head → downgrade 0013 → upgrade head` 通过，Docker 生产镜像构建通过。
- 部署前回滚点为 `/opt/xynigo-auth-test/backups/deploy/pre-procurement-import-cloud-20260826T132407Z/` 和 `/opt/xynigo-auth-test/backups/postgres/pre-procurement-import-cloud-20260826T132407Z.dump`；完整记录见 [`里程碑/20260826-采购协作导入云端上线.md`](里程碑/20260826-采购协作导入云端上线.md)。生产环境没有部署。
